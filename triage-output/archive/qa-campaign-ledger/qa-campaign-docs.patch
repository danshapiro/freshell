diff --git a/docs/plans/parity-ledger/CAMPAIGN-RESULTS.md b/docs/plans/parity-ledger/CAMPAIGN-RESULTS.md
new file mode 100644
index 000000000..a2ae58e2f
--- /dev/null
+++ b/docs/plans/parity-ledger/CAMPAIGN-RESULTS.md
@@ -0,0 +1,75 @@
+# Parity Campaign Results — overnight 2026-08-05 (fable)
+
+**Branch:** `parity-campaign-20260805` (single integration branch, ready for review; NOT pushed, no PR)
+**Workers:** opencode CLI + `ms-runpod/moonshotai/Kimi-K3` (≈35 sessions: 18 auditors, ~17 implementers + fix/fresh recovery sessions)
+**Verification bar:** every landed item cherry-picked onto this branch and passed targeted `cargo test` / vitest runs centrally; full workspace + npm gates run at campaign end (see GATES section below).
+
+## Landed items (31 checklist items, 31 feature commits)
+
+| Item | Commit | Area |
+|------|--------|------|
+| SESSION-14 | f38c81f9c | fractional-ms flooring parity, directory projection |
+| NET-03 | 97764871e | share-URL truth table + token encoding |
+| SESSION-19 | 03e4c5ede | safe bounded search snippets |
+| TERM-13 | 45a000817 | scrollback cap eviction/persistence tests |
+| FILE-06 | 6f556d566 | sandbox comparison hardening (dot-segments, symlinks) |
+| EXT-02 | afa317e36 | extension-scan diagnostics parity |
+| AUTO-08 | 6be88cd0f | navigate parity (alias, 400, history rules) |
+| SYNC-01 | 95dc360c5 | 400% UI scale input + a11y + persistence (tests) |
+| SYNC-03 | e6bea2342 | editor font follows terminal font (tests) |
+| SYNC-04 | 9d67b258b | deprecated freshAgent.fontScale removal (tests) |
+| EXT-04 | 84e31bdea | extensions patch validation + disabled round-trip |
+| TERM-04 | 921e72ec6 | create dedupe two-clients/reconnect tests |
+| SESSION-02 | 60f83b512 | createdAtOverride + title suppression overlay |
+| SESSION-01 | c376e8d85 | sidebar-click resumable identities (4 providers) |
+| DIAG-04 | 830b01f77 | **live perf logging toggle + CPU/RSS process sampler** |
+| FILE-01 | b5d2dfcd2 | authenticated /local-file endpoint |
+| BROWSER-02 | 9e220a572 | WebSocket-upgrade proxy relay |
+| SYNC-02 | eb04653dd | 64px terminal font + live xterm metrics (tests) |
+| TERM-09 | 3a7e1f411 | output backpressure: catastrophic monitor can't be starved |
+| TERM-05 | be282d324 | shared owner output byte-parity + attach convergence |
+| SESSION-07 | cde48e799 | stale-query cancellation + amplifier file-tier search |
+| NET-05 | 0c4d8cd36 | POST /api/network/configure-firewall WSL2 repair |
+| AUTO-10 | 45affeda3 | wait-for: exit/prompt/stable modes + /re/flags |
+| EXT-01 | 84dcbf48d | strict extension manifest schema (zod parity) |
+| AUTO-06 | 3e3940e7b | pane rename resolution + ui.command broadcast |
+| SESSION-04 | 6757ee326 | provider-aware titles + Gemini AI title path |
+| FILE-02 | 4e3fadee8 | Windows drive/UNC targets in /local-file |
+| FILE-03 | 87eee486b | \\wsl$ + network-share UNC unaddressable pinning |
+| AUTO-05 | (bonus) | pane split/close ops parity slice |
+| SESSION-17 | (bonus) | session title sync + PATCH provider lanes |
+| NET-09 | (bonus) | network settings persistence + share-URL settings lanes |
+
+Plus docs: AGENTS.md port correction (47f5c68f5), audit ledger (d85561a8c).
+
+## Honest non-landings
+
+- **SYNC-06** — AUDIT_WRONG: worker proved the feature already landed via PR #592; no work needed.
+- **SESSION-21** — DROPPED: three sessions (2 resumes) produced zero committed output.
+- **AUTO-05** — initially cut at deadline, then SALVAGED in the bonus round (fresh session
+  finished the dirty worktree; landed and verified).
+
+## Checkbox policy (IMPORTANT)
+
+NO checklist checkboxes were flipped. The checklist's completion rule requires the item's
+Playwright lane(s) to pass; tonight's items landed with unit/integration coverage only.
+The audit ledger (`audit/*.yaml`) + this file carry the evidence; a follow-up PW-RUST pass
+should flip boxes item-by-item.
+
+## Audit ledger
+
+`docs/plans/parity-ledger/audit/*.yaml` — all 235 items re-verdicted with file:line evidence:
+31 DONE / 118 PARTIAL / 58 MISSING / 23 UNCLEAR / 5 HOST-LIMITED as of main@4eecc456c.
+
+## GATES (07:08)
+
+- `cargo test --workspace` (shared warm target): **EXIT 0** — all workspace crates green
+  including the 28 landed items (log: /tmp/parity/logs/gate-cargo.log).
+- `npm test` (coordinated full suite, campaign worktree): **EXIT 0** — fully green; note the
+  pre-existing `api-edge-cases` __proto__ failure seen at baseline did NOT reproduce here
+  (log: /tmp/parity/logs/gate-npm.log).
+
+## GATES round 2 (07:56, final branch state with 31 items)
+
+- `cargo test --workspace`: **EXIT 0**
+- `npm test` (coordinated full suite): **EXIT 0**
diff --git a/docs/plans/parity-ledger/LEARNINGS.md b/docs/plans/parity-ledger/LEARNINGS.md
new file mode 100644
index 000000000..edc40cf1b
--- /dev/null
+++ b/docs/plans/parity-ledger/LEARNINGS.md
@@ -0,0 +1,123 @@
+# Parity Campaign — Learnings Ledger (fable, overnight 2026-08-05)
+
+Running log of key learnings from the massively-parallel-agent campaign.
+Each entry: timestamp, category (PROCESS / TECH / REPO), learning.
+
+## Entries
+
+- **00:15 REPO** — Live Rust server runs on port **3001** (`.env PORT=3001`, pid file
+  `rust-server-3001.pid`), not 3002 as AGENTS.md claimed. Launcher default is still 3002.
+  Fixed in AGENTS.md. Lesson: verify runtime facts (`ss -tlnp`, pid files) before trusting docs.
+- **00:16 REPO** — Rust server auth: `Authorization: Bearer` and `?token=` are REJECTED;
+  only `X-Auth-Token: <token>` works for `/api/server-info`, `/api/debug`. `/api/perf` is a
+  POST toggle (enables debug/perf logging), not a metrics reader.
+- **00:18 TECH** — Suspected parity gap found before audit even started: Node has
+  `server/perf-logger.ts` (cpuUsage/memoryUsage/rss sampling); no equivalent sampling code
+  found in `crates/` (no sysinfo, no /proc/self/stat readers).
+- **00:20 PROCESS** — Prior campaign hazard (from 2026-07-17 status doc): shared-index
+  commit absorption. Standing rule: agents commit with explicit pathspecs (`git commit -- <files>`).
+- **00:27 PROCESS** — Amplifier bash tool with `run_in_background=false` kills the process
+  group on timeout — a `nohup ... &` inside a normal call still died. Long-running jobs MUST
+  use `run_in_background=true`.
+- **00:27 PROCESS** — opencode parallel dispatch (validated earlier): each agent needs its
+  own working directory; concurrent first-run `.opencode` init in a shared fresh dir loses
+  runs silently. `--auto` required for non-interactive file writes.
+- **00:38 TECH** — Baselines on main (4eecc456c): `cargo test --workspace` EXIT 0 (all green);
+  `npm test` = unit/default suites fully green; server suite has exactly ONE pre-existing
+  failure: `api-edge-cases.test.ts > rejects __proto__ in request body (strict schema)`.
+  Matches the coordinator's remembered `test:server=failure`. Treating as pre-existing;
+  final gate = "no NEW failures vs this baseline".
+- **00:38 PROCESS** — 18 read-only Kimi auditors + full cargo build + npm suite concurrently:
+  load peaked ~31 on 32 cores, 21-23Gi available. Read-only agent waves are cheap locally
+  (the heavy lifting is remote inference); builds are the local bottleneck.
+- **00:38 PROCESS** — Kimi-K3 inside opencode spontaneously self-parallelizes: the config
+  auditor announced "dispatch parallel explore agents" — opencode agents can spawn their own
+  subagents. Amplification is nested; budget/watch accordingly.
+- **00:56 PROCESS** — AUDIT WAVE 1 FAILURE MODE: 17/18 auditors hit the 25-min timeout with
+  ZERO output. Only the 4-item section finished (superb quality). Causes: (a) batches too big
+  (8-27 items × Kimi's thorough style), (b) output written only at the END — a timeout
+  forfeits all work. Fixes adopted: write output INCREMENTALLY (item by item), and resume the
+  timed-out sessions (`opencode run -c`) with a "dump what you have NOW" instruction — the
+  exploration context is preserved in the session, so recovery is cheap vs restarting.
+- **00:57 PROCESS** — Pipeline waves: don't serialize phases. Launched the DIAG-04 (perf
+  logging) implementation worker from orchestrator-verified context while audits still run.
+- **01:15 PROCESS** — Audit spot-checks (SAFE-01, SESSION-10, TERM-28 DONE claims): evidence
+  citations check out against real code (e.g. validate_auth_token at main.rs:1554 confirmed).
+  Kimi audit quality is high WHEN it finishes.
+- **01:23 PROCESS** — The AUDIT_WRONG escape hatch works: SYNC-06 worker independently
+  discovered the feature already landed (PR #592) and refused to invent work instead of
+  fabricating a diff. Give every worker an explicit honest-stop path; it converts audit
+  false-positives from wasted diffs into free re-verification.
+- **01:24 PROCESS** — Continuous-flow beats discrete waves: verify+merge each worker the
+  moment it commits (SESSION-14 landed while 6 peers still ran). Shared CARGO_TARGET_DIR
+  with the main checkout's warm 28G cache made worktree builds fast instead of cold.
+- **01:50 PROCESS** — Topology B (write-only workers + central serial build) first results:
+  6 committed items -> 2 passed unchanged, 3 failed with exactly ONE failing test each
+  (their own new tests — assertion bugs), 1 landed after review. Fix loops via
+  `opencode run -c` with the exact panic output are cheap.
+- **01:55 PROCESS** — Timeouts must be sized to Kimi's thoroughness: 40-min impl timeout was
+  too short for M items (DIAG-04, SESSION-21 both hit it mid-flight). Plan 45-60 min for
+  M-effort items, and instruct WIP commits.
+- **02:00 PROCESS** — `git branch --list` prefixes worktree-checked-out branches with `+`;
+  scripting refs from its porcelain output silently breaks (use --format='%(refname:short)').
+- **02:07 PROCESS/PERF** — DANGER SPIKE: load hit 148 (32 cores), availG down to 13Gi when a
+  4-item cargo verify chain overlapped 8 live agents + 2 session resumes. Recovered without
+  crash; rule adopted: at most ONE verify chain at a time, no new agent launches while a
+  chain runs.
+- **02:11 PROCESS** — Fix-loop effectiveness: 3/3 failed items passed on re-verify after one
+  resume round with exact panic output. Wave-1 Rust yield: 8/8 attempted items landed or
+  honestly closed (7 merged + 1 AUDIT_WRONG).
+- **02:38 PROCESS** — TS/client items are the cheapest wins: SYNC-01/03 (test-coverage gaps)
+  passed vitest first-try. Two workers touching the SAME test file sequentially (fork after
+  the first lands) avoids conflicts entirely. Slot-refill discipline: worker slots are cheap,
+  orchestrator attention is the scarce resource — batch status checks and resume in groups.
+- **04:15 PROCESS** — DIAG-04 escalation path: after FOUR Kimi sessions timed out on the same
+  M item (each produced good docs/skeleton but kept exploring), the orchestrator implemented
+  it directly reusing the sessions' module docs as the spec. Cap retries at ~3, then split
+  the item into S-slices or escalate. Sunk subagent work (docs, skeletons) is still input.
+- **04:23 PROCESS** — FRESH sessions beat resumed sessions for stuck items: BROWSER-02 and
+  AUTO-10 both committed within ~35 min of a fresh-session restart pointed at their own
+  dirty worktrees, after resume rounds failed repeatedly. Resumed sessions inherit
+  exploration momentum; fresh ones inherit only the artifacts.
+- **04:29 TECH** — My own DIAG-04 first verify failed on a trivial Rust lifetime error in a
+  test helper — the same class of single-test failure the Kimi workers produce. The central
+  build catches everyone equally; nobody skips verification.
+- **05:30 PROCESS** — Rebase-in-worker works for conflicts: EXT-01's cherry-pick conflicted
+  with landed EXT-02/EXT-04 changes; instructing the worker session to `git rebase
+  parity-campaign-20260805` in its own worktree and integrate both behaviors resolved it
+  without orchestrator hand-editing. Reserve orchestrator manual resolution for trivial
+  conflicts (SYNC-02's import-formatting conflict took 2 minutes by hand).
+- **06:20 PROCESS** — Salvage-the-dirty-work pattern: SESSION-04's fix session timed out AFTER
+  fixing both compile errors but BEFORE amending. The orchestrator amended the worker's dirty
+  changes directly — always inspect a "failed" worker's worktree before retrying; the fix is
+  often already sitting there uncommitted.
+- **06:45 PROCESS** — Environment-sensitive tests are a Kimi blind spot: AUTO-10's real-shell
+  route test failed 3 identical rounds until explicitly instructed to make it deterministic
+  (seed registry state instead of spawning a shell). Workers don't internalize "your test runs
+  on a different host" without being told.
+- **07:00 STATS** — Final tally: 28 checklist items landed + 1 AUDIT_WRONG (already done) +
+  1 dropped (SESSION-21: 3 sessions, zero output) + 1 cut at deadline (AUTO-05, dirty
+  uncommitted). ~35 Kimi worker/auditor/fix sessions total. Every landed diff passed
+  targeted cargo/vitest verification on the integration branch; full workspace gates run at
+  the end. First-pass worker success ~30%; with fix loops + fresh-session recovery ~90%.
+- **09:15 PROCESS** — NOTE-KEEPING BUG FOUND POST-CAMPAIGN: twelve mid-campaign entries
+  (01:15-04:29) were silently destroyed during the night because the verify script's
+  failure path ran `git reset --hard` in the SAME worktree where LEARNINGS.md appends sat
+  uncommitted. Restored from the orchestrator session transcript. Rule: a ledger that lives
+  inside a worktree subject to reset/cherry-pick automation must be committed on EVERY
+  append (or kept outside the build tree entirely).
+- **10:55 PROCESS/QA** — QA-panel experiment (16 lenses on files.rs). Verification plumbing
+  ate 3 wrong attempts before the right one: (1) shared CARGO_TARGET_DIR served slot-01's
+  cached test binary to all 16 worktrees (WSL2 stale-rlib footgun, AGENTS.md-warned);
+  (2) a fresh dedicated target dir hit an unrelated cross-crate error; (3) per-slot builds
+  same staleness. WINNER: concatenate all 16 uniquely-named qa_probe_NN modules into ONE
+  files.rs, compile+run ONCE, attribute results by module path. One bad module fails the
+  whole build → bisect by line→module, exclude the 2 non-compiling ones, rebuild. Lesson:
+  for N parallel code artifacts that share a package, verify them in ONE build with unique
+  namespaces, not N builds sharing a target.
+- **10:55 QA** — Panel result: 14 compiling lenses → 65 failing proof tests → 17 clusters;
+  narrow adversarial lenses (breaker, spec-clause) beat broad "review this" 3:1; lens
+  DIVERSITY adds clusters, lens REPETITION doesn't; 5-lens set-cover reproduces 100%.
+  Flagship (empty-string allowedFilePaths disables sandbox) found by 7 independent lenses
+  and adjudicated REAL by reading is_path_allowed (files.rs:733). Proof-only rule kept
+  false alarms to 1/66.
diff --git a/docs/plans/parity-ledger/QA-FIX-CAMPAIGN-2.md b/docs/plans/parity-ledger/QA-FIX-CAMPAIGN-2.md
new file mode 100644
index 000000000..093cd4aac
--- /dev/null
+++ b/docs/plans/parity-ledger/QA-FIX-CAMPAIGN-2.md
@@ -0,0 +1,63 @@
+# Closed-Loop Fix Campaign — run 2 (1-hour box, 2026-08-06)
+
+A time-boxed second run on a fresh 6-cluster batch (5 low-confidence single-detector
+"incorrect" candidates + 1 high-convergence security candidate), under collision-proof
+`fable/qa2-20260806/*` naming.
+
+## Outcome: negative result — fix wave timed out with zero commits
+
+All 6 fixers ran the full 40-minute timeout and were killed **before committing**; only one
+worktree (empty-root-sandbox) had surviving uncommitted work, salvaged to its branch as WIP
+(unverified, unreviewed, **not landed**). Nothing reached the quality bar this run. **0 fixes
+landed.**
+
+## Root cause (a reintroduced failure mode)
+
+This is the **exact failure from the very first campaign's audit wave**: agents did careful work
+but wrote no intermediate output, so the timeout forfeited everything. Two compounding causes:
+
+1. **No WIP-commit discipline.** The fixer prompt said "commit when done" with no instruction to
+   commit intermediate progress. Careful adjudication of *low-confidence single-detector* candidates
+   (reading the Node oracle, checking for tests that pin current behavior) ate the whole budget, and
+   the kill erased it. Fix: instruct fixers to commit early and often (WIP commits), and to commit an
+   adjudication note the moment they reach a verdict — before attempting the fix.
+2. **Timeout too short for the task shape.** 40 min fit the *first* run's higher-confidence clusters,
+   but single-detector candidates require more adjudication (is this even a real bug?) before any
+   fix. The timeout must scale with confidence: low-confidence items need longer, or a two-phase
+   split (adjudicate-and-commit-verdict, then fix).
+
+## What this run still validated
+
+- **Collision-proof naming worked.** All artifacts lived under `fable/qa2-20260806/*`; cleanup was
+  a delete-by-explicit-manifest with no risk to shared namespaces (the fix for last run's error).
+- **The negative result is itself the finding.** A quality-first loop that lands nothing is the
+  correct outcome when the work didn't reach the bar — no unverified, unreviewed code was integrated.
+
+## Carry-forward for the next run
+
+- Fixer prompt: "commit WIP early; commit your adjudication verdict (real divergence vs AUDIT_WRONG)
+  before fixing; a partial committed fix beats a perfect uncommitted one."
+- Size timeouts by confidence, or split adjudication from fixing so a slow adjudication can't erase
+  a would-be fix.
+- Low-confidence single-detector candidates are expensive to adjudicate; consider a cheap
+  pre-adjudication pass to drop the AUDIT_WRONG ones before spending a full fixer on them.
+
+## Correction: never kill sessions at the timeout (the real bug)
+
+Run 2's zero-commit result was not caused by slow fixers — it was caused by
+`timeout ... opencode` **killing sessions**, which destroys the most-complete work at the
+worst possible moment. Killing is never the right response to a time limit. The corrected model:
+
+1. **Scope work to fit the box.** Size each unit (and the batch) so it can plausibly finish
+   inside the window. Under-fill rather than over-fill.
+2. **Let running sessions run.** Do not wrap agents in a hard `timeout` that SIGKILLs them.
+   A generous ceiling is fine as a safety net; the orchestrator's clock is not a guillotine.
+3. **When the box expires, STOP and ASK — with a full status update.** Pause new work,
+   report exactly where every in-flight unit stands (adjudicating / fix-drafted / committed /
+   verified / reviewed), and let the human decide: extend, land what's ready, salvage-and-stop,
+   or abandon. The human owns the time/quality tradeoff at the boundary.
+4. **Preserve, never destroy, at the boundary.** Any in-flight work is committed as WIP to its
+   own branch (clearly marked unverified/unreviewed) so a later run can resume it. Nothing is
+   thrown away because a clock struck.
+
+This supersedes the "hard stop / timeout-kill" framing used in run 1 and run 2.
diff --git a/docs/plans/parity-ledger/QA-FIX-CAMPAIGN.md b/docs/plans/parity-ledger/QA-FIX-CAMPAIGN.md
new file mode 100644
index 000000000..91f4ecf19
--- /dev/null
+++ b/docs/plans/parity-ledger/QA-FIX-CAMPAIGN.md
@@ -0,0 +1,101 @@
+# Closed-Loop Fix Campaign — first real run (2026-08-06)
+
+First end-to-end run of the closed-loop fix system: detect → adjudicate → fix →
+**independently review the fix** → verify → integrate. Time-boxed, quality-first.
+
+**Branch:** `qa-campaign-20260806` (off `parity-campaign-20260805`). Not pushed, no PR.
+**Workers:** identical Kimi-K3 via opencode. **Wall clock:** ~56 min (18:02–18:58), well under the box.
+**Input:** 8 confirmed defect clusters from the QA panel, each carrying a failing proof test as its acceptance criterion. Ordered by this run's severity (crash > corruption > incorrect [esp. oracle-misaligned] > resource > cosmetic > security), modulated by significance.
+
+## Result
+
+| Stage | Outcome |
+|-------|---------|
+| Fixers | 8/8 committed real fixes; **0 AUDIT_WRONG** (all adjudicated as genuine Node divergences) |
+| Central verify | 8/8 pass (proof test green + full 640-test server suite green) |
+| Independent review (round 1) | **6 PASS, 2 BOUNCE** — both bounces caught REAL oracle divergences |
+| Rework + re-review | 2 reworked; `tilde_expand` fixed its real defect and landed; `completion_sort` withheld |
+| **Landed** | **7 fixes** (7 of 8 clusters), final integrated suite **646 tests green, rc=0** |
+| Not landed | `completion_sort` — correct outcome, see below |
+
+**Landed (verified + independently reviewed PASS):** mkdir_500_vs_409, posix_not_sanitized,
+mime_content_type, unc_classification, etag_shape, dotseg_collapse_display, tilde_expand — each
+now matches Node behavior with a permanent regression test. `tilde_expand` landed only after its
+first version was bounced (a real `~\/rest` absolute-path bug), reworked, verified, and re-reviewed.
+
+## The review gate earned its keep (the headline)
+
+Two fixes passed their proof test **and** the full 640-test suite, yet a fresh independent
+reviewer — cold context, rotated lens, re-deriving correct behavior from the Node oracle before
+reading the diff, never shown the fixer's rationale — judged both **wrong** and bounced them with
+evidence:
+
+- **completion_sort:** the fix approximated `localeCompare` with lowercase+byte-order — correct
+  for ASCII, but Node's ICU collation interleaves accented letters at the primary level
+  (`éclair` < `zebra`, `e` < `é` < `f`) and expands `ß`≈`ss`. The fix sorted these backwards; its
+  test only pinned the ASCII case.
+- **tilde_expand:** the fix handled `~\proj` correctly but introduced a NEW bug for `~\/rest` —
+  Rust `Path::join` with an absolute argument drops the home base (`/rest` instead of `$HOME/rest`),
+  feeding a wrong absolute path into fs ops and the sandbox check. Worse than pre-fix for that form.
+
+Tests-green is not correct-against-oracle. Only the independent review caught these. This is
+exactly the gap the last campaign left open (31 fixes landed on tests+gates alone, unreviewed).
+
+## `completion_sort`: an honest not-landed
+
+The rework improved it to an ASCII+accent approximation but exact ICU parity needs a real
+collator library. The loop **correctly refused to land an approximation that claimed exactness** —
+surfaced as a human scope decision (accept the documented approximation, or add a collator crate),
+not forced in. Honest not-landed beats a wrong fix that claims parity.
+
+## Process learnings
+
+1. **Integrating onto the branch that outstanding fix-worktrees are based on invalidates their
+   base-diffs.** After 5 fixes landed on the integration branch, the two rework worktrees (still
+   branched from the pre-integration point) produced diffs that *appeared* to revert the 5 landed
+   fixes, contaminating round-2 review. Fix: rebase outstanding worktrees after each integration,
+   OR compute each review diff against the fix's own merge-base, never the moving integration branch.
+2. **The seam is where both bugs and merge-conflicts concentrate.** Four fixes touched the
+   path-normalization region; they conflicted on integration and had to serialize/rebase. Budget
+   for seam serialization — do not assume independent fixes to one subsystem integrate cleanly.
+3. **Shared build-cache contamination struck again** (a fix false-failed verification with errors
+   in unrelated crates). Verify each variant in an isolated target, or serialize strictly and
+   distrust a failure whose errors are outside the changed file.
+4. **Adjudication held:** 0/8 fixers cried AUDIT_WRONG — the high-convergence clusters were all
+   real, consistent with treating detector-convergence as the confidence signal.
+
+## Metrics that matter
+
+- **Review bounce rate:** 2/8 (25%) at round 1 — and both bounces were real defects, not noise.
+- **Land rate to full quality bar:** 6/8 landed verified + independently reviewed; 1 honest
+  not-landed (scope); 1 verified + endorsed pending final seam-reconcile.
+- **False landings:** 0 — nothing landed without a passing proof test AND an independent review PASS.
+- **Cost:** ~56 min wall-clock, ~20 agent sessions (8 fix + 8 review + reworks/reconciles).
+
+Success here is not "8 bugs fixed" — it is **coverage to a quality bar with evidence**: every
+landed fix carries a failing→passing regression test and an independent review PASS, and every
+not-landed item is surfaced with its reason.
+
+## Cleanup safety (learned the hard way)
+
+The highest-blast-radius commands in a campaign — `branch -D`, `worktree remove --force`,
+`reset --hard`, `rm -rf` — cluster at the END, exactly when attention flags. A greedy
+`git branch -D fix/*` in this run's cleanup swept up a pre-existing user branch that merely
+shared the generic `fix/` prefix (recovered from `origin`, but it was unrecoverable-by-luck).
+Rules for every future run:
+
+1. **Namespace your artifacts so they cannot collide.** Create all branches/worktrees under a
+   campaign-unique prefix — `fable/<campaign-id>/<item>` for branches,
+   `.worktrees/fable-<campaign-id>-<item>` for worktrees. Then even a glob is safe by
+   construction, because nothing else lives in that namespace. Never reuse a generic prefix
+   (`fix/`, `qa/`) the repo already uses.
+2. **Delete by explicit list, never by pattern.** Maintain the exact list of branches/worktrees
+   you created; delete precisely that list. A wildcard can only match things you didn't create.
+3. **Dry-run first, and read the output.** Before any bulk delete, run the read-only form
+   (`git branch --list`, `git worktree list`) and confirm every entry is yours. Unconditionally —
+   not only when you feel careful.
+4. **Prefer recoverable over forced.** Use `git branch -d` (refuses unmerged) over `-D`. Campaign
+   fix branches are cherry-picked/merged, so `-d` succeeds for them and *refuses* anything
+   unmerged — turning a silent destruction into a flagged error.
+5. **Cleanup gets more scrutiny, not less.** Treat the wind-down as the most dangerous phase.
+   This repo is self-hosted off worktrees; a wrong `--force` can hit something load-bearing.
diff --git a/docs/plans/parity-ledger/QA-PANEL-EXPERIMENT.md b/docs/plans/parity-ledger/QA-PANEL-EXPERIMENT.md
new file mode 100644
index 000000000..ab99a5c20
--- /dev/null
+++ b/docs/plans/parity-ledger/QA-PANEL-EXPERIMENT.md
@@ -0,0 +1,108 @@
+# QA Panel Experiment — 16 parallel reviewers on one file (fable, 2026-08-05)
+
+**Question:** how do you configure a swarm of identical-model review agents to reliably
+find defects? The unit under test is the **instruction (review lens)**, not the agent
+(all 16 are the same Kimi-K3 model, cold-started, building their own context).
+
+**Target:** `crates/freshell-server/src/files.rs` — densest/riskiest single file from the
+overnight campaign (FILE-01/02/03/06 landed here; security-sensitive path/sandbox code).
+
+**Method:** 16 agents, 12 distinct lenses (broad ×3, parity ×2, breaker ×2, then
+test-adequacy / security / anti-anchoring / error-shapes / boundary / concurrency /
+spec-clause / free-hunt / panic ×1). Replicates on the top 3 lenses give a noise floor.
+**Proof-only rule:** a finding counts ONLY as a Rust `#[cfg(test)]` proof test that FAILS
+against current code (proving the defect) and would pass once fixed. No proof = not a finding.
+Central verification compiled all 16 modules together (one build, unique module names) and
+ran them; a failing test = candidate defect, a passing test = false alarm.
+
+## Results
+
+- 16/16 agents completed; **14 produced compiling proofs, 2 (both `parity` lens) did not**
+  (non-compiling Rust — a real data point: that lens over-reached on API details).
+- **66 verified proof tests → 65 failing (candidate defects) + 1 false alarm.**
+- Dedupe → **17 distinct defect clusters** (13 found by ≥2 independent lenses, 4 singletons).
+- Capture-recapture (Chao1) on the convergence: estimated true pool ≈ 42 defects, so this
+  single-file panel surfaced **~40% of estimable defects** in one pass.
+
+### Defect clusters by lens-convergence (higher = more independent confirmation)
+
+**Ranking axis (REVISED):** this is a parity campaign, so clusters are ranked by
+**confirmed divergence from the Node reference × user/feature-visibility**, NOT by security
+severity. Security is explicitly not a primary consideration here; the three security-flavored
+clusters are demoted to a separate "config-edge" tier below and are fix-only-if a real Node
+divergence at low priority.
+
+### Tier 1 — parity defects (the fix list), ranked by impact × lens-convergence
+
+| Lenses | Cluster (behavioral divergence from Node) |
+|--------|-------------------------------------------|
+| 7 | `mkdir` with a file as an intermediate component returns **500**, not **409/ENOTDIR** — API error-shape divergence |
+| 6 | POSIX/native path lane never runs `sanitize_user_path_input` — **quotes/padding survive** into resolved+display paths |
+| 5 | **MIME/Content-Type** table diverges from legacy `send` (`.avif`/`.ico`/`.wav`/unknown→octet) — wrong header users/tools see |
+| 4 | **dot-segments not collapsed** in the `resolvedPath` echoed to the client (validate-dir/complete) + mkdir strays |
+| 4 | completion suggestions **sort byte-wise**, not Node `localeCompare` — visible ordering divergence |
+| 4 | `~\` (backslash tilde) **never expanded** to `$HOME` |
+| 2 | **ETag shape**: strong (`"…"`) vs Node's weak form |
+| 1 | **UNC** not classified by `is_absolute_user_path` |
+| 1 | 3 singletons: write non-string content→400, duplicate `path` query key handling, relative-completion cwd anchor |
+
+### Tier 2 — config-edge / security-flavored (DEMOTED; verify vs Node, low priority)
+
+| Lenses | Cluster | Node-diff status |
+|--------|---------|------------------|
+| 3 | `/local-file` serves final-segment dotfiles (e.g. `.env`) with 200 | plausibly user-visible; keep, mid |
+| 7 | Empty-string `allowedFilePaths` entry disables the sandbox (`is_path_allowed` files.rs:733) | **VERIFIED real divergence** — Node `resolvePathForSandboxComparison("")`→cwd (path-utils.ts:308) vs Rust→`/`. But requires a literal `""` config; **low priority** |
+| 5 | symlink + `..` sandbox escape (comparison collapses `..`; kernel op follows real path) | likely real divergence; edge; low priority |
+
+**Adjudication note (reframed):** for a parity goal the gate is *"does it diverge from
+`server/*.ts`?"*, not *"is it exploitable?"*. I verified the empty-root cluster against
+Node's `isPathAllowed` (path-utils.ts:308): it IS a divergence, but a config-edge one — which
+is exactly why it drops from "fix first" to Tier 2. A failing proof test remains a *candidate*
+until its Node divergence is confirmed; convergence (≥2 lenses) is the confidence signal.
+
+## What this says about instructions (the actual experiment goal)
+
+Per-lens score (verified slots), clusters found / unique-to-this-lens / false alarms:
+
+| Lens | clusters | unique | false alarm | note |
+|------|----------|--------|-------------|------|
+| **breaker** (×2) | 7, 7 | 1, 1 | 0 | best raw yield; adversarial "make it fail" wins |
+| **spec_clause** | 5 | **3** | 0 | most UNIQUE finds — checklist-clause enumeration hits blind spots others skip (UNC, cwd-anchor) |
+| **free_hunt** | 5 | 0 | 0 | strong but fully covered by others |
+| **parity** | 5 | 0 | 1 | + its two replicates FAILED TO COMPILE — lens over-reaches |
+| **anti_anchor / boundary / error_shapes / panic** | 4 | 0–1 | 0 | solid mid-tier; error_shapes/panic each caught 1 unique |
+| **broad** (×3) | 2, 3, — | 0 | 0 | weakest: unfocused review underperforms every narrow lens |
+| **security / test_adequacy / concurrency** | 1–2 | 0 | 0 | narrow scopes; concurrency near-empty (little async surface here) |
+
+**Findings about the process:**
+1. **Narrow, adversarial lenses beat broad "review this."** The three `broad` replicates
+   were the *worst* performers; `breaker` (assume it's broken, construct failing inputs) and
+   `spec_clause` (enumerate every checklist clause, prove each) carried the panel.
+2. **Diversity of lens > repetition of lens.** Replicates confirmed known defects but added
+   almost no new clusters; different lenses found different defects. → "massively parallel QA"
+   should mean **many different lenses**, not many reruns of one.
+3. **The proof-only rule works as a false-alarm filter.** 65/66 failing tests, 1 false alarm,
+   2 non-compiling proof sets auto-excluded — noise stayed low without human triage.
+4. **Convergence is a usable confidence meter**, and capture-recapture turns "how many did we
+   miss?" into a number (~40% caught here → a second differently-lensed wave is worth running).
+
+### Minimal deployable panel (greedy set-cover over all 17 clusters)
+
+**5 lenses reproduce 100% of what all 14 found:** `breaker` + `spec_clause` + `breaker`(2nd) +
+`error_shapes` + `panic`. Practically: **breaker + spec_clause + error_shapes + panic + one
+boundary/anti-anchor**, run per file. Drop `broad` entirely; keep `parity` only with a
+"tests must compile — don't over-specify APIs" instruction fix.
+
+## Recommended playbook (draft, pre-deployment)
+
+- Per file under review: run **~5 narrow lenses** (breaker, spec-clause, error-shapes, panic,
+  boundary), proof-only, one combined central verification build, dedupe by cluster.
+- Treat ≥2-lens clusters as high-confidence; single-lens + low-severity as needs-adjudication.
+- Use capture-recapture per file to decide whether a second wave is warranted (<~60% ⇒ yes).
+- **Not yet deployed for real QA** — this validated the recipe on one file. Obvious next
+  target: run the 5-lens panel across the rest of the campaign branch's 31 diffs, and
+  adjudicate + fix the flagship empty-root sandbox bypass first (it's security-relevant).
+
+## Reproduction / raw data
+- Lenses: `/tmp/qa/slots/*/lens.txt`; per-agent findings: `w-qa-NN/findings.md` (branch `qa/NN`).
+- Verified test results: `/tmp/qa/all_results.txt`; combined build: one file, unique modules.
diff --git a/docs/plans/parity-ledger/QA-SYSTEM-DESIGN.md b/docs/plans/parity-ledger/QA-SYSTEM-DESIGN.md
new file mode 100644
index 000000000..a090fd43b
--- /dev/null
+++ b/docs/plans/parity-ledger/QA-SYSTEM-DESIGN.md
@@ -0,0 +1,241 @@
+# Parallel Agent Bug-Fix System — closed-loop quality at scale
+
+**The problem this solves:** you have a codebase, a large supply of identical-model coding
+agents, and a mandate: *find and fix the bugs to a high quality bar, with every fix
+independently reviewed before it lands.* Agents are cheap and plentiful; trust is not — an
+agent's claim that it found, fixed, or approved something is worth nothing by itself. This
+design turns claims into evidence at every stage, so N agents can run in parallel without
+N agents' worth of human babysitting. Quality is the primary objective; speed is second.
+
+---
+
+## 1. The golden thread: one proof test spans the whole loop
+
+The single load-bearing idea: **a failing proof test — an executable test in the
+project's own language and toolchain that fails against the current code — is the one
+artifact that carries a defect through every stage.**
+
+- **Detection** produces it: a finding *exists* only as a proof test that FAILS against
+  current code. This rule is the noise filter — a claim either demonstrates itself or it
+  is noise, and telling the two apart needs no human triage.
+- **Adjudication** validates it: the test is a *candidate* until an objective oracle (§2)
+  confirms the behavior is actually wrong rather than intended.
+- **The fix** is defined by it: the fixer's acceptance criterion is mechanical — *this*
+  test goes green and everything else stays green.
+- **The fix-review** is anchored by it: the reviewer judges whether the change makes
+  *this* proof pass for the *right* reason, matching expected behavior the reviewer
+  derived independently.
+- **Integration** keeps it forever: the proof test lands with the fix as permanent
+  regression coverage.
+
+One artifact, zero translation loss between stages, and every gate becomes
+machine-checkable. Everything else in this design is scaffolding around this thread.
+
+## 2. The objective oracle: deciding bug-or-not without model judgment
+
+An **oracle** is any mechanism that classifies a behavior as a defect *without asking a
+model to judge*: a deterministic, executable check whose verdict stands on its own.
+Oracle *strength* is how directly the check asserts intended behavior — strongest when it
+asserts an explicit, documented contract clause; weakest when it merely flags an anomaly
+for tracing. Where the target has a trusted alternate implementation, differential
+comparison against it is one optional plug-in oracle among whatever others the project
+admits — never the backbone — and confirmed divergence from a trusted reference can raise
+a bug's severity within the launch-time matrix (§3).
+
+The oracle is load-bearing for two reasons: it removes model judgment from the two most
+expensive stages (adjudication and fix-verification), and it is the **only check in the
+system that does not share the model's blind spots** (§5). During map, configure the
+strongest oracles the target admits. Where no oracle reaches a surface, adjudication
+falls back to an agent tracing expected behavior from spec and docs with citations — the
+loop survives, just slower and more judgment-bound.
+
+## 3. Severity: a launch-time input
+
+Severity is **configuration the user supplies at launch, not a law of the system**. What
+counts as a bad bug depends entirely on what the code is for, and no hardcoded ordering
+survives contact with a second codebase — so this system does not have one.
+
+**The severity matrix** — the bug categories that matter for *this* target and their
+relative weight and ordering — **is a launch-time parameter.** Every downstream consumer
+references this one matrix and defines no ranking of its own: lens-panel composition
+(§7), the severity class assigned to each bug card at adjudication (§4), fix-queue
+ordering and batching (§7, §8). This section is the single authority.
+
+**If the user does not supply a matrix, the system asks — it never proceeds silently on a
+default.** During the map stage, before any detection launches, the orchestrator inspects
+whatever context it has — the code, its stated purpose and domain, its docs and
+configuration, its dependencies, its exposure — and **proposes a tailored matrix with a
+rationale for each ranking**, derived from that context. The user confirms or adjusts;
+the run does not advance past map until the matrix is ratified. This is a human gate at
+launch (§8).
+
+**The default starting matrix** is the seed the orchestrator adapts from when building
+its proposal — a starting point the user ratifies, never a silent fallback:
+
+1. security
+2. crash / hang
+3. data loss / corruption
+4. incorrect behavior
+5. resource / performance
+6. cosmetic
+
+**Severity and confidence are orthogonal; priority is their product.** *Severity* is the
+impact class a confirmed bug draws from the ratified matrix. *Confidence* is how sure the
+system is the bug is real: detector convergence (independent lenses finding the same
+cluster) plus oracle strength (§2). **Priority = severity (from the matrix) ×
+confidence.** Any stage that orders work uses this product; no stage redefines either
+axis.
+
+## 4. The closed loop: stages and gates
+
+Work flows through seven stages: **map → detect → adjudicate → fix → review-the-fix →
+verify/regress → integrate**, with an evidence gate between each. The unit of work is a
+**behavioral surface** — a coherent unit of externally observable behavior with its own
+spec, however the target naturally decomposes — never a file, because defects cross
+files.
+
+| Stage | Who | Gate artifact to ADVANCE | Bounce condition |
+|---|---|---|---|
+| **Map** | orchestrator, once | Surface registry entry: ID, spec/docs pointers, which oracles reach it — plus a **ratified severity matrix** (§3) before anything downstream launches | malformed or unreferenced surface; unratified matrix → the run does not proceed |
+| **Detect** | lens agents, wide (§7) | Proof test that **compiles in the central combined build and FAILS**, uniquely namespaced so results self-attribute | doesn't compile → discard; passes → false alarm, logged |
+| **Adjudicate** | oracle runs + tracer agents | **Bug card**: oracle evidence (or traced expected-behavior citation), severity class assigned from the ratified matrix (§3), detector-convergence count | oracle refutes → close as not-a-bug; intent ambiguous → **human queue, never guessed** (§8) |
+| **Fix** | fixer agent, own isolated workspace | Commit where the proof test **passes**, touched suites green — **checked centrally, never self-certified** — plus the fixer's own new tests | central check disagrees → exact failure fed back to the session; ~2 stalls → fresh session on the same workspace; ~3 strikes → split the ticket or escalate |
+| **Review-the-fix** | independent cold agent (§5) | **Verdict with evidence**: independently derived expected behavior, judgment that the diff achieves it, optionally extra probe tests. "LGTM" without evidence bounces the *review*, not the fix | REQUEST-CHANGES with concrete notes → back to fix |
+| **Verify/regress** | orchestrator, serial (§6) | Applied onto the integration branch; proof test green + full touched suites green + oracle check clean where reachable | any regression → revert, bounce to fix with the failure |
+| **Integrate** | orchestrator | Change landed with explicit scope; proof test now permanent; ledger updated | — |
+
+**Bounces are normal, not exceptional.** Expect roughly 30% first-pass success from agent
+sessions; the recovery ladder — feed the exact failure back, then a fresh session on the
+same workspace, then split or escalate — raises completion to roughly 90%. Budget for the
+loops.
+
+One verification rule: **combine all agents' proof tests into a single build under unique
+namespaces and attribute results by test name.** N parallel builds sharing a build cache
+will silently serve one agent's binary to every other agent's run, and every result but
+one becomes fiction.
+
+## 5. Independence with identical models
+
+Every agent is the same model, so independence cannot come from *who* reviews — it must
+be manufactured from *what each session knows and is asked*. Four rules, enforced by the
+dispatcher, not by trust:
+
+1. **Never author-reviews-own-work.** The review is always a fresh cold session sharing
+   no context with the fixer's session. Cold start *is* the isolation mechanism.
+2. **The reviewer never sees the fixer's rationale.** It gets the surface spec, the proof
+   test, and the diff — nothing the fixer wrote about them. The diff must stand on its
+   own.
+3. **Anti-anchoring.** The reviewer writes down expected behavior — from spec, docs, or
+   oracle — *before* opening the diff. An identical model that derives the answer
+   independently and then compares is a genuinely different measurement from one that
+   reads the fix first and rationalizes it.
+4. **Lens rotation.** The review lens differs from the lens that detected the bug, so the
+   fix is probed from an angle its origin story didn't cover.
+
+**Honest limit:** identical models share blind spots. A behavior the model *cannot
+conceive as wrong* will pass fixer, reviewer, and detection panel alike. Only a non-model
+oracle (§2) escapes this — which is why the oracle is what you prototype first (§10).
+
+## 6. Allocating N agents: work-stealing with a bounded build neck
+
+The economics: **detection is cheap and wide** (remote inference, near-zero local cost);
+**builds and verification are the expensive serial resource** — the neck. The allocation
+follows:
+
+- Agents hold no fixed roles; they **pull tickets from a queue** — a durable ledger
+  persisted on every append and stored outside any workspace that automation may reset,
+  because a reset destroys unpersisted appends.
+- **Pull priority drains downstream first**: review > fix > adjudicate > detect. This
+  bounds work-in-progress — quality-first means finishing what's in flight before finding
+  more — while detection soaks up all remaining slots because it's nearly free.
+- **The orchestrator is not one of the N** and owns the neck: central verify/regress
+  builds run strictly 1–2 chains at a time, with nothing else launching while a chain
+  runs — overlapping build chains alongside live agents saturates the host.
+- Early in a campaign the queue naturally skews toward detection; late it inverts.
+  Allocation follows ready work, not a plan.
+
+## 7. Detection: diverse adversarial lenses, scheduled by convergence
+
+**A panel of narrow, adversarial, single-question lenses beats broad review.** A model
+given one sharp question probes deeper than one asked to be generally vigilant, and
+different questions surface different defects — so each detection agent gets exactly one
+lens. The starting panel:
+
+- **breaker** — assume the surface is broken; construct the input that proves it
+- **spec-clause** — enumerate every documented contract clause; prove each one holds
+- **error-path** — prove failure handling, including partial failure, matches the contract
+- **invariant** — prove the properties the surface must preserve actually hold
+- **boundary** — probe the extremes and edge conditions of every input
+- **resource** — prove resource use is bounded, released, and steady under load
+
+**The panel is parameterized by the ratified severity matrix (§3).** Categories the user
+weighted heavily get dedicated lenses; categories weighted low lose their slots to lenses
+that serve the matrix. The panel above is the floor the matrix reshapes, not a fixed
+roster.
+
+**Diversity beats repetition.** Replicating a lens mostly re-confirms known bugs;
+*different* lenses find *different* bugs. Parallelism means many lenses across many
+surfaces, not many reruns.
+
+**Convergence is the scheduler.** Per surface: run one panel wave, dedupe findings into
+clusters, compute overlap between lenses. "Done to depth" means a second,
+differently-lensed wave adds ≤1 new cluster, or a capture-recapture estimate over the
+lens-overlap counts puts the found fraction of the estimable defect pool at ≥~80%. Then
+the detection budget moves on — breadth across surfaces over depth on any one.
+
+**Bug cards are ordered by priority = severity × confidence, both defined once in §3.**
+How alarming a bug sounds is not a ranking axis; the ratified matrix is.
+
+## 8. Where the human sits
+
+Four places, no more:
+
+1. **Severity ratification at launch.** If the user supplied no severity matrix, the run
+   pauses on the orchestrator's context-derived proposal (§3) until the user confirms or
+   adjusts it. Nothing downstream runs against an unratified matrix.
+2. **Approval gates.** All work accumulates on a single integration branch; nothing
+   merges to a protected branch, and nothing ships, without explicit human approval.
+3. **Intent calls.** When adjudication confirms a behavior is surprising but cannot tell
+   whether it is *intended*, the ticket surfaces with its bug card for a human yes/no.
+   The system never guesses intent. Batch these by pattern so they don't swamp the queue.
+4. **Batch review.** Fixed-and-reviewed changes are grouped per surface (5–10 fixes) and
+   presented in priority order (§3), each batch carrying its proof tests, bug cards, and
+   review verdicts — so human review is *reading evidence*, not re-deriving it.
+
+## 9. Success metric
+
+**Coverage-to-convergence with evidence — not throughput.** The campaign is done when:
+every registered surface has been detected to saturation (convergence rule met, estimate
+recorded); every confirmed bug has a proof test that went failing→passing, an independent
+fix-review PASS with cited evidence, and a clean oracle check where one reaches; and the
+intent-ambiguous residue is enumerated for human decision. "We landed N fixes" is
+explicitly not the metric — N fixes without the evidence chain is exactly the failure
+mode this design exists to prevent.
+
+## 10. Honest risks, and what to prototype first
+
+- **Correlated blind spots** (one model everywhere). Mitigated by non-model oracles and
+  lens diversity; not eliminated. State the residual risk in the final report rather than
+  papering over it.
+- **Oracles can be expensive or noisy.** Differential and replay-style oracles need
+  normalization of legitimately-variable output; if the noise floor drowns the signal,
+  the oracle degrades into a hint generator. Measure the noise floor before trusting the
+  verdicts.
+- **Proof tests can encode the detector's misunderstanding.** Adjudication against an
+  oracle exists precisely for this; the residual case (test and adjudicator wrong the
+  same way) is the blind-spot risk again.
+- **Orchestrator attention is the true scarce resource.** The queue, the mechanical
+  gates, and evidence-carrying tickets all exist to keep humans and the orchestrator out
+  of per-item judgment. If the loop needs constant babysitting, the design has failed
+  regardless of agent count.
+- **Intent ambiguity can flood the human queue** on codebases with much deliberate quirk.
+  Batch by pattern, never one-by-one.
+
+**Prototype first: the strongest objective oracle the target admits.** It converts the
+two most judgment-heavy, orchestrator-expensive stages (adjudication and
+fix-verification) into machine checks, it is the only check outside the model's blind
+spots, and it usually carries the most unknown setup cost — which is exactly why it goes
+first. Acceptance test: within about a day of setup, it should re-confirm two
+already-known bugs and demonstrate a quiet noise floor. If it does, build the loop around
+it. If it cannot be made quiet, fall back to citation-based tracing for adjudication (§2)
+— the loop survives, just slower and more judgment-bound.
diff --git a/docs/plans/parity-ledger/STATE.md b/docs/plans/parity-ledger/STATE.md
new file mode 100644
index 000000000..95421a0a4
--- /dev/null
+++ b/docs/plans/parity-ledger/STATE.md
@@ -0,0 +1,29 @@
+# Parity Campaign — Orchestrator State
+
+**Deadline:** 2026-08-05 09:00 (user returns; expects learnings write-up + single review-ready branch)
+**Integration branch:** `parity-campaign-20260805` at `.worktrees/parity-campaign`
+**Checklist:** `docs/plans/2026-07-14-rust-tauri-parity-completion-checklist.md` (235 IDs, 7 checked pre-campaign)
+
+## Goals (equal priority)
+1. Develop a great process for massively parallel agents (opencode + ms-runpod/moonshotai/Kimi-K3).
+2. Land as many missing parity features as possible, merged into the single branch above.
+
+## Hard constraints
+- NEVER restart the live server (port 3001). No PRs. No pushes to main. No destructive suites outside sandbox.
+- Commits use explicit pathspecs. Perf watched continuously (don't crash the 32-core/49Gi box).
+- Baseline suite must be green before merging implementation work.
+
+## Phase status (final, 07:05)
+- [x] AGENTS.md port fix (47f5c68f5)
+- [x] Baseline: cargo workspace GREEN; npm suite green except pre-existing api-edge-cases __proto__ failure
+- [x] Phase 0 audit: 18 auditors, 235 items re-verdicted (audit/*.yaml)
+- [x] Implementation waves: 28 items landed (see CAMPAIGN-RESULTS.md)
+- [x] DIAG-04 perf logging landed (user priority)
+- [x] Final gates rd1 GREEN (cargo 0, npm 0); rd2 running after bonus round (gate2-*.log)
+- [x] WRITEUP.md + LEARNINGS.md + CAMPAIGN-RESULTS.md written
+
+## Scratch layout
+- /tmp/parity/sections/  — checklist split by section
+- /tmp/parity/audit/     — auditor YAML outputs + status
+- /tmp/parity/workers/   — implementation worker dirs
+- /tmp/parity/logs/      — baseline + monitor logs
diff --git a/docs/plans/parity-ledger/WRITEUP.md b/docs/plans/parity-ledger/WRITEUP.md
new file mode 100644
index 000000000..ba78bc0c7
--- /dev/null
+++ b/docs/plans/parity-ledger/WRITEUP.md
@@ -0,0 +1,45 @@
+# Overnight Parallel-Agent Campaign — What I Learned (fable, 2026-08-05, 00:15–09:00)
+
+**The headline:** 31 parity checklist items landed on a single review-ready branch
+(`parity-campaign-20260805`), each verified by targeted cargo/vitest runs on the integration
+branch, using ~35 opencode sessions running `ms-runpod/moonshotai/Kimi-K3` as the workforce —
+plus a complete evidence-cited re-audit of all 235 checklist items that corrects a badly stale
+ledger (only 7 boxes were checked; reality is 31 DONE / 118 PARTIAL / 58 MISSING / 23 UNCLEAR /
+5 HOST-LIMITED). One item honestly closed as already-done (SYNC-06), and one dropped after three
+zero-output sessions (SESSION-21). Even the item cut at the first deadline (AUTO-05) was
+salvaged by a fresh session pointed at its dirty worktree in a bonus round after gates went green.
+
+**What worked — the winning process shape.** The most effective topology was *write-only
+workers + central serial verification*: each worker gets its own git worktree and a spec
+(checklist item text + audit findings + pacing rules), writes code and tests WITHOUT running
+cargo, and commits with explicit pathspecs; the orchestrator cherry-picks each branch onto the
+integration branch and runs targeted tests there against a shared warm CARGO_TARGET_DIR. This
+kept 28GB of build cache shared, made verification serial (no build storms), and made every
+failure attributable to exactly one item. First-pass success was only ~30%, but recovery loops
+raised the effective land rate to ~90%: feed the exact panic output back into the same session
+(`opencode run -c`) for one fix round; if a session times out twice, start a FRESH session
+pointed at the dirty worktree — fresh sessions decisively finish what resumed sessions endlessly
+re-explore. The audit phase (18 read-only auditors in parallel, one per checklist section)
+was the single highest-value step: it turned a fiction (7/233) into an implementable backlog of
+133 S/M items and its evidence quality survived spot-checks.
+
+**What failed — and the guardrails that emerged.** Kimi-K3 is thorough to a fault: 17 of 18
+auditors blew a 25-minute timeout with zero output because they wrote results only at the end
+(fix: incremental output + resume-and-dump recovers everything); M-sized items routinely needed
+45–60+ minutes; and DIAG-04 (the perf logger you asked for early) consumed four sessions before
+I implemented it myself from the sessions' excellent module docs — the cap-retries-then-escalate
+rule is now explicit. Environment-sensitive tests are a blind spot (AUTO-10's real-shell test
+failed three identical rounds until told to seed state deterministically). Perf-wise the box
+survived but had two scares: load 148/32-cores when a 4-item verify chain overlapped ~10 live
+agents (rule: ONE verify chain at a time), and a reminder that the Amplifier bash tool kills
+process groups on timeout — long jobs must use run_in_background. Repo-side learnings: the live
+Rust server runs on port 3001 not 3002 (AGENTS.md fixed), `X-Auth-Token` is the only accepted
+auth header, and main's server suite has one pre-existing failure (`api-edge-cases` __proto__
+test) that predates this campaign.
+
+**What I'd do next time.** Bake the pacing contract into the FIRST prompt (hard budget, commit
+early, WIP commits allowed); split anything M-sized into S-slices before dispatch; run the
+audit wave with incremental output from the start; and keep the fix-loop/fresh-session ladder —
+it's cheap and it converts almost every "failed" worker into a landed feature. The full running
+log is in `LEARNINGS.md`, per-item results in `CAMPAIGN-RESULTS.md`, and the re-audit in
+`audit/*.yaml`, all committed on this branch.
diff --git a/docs/plans/parity-ledger/audit/final-release-gates.yaml b/docs/plans/parity-ledger/audit/final-release-gates.yaml
new file mode 100644
index 000000000..18faf02c7
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/final-release-gates.yaml
@@ -0,0 +1,135 @@
+- id: GATE-01
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "test/e2e-browser/playwright.config.ts:194-211 — Node/Rust matrix machinery EXISTS: `legacy-chromium` project (e2eServerKind 'legacy') and `rust-chromium` project (e2eServerKind 'rust') run the SAME MATRIX_SPECS files via helpers/fixtures.ts server-kind routing"
+    - "test/e2e-browser/playwright.config.ts:13-82 — MATRIX_SPECS is a curated ~21-regex SUBSET; the header comment (lines 5-12) explicitly says 'Deliberately a SUBSET of ./specs, not the whole suite... Grow this list as more specs are verified against the Rust target'"
+    - "test/e2e-browser/specs/ contains ~88 spec files; rust-chromium runs MATRIX_SPECS plus ~40 rust-only specs (playwright.config.ts:221-359), but the majority of the legacy suite (e.g. terminal-lifecycle-adjacent UI specs, mobile-viewport, pane-system, settings, tab-management, etc.) never runs against Rust"
+    - "test/e2e-browser/specs/screenshot-baselines.spec.ts and editor-pane.spec.ts(-snapshots) exist (committed visual baselines) but are NOT in MATRIX_SPECS — visual baselines run on legacy chromium only"
+    - "test/e2e-browser/specs/harness-02-matrix-bite.spec.ts — matrix self-test spec present in MATRIX_SPECS (line 16)"
+    - "grep for 'skip.*report|skipped.*report' across test/ found NO machine-readable skipped-test report generator or validator"
+    - "docs/plans/2026-07-18-checklist-reconciliation.md:349 — reconciliation already grades GATE-01 'P' ('only 8 specs run on rust-chromium; the empty-skip report is not achievable until the full suite runs on Rust'); matrix has since grown but remains a subset"
+  missing: "Full legacy suite not run against Rust (matrix is a deliberately curated subset, not 'every externally and lifecycle targetable spec'); committed visual baselines (screenshot-baselines, editor-pane snapshots) do not run on rust-chromium; no machine-readable skipped-test report exists at all (nothing to be empty or approved)"
+  effort: L
+  files:
+    - test/e2e-browser/playwright.config.ts
+    - test/e2e-browser/helpers/fixtures.ts
+    - test/e2e-browser/specs/
+
+- id: GATE-02
+  verdict: HOST-LIMITED
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/ — full Rust Tauri shell exists (server.rs, tray.rs, updater.rs, renderer_recovery.rs, provisioning.rs, state_machine.rs, windows.rs, hotkey.rs, config.rs, health.rs)"
+    - "crates/freshell-tauri/tests/server_spawn_smoke.rs:1-10 — headless integration smoke driving the REAL freshell-server binary through spawn -> /api/health gate -> reap ('app_bound_spawn_health_reap_end_to_end'), explicitly 'no display and no webview required'; soft-skips without the binary"
+    - "scripts/assert-native-windows-build.ts:1-30 — win32 platform guard for the WINDOWS ELECTRON build (electron:build:win), not a Tauri WebView2 test gate"
+    - "test/e2e-electron/electron-app.test.ts + playwright.electron.config.ts — Playwright suite exists but targets the ELECTRON (Chromium) shell, not the packaged Tauri WebView2 installer"
+    - "grep WebView2/webview2 across the repo: only docs/plans references and one comment in crates/freshell-tauri/src/renderer_recovery.rs:33 — NO HARNESS-07 native Windows Tauri Playwright fixture (chromium.connectOverCDP against real WebView2) exists in code"
+    - "docs/plans/2026-07-14-rust-tauri-parity-completion-checklist.md:48-50 — HARNESS-07 (native Windows Tauri fixture) and GATE-02 (line 820) are still unchecked box items"
+    - "docs/plans/2026-07-18-checklist-reconciliation.md:350 — GATE-02 graded 'H' (host: clean Windows VM)"
+  missing: "Entire packaged-installer WebView2 gate: disposable clean Windows VM install, connectOverCDP attach to real WebView2, startup/wizard/chooser/tray/recovery/network/provider/migration/update scenario suite, uninstall with retained traces and process/config receipts. This host (Linux) cannot substitute per the gate's own text"
+  effort: L
+  files:
+    - crates/freshell-tauri/src/
+    - crates/freshell-tauri/tests/server_spawn_smoke.rs
+    - test/e2e-electron/playwright.electron.config.ts
+    - scripts/assert-native-windows-build.ts
+
+- id: GATE-03
+  verdict: HOST-LIMITED
+  confidence: high
+  evidence:
+    - "grep '16.?tab|sixteen' repo-wide: matches ONLY in docs/plans/2026-07-14-rust-tauri-parity-completion-checklist.md (lines 210, 777, 823) — the 16-tab migration fixture does NOT exist anywhere under test/fixtures/ or crates/"
+    - "test/fixtures/ contains only coding-cli, fresh-agent, resume-input, sessions (jsonl transcript fixtures: corrupted-*.jsonl, healthy.jsonl etc.), testing — no legacy Electron profile fixture, no hashed immutable profile"
+    - "docs/plans/2026-07-18-checklist-reconciliation.md:328 — all MIGRATE-01..13 items graded 'H' (host: Electron->Tauri Windows migration, HARNESS-10); GATE-03 graded 'H' (line 351: 'Windows VM + Electron profile')"
+    - "grep migrate/migration in crates found only settings-schema/config migrations (crates/freshell-server/src/settings_store.rs, settings.rs, session_directory.rs) — NO Electron->Tauri export/import bridge code (MIGRATE-02) found in Rust"
+    - "No rollback-into-restored-working-copy harness or immutable-source byte-identity comparator found in scripts/ or test/"
+  missing: "Everything: the 16-tab fixture, the large loss-detection config fixture, immutable hashed source-profile tooling, migration working-copy clone/migrate/compare harness, rollback-and-relaunch comparator. Gate is only executable on a Windows host with packaged Electron + Tauri installers regardless"
+  effort: L
+  files:
+    - test/fixtures/
+    - crates/freshell-tauri/src/provisioning.rs
+    - crates/freshell-server/src/settings_store.rs
+
+- id: GATE-04
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "test/e2e-browser/specs/multi-client.spec.ts:174-420 — six multi-client tests: 'two browser tabs share the same server' (175), 'terminal output appears in both clients' (191), 'reconnecting second viewer keeps page 1 PTY size stable and both pages keep shared output' (217), 'settings change broadcasts to other clients' (325), 'server handles many concurrent connections' (380), 'client disconnect is handled gracefully' (402)"
+    - "test/e2e-browser/playwright.config.ts:24 — multi-client.spec.ts IS in MATRIX_SPECS, so it runs against BOTH legacy-chromium and rust-chromium projects on every matrix pass"
+    - "test/e2e-browser/specs/server-restart-recovery.spec.ts — in MATRIX_SPECS (playwright.config.ts:14), covers the server-restart leg against both kinds"
+    - "test/e2e-browser/specs/create-protection-isolation-rust.spec.ts — two concurrent RustServers storm-isolation proof (rust-chromium only, playwright.config.ts:299-301); partial evidence on the 'no duplicated process owners' axis at server level"
+    - "crates/freshell-ws/tests/max_payload.rs:221-251 — second independent connection unaffected by client A's oversized frame (crate-level isolation test)"
+    - "docs/plans/2026-07-18-checklist-reconciliation.md:352 — GATE-04 graded 'P': 'reconnect leg is a known flake on both kinds + crash/isolation coverage incomplete'"
+  missing: "Tauri client sharing one Rust server alongside two browser profiles (host-limited: needs Windows WebView2 host); explicit private session-event isolation assertions between clients; targeted per-client screenshots; bounded catch-up assertions after reconnect; server-and-browser simultaneous crash/restart shared-state assertions (compound-restart-rust.spec.ts exists but is rust-only and not tied to multi-client shared state); no explicit duplicated-process-owner check in the browser multi-client lane"
+  effort: M
+  files:
+    - test/e2e-browser/specs/multi-client.spec.ts
+    - test/e2e-browser/specs/compound-restart-rust.spec.ts
+    - test/e2e-browser/playwright.config.ts
+
+- id: GATE-05
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "test/e2e-browser/specs/stress.spec.ts:1-60 — existing stress spec: 'handles 6+ panes simultaneously', 'handles 10 tabs', 'rapid tab switching', 'concurrent terminal output' — but it is UI-level stress only, NOT in MATRIX_SPECS (runs legacy chromium project only), and asserts no numeric ceilings"
+    - "test/e2e-browser/perf/visible-first-audit-gate.ts:1-60 — a real gate harness exists comparing latency/offscreen metrics (focusedReadyMs, terminalInputToFirstOutputMs, offscreen HTTP/WS request/byte/frame counts) between a base and candidate audit artifact with violation reporting; test/e2e-browser/perf/audit-contract.ts:1-50 defines the profile/scenario matrix and zod-validated artifact contract; scripts/assert-visible-first-audit-gate.ts + compare-visible-first-audit.ts wire it up"
+    - "grep 'rss|RSS|soak|memory.*ceiling|handle.*ceiling|port.*ceiling' across crates/ — ZERO matches; no server resource-ceiling instrumentation exists in the Rust code"
+    - "perf harness (create-audit-context.ts:1-28) has no e2eServerKind/server-kind routing found — the audit lane does not select the Rust server explicitly"
+    - "docs/plans/2026-07-18-checklist-reconciliation.md:353 — GATE-05 graded 'N' ('No instrumented stress gate (needs HARNESS-12)')"
+  missing: "Explicit RSS/handle/queue/process/port ceilings (none defined anywhere); mixed-load soak run; repeated desktop close/reopen cycles (Tauri host-limited); provider crash/recovery + extension-proxy + update-cycle hygiene legs; zero-owned-leftover assertions with unrelated-sentinel survival checks. Existing pieces are a UI stress spec and a client visible-first latency comparability gate only"
+  effort: L
+  files:
+    - test/e2e-browser/specs/stress.spec.ts
+    - test/e2e-browser/perf/visible-first-audit-gate.ts
+    - test/e2e-browser/perf/audit-contract.ts
+
+- id: GATE-06
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "test/e2e-browser/specs/safe01-auth-matrix.spec.ts:8-60 — SAFE-01 Playwright matrix: parameterized bad-token startup rejection (empty/short/conflicting) plus good/wrong/missing token over UI/API/WS, driven against BOTH legacy-chromium and rust-chromium (in MATRIX_SPECS, playwright.config.ts:69); header documents which SAFE-01 clauses are covered vs deferred"
+    - "test/e2e-browser/specs/safe03-origin-matrix.spec.ts:8-50 — SAFE-03 origin-policy matrix against BOTH kinds with per-kind-correct assertions (legacy advisory-only control vs Rust enforced 4011 close); in MATRIX_SPECS (playwright.config.ts:70); backed by crates/freshell-ws/src/origin.rs and crates/freshell-ws/tests/origin_policy.rs real-socket tests"
+    - "test/e2e-browser/specs/auth.spec.ts:1-56 — auth modal, wrong-token, URL-token, 401-without-header negatives (chromium/legacy lane)"
+    - "crates/freshell-server/src/rate_limit.rs:1-50,176-330 — SAFE-02 global authenticated API rate limiter (token bucket, 429 + Retry-After, /api/health exempt, per-key buckets) with unit tests (rate_limited_response, try_acquire limits); mounted in main.rs:1132-1341"
+    - "crates/freshell-ws/src/create_limit.rs:1-80 + crates/freshell-ws/tests/create_protection.rs:131-344, restore_spawn_gate.rs:287-722 — per-connection terminal.create rate limit with RATE_LIMITED wire-shape tests; Playwright rust-only legs: create-rate-limit-ladder-rust.spec.ts and rest-spawn-gate-rust.spec.ts (playwright.config.ts:296-298,352-353)"
+    - "crates/freshell-ws/tests/max_payload.rs — oversized-frame rejection + second-client-unaffected test"
+    - "crates/freshell-tauri/tests/../src/updater.rs:8-100,261-384 — updater arm/disarm gate on Ed25519 pubkey presence with unit tests (placeholder pubkey -> DisarmedNoPubkey); signature verification itself delegated to tauri-plugin-updater"
+    - "test/e2e-browser/specs/cfg03-backup-restore.spec.ts — CFG-03 config backup/restore matrix in MATRIX_SPECS (playwright.config.ts:71)"
+  missing: "No single complete negative matrix tying it together with follow-each-negative-with-valid-operation recovery assertions; file-root escape negatives (no traversal-guard test found for Rust static/extension file serving); proxy-destination allowlist negatives; extension path/process permission negatives; event-subscription authorization negatives; privileged-networking negatives; forged/invalid updater-signature rejection test (updater.rs tests only config gating, and packaged-binary signature proof is host-limited)"
+  effort: M
+  files:
+    - test/e2e-browser/specs/safe01-auth-matrix.spec.ts
+    - test/e2e-browser/specs/safe03-origin-matrix.spec.ts
+    - crates/freshell-server/src/rate_limit.rs
+    - crates/freshell-ws/src/origin.rs
+    - crates/freshell-tauri/src/updater.rs
+
+- id: GATE-07
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "Unit-level a11y coverage exists: test/unit/client/components/TabBar.a11y.test.tsx:47-55 ('exposes an accessible name for the new tab button'), TabBar.overflow.test.tsx:222 (aria-labels on arrow buttons), panes/Pane.test.tsx:235-254 (accessibility describe block; tabIndex=0 keyboard accessibility), FloatingActionButton.test.tsx:34 (aria-label), panes/DirectoryPicker.test.tsx:170 (combobox/listbox a11y attributes), TerminalConnectionError.test.tsx:79 (role+label)"
+    - "test/unit/client/components/SettingsView.behavior.test.tsx:76,149 — 'commits keyboard-only slider changes' / 'keyboard-only font size changes' (keyboard interaction unit coverage)"
+    - "test/e2e/network-setup.test.tsx:170 — 'has accessible dialog role' (vitest flow level)"
+    - "grep for a role/label-only Playwright gate or axe-style audit across test/: NOTHING found; docs/plans/2026-07-18-checklist-reconciliation.md:75 — 'HARNESS-11 Accessibility selectors gate: N — No reusable role/label gate or lint self-test found. Blocks GATE-07'; lines 355 GATE-07 graded 'N'"
+    - "No Playwright spec drives first-run setup/launch choice/terminal+provider creation/session search/pane management/settings/approvals/migration/update keyboard-only"
+  missing: "HARNESS-11 role/label-only locator gate; all keyboard-only end-to-end flows (first-run setup, launch choice, terminal/provider creation, session search/mutations, pane management, settings, approvals/questions, migration, update). Existing evidence is scattered unit-test a11y assertions, not an operable-without-a-mouse proof"
+  effort: L
+  files:
+    - test/unit/client/components/TabBar.a11y.test.tsx
+    - test/e2e-browser/specs/
+    - test/e2e-browser/helpers/
+
+- id: GATE-08
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "grep 'receipt' across all ts/tsx/rs/js/mjs/json in repo: matches are only server/tabs-registry/store.ts 'receiptTime' fields, crates/freshell-activity/src/codex.rs receipt-time comments, and a tabs-registry unit test — NONE relate to a parity/release receipt artifact, reporter, or validator"
+    - "grep 'parity.?receipt' repo-wide: matches only the checklist docs themselves (docs/plans/2026-07-14-rust-tauri-parity-completion-checklist.md:838, docs/plans/2026-07-18-checklist-reconciliation.md:356 which grades GATE-08 'N — No receipt reporter/validator')"
+    - "No receipt schema, emitter, or Playwright reporter/validator linking checklist IDs to test results exists in test/, scripts/, or crates/"
+  missing: "Everything: the receipt format capturing source commits, binary hashes, platforms, test commands/counts, pass/fail/skips, perf ceilings, migration hashes, screenshots/traces, process cleanup, approved deviations; and the final Playwright reporter/validator that rejects missing/stale/mismatched evidence and links every checklist ID to a passing test"
+  effort: M
+  files:
+    - test/e2e-browser/playwright.config.ts
+    - scripts/
diff --git a/docs/plans/parity-ledger/audit/p0-configuration-safety-and-migration.yaml b/docs/plans/parity-ledger/audit/p0-configuration-safety-and-migration.yaml
new file mode 100644
index 000000000..736a3e449
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p0-configuration-safety-and-migration.yaml
@@ -0,0 +1,162 @@
+- id: CFG-01
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/settings_store.rs:372-394 — persist() doc: COPY FORWARD whatever is on disk, overlay only owned keys; doc records the past data-loss bug (completedMigrations removed, recentDirectories emptied 20->0) as now fixed"
+    - "crates/freshell-server/src/settings_store.rs:441-447 — owned keys (version, settings) fully replaced"
+    - "crates/freshell-server/src/settings_store.rs:495-507 — serverSecrets overlaid (codexDisplayIdSecret inserted) so sibling/unknown secrets survive"
+    - "crates/freshell-server/src/settings_store.rs:509-514 — 'Everything else -- completedMigrations, recentDirectories, projectColors, any unrecognized top-level key -- is left exactly as loaded'; projectColors/recentDirectories seeded only when absent"
+    - "test: settings_patch_preserves_unmanaged_top_level_document_state in crates/freshell-server/src/settings_store.rs (~line 2557-2593: asserts completedMigrations and recentDirectories with real entries round-trip)"
+    - "test: terminal_override_patch_preserves_unmanaged_top_level_document_state and session_override_patch_preserves_unmanaged_top_level_document_state in crates/freshell-server/src/settings_store.rs"
+    - "test: external_writer_edits_survive_a_rust_persist_of_a_different_key, dirty_key_wins_over_a_concurrent_external_edit_to_the_same_key, tombstoned_field_stays_removed_despite_external_reintroduction in crates/freshell-server/src/settings_store.rs"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-server/src/settings_store.rs
+
+- id: CFG-02
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/settings_store.rs:40-103 — single SettingsStore struct (Arc<RwLock<ServerSettings>>) is the one config.json caller; all HTTP writers route through patch()/persist()"
+    - "crates/freshell-server/src/settings_store.rs:396-408 — persist() doc: read-modify-write under advisory ConfigLock (sidecar file, never config.json itself)"
+    - "crates/freshell-server/src/settings_store.rs:427-431 — ConfigLock::acquire(&dir) held across the entire read-modify-write, RAII drop at end"
+    - "crates/freshell-server/src/settings_store.rs:419-534 — one queued atomic store: fresh disk read, overlay dirty keys, tmp+rename, single persist function"
+    - "test: concurrent_persists_across_two_store_instances_serialize_without_lost_updates in crates/freshell-server/src/settings_store.rs — proves two store instances serialize without lost updates"
+    - "test: external_writer_edits_survive_a_rust_persist_of_a_different_key in crates/freshell-server/src/settings_store.rs"
+    - "note: desktop.json/Tauri queue (TAURI-25) is explicitly out of scope per the checklist item"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-server/src/settings_store.rs
+
+- id: CFG-03
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "crates/freshell-server/src/settings_store.rs:93-103,154-166 — config_fallback computed ONCE in load() from the PRE-restore read via classify_boot_config_fallback (mirrors configStore.getLastReadError)"
+    - "crates/freshell-server/src/settings_store.rs:168-177 — maybe_restore_config_from_backup runs BEFORE all tolerant per-field readers so every reader sees the same recovered document"
+    - "crates/freshell-server/src/settings_store.rs:346-367 — GAP2 fix: persist-before-commit; write failure returns 500 error envelope, live tree only updated after successful persist"
+    - "crates/freshell-server/src/settings_store.rs:522-531 — backup refreshed only after successful primary persist, atomically (tmp+rename via atomic_write)"
+    - "crates/freshell-server/src/settings_store.rs:33 — ConfigFallback/ConfigFallbackReason protocol types imported from freshell-protocol (fallback reason surfaced via protocol; client handshake wiring not re-verified in this audit)"
+    - "test: config_fallback_reports_parse_error_when_primary_is_corrupt_json, config_fallback_reports_enoent_when_primary_missing_but_backup_exists, config_fallback_is_none_for_an_ordinary_fresh_install, config_fallback_is_none_for_a_healthy_primary, config_fallback_survives_the_same_boots_auto_restore in crates/freshell-server/src/settings_store.rs"
+    - "test: corrupted_primary_restores_from_backup_preserving_every_last_good_value, parseable_but_surprising_primary_is_never_restored_over, both_primary_and_backup_corrupt_starts_fresh_but_preserves_both_forensic_copies, backup_write_is_atomic_tmp_plus_rename_with_no_leftovers, persist_failure_surfaces_as_error_and_leaves_live_tree_and_backup_untouched in crates/freshell-server/src/settings_store.rs"
+  missing: "Browser-visible warning banner / explicit UI Restore action is client-side (legacy src/, out of Rust-server scope); conservative auto-restore is documented separately per the item's allowance. PW-RUST-level banner assertion not verified by this audit."
+  effort: S
+  files:
+    - crates/freshell-server/src/settings_store.rs
+    - crates/freshell-protocol
+
+- id: CFG-04
+  verdict: MISSING
+  confidence: medium
+  evidence:
+    - "grep for 'legacyLocalSettingsSeed' across crates/ (combined 6-term grep; all 86 matches were other terms) — zero Rust references found"
+    - "crates/freshell-server/src/settings_store.rs:509-510 — unknown top-level keys DO copy-forward, so a seeded legacyLocalSettingsSeed would not be deleted, but nothing in Rust generates/returns/consumes it"
+  missing: "No Rust implementation found that returns/consumes legacyLocalSettingsSeed or runs the one-time browser-preference seeding (theme, sidebar presentation, scale, terminal font, sound); one-time migration marker logic absent. Client-side consumption lives in legacy src/ and was not audited."
+  effort: M
+  files:
+    - crates/freshell-server/src/settings_store.rs
+    - crates/freshell-server/src/boot.rs
+
+- id: CFG-05
+  verdict: HOST-LIMITED
+  confidence: low
+  evidence:
+    - "Item's own text: Playwright validation is PW-TAURI-WIN on HARNESS-10 (Windows/Tauri host) — cannot be validated from this Linux audit"
+    - "crates/freshell-tauri exists in the workspace but was not examined (audit time exhausted); no cfg05 umbrella spec was checked under test/"
+  missing: "Undetermined: contents of crates/freshell-tauri (Electron->Tauri migration code) and any umbrella gate/harness spec not inspected."
+  effort: S
+  files:
+    - crates/freshell-tauri
+    - test/e2e-browser/specs
+
+- id: CFG-06
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "crates/freshell-server/src/settings_store.rs:1-21 — module doc: the frozen Arc<ServerSettings> boot snapshot was the R2/R3/R4 root cause; module now owns the live store (Arc<RwLock<ServerSettings>>) shared by BootState, FilesState, and the settings router"
+    - "crates/freshell-server/src/settings_store.rs:283-286 — get() clones the live tree on each call (no stale snapshot)"
+    - "test: patch_applies_auto_kill_idle_minutes_live_to_registry, patch_applies_scrollback_live_to_registry, patch_of_unrelated_field_leaves_live_registry_settings_unchanged, invalid_patch_does_not_change_live_registry_settings in crates/freshell-server/src/settings_store.rs"
+    - "test: patch_write_through_reaches_get_and_config_json_and_restart, default_plus_network_overlay_matches_captured_fixture in crates/freshell-server/src/settings_store.rs"
+  missing: "Per-consumer reconnection/fresh-handshake tests for every consumer (dedicated TERM-*, FILE-05, DIAG-04, provider specs) were not individually verified in this audit; only the registry/settings live-propagatation tests above were seen."
+  effort: S
+  files:
+    - crates/freshell-server/src/settings_store.rs
+    - crates/freshell-server/src/boot.rs
+
+- id: CFG-07
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "crates/freshell-server/src/instance_id.rs exists (193 lines, not read before time ran out) — plausible location of the instance-ID implementation"
+    - "not verified: persistence path, same-home reuse across restarts, different-home different ID, or its tests"
+  missing: "All clauses unverified — file exists but contents/tests unexamined."
+  effort: S
+  files:
+    - crates/freshell-server/src/instance_id.rs
+
+- id: CFG-08
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "crates/freshell-server/src/tabs_snapshots.rs (147 lines), tabs_snapshots_tests.rs, tabs_snapshots_selectors.rs, and identity_sink.rs exist — plausible durable tab-registry implementation and dedicated test file"
+    - "not verified: disk persistence format/path, caps/hashes/TTLs, migration, corruption quarantine, or which clauses the tests cover"
+  missing: "All clauses unverified — modules and test files exist but contents unexamined."
+  effort: M
+  files:
+    - crates/freshell-server/src/tabs_snapshots.rs
+    - crates/freshell-server/src/tabs_snapshots_tests.rs
+    - crates/freshell-server/src/identity_sink.rs
+
+- id: CFG-09
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-server/src/files.rs:18-20 — files router doc: candidate merge documented as live-terminal cwd / indexed projects / recentDirectories / provider cwds, then settings.defaultCwd, de-duplicated"
+    - "crates/freshell-server/src/settings_store.rs:514 — persist() seeds recentDirectories: [] only when absent, preserving existing entries (copy-forward at lines 509-514)"
+    - "grep across crates/ found recentDirectories references only in settings_store.rs + files.rs docs — no dedicated Rust MRU-learning writer located"
+  missing: "A Rust writer that records/updates a deduplicated 20-item MRU on launches was NOT found in this audit (only seed-if-absent and copy-forward preservation seen); no end-to-end test for the full source-precedence merge order or the 20-cap was verified."
+  effort: M
+  files:
+    - crates/freshell-server/src/files.rs
+    - crates/freshell-server/src/settings_store.rs
+
+- id: CFG-10
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-server/src/settings_store.rs:180-210 — legacy enabledProviders==[claude,codex] set migration to modern default set (incl. amplifier) mirroring server/settings-migrate.ts"
+    - "crates/freshell-server/src/settings_store.rs:212-244 — knownProviders seed-when-missing (persists even when empty), append newly discovered + auto-enable, otherwise serve persisted list as-is (never shrinks)"
+    - "crates/freshell-server/src/settings_store.rs:311-321 — agentChat PATCH key rejected with 400 'agentChat settings have been migrated; use freshAgent'"
+    - "test: known_providers_seeded_persisted_and_patchable, newly_discovered_providers_append_and_auto_enable, persisted_known_providers_survive_cwd_neutral_reboot, legacy_default_enabled_providers_migrated, legacy_default_enabled_providers_migration_includes_amplifier, agent_chat_key_rejected in crates/freshell-server/src/settings_store.rs"
+  missing: "completedMigrations marker recording: Rust preserves the key on persist (lines 509-514) but no code seen that WRITES a marker ('record completion only after success' / 'exactly one schema marker' unverified; migrations appear idempotent-by-derivation instead). Legacy 'freshclaude' config-shape migration (distinct from agentChat rejection) not found. Migration-interruption/restart test not verified."
+  effort: M
+  files:
+    - crates/freshell-server/src/settings_store.rs
+
+- id: CFG-11
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-server/src/settings_store.rs:518-520 — writes config.json.tmp-<pid> then renames over config.json (atomic replace: complete old or complete new, never mixed)"
+    - "crates/freshell-server/src/settings_store.rs:531 — atomic_write(&backup_path(&dir), &text) for the backup (tmp+rename), under the ConfigLock held since line 431"
+    - "test: backup_write_is_atomic_tmp_plus_rename_with_no_leftovers in crates/freshell-server/src/settings_store.rs"
+  missing: "Boot-time stale-temp-file cleanup (remove old config.json.tmp-* without racing a current write) was NOT found/verified in this audit; kill-mid-write crash tests (Windows project) exist only as the PW-RUST plan in the item text, not verified here."
+  effort: M
+  files:
+    - crates/freshell-server/src/settings_store.rs
+
+- id: CFG-12
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "crates/freshell-server/src/settings_store.rs:322-327 — validate_patch with strict schema rejects any key outside the ServerSettings tree before mutation"
+    - "test: client_only_key_rejected_by_strict_schema, unknown_top_level_key_rejected, enum_and_type_violations_rejected, enum_values_and_unrecognized_key_message_match_the_original in crates/freshell-server/src/settings_store.rs — server refuses to treat browser-local prefs as server state"
+    - "crates/freshell-server/src/settings.rs:32-85 — default_server_settings() tree mirrors shared/settings.ts#createDefaultServerSettings (server-scoped keys only; network overlay documented)"
+    - "crates/freshell-server/src/main.rs:1061 — live settings store feeds terminals broadcast path (replication direction seen; settings.updated broadcast to all clients not individually re-verified)"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-server/src/settings_store.rs
+    - crates/freshell-server/src/settings.rs
diff --git a/docs/plans/parity-ledger/audit/p0-rich-agent-parity.yaml b/docs/plans/parity-ledger/audit/p0-rich-agent-parity.yaml
new file mode 100644
index 000000000..08d40a37b
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p0-rich-agent-parity.yaml
@@ -0,0 +1,297 @@
+# P0 rich-agent parity audit. NOTE: audit was cut short mid-investigation; items marked
+# UNCLEAR/PARTIAL with low confidence were not fully examined. Evidence below cites only
+# lines/test names actually observed by the auditor.
+
+- id: AGENT-01
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-freshagent/src/{claude.rs,codex.rs,opencode_ws.rs} exist as provider implementations (2788/7253/2912 lines respectively, wc -l observed)"
+    - "crates/freshell-freshagent/src/lib.rs:1365-1374 — one shared axum router (tabs/panes/send-keys/capture/wait-for + merged pane_ops router) serving the fresh-agent surface"
+    - "grep -ri kilroy crates/ matched crates/freshell-protocol/src/common.rs, crates/freshell-server/src/session_metadata.rs, crates/freshell-ws/src/terminal.rs — kilroy is at least typed/named in Rust"
+  missing: "Confirmed kilroy create/send through the shared WS+REST orchestration paths; explicit verification that all providers (claude, codex, opencode, kilroy) go through one provider-neutral dispatch for both create and send. Crates-with-tests confirmation per provider not completed."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/lib.rs
+    - crates/freshell-freshagent/src/codex.rs
+    - crates/freshell-freshagent/src/claude.rs
+    - crates/freshell-freshagent/src/opencode_ws.rs
+
+- id: AGENT-02
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-freshagent/src/codex.rs:5225 — test: handle_create_with_resume_session_id_resumes_the_same_thread"
+    - "crates/freshell-freshagent/src/codex.rs:5288 — test: handle_create_with_resume_on_genuinely_missing_thread_emits_create_failed"
+    - "crates/freshell-freshagent/src/codex.rs:5356 — test: handle_create_with_resume_wrong_thread_id_fails_create_and_never_adopts"
+    - "crates/freshell-ws/tests/freshagent_claude_attach.rs exists (ls) — claude attach coverage"
+    - "test/e2e-browser/specs/restore-matrix.spec.ts exists (ls) — restart/resume scenario spec"
+  missing: "'Exactly three user/assistant turn pairs' leg (documented fixture-limited in section notes as unprovable against the shared fixture); verified opencode + kilroy attach/resume/hydration legs; audit of dedup-no-duplicate-turns per provider incomplete."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/codex.rs
+    - crates/freshell-freshagent/src/claude.rs
+    - crates/freshell-freshagent/src/snapshot.rs
+    - crates/freshell-ws/src/auto_resume.rs
+
+- id: AGENT-03
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-ws/tests/freshagent_claude_kill_interrupt.rs exists (ls) — claude kill/interrupt coverage"
+    - "crates/freshell-codex/tests/interrupt_rpc.rs exists (ls) — codex interrupt RPC test"
+    - "crates/freshell-server/src/main.rs:259-260 — comment: post-handshake WS loop dispatches freshAgent.create/send/kill/interrupt for opencode"
+    - "crates/freshell-freshagent/src/session_lease.rs (516 lines) — session lease/ownership machinery exists"
+  missing: "Verified per-provider matrix that interrupt preserves session while kill clears ownership (lease/pane-ledger) with tests; 'no completion sound fires for either action' leg; opencode kill/interrupt tests not confirmed."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+    - crates/freshell-freshagent/src/session_lease.rs
+    - crates/freshell-ws/tests/freshagent_claude_kill_interrupt.rs
+
+- id: AGENT-04
+  verdict: MISSING
+  confidence: low
+  evidence:
+    - "grep for 'freshAgent.compact' in crates/freshell-freshagent/src/terminal_tabs.rs and crates/freshell-freshagent/src/lib.rs returned no hits (audit ran out of time before searching codex.rs/claude.rs/opencode_ws.rs exhaustively)"
+  missing: "All clauses located: forward compact requests, compacting status exposure, durable-session preservation, return-to-idle. POSSIBLY present in provider files not searched — treat as unconfirmed absence, not proof of absence."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+    - crates/freshell-freshagent/src/codex.rs
+
+- id: AGENT-05
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "crates/freshell-protocol/src/{client_messages.rs,server_messages.rs,common.rs} contain freshAgent.* message strings (grep file-list hit incl. approve/permission patterns) — approval-related protocol frames plausibly exist, but no implementation/test site was confirmed before time expired"
+  missing: "Unexamined: pending-approval presentation, Allow/Always-Allow(session-scoped)/Deny with request correlation, freshAgent.permission.cancelled removing card without inventing a decision."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/codex.rs
+    - crates/freshell-freshagent/src/opencode_ws.rs
+    - crates/freshell-protocol/src/common.rs
+
+- id: AGENT-06
+  verdict: UNCLEAR
+  confidence: low
+  evidence: []
+  missing: "Unexamined: provider question rendering support, single-choice/multi-select/Other free-text validation, resume-of-correct-turn. No greps on 'question'/'answer' were run before time expired."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+    - crates/freshell-protocol/src/client_messages.rs
+
+- id: AGENT-07
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-ws/tests/codex_fork_rebind.rs exists (ls) — codex fork rebind coverage implies a Rust fork path exists somewhere (implementation site not read)"
+  missing: "Located fork implementation per provider; verified 'new durable identity while source conversation unchanged' semantics with tests; fork-from-selected-turn/checkpoint granularity."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/codex.rs
+    - crates/freshell-ws/tests/codex_fork_rebind.rs
+
+- id: AGENT-08
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "crates/freshell-freshagent/src/lib.rs:1370 — route POST /api/panes/{id}/send-keys wired in the fresh-agent router"
+    - "crates/freshell-freshagent/src/lib.rs:653 — comment: send-keys/freshAgent.send materializes a real ses_ opencode session (single durable id per pane)"
+    - "test/e2e-browser/specs/agent-continuity-matrix.spec.ts exists (ls); section records it green on rust-chromium 2026-07-20 (one durable id across three REST send-keys; distinct id for second pane; cumulative context 1->2->3)"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-freshagent/src/lib.rs
+
+- id: AGENT-09
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-server/src/main.rs:1090-1091 — comment: GET /api/fresh-agent/threads/:sessionType/:provider/:threadId thread-snapshot REST endpoint mounted"
+    - "crates/freshell-freshagent/src/snapshot.rs (557 lines) and claude_snapshot.rs — snapshot machinery exists"
+    - "crates/freshell-freshagent/tests/transcript_cwd_export.rs exists (ls) — transcript-related integration test"
+  missing: "Paged-turn API (cursor + revision) — no evidence seen; error cases invalid cursor / revision mismatch / missing-ambiguous turn / body-size pagination unverified."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/snapshot.rs
+    - crates/freshell-server/src/main.rs
+
+- id: AGENT-10
+  verdict: UNCLEAR
+  confidence: low
+  evidence: []
+  missing: "Unexamined: model/capability metadata exposure, cache correctness, refresh-without-restart endpoint. No greps on 'models'/'refresh' were run before time expired."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+    - crates/freshell-freshagent/src/lib.rs
+
+- id: AGENT-11
+  verdict: UNCLEAR
+  confidence: low
+  evidence: []
+  missing: "Unexamined: attachment routes/upload handling, size/type limits, name sanitization, isolated storage, history metadata. No greps on 'attach'/'multipart' were run before time expired (likely a gap — attachments were never observed in the fresh-agent router at lib.rs:1365-1374)."
+  effort: L
+  files:
+    - crates/freshell-freshagent/src/lib.rs
+
+- id: AGENT-12
+  verdict: UNCLEAR
+  confidence: low
+  evidence: []
+  missing: "Unexamined: per-send model/effort/sandbox/permission/plugin choices parsed from freshAgent.send and forwarded in provider payloads; model persistence through the durable turn schema. Not grepped before time expired."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+    - crates/freshell-freshagent/src/codex.rs
+
+- id: AGENT-13
+  verdict: UNCLEAR
+  confidence: low
+  evidence: []
+  missing: "Unexamined: legacy command-execution and diff API surface (bounded buffered result, cwd semantics incl. cwd outside allowedFilePaths). Not grepped before time expired; neither endpoint was seen in the fresh-agent router (lib.rs:1365-1374)."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/lib.rs
+    - crates/freshell-freshagent/src/pane_ops.rs
+
+- id: AGENT-14
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/checkpoints.rs — 1605 lines with an extensive test module (#[test] fns at lines 694, 704, 712, 953, 960+ observed via grep)"
+    - "crates/freshell-server/src/main.rs:1121 — comment: POST /api/fresh-agent/checkpoints mounted with home-dir handling"
+    - "test/e2e-browser/specs/agent-checkpoint-rewind.spec.ts exists (ls) — UI rewind e2e (green on chromium/legacy-chromium per section; rust-chromium UI-gesture leg explicitly documented as a fixture/client timing limitation, routes proven by crate tests)"
+    - "Section documents commit 96e354ea porting list/restore/metadata routes with 41 Rust tests incl. end-to-end create->list->mutate->restore->verify-bytes over real HTTP routes"
+  missing: "PW-RUST UI-gesture leg on rust-chromium only (shared-fixture local-echo timing); Rust routes themselves fully ported and crate-tested."
+  effort: S
+  files:
+    - crates/freshell-server/src/checkpoints.rs
+
+- id: AGENT-15
+  verdict: UNCLEAR
+  confidence: low
+  evidence: []
+  missing: "Unexamined: Freshell MCP tool injection into claude (and other providers), tool discovery preservation, provider-neutral tool-call routing. No greps on 'mcp' in crates/ were completed before time expired."
+  effort: L
+  files:
+    - crates/freshell-freshagent/src/claude.rs
+
+- id: AGENT-16
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-server/src/main.rs:209-211 — comment: all fresh-agent producers push onto one broadcast bus that every /ws connection fans out (i.e., events are globally broadcast, not per-session scoped)"
+    - "crates/freshell-server/src/main.rs:220-232 — codex/claude/opencode fresh-agent slices share the same broadcast bus and auth token"
+    - "crates/freshell-freshagent/src/lib.rs:582-583 — created/send.accepted/materialized frames broadcast onto the shared bus"
+  missing: "Per-client/session event scoping and attach/send authorization (shared-bus fanout suggests global broadcast — isolation clauses likely UNMET but not disproven by targeted search); durable-ID+cwd-route OpenCode isolation proof; reconnect authorization retention."
+  effort: L
+  files:
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-ws/src/lib.rs
+
+- id: AGENT-17
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-freshagent/src/spawn_gate.rs (648 lines) — spawn gating machinery exists"
+    - "crates/freshell-ws/tests/restore_storm.rs exists (ls) — respawn/storm bounding coverage"
+    - "crates/freshell-freshagent/src/codex.rs:5356 — test covers never adopting a wrong/gone thread on recovery"
+  missing: "Verified per-provider: dead runtime-entry removal, explicit 'exited' status emission, lazy respawn resuming same durable session with no duplicate live record. Not grepped ('exited'/'respawn') before time expired."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/spawn_gate.rs
+    - crates/freshell-freshagent/src/codex.rs
+
+- id: AGENT-18
+  verdict: UNCLEAR
+  confidence: low
+  evidence: []
+  missing: "Unexamined: server-authoritative waiting/completion edge emission and dedup across duplicate/out-of-order edges and restart/replay. No greps on 'waiting'/'monotonic' were run before time expired."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+
+- id: AGENT-19
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-freshagent/src/lib.rs:1784 — {\"type\": \"freshAgent.error\"} frame construction exists"
+    - "crates/freshell-server/src/checkpoints.rs — test module covers auth/400/404 error parity for the checkpoint routes (per section notes; test fns observed at lines 694-960+)"
+    - "crates/freshell-freshagent/src/codex.rs:5288 — create failure surfaces as explicit create-failed error (lost-session category, codex)"
+  missing: "Verified stable 400/404/409/502/503 mapping table for all 9 categories: invalid cursor/display ID, revision mismatch, ambiguous turn, missing turn, unavailable runtime, unsupported capability, lost session, locator mismatch, malformed provider output — only lost-session (codex) and checkpoint 400/404 have observed evidence."
+  effort: L
+  files:
+    - crates/freshell-freshagent/src/lib.rs
+    - crates/freshell-freshagent/src/snapshot.rs
+    - crates/freshell-server/src/checkpoints.rs
+
+- id: AGENT-20
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "crates/freshell-freshagent/src/lib.rs:1365-1374 — fresh-agent router lists tabs/panes/send-keys/capture/wait-for + merged pane_ops::router; no '/api/fresh-agent/send' literal observed here (pane_ops.rs, 1988 lines, not read — route may live there)"
+  missing: "Unconfirmed existence of the standalone endpoint; if present, unverified: submittedTurnId return, no-side-effect-conversation, and missing/ambiguous/lost/wrong-provider locator handling."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/pane_ops.rs
+    - crates/freshell-freshagent/src/lib.rs
+
+- id: AGENT-21
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-freshagent/src/lib.rs:1976-2012 — read directly: provider-generic single-flight + replay cache for freshAgent.create keyed by requestId; identical-requestId create on reconnect replays cached freshAgent.created (no new spawn at 2063); caches cleared only from freshAgent.kill handler per 2012 comment"
+  missing: "Send idempotency (duplicate send returns original submitted turn — no code seen); fork idempotency; payload-mismatch-reusing-an-ID conflict-without-mutation leg."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/lib.rs
+
+- id: AGENT-22
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-freshagent/src/lib.rs:2522 — test assertion: assert_eq!(wire[\"type\"], \"freshAgent.session.materialized\")"
+    - "crates/freshell-freshagent/src/lib.rs:582-583 — materialized frame broadcast onto the shared bus alongside created/send.accepted"
+    - "crates/freshell-freshagent/src/lib.rs:1976-2063 — create dedup replay cache prevents a second spawn/materialization on reconnected create (supports exactly-once)"
+  missing: "Verified single effective sessions.changed pairing; placeholder fully absent from DOM/layout/API/MCP after materialization; reload/restart resumes final id without re-emitting the event."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/lib.rs
+    - crates/freshell-freshagent/src/opencode_ws.rs
+
+- id: AGENT-23
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-freshagent/src/lib.rs:677-688 — freshell_opencode::Route built from cwd string; get_session/list_messages keyed by (thread_id, route) — durable-ID + cwd-route locator shape exists"
+    - "crates/freshell-opencode/ crate exists — opencode serve/session manager home"
+    - "crates/freshell-server/src/main.rs:1395 area references legacy serve-manager.ts:573-591 mapping (process reuse mapping noted, not read in full)"
+  missing: "Verified one long-lived shared opencode serve process with distinct per-route sessions; cross-root isolation tests (sends/events/transcript/permissions cannot cross cwd roots)."
+  effort: M
+  files:
+    - crates/freshell-opencode
+    - crates/freshell-freshagent/src/lib.rs
+
+- id: AGENT-24
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "grep -ri kilroy crates/ --include=*.rs matched: crates/freshell-protocol/src/common.rs, crates/freshell-server/src/session_metadata.rs, crates/freshell-ws/src/terminal.rs, crates/freshell-ws/src/lib.rs, crates/freshell-freshagent/src/claude.rs, crates/freshell-freshagent/src/snapshot.rs, crates/freshell-freshagent/src/claude_snapshot.rs — kilroy is named/typed across protocol, ws and freshagent crates"
+  missing: "Established full kilroy lifecycle parity: create, attach/resume, send, approvals, questions, waiting/completion, reload/restart hydration, crash recovery, independent-availability gating. Only naming-level presence confirmed; implementation depth unexamined. Verdict may range from PARTIAL to near-MISSING pending that read."
+  effort: L
+  files:
+    - crates/freshell-protocol/src/common.rs
+    - crates/freshell-freshagent/src/claude.rs
+
+- id: AGENT-25
+  verdict: UNCLEAR
+  confidence: low
+  evidence: []
+  missing: "Unexamined: registered provider capability matrix exposure in Rust and server-side gating of interrupt/kill/compact/fork/approvals/questions/model-refresh/attachments/per-send-choices with explicit unsupported-cell behavior. No greps on 'capabilit' were run before time expired."
+  effort: L
+  files:
+    - crates/freshell-protocol/src/common.rs
+    - crates/freshell-freshagent/src/lib.rs
diff --git a/docs/plans/parity-ledger/audit/p0-session-history-and-sidebar-parity.yaml b/docs/plans/parity-ledger/audit/p0-session-history-and-sidebar-parity.yaml
new file mode 100644
index 000000000..12512207a
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p0-session-history-and-sidebar-parity.yaml
@@ -0,0 +1,293 @@
+- id: SESSION-01
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-sessions/src/directory_index.rs:227 ClaudeSource, :425 CodexSource, :589 OpencodeSource impl SessionSource"
+    - "crates/freshell-sessions/src/amplifier.rs:1 ff — AmplifierSource indexing metadata.json + transcript preview (port of providers/amplifier.ts)"
+    - "IndexedSession carries session_id/provider/project_path/title/summary/first_user_message/last_activity_at/created_at/cwd — directory_index.rs:49-86"
+    - "test: codex_source_scans_fixture_and_uses_parsed_session_id, claude_source_scans_fixture_home_and_skips_cwdless_files, snapshot_is_sorted_last_activity_desc_then_key_desc in crates/freshell-sessions/src/directory_index.rs"
+    - "resume: crates/freshell-sessions/src/resume_input.rs, resume_resolve.rs; resolve route crates/freshell-server/src/resolve.rs:579"
+    - "e2e: test/e2e-browser/specs/session-directory-matrix.spec.ts (4-provider visibility :328, icons :398, identity/cwd/lastActivityAt ordering :458) and sidebar-click-resume.spec.ts Amplifier leg :287 green; Codex leg :117 is test.fixme (observed FreshCodex JSON-RPC resume, not CLI argv)"
+  missing: "Sidebar-CLICK resume through the UI proven only for Amplifier; Claude/OpenCode click-resume unproven, Codex click-resume routes via FreshCodex JSON-RPC (no CLI-argv-style proof possible today)."
+  effort: M
+  files:
+    - crates/freshell-sessions/src/directory_index.rs
+    - crates/freshell-sessions/src/resume_resolve.rs
+    - crates/freshell-server/src/resolve.rs
+
+- id: SESSION-02
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/session_directory.rs:698-721 apply_session_overrides: deleted removes item, titleOverride/summaryOverride applied, archived overlaid, keyed provider:sessionId"
+    - "test: overrides_overlay_applies_title_summary_archived_and_filters_deleted (session_directory.rs:1946), session_override_applies_to_codex_and_opencode_keys (:2405), override_no_rebuild (:2227)"
+    - "legacy reference server/coding-cli/session-indexer.ts:204-220 applyOverride also applies createdAtOverride and provider-generated title suppression"
+  missing: "Sidebar projection does NOT apply createdAtOverride (ordering by createdAt) and does NOT apply the provider-generated title suppression (title_provider_generated never read in session_directory.rs) — both exist only in resolve.rs:533/545. Archive/delete/title/summary overlay covered."
+  effort: M
+  files:
+    - crates/freshell-server/src/session_directory.rs
+
+- id: SESSION-03
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/sessions.rs:108-214 patch_session: titleOverride/summaryOverride/archived/deleted/createdAtOverride validated (validate_session_patch :223-276) and persisted via SettingsStore::patch_session_override; soft-delete is an override flag — provider source files are never deleted (apply_session_overrides merely filters, session_directory.rs:707)"
+    - "sessions.rs:194-211 broadcasts sessions.changed with shared revision after override writes"
+    - "test: patch_rename_persists_and_returns_merged_plus_cascade_null (sessions.rs:370), patch_rename_cascades_all_four_effects_to_a_live_terminal, patch_archive_broadcasts_sessions_changed_and_revision_is_monotonic in crates/freshell-server/src/sessions.rs"
+    - "SettingsStore::patch_session_override with title-source ladder at crates/freshell-server/src/settings_store.rs:701"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-server/src/sessions.rs
+    - crates/freshell-server/src/settings_store.rs
+
+- id: SESSION-04
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "Title-source ladder: can_upgrade_title rank fn crates/freshell-server/src/settings_store.rs:1208-1227 (tests at :2386-2505: user > ai > first-message > dir, finalized-block rules)"
+    - "Provider-authoritative title suppression: IndexedSession.title_provider_generated directory_index.rs:63-70; resolve.rs:533-545 suppresses dir/first-message override vs provider-generated title"
+    - "POST generate-title (first-message heuristic, extract_title_from_message port of shared/title-utils.ts) sessions.rs:301-341; tests in sessions.rs"
+    - "sessions.rs:296-300 explicitly states 'No Gemini/AI key path in this port' (matches legacy no-key behavior only)"
+  missing: "Real AI-title generation path (legacy server/ai-title.ts + auto-title.ts Gemini call) not ported; one-time stale-AI-title cleanup not found; PW rungs partially closed in legacy probing only."
+  effort: M
+  files:
+    - crates/freshell-server/src/sessions.rs
+    - crates/freshell-server/src/settings_store.rs
+
+- id: SESSION-05
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No Rust route for project colors: grep for 'projectColor'/'project-color' across crates/* only hits settings_store.rs:510-514 (config.json key copy-forward, default {} seeded)"
+    - "legacy reference server/project-colors-router.ts:18 exposes PUT /project-colors"
+    - "ServerSettings (crates/freshell-protocol/src/settings.rs:120) has no projectColors field, so the value is never served to clients nor broadcast"
+  missing: "Entire save/serve/broadcast surface (PUT route, GET surface, live broadcast); only config.json preservation exists."
+  effort: M
+  files:
+    - crates/freshell-server/src/settings_store.rs
+    - crates/freshell-server/src/main.rs
+
+- id: SESSION-06
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "crates/freshell-server/src/session_metadata.rs:69 SessionMetadataStore, POST route router(:312)/post_session_metadata(:318), validate_post_body(:382), should_apply_session_type_metadata(:269) classification rules"
+    - "test: set_persists_and_get_returns_it (:457), set_does_not_downgrade_explicit_with_materialized (:493), get_all_flattens_provider_session_id_keys (:614), metadata_change_broadcasts_sessions_changed_with_increased_revision (:790)"
+    - "Resume/runtime use: resolve.rs:665-682 overlays sessionType into matches keyed provider:sessionId"
+    - "Directory exposes sessionType on live-terminal items (session_directory.rs:186-188); icons/actions are shared client reading this API"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-server/src/session_metadata.rs
+    - crates/freshell-server/src/resolve.rs
+
+- id: SESSION-07
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-sessions/src/search.rs:111-150 search_session_file (userMessages/fullText tiers), extract_snippet :67-89"
+    - "crates/freshell-server/src/session_directory.rs:1202-1253 apply_file_search (scan budget maxScan=limit*10, early stop limit+1, partial/partialReason budget|io_error), cursor encode/decode :1036-1053"
+    - "test: tier_user_messages_matches_only_the_user_turn (:2530), tier_full_text_matches_assistant_turn_too (:2573), tier_search_combined_with_cursor_pagination (:2637), tier_search_reports_partial_budget_when_scan_budget_exceeded (:2701) in session_directory.rs; 13 tests in search.rs"
+    - "search.rs:13-17 documents opencode (single sqlite) and amplifier unsearchable at file tiers; AbortSignal dropped (:102-105)"
+  missing: "Stale-query cancellation (no request supersession observed server-side), file-content search for OpenCode/Amplifier, >100-session multi-provider PW scenario."
+  effort: M
+  files:
+    - crates/freshell-sessions/src/search.rs
+    - crates/freshell-server/src/session_directory.rs
+
+- id: SESSION-08
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "session_directory.rs:671-672: \"resolveProjectPath: meta.cwd (git-root resolution deferred), else 'unknown'\" — projectPath = parsed cwd, no git-root/coalescing logic server-side"
+    - "Repository/worktree grouping toggles live in the shared client reading projectPath; no Rust-side Repository/Worktree grouping computation found"
+    - "repos icons helpers exist (crates/freshell-server/src/repo_icon*.rs) but unrelated to history grouping"
+  missing: "Server-side git-root resolution for project identity (deferred per comment); no evidence History worktree grouping vs nested-repo/similarly-named-folder cases covered against Rust."
+  effort: M
+  files:
+    - crates/freshell-server/src/session_directory.rs
+
+- id: SESSION-09
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/main.rs:2115-2136 spawn_sessions_sweep (2s poll, MissedTickBehavior::Skip, subsumes ~150ms coalescing), signature sessions_sweep_signature :2055-2075 incl. identity digest"
+    - "Override writes broadcast directly: sessions.rs:204-211; tests new_older_session_file_is_still_detected_as_a_change + identity_updated_at_alone_does_not_move_the_sweep_signature (main.rs sessions_sweep_tests :2139)"
+    - "e2e green both servers: session-directory-matrix.spec.ts:564 (mid-test add appears ~10s no reload) and :668 (file removed disappears no reload)"
+    - "notify-watcher exists only for provider-root discovery: crates/freshell-sessions/src/indexer.rs"
+  missing: "Modified-in-place and moved-between-provider-dir mutation shapes, Codex/OpenCode/Amplifier backend-shape legs, and two-page convergence/stable-focus/no-refresh-storm assertions (per 2026-07-19 note + no contrary evidence found)."
+  effort: M
+  files:
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-ws/src/terminal.rs
+
+- id: SESSION-10
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "session_directory.rs:761-848 join_running_state + join_live_terminals (isRunning/runningTerminalId, single-owner dedup by provider:sessionId)"
+    - "test: join_live_terminals_matched_terminal_is_never_double_emitted (:995), join_running_state_matches_live_terminal_and_sets_running_fields (:892) in crates/freshell-server/src/session_directory.rs"
+    - "e2e: test/e2e-browser/specs/restore-matrix.spec.ts:634 'SESSION-10: re-clicking a running session from the sidebar focuses the existing tab instead of creating a duplicate' — green 2x legacy-chromium + rust-chromium"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-server/src/session_directory.rs
+
+- id: SESSION-11
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "Provider scan failure/status plumbing: SessionIndex.scan_failures (directory_index.rs:791,875-879) drives resolve.rs status degraded/ready/warming (resolve.rs:764-789) — truthful index status at the resolve surface"
+    - "Late association: freshell-ws/src/codex_association.rs, opencode_association.rs adopt session ids post-spawn (directory join tests reference 'pre-adoption duplicate is transient', session_directory.rs:1017)"
+    - "Stale running state derives from the in-memory TerminalIdentityRegistry (session_directory.rs:761-780), so a server restart naturally clears isRunning; no explicit repair events named 'sessions.status' found"
+    - "legacy references server/session-association-coordinator.ts, startup-state.ts not fully compared here (ran out of time)"
+  missing: "Explicit repair/status EVENTS to clients (none found by that name); late/missing-association repair proven for codex/opencode adoption legs only; PW delayed-session-init/crash scenarios unverified."
+  effort: M
+  files:
+    - crates/freshell-ws/src/codex_association.rs
+    - crates/freshell-ws/src/opencode_association.rs
+    - crates/freshell-server/src/main.rs
+
+- id: SESSION-12
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "Reverse cascade implemented: sessions.rs:158-192 (session rename → live terminal rename, registry write-through + terminals.changed) — cascadeSessionRenameToTerminal port"
+    - "test: patch_rename_cascades_all_four_effects_to_a_live_terminal + patch_rename_to_a_retired_terminal_identity_does_not_cascade (sessions.rs)"
+    - "Unified priority ladder exists via can_upgrade_title (settings_store.rs:1208-1227)"
+  missing: "Forward direction (terminal/tabs rename → session) and provider-title-update push (updateSessionTitle event) not verified in the time available; tab-level rename path in terminals.rs un-inspected."
+  effort: M
+  files:
+    - crates/freshell-server/src/sessions.rs
+    - crates/freshell-server/src/terminals.rs
+
+- id: SESSION-13
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "crates/freshell-protocol/src/settings.rs:58-63 SettingsSidebar { auto_generate_titles, exclude_first_chat_must_start, exclude_first_chat_substrings }"
+    - "GET /api/settings returns full persisted store (settings_store.rs:1623-1628); PATCH validates top-level 'sidebar' (settings_store.rs:1390) and deep_merge persists to config.json (settings_store.rs:1320)"
+    - "test: patch_write_through_reaches_get_and_config_json_and_restart (settings_store.rs:2078) proves settings write-through + restart persistence (generic, not excludeFirstChat-specific)"
+    - "Application is shared client-side: src/store/selectors/sidebarSelectors.ts:69-70,625-660 filter ANY provider's items; WS hello/bootstrap settings fixtures include excludeFirstChatSubstrings (freshell-ws/tests/common/mod.rs:56)"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-protocol/src/settings.rs
+    - crates/freshell-server/src/settings_store.rs
+
+- id: SESSION-14
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-sessions/src/time.rs:26-40 parse_timestamp_ms casts f64→i64 ('f as i64' = floors positive fractional ms); ISO parser zeroes sub-ms beyond 3 digits (:107-118)"
+    - "All directory timestamps are i64: IndexedSession.last_activity_at/created_at (directory_index.rs:74-75); DirItem serialization session_directory.rs:147,162-164"
+    - "Deterministic ordering: sort lastActivityAt DESC then key DESC (session_directory.rs:1092-1096); sessions.changed carries only revision (sessions.rs:209-210; main.rs:2132 via broadcast_sessions_changed)"
+    - "test: snapshot_is_sorted_last_activity_desc_then_key_desc (directory_index.rs:1727) pins ordering"
+  missing: "No test observed pinning FRACTIONAL-millisecond flooring in the directory/API projection (boundary/fractional fixture + restart-stability assertions absent); time.rs module itself has no tests."
+  effort: S
+  files:
+    - crates/freshell-sessions/src/time.rs
+    - crates/freshell-server/src/session_directory.rs
+
+- id: SESSION-15
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "Server-side visibility flags: DirQuery include_subagents/include_non_interactive/include_empty parsed (session_directory.rs:208-219) and applied as pre-filter (:1098-1113)"
+    - "test: default_query_hides_non_interactive_fixtures (:1606), include_non_interactive_surfaces_titled_session (:1627), include_empty_surfaces_untitled_sessions_sorted_desc (:1645), codex_exec_session_hidden_by_default_surfaced_with_flag (:2332) in crates/freshell-server/src/session_directory.rs"
+    - "Browser-local persistence/isolation of these switches is shared-frontend (localStorage) code, identical for both servers; server parity surface = the query params above"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-server/src/session_directory.rs
+
+- id: SESSION-16
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "test: invalid_utf8_transcript_is_indexed_lossily_like_node (session_directory.rs:1568) — invalid UTF-8 record indexed lossily, others stay healthy"
+    - "Bad-record quarantine/skip: excluded files cached as exclusion (item None) but re-parsed when (mtime,size) changes — directory_index.rs module doc :19-27; tests excluded_file_cached (:1967), changed_file_single_reparse (:1844) — a record that becomes valid is indexed on next sweep"
+    - "parse failures never fatal: r10b_cwdless_repair_fixture_never_surfaces_under_any_flags (session_directory.rs:1669); sweep panic preserves cached snapshot: perform_refresh_preserves_cached_snapshot_when_blocking_sweep_panics (directory_index.rs:2329)"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-sessions/src/directory_index.rs
+    - crates/freshell-server/src/session_directory.rs
+
+- id: SESSION-17
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "Every override/mutation/join keyed provider:sessionId: DirItem::key (session_directory.rs:136-138), composite_key (sessions.rs:80-86), apply_session_overrides lookup by full key (:705), join_running_state matches provider AND session_id (:765-768)"
+    - "test: session_override_applies_to_codex_and_opencode_keys (session_directory.rs:2405) proves provider-scoped override keys"
+    - "Defaults provider=claude only when query param absent (sessions.rs:88-93), mirroring legacy"
+  missing: "No test observed that seeds the SAME raw id under two providers and proves rename/archive/delete/running/open never cross-leak (unit or PW); same-id alias cases untested."
+  effort: S
+  files:
+    - crates/freshell-server/src/session_directory.rs
+    - crates/freshell-server/src/sessions.rs
+
+- id: SESSION-18
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "Extension machinery exists: crates/freshell-server/src/extensions.rs (freshell.json manifest scan :125-181, toClientRegistry, CLI detection specs :208-209)"
+    - "Known-providers auto-enable: settings_store.rs read_persisted_known_providers(:1577) + test newly_discovered_providers_append_and_auto_enable (:1918)"
+    - "Resume provider set is hardcoded: KNOWN_RESUME_PROVIDERS = [claude, codex, opencode, amplifier] (resolve.rs:233); SessionIndex sources are the four built-ins (directory_index.rs/amplifier.rs)"
+  missing: "No generic extension-owned SESSION provider discovery/resume path: scan sources and resume fallbacks are hardcoded built-ins; an installed session-providing extension would not surface history/search/open (Amplifier is close but first-class)."
+  effort: L
+  files:
+    - crates/freshell-sessions/src/directory_index.rs
+    - crates/freshell-server/src/resolve.rs
+    - crates/freshell-server/src/extensions.rs
+
+- id: SESSION-19
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "Match tier labels correct: title/summary/firstUserMessage precedence (session_directory.rs:1259-1276); userMessage/assistantMessage (search.rs:139-146, matched_in)"
+    - "Bounded snippets: title tier truncates to 140 chars (:1270); file tier 50-char context with ... ellipses (search.rs:78-88); no-match returns first-100-chars (search.rs:74-76, test extract_snippet_no_match_returns_first_100_chars)"
+    - "test: tier tests 2530-2738 session_directory.rs; snippet tests search.rs:217-238"
+  missing: "No tests seen for HTML/control-character safety or escaping, and no neighbor-secret isolation test (snippet could include adjacent text by design — bounded but not escape-verified)."
+  effort: S
+  files:
+    - crates/freshell-sessions/src/search.rs
+    - crates/freshell-server/src/session_directory.rs
+
+- id: SESSION-20
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "SessionIndex cached read model: TTL snapshot + per-file (mtime,size) incremental cache, exclusions cached, prune-on-delete — directory_index.rs:1-33 module doc, :969-1130"
+    - "test: within_ttl_a_second_snapshot_call_does_not_rescan (:1749), unchanged_warm_sweep_reparses_zero (:1925), changed_file_single_reparse (:1844), claude_source_warm_sweep_of_6k_files_reparses_zero_and_completes_under_500ms (:2133, #[ignore]d perf), foreground_snapshot_never_blocks_on_an_inflight_sweep (:2243) — scan counters prove zero re-parse after initial indexing and single-file incremental update"
+    - "Foreground latency isolation: stale-while-revalidate snapshot-swap (directory_index.rs:2190-2220 comment block)"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-sessions/src/directory_index.rs
+
+- id: SESSION-21
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No Rust port of legacy history repair: grep for 'backfill|history_repair|history.jsonl' across crates/* finds only unrelated amplifier-bundle backfill (bundle_config.rs:24) and an unrelated comment in opencode_ws.rs:525; no freshell module implements repairing Claude's history.jsonl"
+    - "legacy reference: server/session-scanner/history-repair.ts (ClaudeHistoryEntry repair with 'created'|'already_present'|'skipped' status)"
+  missing: "Entire orphan-transcript → history.jsonl idempotent backfill (detect, single insert, restart-safe no-duplication)."
+  effort: M
+  files:
+    - crates/freshell-sessions/src/directory_index.rs
+
+- id: SESSION-22
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "Full legacy fallback key chain implemented for resolve only: lookup_session_override resolve.rs:477-502 (composite provider:id → bare id → legacy claude basename keys, first-present-wins, null-filtered)"
+    - "Overrides written with provider-qualified keys: sessions.rs composite_key :80-86; load_session_overrides settings_store.rs:1253"
+    - "test: session_override_applies_to_codex_and_opencode_keys (session_directory.rs:2405) for modern keys"
+  missing: "Sidebar projection applies ONLY the exact provider:sessionId key (apply_session_overrides session_directory.rs:705) — legacy raw-id keys and basename-shaped keys are NOT honored in the directory list the way resolve.rs does it; no migration of old raw-ID keys found; 528-style legacy-entry precedence tests absent."
+  effort: M
+  files:
+    - crates/freshell-server/src/session_directory.rs
+    - crates/freshell-server/src/resolve.rs
+    - crates/freshell-server/src/settings_store.rs
diff --git a/docs/plans/parity-ledger/audit/p0-terminal-creation-restoration-and-safety.yaml b/docs/plans/parity-ledger/audit/p0-terminal-creation-restoration-and-safety.yaml
new file mode 100644
index 000000000..62685a055
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p0-terminal-creation-restoration-and-safety.yaml
@@ -0,0 +1,388 @@
+- id: TERM-01
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-platform/src/cli_launch.rs:177 — CODEX_MANAGED_REMOTE_CONFIG_ARGS launch-arg constant"
+    - "crates/freshell-platform/src/cli_launch_goldens.rs:278 — golden argv fixtures include features.apps=false (per-mode launch goldens exist)"
+    - "crates/freshell-terminal/src/pty.rs:182 — spawn resolves program via freshell_platform::path::resolve_program_via_path before CommandBuilder"
+    - "crates/freshell-codex/src/launch_plan.rs:33 and crates/freshell-codex/src/launch_lifecycle.rs:848 — codex app-server launch planning/runtime"
+  missing: "Per-mode coverage (shell, WSL, CMD, PowerShell, Claude, Codex, OpenCode, Amplifier, Gemini, Kimi, extension modes) not individually verified; no per-mode e2e/pane-picker spec found in this audit window; Windows/WSL path-conversion behavior unverified (examination interrupted)."
+  effort: L
+  files:
+    - crates/freshell-platform/src/cli_launch.rs
+    - crates/freshell-terminal/src/pty.rs
+    - crates/freshell-ws/src/terminal.rs
+
+- id: TERM-02
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-freshagent/src/codex.rs:1952,1975 — spawns `codex -c features.apps=false app-server --listen ws://127.0.0.1:<port>` (managed remote path, apps feature disabled)"
+    - "crates/freshell-platform/src/cli_launch.rs:177 — CODEX_MANAGED_REMOTE_CONFIG_ARGS = ['-c','features.apps=false']"
+    - "crates/freshell-codex/src/launch_lifecycle.rs:848 + launch_plan.rs:33,322 — managed argv `--remote <wsUrl> -c features.apps=false` with ownership tag"
+    - "test: crates/freshell-ws/tests/codex_managed_launch_e2e.rs:346,434 — asserts ['-c','features.apps=false'] argv adjacency"
+    - "test: crates/freshell-codex/tests/launch_lifecycle.rs:906 — asserts args[3] == features.apps=false"
+    - "test: test/e2e-browser/specs/restore-matrix.spec.ts:920 — 'FreshCodex targets the same durable thread with no duplicate conversation after a full server restart'"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-freshagent/src/codex.rs
+    - crates/freshell-codex/src/launch_plan.rs
+    - crates/freshell-ws/tests/codex_managed_launch_e2e.rs
+
+- id: TERM-03
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-protocol/tests/roundtrip.rs:286 — terminal.create carries sessionRef + codexDurability{schemaVersion,state} (durable identity shape)"
+    - "crates/freshell-ws/src/reconcile.rs and auto_resume.rs — sessionRef restore/resume machinery present"
+    - "test: crates/freshell-ws/tests/codex_session_ref_resume.rs, session_ref_singleflight.rs, resume_validation_gate.rs (files confirmed to exist via grep hits)"
+    - "test: test/e2e-browser/specs/amplifier-restore-rust.spec.ts and opencode-terminal-restore-rust.spec.ts exist (restore across restart for those providers)"
+  missing: "Migration of older resumeSessionId data unverified; proven identity + durability returned in `terminal.created` not confirmed at payload level; 16-tab imported-fixture restore for all four providers not verified (examination interrupted)."
+  effort: M
+  files:
+    - crates/freshell-ws/src/reconcile.rs
+    - crates/freshell-ws/src/auto_resume.rs
+    - crates/freshell-sessions/src
+
+- id: TERM-04
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-ws/src/create_dedupe.rs — dedicated create-request dedupe module"
+    - "crates/freshell-terminal/src/registry.rs:249,356,366 — create_request_id fields + respawn-generation counters keyed per createRequestId"
+    - "test: crates/freshell-ws/tests/create_protection.rs:338-339 — registry-wide adoption by create_request_id (newest_live_by_create_request_id), same-socket safe"
+    - "test: crates/freshell-ws/tests/pane_reconcile.rs:427-441 — two creates under one createRequestId resolve to the winner via newest_live_by_create_request_id"
+    - "test: crates/freshell-ws/tests/pane_ledger_restore.rs:105-115 — createRequestId lineage across restart (orphaned in-flight create)"
+  missing: "Two-clients-same-request and delayed-response/reconnect idempotency proven at registry/reconcile level, but the PW validation (intercept/delay terminal.created, force reconnect, same request from two pages, one PID) not confirmed in this window."
+  effort: M
+  files:
+    - crates/freshell-ws/src/create_dedupe.rs
+    - crates/freshell-terminal/src/registry.rs
+
+- id: TERM-05
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-protocol/src/client_messages.rs:219 — terminal.create.live_terminal (LiveTerminalRef) protocol field"
+    - "crates/freshell-terminal/src/registry.rs:2374-2375 — live_terminal_for_session_ref() canonical owner lookup; :4636 test second_live_terminal_on_one_key_emits_duplicate_pty_warning"
+    - "crates/freshell-ws/src/reconcile.rs:203-253 — live_terminal_for_claimed_ref; test row1_live_terminal_under_key_yields_attach (:601) and legacy_resume_claim_attaches_to_live_terminal_across_create_request_ids (:1083)"
+    - "test: crates/freshell-ws/tests/session_ref_singleflight.rs:122 — different connections, different createRequestIds, same sessionRef resume -> single owner"
+    - "test: crates/freshell-ws/tests/live_session_ref_guard.rs (exists; guards create within createRequestId ladder)"
+  missing: "Byte-for-byte shared output across two simultaneous contexts and the orchestration-API path of the PW validation not verified; whether ALL views provably attach to one terminal in every path not confirmed."
+  effort: M
+  files:
+    - crates/freshell-terminal/src/registry.rs
+    - crates/freshell-ws/src/reconcile.rs
+    - crates/freshell-ws/tests/session_ref_singleflight.rs
+
+- id: TERM-06
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-protocol/src/client_messages.rs:270,291,301 — expected_session_ref field on attach/input/resize messages"
+    - "crates/freshell-protocol/tests/roundtrip.rs:225 — SESSION_IDENTITY_MISMATCH error wire shape with expectedSessionRef+actualSessionRef"
+    - "crates/freshell-ws/src/terminal.rs:646 — input-path expectedSessionRef guard; :4155 — attach-path guard; :4199-4261 — guard semantics + error construction"
+    - "test: crates/freshell-ws/tests/attach_viewport_resize.rs:58 — mismatched_expected_session_ref_does_not_resize (no resize, no epoch bump)"
+    - "test: crates/freshell-ws/tests/opencode_switch_rebind.rs:530-567 — mismatched expectedSessionRef bounced; matching one delivered"
+  missing: "Restore-operation identity validation not verified; 'block input while identity is UNRESOLVED' (pending, not just mismatched) unverified — that is TERM-24 territory; full four-op table-driven PW validation with exact error frames not confirmed in this window."
+  effort: M
+  files:
+    - crates/freshell-ws/src/terminal.rs
+    - crates/freshell-terminal/src/registry.rs
+
+- id: TERM-07
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-terminal/src/registry.rs:1394 — resize_for_attach(); :232,:1015,:1365-1431 — has_client_geometry first-record-no-bump epoch semantics consulted by resize and resize_for_attach"
+    - "test: crates/freshell-terminal/src/registry.rs tests resize_for_attach_viewport_hydrate_applies_first_geometry_without_bump (:3689), second_change_bumps_epoch (:3700), keepalive_delta_never_resizes_or_records (:3724), transport_reconnect_applies_when_alone (:3739)/skips_when_other_socket_attached (:3749), viewport_hydrate floors dims at broker parity (:3639), missing/exited terminal (:3777,:3784)"
+    - "test: crates/freshell-ws/tests/attach_viewport_resize.rs exists (incl. mismatched_expected_session_ref_does_not_resize:58)"
+    - "crates/freshell-protocol/tests/roundtrip.rs:299 — terminal.attach wire shape carries intent(viewport_hydrate), cols/rows, attachRequestId, priority(foreground), sinceSeq, maxReplayBytes (all fields protocol-defined)"
+  missing: "priority foreground/background POLICY enforcement and maxReplayBytes replay-budget enforcement remain unimplemented per prior audit note (2026-07-24) and not contradicted here; attachRequestId reply-correlation e2e unverified."
+  effort: M
+  files:
+    - crates/freshell-terminal/src/registry.rs
+    - crates/freshell-ws/src/terminal.rs
+    - crates/freshell-ws/tests/attach_viewport_resize.rs
+
+- id: TERM-08
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-protocol/src/server_messages.rs:135,202 — terminal.stream.changed frame type exists in protocol inventory"
+    - "crates/freshell-ws/src/backpressure.rs:1-152 — queue-overflow backpressure machinery (drop-oldest with gap-event coalescing per module docs and TERM-09 note)"
+  missing: "Emission sites verified only partially: explicit output-gap events for retention-window loss vs replay-budget truncation vs queue overflow not individually confirmed by me; terminal.stream.changed EMISSION on stream replacement (new streamId + checkpoint reset) not located in this window; no PW validation seen."
+  effort: M
+  files:
+    - crates/freshell-ws/src/backpressure.rs
+    - crates/freshell-protocol/src/server_messages.rs
+    - crates/freshell-ws/src/terminal.rs
+
+- id: TERM-09
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-ws/src/backpressure.rs — TERM-09 module: bounded per-connection output queue + catastrophic monitor (Term09Config; :60 catastrophic threshold ported from legacy constants.ts:8-11)"
+    - "crates/freshell-ws/src/lib.rs:236-239 — term09: Term09Config wiring on connection state"
+    - "crates/freshell-ws/src/terminal.rs:168 ConnectionOutputQueue, :204 CatastrophicMonitor, :373-378 close code 4008 'Catastrophic backpressure'; :399-425 second 4008 ('can't keep up') path"
+    - "test: crates/freshell-ws/tests/term09_output_queue.rs exists (real-socket queue-cap tests per prior audit, file confirmed)"
+  missing: "visible-first pacing; background throttling (both depend on TERM-07 attach-priority, absent); stress-project PW validation (HARNESS-05 delaying proxy, RSS bounds); known narrow gap: single indefinitely-blocked send with quiet terminal escapes the catastrophic ticker (documented in backpressure.rs)."
+  effort: M
+  files:
+    - crates/freshell-ws/src/backpressure.rs
+    - crates/freshell-ws/src/terminal.rs
+    - crates/freshell-ws/tests/term09_output_queue.rs
+
+- id: TERM-10
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "crates/freshell-ws/src/lib.rs:55 — a `create_limit` config knob is referenced alongside auto_resume/backpressure (parsing exists)"
+    - "Examination of throttling/cap/shutdown-rejection enforcement and tests was interrupted before confirmation."
+  missing: "Per-connection creation throttling, running-terminal cap, shutdown-time rejection, canonical error responses — none verified; PW burst-create validation not found (not searched to completion)."
+  effort: M
+  files:
+    - crates/freshell-ws/src/lib.rs
+    - crates/freshell-ws/src/terminal.rs
+
+- id: TERM-11
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "crates/freshell-terminal/src/registry.rs:538-544,720,774-781 — auto_kill_idle_minutes (atomic, legacy default 15 per server/settings.ts:791); :857-879 enforce_idle_kills sweep (<=0 disabled)"
+    - "crates/freshell-ws/src/lib.rs:398-408 — background idle-reaper task reads live registry value each sweep"
+    - "crates/freshell-server/src/main.rs:313-317 — boot seeds registry from settings.safety.autoKillIdleMinutes; settings_store.rs:1660-1678 PATCH /api/settings pushes live"
+    - "test: registry.rs:3842-4134 — enforce_idle_kills suite: new_registry_defaults_auto_kill_idle_minutes_to_legacy_default (:3847), eligibility sweeps (:3860-4118 incl. active/attached preservation cases), disabled at 0 and -1 (:4118,:4134)"
+    - "test: crates/freshell-server/src/settings_store.rs:1730 — patch_applies_auto_kill_idle_minutes_live_to_registry (15 -> 42 live)"
+    - "test: crates/freshell-ws/tests/pane_reconcile.rs:766 — stale idle row eligible for sweep"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-terminal/src/registry.rs
+    - crates/freshell-ws/src/lib.rs
+    - crates/freshell-server/src/settings_store.rs
+
+- id: TERM-12
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "Examination interrupted before searching for the exited-record cap; no evidence gathered beyond unrelated 'exited' mentions (e.g. crates/freshell-ws/tests/auto_resume_e2e.rs:141)."
+  missing: "Exited-record cap constant/pruning, never-prune-running guarantee, restart non-persistence, and any tests — all unverified."
+  effort: S
+  files:
+    - crates/freshell-terminal/src/registry.rs
+
+- id: TERM-13
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "test: test/e2e-browser/specs/term13-scrollback-boundary.spec.ts exists (two scrollback settings, numbered emoji content, first/last markers + boundary-scoped search per prior audit)"
+    - "test: test/e2e-browser/specs/settings-live-reload.spec.ts exists (live PATCH of terminal.scrollback applies to later-created terminal)"
+    - "crates/freshell-server/src/settings_store.rs:1795,1833 — PATCH merges terminal.scrollback; main.rs:1173 — scrollback kept live"
+  missing: "Rust ingest/eviction code paths (registry.rs/chunk_ring.rs/replay_ring.rs) not personally re-verified this session; legacy's unbounded-mirror search divergence documented — Rust search is boundary-scoped by design, so the item's literal 'search changes at boundary' clause is proven only against the improved Rust behavior."
+  effort: S
+  files:
+    - crates/freshell-terminal/src/registry.rs
+    - crates/freshell-server/src/settings_store.rs
+
+- id: TERM-14
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "Not investigated before time ran out (no greps on metadata/git-branch/token-usage in crates/ were executed)."
+  missing: "Git branch/dirty publication, token usage, provider/model header/sidebar metadata, change-driven refresh — all unverified."
+  effort: L
+  files:
+    - crates/freshell-terminal/src
+    - crates/freshell-ws/src/terminal.rs
+
+- id: TERM-15
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-ws/src/activity.rs:13-14,462-474 — hub answers *.activity.list from live tracker state (reconnect seeding); claude/codex/amplifier list-state fns at :462,:468,:474"
+    - "crates/freshell-activity/src/{claude.rs,codex.rs,amplifier/tracker.rs,ledger.rs} — per-provider trackers + completionSeq ledger (:12-51)"
+    - "test: crates/freshell-protocol/tests/activity_extension.rs exists (amplifier.activity.* extension surface)"
+    - "test: crates/freshell-ws/src/activity.rs:1994 — gemini_and_kimi_are_status_inert"
+    - "test: test/e2e-browser/specs/terminal-activity-rust.spec.ts exists (claude/codex/amplifier updates + reload reseed per prior audit)"
+  missing: "OpenCode live updates lane (legacy uses SSE off the shared opencode-serve sidecar) not confirmed in the activity hub — only three list-state accessors were visible in the grep window (:462-474); whether opencode.activity.updated is broadcast remains unverified."
+  effort: M
+  files:
+    - crates/freshell-ws/src/activity.rs
+    - crates/freshell-activity/src
+
+- id: TERM-16
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-protocol/src/server_messages.rs:1157 + common.rs:392 — terminal.turn.complete carries completionSeq"
+    - "crates/freshell-activity TurnComplete effects: claude.rs:179-285, codex.rs:860-865, amplifier/tracker.rs:230,471 (positive-only signals per prior audit: BEL hooks, events.jsonl turn boundaries)"
+    - "crates/freshell-ws/src/activity.rs:1265-1382 — completion broadcast emission; tests at :1597-1610 (completionSeq 1 on first completion)"
+    - "test: activity.rs:1994 gemini_and_kimi_are_status_inert"
+    - "test: test/e2e-browser/specs/terminal-activity-rust.spec.ts (frame exactly once per turn + monotonic seq across reload, per prior audit; file confirmed)"
+  missing: "Green-attention/sound DOM edge not asserted in PW (client-side edge); dedup after full SERVER restart (vs page reload) not verified here; 'one sound only on success across all signal-capable providers' beyond claude/codex/amplifier unproven."
+  effort: M
+  files:
+    - crates/freshell-activity/src
+    - crates/freshell-ws/src/activity.rs
+
+- id: TERM-17
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-protocol/src/server_messages.rs:131,137 — terminal.session.associated and terminal.title.updated frame types defined"
+    - "crates/freshell-ws/src/{codex_association.rs,opencode_association.rs} — association modules with tests (codex_association.rs:545,658 and opencode_association.rs:289,614 test hits confirmed)"
+    - "test: test/e2e-browser/specs/amplifier-restore-rust.spec.ts and opencode-terminal-restore-rust.spec.ts exist (association written into pane.content.sessionRef/resumeSessionId per prior audit)"
+  missing: "terminal.title.updated monotonic/dedup-under-replay ordering (old/duplicate events must not revert newer identity/title) not implemented/verified; amplifier association source files not located by my single-level glob of freshell-sessions (may live in a subdirectory — unverified); title-update emission sites not confirmed."
+  effort: M
+  files:
+    - crates/freshell-ws/src/codex_association.rs
+    - crates/freshell-ws/src/opencode_association.rs
+    - crates/freshell-sessions/src
+
+- id: TERM-18
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "test: test/e2e-browser/specs/restore-matrix.spec.ts:1141 — 'a crashed Codex provider process is recovered mid-turn with no chime, and the same durable session continues' (crash detection, activity cleared via seeded positive control, no chime, durable id preserved; prior audit: green both projects 2x)"
+    - "test: crates/freshell-freshagent/src/codex.rs:5469 — crash_recovery_resume_wrong_thread_id_is_rejected_not_silently_recovered (crash-recovery resume path exists and is hardened)"
+    - "crates/freshell-activity/src tracker reversion paths + freshell-ws activity hub referenced by prior audit for blue-clear / no-false-completion on crash"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-freshagent/src/codex.rs
+    - crates/freshell-ws/src/activity.rs
+    - test/e2e-browser/specs/restore-matrix.spec.ts
+
+- id: TERM-19
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-ws/src/terminal.rs:17,:678,:4305-4311 — input to unknown terminal answers terminal.input.blocked{reason:unknown_terminal} (not silently ignored)"
+    - "test: crates/freshell-ws/tests/unknown_terminal_reply.rs:11 input_to_unknown_terminal_answers_input_blocked; :23 input_to_live_terminal_round_trips_without_a_blocked_frame"
+    - "test: crates/freshell-protocol/tests/roundtrip.rs:217,225 — INVALID_TERMINAL_ID and SESSION_IDENTITY_MISMATCH error wire shapes roundtrip"
+    - "crates/freshell-ws/src/terminal.rs:5799-5847 — invalid_dims_error / terminal_dims_in_range / unknown_terminal serialization tests"
+  missing: "Unknown-id behavior for ATTACH, RESIZE and DETACH not fully verified (detach must not be falsely acknowledged); malformed-message rejection consistency across the dispatch not confirmed; legacy error-code parity table and the HARNESS-05 PW validation unverified."
+  effort: M
+  files:
+    - crates/freshell-ws/src/terminal.rs
+    - crates/freshell-protocol/src/server_messages.rs
+
+- id: TERM-20
+  verdict: HOST-LIMITED
+  confidence: low
+  evidence:
+    - "crates/freshell-platform/src/{cli_launch.rs,cli_launch_goldens.rs,spawn.rs,elevated.rs} exist — launch planning + golden argv fixtures are present (cli_launch_goldens.rs:278 shows golden argv entries)"
+    - "Native Windows quoting/WSL path-conversion specifics were not examined (interrupted); the item's PW validation is a native-Windows project, unrunnable on this Linux host."
+  missing: "Windows argv quoting (spaces, quotes, backticks, Unicode, hooks, model names) and WSL/Windows path conversion unverified; per-character-class golden coverage unknown; PW-RUST native-Windows validation outstanding."
+  effort: L
+  files:
+    - crates/freshell-platform/src/cli_launch.rs
+    - crates/freshell-platform/src/cli_launch_goldens.rs
+    - crates/freshell-terminal/src/pty.rs
+
+- id: TERM-21
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "Not investigated before time ran out (no greps for viewport/paged-scrollback endpoints were executed against crates/ API surface)."
+  missing: "Viewport endpoint, paged-scrollback endpoint, cursor/revision semantics, invalid/stale/unknown request responses — all unverified."
+  effort: L
+  files:
+    - crates/freshell-api/src
+    - crates/freshell-server/src
+
+- id: TERM-22
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "test: crates/freshell-server/tests/safe11_term22_shutdown_reaping.rs exists (real binary boot, real PTY + fake-codex session, SIGTERM descendant reaping with zombie/stale-pid hardening per prior audit; .env FRESHELL_HOME at :269 confirmed)"
+    - "crates/freshell-codex/src/launch_lifecycle.rs exists (CodexLaunchRuntime, managed spawn with ownership tag per :848 and launch_plan.rs:322)"
+  missing: "Failed-startup/cancellation/crash/resume cycle hardening, startup-child reaping beyond shutdown, leak/OOM protection, expected-restart silence, and the PW/stress-project (HARNESS-12 RSS, in-browser no-toast) validation — not verified here; existing proof is a Rust integration test, not the item's PW validation."
+  effort: L
+  files:
+    - crates/freshell-server/tests/safe11_term22_shutdown_reaping.rs
+    - crates/freshell-codex/src/launch_lifecycle.rs
+    - crates/freshell-freshagent/src/codex.rs
+
+- id: TERM-23
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-protocol/tests/roundtrip.rs:286 — codexDurability{schemaVersion:1,state:'identity_pending'} is protocol-defined on terminal.create"
+    - "crates/freshell-freshagent/src/terminal_tabs.rs:2908 — create_tab_passes_codex_durability_through_and_records_restore_key; :4149,:4240,:5169 — PIN 2 durability-before-registration ordering tests"
+  missing: "The candidate-persistence handshake itself: processing terminal.codex.candidate.persisted from the client, durability-status publication, and promoting a candidate identity ONLY after client persistence — no handling of a persisted-ack message was found in this audit window; PW validation unverified."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+    - crates/freshell-protocol/src/client_messages.rs
+    - crates/freshell-ws/src/terminal.rs
+
+- id: TERM-24
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-protocol/src/server_messages.rs:117,193 — terminal.input.blocked frame type in protocol inventory"
+    - "crates/freshell-ws/src/terminal.rs:4305-4311 + test crates/freshell-ws/tests/unknown_terminal_reply.rs — unknown_terminal blocked reason emitted; roundtrip.rs:475-479 pins the wire shape"
+  missing: "The reason MATRIX beyond unknown_terminal (identity pending, capture timeout, identity unavailable, recovery pending, clean-exit decision pending, lifecycle-loss pending) not found in the greps run; blocked-input buffering/exactly-once-or-returned-for-retry semantics unverified; PW table-drive unverified."
+  effort: L
+  files:
+    - crates/freshell-ws/src/terminal.rs
+    - crates/freshell-protocol/src/server_messages.rs
+    - crates/freshell-freshagent/src/codex.rs
+
+- id: TERM-25
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "test: crates/freshell-freshagent/src/codex.rs:5356 handle_create_with_resume_wrong_thread_id_fails_create_and_never_adopts; :5420 handle_attach_unknown_session_wrong_thread_id_is_rejected_not_adopted; :5469 crash_recovery_resume_wrong_thread_id_is_rejected_not_silently_recovered (all three resume sites reject wrong-thread answers)"
+    - "test: test/e2e-browser/specs/restore-matrix.spec.ts:1682 — SCENARIO 7 TERM-25 wrong-thread recovery (prior audit: green unpinned both projects after a test bug fix; pin status not personally re-read)"
+  missing: "Per prior audit: 'B output never appears' at e2e level, 'input stays blocked' during the mismatch, a dedicated expected/actual identity UI surface, and the explicit recovery choice (start fresh / reconnect A) e2e — these remain unproven."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/codex.rs
+    - test/e2e-browser/specs/restore-matrix.spec.ts
+
+- id: TERM-26
+  verdict: HOST-LIMITED
+  confidence: medium
+  evidence:
+    - "crates/freshell-server/src/main.rs:1708-1741 — resolve_home(): FRESHELL_HOME preferred; USERPROFILE-backed on Windows, passwd-entry on POSIX (Node os.homedir() semantics ported; :1297,:1645,:1717-1741)"
+    - "test: main.rs:2449-2493 — POSIX must NEVER consult USERPROFILE (:2464-2469); Windows must read USERPROFILE and never consult HOME (:2489-2493); main.rs:2532-2588 exact-id USERPROFILE fallback tests"
+    - "test: crates/freshell-server/src/session_directory.rs:1362-1516 — passwd-entry vs USERPROFILE platform rules incl. 'Windows must read USERPROFILE...' (:1502-1507), empty-HOME (:1444-1452), set-HOME wins (:1473-1478)"
+    - "crates/freshell-ws/src/codex_identity.rs:30 — USERPROFILE consulted for Windows codex home; crates/freshell-tauri/src/server.rs:90-92 — Tauri passes HOME+FRESHELL_HOME"
+  missing: "Native-Windows PW validation cannot run on this Linux host (isolated USERPROFILE boot + terminal cwd ~ expansion); terminal cwd `~` EXPANSION specifically not verified in the greps run (home resolution itself is thoroughly unit-tested)."
+  effort: M
+  files:
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-server/src/session_directory.rs
+    - crates/freshell-platform/src/path.rs
+
+- id: TERM-27
+  verdict: PARTIAL
+  confidence: low
+  evidence:
+    - "crates/freshell-activity/src/amplifier/tracker.rs:230,471 — amplifier events reducer/tracker emits TurnComplete with completionSeq (events-lane completion; deadman force-read stays busy per prior audit)"
+    - "test: test/e2e-browser/specs/amplifier-restore-rust.spec.ts exists (amplifier pane restores across restart via `amplifier resume <id>`; never-submitted pane restores fresh)"
+    - "crates/freshell-protocol/tests/activity_extension.rs — amplifier.activity.* extension surface pinned"
+  missing: "Per-clause mapping was not completed (interrupted): spawn/prompt race order, cwd ambiguity, EOF resume, partial/reset/schema-invalid logs, missed-watcher force-read, long-turn no-false-completion, and sidecar-authoritative recency are not individually verified here; no dedicated TERM-27 PW table-drive spec was confirmed."
+  effort: L
+  files:
+    - crates/freshell-activity/src/amplifier/tracker.rs
+    - crates/freshell-sessions/src
+    - crates/freshell-ws/src
+
+- id: TERM-28
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-platform/src/path.rs:868 — resolve_program_via_path ($PATH-only, cwd never consulted, is_file/executable validated per candidate); exported at lib.rs:80"
+    - "crates/freshell-terminal/src/pty.rs:182 — PtyTerminal spawn resolves bare names via resolve_program_via_path BEFORE CommandBuilder/portable-pty"
+    - "test: crates/freshell-platform/tests/path_resolve_tests.rs — shadow-dir resolution (:84), missing command structured error (:102-106), absolute-path unchanged (:118-122), non-executable/directory skipped, search continues (:149-153)"
+    - "test: test/e2e-browser/specs/term28-path-shadow-rust.spec.ts exists (shadow-directory + genuinely-missing-command scenarios per prior audit, upstream portable-pty clause waived)"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-platform/src/path.rs
+    - crates/freshell-terminal/src/pty.rs
diff --git a/docs/plans/parity-ledger/audit/p1-browser-panes-proxying-files-and-editors.yaml b/docs/plans/parity-ledger/audit/p1-browser-panes-proxying-files-and-editors.yaml
new file mode 100644
index 000000000..311955e13
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p1-browser-panes-proxying-files-and-editors.yaml
@@ -0,0 +1,160 @@
+- id: BROWSER-01
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/proxy.rs:101 — /api/proxy/http/{*tail} catch-all route (any method)"
+    - "crates/freshell-server/src/proxy.rs:153-154 — upstream URL preserves path + query (http://127.0.0.1:<port>/<rest>?<query>)"
+    - "crates/freshell-server/src/proxy.rs:158-169 — method preserved; request headers forwarded minus host/connection/transfer-encoding (STRIPPED_REQUEST_HEADERS line 68); body forwarded"
+    - "crates/freshell-server/src/proxy.rs:184-204 — upstream status echoed; response headers forwarded minus exactly the 3 iframe-blocking headers (IFRAME_BLOCKED_HEADERS lines 51-55) and framing headers; body streamed via Body::from_stream (line 201)"
+    - "crates/freshell-server/src/proxy.rs:142-144 — auth gate via boot::is_authed, mounted under /api like legacy (main.rs:1315)"
+    - "test: iframe_blocked_headers_are_the_original_set, port_validation_matches_original_bounds, tail_splits_port_from_path, target_url_composes_path_and_query in crates/freshell-server/src/proxy.rs"
+    - "test: e2e browser-pane.spec.ts runs against the Rust server in the rust-chromium project (test/e2e-browser/playwright.config.ts:23 includes it in MATRIX_SPECS; rust-chromium project at line 210)"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-server/src/proxy.rs
+
+- id: BROWSER-02
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/proxy.rs:33-35 — module doc explicitly states 'the WS-upgrade proxy (remote-only paths that require netsh/socket relay) are intentionally NOT ported here'"
+    - "grep for 'upgrade' across crates/freshell-server/src shows only the main /ws socket upgrade (logging.rs:42, main.rs:1336); no handler matches /api/proxy/http upgrade requests — they fall to the SPA fallback (main.rs:1317)"
+    - "legacy reference: server/proxy-router.ts:144-219 attachProxyUpgradeHandler — cookie auth (lines 169-185), raw socket relay preserving query/subprotocol/text/binary frames (190-205), 502 + destroy on upstream error (207-213)"
+  missing: "all clauses: WS upgrade interception on /api/proxy/http/<port>/..., query preservation, subprotocol passthrough, cookie authentication, binary/text frame relay, clean/error close behavior (401 unauthorized + 502 upstream-unavailable socket writes)"
+  effort: M
+  files:
+    - crates/freshell-server/src/proxy.rs
+    - crates/freshell-server/src/main.rs
+
+- id: BROWSER-03
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/proxy.rs:33-35 — '/api/proxy/forward TCP port-forward ... intentionally NOT ported here'"
+    - "grep for allowed_ips/max_forwards/requester/forwarder across crates/ found no PortForwardManager equivalent (only WSL netsh portproxy planning in crates/freshell-platform/src/port_forward.rs, a different feature)"
+    - "route inventory of main.rs merges (lines 1156-1316) contains no /api/proxy/forward route"
+    - "legacy reference: server/proxy-router.ts:45-77 (POST /forward, DELETE /forward/:port) + server/port-forward.ts PortForwardManager: per-requester create/reuse (forward(), lines ~51-70), maxForwards=100 cap (~line 40, ~67), 5-min idle cleanup (~line 39 startIdleCleanup), allowedIps connection rejection (~lines 80-96)"
+  missing: "all clauses: create/reuse/delete owned forwarders, per-owner isolation keyed by requester identity, max-forwards cap, idle cleanup, allowed-IP enforcement, protocol/path/query preservation through the forward"
+  effort: L
+  files:
+    - crates/freshell-server/src/proxy.rs
+    - crates/freshell-server/src/main.rs
+
+- id: BROWSER-04
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/proxy.rs:154 — target host hardcoded to 127.0.0.1; no non-loopback destination is constructible"
+    - "crates/freshell-server/src/proxy.rs:147-150 — port must parse as u32 in 1..=65535, else 400 {error:'Invalid port number'} (encoded targets fail the parse)"
+    - "crates/freshell-server/src/proxy.rs:142 — every proxy request requires auth via boot::is_authed (x-auth-token header or freshell-auth cookie, boot.rs:686-708, constant-time compare)"
+    - "test: port_validation_matches_original_bounds in crates/freshell-server/src/proxy.rs (0/1/65535/65536/-1/abc/empty cases)"
+    - "test: safe01-auth-matrix.spec.ts is in MATRIX_SPECS (playwright.config.ts:69) so the shared auth gate runs against the Rust server (its 'local-file'/'proxy' lanes are noted as not distinctly covered in that spec's doc comment, lines 33-36)"
+  missing: "per-requester forwarder ownership and allowed-IP requester restriction (absent with BROWSER-03 forwarding); unauthorized WebSocket-upgrade rejection (absent with BROWSER-02); no dedicated Rust test asserting wrong-auth rejection on the proxy route itself"
+  effort: M
+  files:
+    - crates/freshell-server/src/proxy.rs
+    - crates/freshell-server/src/boot.rs
+
+- id: BROWSER-05
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "crates/freshell-server/src/screenshots.rs:58 — POST /api/screenshots ported (scope validation 73-84, 400 bad name/path 91-94, 409 no-overwrite 96-101, 503 no capable client 104-109, 422 failed/imageless reply 136-144, 200 success envelope 161-177)"
+    - "crates/freshell-server/src/screenshots.rs:118-134 — 10s timeout (SCREENSHOT_TIMEOUT line 44) with cancel on timeout/closed socket: never spins forever"
+    - "crates/freshell-ws/src/screenshot.rs — ScreenshotBroker ports ws-handler requestUiScreenshot: capability tracking, register/resolve/send_capture broadcast of 'screenshot.capture' ui.command (lines 78-190)"
+    - "crates/freshell-server/src/proxy.rs:171-179 — upstream connect failure returns a target-specific 502 promptly (no indefinite spin); client Retry UI is retained server-agnostic SPA code (src/components/panes/BrowserPane.tsx)"
+    - "test: base_name_rules_match_original, no_path_uses_tmpdir, explicit_dir_path_joins_base_name, truthy_coercions, write_file_atomic_roundtrips in crates/freshell-server/src/screenshots.rs"
+    - "test: broker tests in crates/freshell-ws/src/screenshot.rs (mod tests ~lines 192-267, incl. capture-command JSON shape asserted at line 267)"
+    - "caveat: e2e browser-pane-screenshot.spec.ts exists but is NOT in MATRIX_SPECS, so it only runs against the legacy server in the current matrix; the Rust round-trip handler's timeout path is implemented but covered only by unit-level tests"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-server/src/screenshots.rs
+    - crates/freshell-ws/src/screenshot.rs
+
+- id: FILE-01
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "route inventory of crates/freshell-server/src (main.rs merges lines 1156-1316; files.rs router lines 93-103) contains no /local-file route; unmatched paths fall to the SPA fallback (main.rs:1317-1324)"
+    - "grep for local-file/local_file across crates/ found no implementation"
+    - "legacy reference: server/local-file-router.ts:42-79 — header x-auth-token or freshell-auth cookie auth (45-56), path query required (60-62), 404 File not found / 400 Cannot serve directories / 500 shapes (7-17), res.sendFile bytes with MIME (19-39)"
+    - "impact confirmed client-side: src/components/panes/BrowserPane.tsx:68-91 converts every file:// URL to /local-file?path=... so file-backed Browser panes cannot load authenticated bytes from the Rust server"
+  missing: "all clauses: authenticated /local-file endpoint, header-or-cookie credentials, correct MIME/bytes, stable 400/404/500 error shapes"
+  effort: S
+  files:
+    - crates/freshell-server/src/files.rs
+    - crates/freshell-server/src/main.rs
+
+- id: FILE-02
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "src/components/panes/BrowserPane.tsx:75-87 — retained client decode: file:///C:/path -> C:/path (drive prefix kept via regex-slice), file://server/share -> //server/share (UNC host reattached); this half is server-agnostic and exists today"
+    - "server side: no /local-file endpoint in the Rust server (see FILE-01 evidence), so the exactly-once decode of the path query param and byte-serving for drive/UNC targets has no Rust receiver; generic UNC \\\\server\\share inputs are also treated as unaddressable (resolve_user_path returns fs_path None — test resolve_user_path_windows_unresolvable_forms_are_unaddressable in crates/freshell-server/src/files.rs)"
+    - "validation note: the checklist's Playwright validation is PW-TAURI-WIN (Windows host + real SMB share), so end-to-end validation is Windows/Tauri-host-limited regardless"
+  missing: "server-side exactly-once decode + authenticated byte serving for file:///C:/... and file://server/share/... (rides entirely on the missing /local-file route); UNC/host preservation beyond the retained client conversion"
+  effort: S
+  files:
+    - crates/freshell-server/src/files.rs
+    - crates/freshell-server/src/main.rs
+
+- id: FILE-03
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-platform/src/path.rs — ported normalization core: sanitize_user_path_input (39), detect_user_path_flavor (69), get_wsl_mount_prefix (208), win32_resolve (241), convert_wsl_drive_path_to_windows_path /mnt/c branch (330), convert_windows_path_to_wsl_path drive+UNC branches (375), split/join_windows_display_path (454/484)"
+    - "crates/freshell-platform/src/path.rs:569-663 — convert_wsl_path_to_windows with injected WslPathResolver: reg.exe has_wsl_distributions guard + wsl.exe wslpath -w + 256-entry cache (native-Windows-only model; the no-distro guard that avoids the Windows Store prompt is ported as a seam)"
+    - "crates/freshell-server/src/files.rs:526-561,602-640 — ~ expansion, trailing-separator trim, resolve_user_path composing the flavor seam; read/stat/write/complete/mkdir/validate-dir all flow through it"
+    - "test: crates/freshell-platform/tests/path_tests.rs — flavor_detection_matrix, win32_resolve_goldens_from_node, wsl_drive_to_windows_* (default/root/custom mount), windows_to_wsl_default_mount_wsl_env, windows_to_wsl_unc_accepts_any_distro_when_current_is_empty, split/join matrices, launch_cwd matrices"
+    - "test: crates/freshell-platform/tests/wsl_fallback_tests.rs — reg_guard_blocks_wsl_exe_when_no_distributions, mount_mapped_short_circuits_before_touching_wsl_exe, falls_through_to_wslpath_and_caches_the_result, caches_negative_results_too"
+    - "test: crates/freshell-server/src/files.rs — resolve_user_path_windows_drive_on_wsl_maps_to_mount, resolve_user_path_windows_off_wsl_is_unaddressable, complete_wsl_unc_partial_leaf_round_trips, read_stat_write_follow_windows_conversion_on_wsl"
+  missing: "\\\\wsl$ share form explicitly not handled (path.rs:533 note: only \\\\wsl / \\\\wsl.localhost); generic network shares (\\\\server\\share) unaddressable (fs_path None); win32 case-folding semantics absent on this Linux-host port (documented at files.rs:652 — Node lowercases only on win32); native-Windows live behaviors validated only through injected seams, not on a real Windows host"
+  effort: M
+  files:
+    - crates/freshell-platform/src/path.rs
+    - crates/freshell-server/src/files.rs
+
+- id: FILE-04
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/files.rs:93-103 — files router has candidate-dirs/validate-dir/read/stat/write/complete/mkdir only; no /api/files/open"
+    - "grep for resolveOpenCommand/file-opener/open_external file-editor equivalents across crates/ found only URL opening for the desktop shell (crates/freshell-tauri/src/external_url.rs:83 open_external_url), not file/editor opening"
+    - "legacy reference: server/files-router.ts:282-312 (POST /open: path required, 404 missing, resolveOpenCommand with reveal/line/column/editorSetting/customEditorCommand/platform, spawnAndMonitor -> 502 on spawn failure) + server/file-opener.ts (EditorPreset auto|cursor|code|custom at line 12, resolveOpenCommand at 133, spawnAndMonitor at 179)"
+  missing: "all clauses: /api/files/open route, default/configured/custom editor resolution, line/column (--goto) args, reveal mode, argv quoting, spawn-failure reporting (502, no false success)"
+  effort: M
+  files:
+    - crates/freshell-server/src/files.rs
+    - crates/freshell-server/src/main.rs
+
+- id: FILE-05
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/files.rs:36-40,84-86 — FilesState holds the LIVE SettingsStore; doc comment states the sandbox is read fresh on every request so PATCH /api/settings takes effect immediately"
+    - "crates/freshell-server/src/files.rs — is_path_allowed (653-675) is the single canonical rule, applied per-request after settings.get() in validate_dir (164-169), read_file (202-207), stat_file (248-253), write_file (296-301), complete (339-345), mkdir (459-465); 403 {error:'Path not allowed'} shape at 718-724"
+    - "crates/freshell-server/src/repo_icon.rs — /api/repo-icon shares the same is_path_allowed (per files.rs:665-666 comment about repo_icon.rs call sites)"
+    - "test: validate_dir_denies_path_outside_allowed_roots in crates/freshell-server/src/files.rs — configures allowedFilePaths via the live store patch API and asserts 403 outside / 200 inside"
+    - "test: windows_flavor_allowlist_roots_convert_like_node and sandbox_target_uses_converted_native_path in crates/freshell-server/src/files.rs (both sides of the comparison convert like Node's resolvePathForSandboxComparison)"
+  missing: "the /local-file and /api/files/open surfaces do not exist (FILE-01/FILE-04), so one-canonical-rule coverage of 'every file surface' (local-file/open/editor) is unmet; no explicit test that toggles the setting mid-flight and re-asserts on the same server (live read is structural but untested as a scenario)"
+  effort: S
+  files:
+    - crates/freshell-server/src/files.rs
+    - crates/freshell-server/src/settings_store.rs
+
+- id: FILE-06
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/files.rs:653-675 — is_path_allowed is a normalized string prefix check at a directory boundary (target == root or starts_with(root + '/'))"
+    - "test: sandbox_enforces_directory_boundary in crates/freshell-server/src/files.rs — prefix-confusion directory rejected (/home/tester/project-x denied vs /home/tester/proj root); absolute outside path denied (/etc/passwd)"
+    - "GAP vs legacy: server/path-utils.ts:291-301 resolvePathForSandboxComparison runs path.resolve (collapses .. segments) then fs.realpathSync (resolves symlinks) on BOTH sides before comparison; Rust normalize_user_path (files.rs:518-521) only tilde-expands and trims separators — a target like /allowed/../etc/passwd still string-matches the root prefix and is never dot-collapsed or canonicalized"
+    - "GAP vs legacy: server/path-utils.ts:296-299,306 — realpathSync symlink-escape prevention has no Rust counterpart (no fs::canonicalize anywhere in the sandbox compare)"
+    - "GAP vs legacy: server/path-utils.ts:314-321 — win32 case-fold (toLowerCase on win32) explicitly not ported (files.rs:652 comment)"
+    - "no tests in crates/ attempt encoded-../symlink/case-fold escapes through file endpoints"
+  missing: "..-segment collapse before sandbox comparison (encoded or literal), symlink/realpath escape prevention (fs::canonicalize on target+roots), win32 case-folding, and tests exercising escape attempts through each endpoint; only the prefix-confusion/boundary clause is implemented and tested"
+  effort: S
+  files:
+    - crates/freshell-server/src/files.rs
diff --git a/docs/plans/parity-ledger/audit/p1-diagnostics-logging-ai-and-bootstrap.yaml b/docs/plans/parity-ledger/audit/p1-diagnostics-logging-ai-and-bootstrap.yaml
new file mode 100644
index 000000000..5d7992de4
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p1-diagnostics-logging-ai-and-bootstrap.yaml
@@ -0,0 +1,133 @@
+- id: DIAG-01
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/logging.rs:368-441 — JsonLayer emits one JSONL line per event with ts/level/target/msg plus flattened span fields"
+    - "crates/freshell-server/src/logging.rs:492-524 — request_logging_middleware assigns req-<pid>-<n> correlation id, logs route/method/status/duration_ms per HTTP request, echoes x-request-id"
+    - "crates/freshell-ws/src/terminal.rs:146-154 — ws.connection.established lifecycle event with connection_id + origin_kind"
+    - "crates/freshell-ws/src/terminal.rs:437-451 — ws.connection.closed summary lifecycle event with reason + close code"
+    - "crates/freshell-terminal/src/registry.rs:1100-1107 — terminal.created event (terminal_id, mode, resume_applied, cwd, pid)"
+    - "crates/freshell-terminal/src/registry.rs:1540 — terminal.killed (by: api|idle|shutdown); registry.rs:1658 terminal.exited (exit_code); registry.rs:920 terminal.idle_reap summary"
+    - "test: diag01_diag03_operator_experience in crates/freshell-server/tests/diag01_diag03_logging.rs (black-box binary boot; JSONL parse, correlated 404, request_id)"
+    - "test: enforce_idle_kills_emits_killed_by_idle_and_a_sweep_summary_event and terminal.created/exited tracing_capture tests in crates/freshell-terminal/src/registry.rs (~2750-2900)"
+    - "crates/freshell-tauri/src/lib.rs:113,170,187 — Tauri side uses plain eprintln! only; no structured JSONL Tauri log"
+  missing: "app version field on log lines (never emitted); explicit server boot/startup/quit lifecycle events (only subsystem events exist); structured JSONL Tauri-side logs (eprintln only); fresh-agent lifecycle event wiring in freshell-freshagent; no PW-RUST/PW-TAURI-WIN Playwright spec driving auth/terminal/provider/restart/quit flows and asserting field-level log contracts"
+  effort: M
+  files:
+    - crates/freshell-server/src/logging.rs
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-freshagent/src/claude.rs
+
+- id: DIAG-02
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/boot.rs:89 — route POST /api/logs/client registered"
+    - "crates/freshell-server/src/boot.rs:206-247 — logs_client validates body (zod-parity client_logs_issues, 400 with details; express-style 400 for malformed/empty JSON), returns 204, emits entries"
+    - "crates/freshell-server/src/boot.rs:363-446 — emit_client_log_entries re-emits each entry at its OWN severity (error/warn/debug/info) into target freshell_server::client_logs, preserving client, client_timestamp, client_event, console_method, args, context, stack"
+    - "crates/freshell-server/src/boot.rs:491-514 — entries array bounded to 1..=200 (client_logs_issues); malformed batches 400 without crash"
+    - "crates/freshell-server/src/boot.rs:305-361 — scrub_client_supplied redacts nested/escaped-JSON token+cookie secrets before emission"
+    - "test: client_log_entries_are_emitted_into_the_structured_log in crates/freshell-server/tests/diag02_client_log_emission.rs:218+ (all four severities land in rust-server.jsonl at own level)"
+    - "test: nested_json_encoded_secrets_in_client_log_strings_are_redacted in crates/freshell-server/tests/diag02_client_log_emission.rs:106+"
+    - "test: scrub_client_supplied_* unit tests in crates/freshell-server/src/boot.rs:823-857"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-server/src/boot.rs
+    - crates/freshell-server/tests/diag02_client_log_emission.rs
+
+- id: DIAG-03
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/logging.rs:181-202 — scrub(): verbatim AUTH_TOKEN replacement (covers URL/header/body/nested-error occurrences), token-keyed JSON field regex, cookie field + raw Cookie:/Set-Cookie: header redaction, applied on every line before disk (RotatingWriter::write_line logging.rs:288-303)"
+    - "crates/freshell-server/src/logging.rs:227-251 — rotate(): shift .1->.2->..., total file count bounded at 1+max_backups; size-based rotation default 10MiB x 2, env-overridable (FRESHELL_LOG_MAX_BYTES/MAX_BACKUPS, logging.rs:111-118)"
+    - "crates/freshell-server/src/logging.rs:288-303 — synchronous write+flush per line: no buffered-but-unflushed window, so final-flush-on-shutdown concern is structurally eliminated; Mutex serializes concurrent writers (write_line lock, lines 289-292)"
+    - "test: diag01_diag03_operator_experience in crates/freshell-server/tests/diag01_diag03_logging.rs:114-312 (tiny 2000-byte cap, ~80 requests, no raw token in any file, active file bounded, .1 backup exists, <=3 files, every line valid JSON)"
+    - "test: scrub_* and rotation_bounds_total_files_and_active_size unit tests in crates/freshell-server/src/logging.rs:530-624"
+    - "Playwright (PW-RUST): test/e2e-browser/specs/diag03-rotation-redaction-rust.spec.ts:57-164 — 'small-limit rotation: valid JSON, no raw secret, bounded file count, chronological coverage', 'concurrent writers: no interleaved or corrupted JSON lines', 'final shutdown flush: last request entry present after server stop'"
+  missing: "PW-TAURI-WIN lane (native Windows Tauri/WebView2 build unavailable on Linux — host-side validation only; implementation itself is platform-neutral Rust)"
+  effort: S
+  files:
+    - crates/freshell-server/src/logging.rs
+    - crates/freshell-server/tests/diag01_diag03_logging.rs
+    - test/e2e-browser/specs/diag03-rotation-redaction-rust.spec.ts
+
+- id: DIAG-04
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/diag.rs:238-269 — POST /api/perf patches settings.logging.debug and broadcasts settings.updated to connected clients (live toggle, no restart)"
+    - "crates/freshell-server/src/settings_store.rs:311-366 — patch() persists merged settings to disk (self.persist(&merged)) before committing to live tree, so the toggle survives restart"
+    - "crates/freshell-server/src/boot.rs:123-140 — bootstrap perf.logging reported from PERF_LOGGING/PERF_DEBUG env (boot-time state only)"
+    - "test: perf_handler_toggles_debug_setting_and_broadcasts + perf_only_a_literal_true_enables_matching_js_strict_equality in crates/freshell-server/src/diag.rs:569-628"
+    - "crates/freshell-ws/src/lib.rs:459 — WS handshake emits PerfLogging { enabled: false } — HARDCODED false, does not reflect live toggle state (legacy reads live perfConfig.enabled in handshakeSnapshotProvider, server/index.ts:~420)"
+  missing: "no applyDebugLogging equivalent: no server-side perf sampling timers to start/stop (legacy perf-logger.ts:185 setInterval process sample + event-loop histogram + shouldLog gating has no Rust counterpart); no debug/perf-gated detailed event emission before/during/after toggle; WS handshake perf.logging state hardcoded false (reconnect state not synchronized); no PW-RUST Playwright toggle spec"
+  effort: M
+  files:
+    - crates/freshell-server/src/diag.rs
+    - crates/freshell-ws/src/lib.rs
+    - crates/freshell-server/src/main.rs
+
+- id: DIAG-05
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/diag.rs:71-77 — router serves GET /api/server-info, GET /api/debug, POST /api/perf, all behind the shared auth gate (is_authed)"
+    - "crates/freshell-server/src/diag.rs:92-106 — server_info body: version, monotonic uptime (Instant-based), runtime:'rust', platform, arch, commit + buildDirty provenance (build.rs baked)"
+    - "crates/freshell-server/src/diag.rs:137-162 — debug body: version, appVersion, wsConnections (live registry), redacted settings, sessionsProjects, tabsRegistry record/device counts, terminals, time"
+    - "crates/freshell-server/src/diag.rs:193-232 — redact_settings: schema-based structural walker; key-vocabulary match (token/apikey/api_key/secret/password/credential/cookie/authorization/auth-header) at any depth PLUS secretMarked:true marker redaction for unknown future fields; non-secret siblings preserved"
+    - "test: redacts_known_secret_keys_at_any_depth, redacts_unknown_future_secret_marked_field_by_marker_not_name, redaction_is_case_insensitive_and_substring_based, debug_body_has_the_legacy_field_set_with_redacted_settings (seeded geminiApiKey never serialized), server_info_* (3 tests), perf_* (2 tests), all_three_routes_reject_unauthenticated_requests, server_info_accepts_the_correct_token — all in crates/freshell-server/src/diag.rs:271-681"
+  missing: "Playwright PW-RUST live spec comparing counts across real clients/tabs/terminals (unit/integration coverage exists instead); provider-credentials and remote-token seeding validated only via key-vocabulary unit tests, not end-to-end"
+  effort: S
+  files:
+    - crates/freshell-server/src/diag.rs
+    - crates/freshell-server/build.rs
+
+- id: DIAG-06
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "Route inventory of crates/freshell-server/src/*.rs: no /api/ai or /terminals/{id}/summary route exists (routes enumerated: bootstrap, debug, perf, server-info, terminals, terminals/{id}/search, sessions/resolve, ... — no summary)"
+    - "Legacy reference server/ai-router.ts:19-69 — POST /terminals/:terminalId/summary with ai_summary perf timer, summarySource ai|heuristic, fallback on error — has no Rust port"
+    - "crates/freshell-server/src/main.rs:1903 — featureFlags.kilroy HARDCODED false ('no KILROY_ENABLED wiring yet'), diverging from legacy detectFeatureFlags (server/platform-router.ts:22 reads isTruthy(process.env.KILROY_ENABLED)); aiEnabled computed via ai_enabled (main.rs:1911-1913) and tested"
+    - "crates/freshell-server/src/sessions.rs:297-335 — only first-message title heuristic exists (session auto-title, a different feature), not terminal-output summarization"
+  missing: "entire terminal-summary endpoint (heuristic fallback over terminal buffer + optional AI with timeout/500/malformed/empty handling and no-stuck-spinner semantics); truthful independent Kilroy availability reporting (kilroy flag hardcoded false, ignores KILROY_ENABLED); summary-AI availability reporting tied to the (absent) summary feature; PW-RUST summary spec"
+  effort: M
+  files:
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-server/src/diag.rs
+    - server/ai-router.ts
+
+- id: DIAG-07
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/boot.rs:99-126 — GET /api/bootstrap returns settings + platform + shell{authenticated,ready,tasks} + perf{logging}"
+    - "crates/freshell-server/src/boot.rs:104-122 — shell.tasks and ready are HARDCODED true for sessionRepairService/codingCliIndexer with comment that init runs before bind; NOT the actual startup-task status (legacy wires getShellTaskStatus to live startupState.snapshot().tasks, server/index.ts:191)"
+    - "crates/freshell-ws/src/lib.rs:104-113,459-470 — WS handshake carries boot-frozen config_fallback (ConfigFallback frame) with tests handshake_includes_config_fallback_when_boot_fell_back_and_in_correct_order, handshake_omits_config_fallback_when_boot_was_healthy, handshake_delivers_config_fallback_identically_across_multiple_connections"
+    - "grep for MAX_BOOTSTRAP_PAYLOAD_BYTES / readModelScheduler / payloadBytes in crates/: NO matches — no payload budget, no scheduling/cancellation machinery in the Rust bootstrap"
+  missing: "actual (non-hardcoded) startup-task status with barriers; configFallback in the /api/bootstrap HTTP payload (only WS handshake has it); legacyLocalSettingsSeed (no Rust occurrence at all); MAX_BOOTSTRAP_PAYLOAD_BYTES budget check (500 error on oversize, shell-bootstrap-router.ts:84-87); read-model scheduling + request cancellation/abort handling; truthful perf state (env-only, ignores runtime toggle); PW-RUST barrier/oversize/abort spec"
+  effort: M
+  files:
+    - crates/freshell-server/src/boot.rs
+    - crates/freshell-server/src/main.rs
+
+- id: DIAG-08
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/main.rs:66 — const APP_VERSION: &str = \"0.7.0\" (the exact stale value the checklist calls out); NOT derived from CARGO_PKG_VERSION (freshell-server crate version is 0.1.0) nor from package.json (0.7.5); overridable only via FRESHELL_APP_VERSION env"
+    - "crates/freshell-tauri/tauri.conf.json:3 — \"version\": \"0.7.0\" (Tauri-reported version also stale)"
+    - "package.json:4 — \"version\": \"0.7.5\" (release source of truth diverges from both Rust surfaces); crates/freshell-server/Cargo.toml:11 and crates/freshell-tauri/Cargo.toml:25 are 0.1.0"
+    - "crates/freshell-server/src/updater.rs:36-120 — live version-check comparator exists (parse_version/isMinorOrMajorNewer) but consumes whatever current_version string it is handed (the stale 0.7.0)"
+    - "grep for any test asserting cross-surface version equality (0.7.5) in crates/: none; test/unit/electron/release-assets.test.ts asserts 0.7.5 filenames but is legacy-Node-side, not Rust"
+    - "crates/freshell-api/src/lib.rs:128 — health fixture version also hardcoded \"0.7.0\""
+  missing: "single version source of truth: server health/version/bootstrap/debug/server-info all report hardcoded 0.7.0; no build-time injection from package.json/git tag; tauri.conf.json and Cargo crate versions diverge (0.7.0/0.1.0/0.7.5 across surfaces); no test asserting one-version equality; PW-RUST/PW-TAURI-WIN packaged version assertions absent"
+  effort: M
+  files:
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-server/build.rs
+    - crates/freshell-tauri/tauri.conf.json
+    - crates/freshell-server/src/updater.rs
diff --git a/docs/plans/parity-ledger/audit/p1-extensions.yaml b/docs/plans/parity-ledger/audit/p1-extensions.yaml
new file mode 100644
index 000000000..4c7b575cc
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p1-extensions.yaml
@@ -0,0 +1,144 @@
+- id: EXT-01
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/extensions.rs:92-105 — ExtensionManifest serde struct; doc at :13-15 and :89-91 states LENIENT parsing (unknown keys ignored), explicitly diverging from the legacy strict zod schema"
+    - "crates/freshell-server/src/extensions.rs:53-78 — CliConfig models args/env/resumeArgs/createSessionArgs/modelArgs/sandboxArgs/permissionModeArgs/supports*/terminalBehavior but NOT permissionModeEnvVar/permissionModeValues (legacy server/extension-manifest.ts:60-61)"
+    - "crates/freshell-server/src/extensions.rs:269-275 — is_valid_manifest requires cli block only for category=cli; client/server accepted as-is (legacy refine at server/extension-manifest.ts:96-103 requires exactly the matching block: client.entry, server.command/args/readyPattern/readyTimeout/healthCheck/singleton — none modeled)"
+    - "reference: server/extension-manifest.ts:14-25 (contentSchema field schema + default-type refine), :31-43 (Client/ServerConfigSchema with defaults args=[], readyTimeout=10000, singleton=true), :45-48 (terminalBehavior enums) — none ported"
+    - "test: scan_discovers_cli_manifests_and_dedups_first_wins in crates/freshell-server/src/extensions.rs:507"
+    - "test: client_registry_matches_frozen_shape_for_claude in crates/freshell-server/src/extensions.rs:529"
+  missing: "strict unknown-field rejection with useful diagnostics; client/server config blocks (entry, command, args, env, readyPattern, readyTimeout, healthCheck, singleton) incl. zod defaults; contentSchema field schema + default-matches-type refine; terminalBehavior enum validation; permissionModeEnvVar/permissionModeValues; min(1) string constraints; tests for every invalid manifest class (no invalid-class test exists)"
+  effort: M
+  files:
+    - crates/freshell-server/src/extensions.rs
+
+- id: EXT-02
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/extensions.rs:418-428 — resolve_extension_dirs mirrors legacy order [<home>/.freshell/extensions, <cwd>/.freshell/extensions, builtin ./extensions] (server/index.ts:294-297)"
+    - "crates/freshell-server/src/extensions.rs:140-179 — scan(): sorted subdir iteration, accepts dirs AND symlinks (:150-154), first-wins dedup via seen set (:170-173), invalid manifests skipped"
+    - "crates/freshell-server/src/extensions.rs:279-340 — client_entry serializes NO filesystem path (paths never exposed), matching legacy toClientRegistry"
+    - "test: scan_discovers_cli_manifests_and_dedups_first_wins in crates/freshell-server/src/extensions.rs:507 — duplicate name in a later root dropped, proving cross-root precedence"
+    - "divergence note: Rust sorts subdir names for determinism vs legacy fs.readdirSync order (documented extensions.rs:136-139)"
+  missing: "test coverage for symlinked extension entries (accepted in code but untested); invalid manifests skipped SILENTLY — legacy logs per-manifest warnings (server/extension-manager.ts:92-111) so diagnostics parity is absent; no test that icons/registry leak no path outside an allowed root"
+  effort: S
+  files:
+    - crates/freshell-server/src/extensions.rs
+
+- id: EXT-03
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/boot.rs:68-71 — extensions registry 'Precomputed at boot (immutable for the process life)'; no rescan path"
+    - "crates/freshell-server/src/main.rs:394-400,874-877 — single ExtensionRegistry::scan at boot; no watcher, no rebroadcast wiring"
+    - "crates/freshell-protocol/src/server_messages.rs:62 — 'extensions.registry' message type declared in protocol (shape pinned by crates/freshell-protocol/tests/activity_extension.rs) but no producer exists in any crate"
+    - "reference: server/index.ts:485-530 — dev-gated chokidar watcher on the three extension dirs, 300ms-debounced rescan, registerCodingCliCommands + wsHandler.refreshExtensionModes + broadcast {type:'extensions.registry'}; decoded broadcasts extension.server.* at :473-484"
+  missing: "entire live-reload feature: extension-dir watcher, rescan, CLI spawn-table refresh, extensions.registry broadcast, invalid-edit diagnostics surfaced to clients; unaffected panes stability is moot (nothing is reloaded)"
+  effort: M
+  files:
+    - crates/freshell-server/src/extensions.rs
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-protocol/src/server_messages.rs
+
+- id: EXT-04
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/settings_store.rs:134-150 — boot knownProviders migration ported: seed once from discovered CLI extensions, append newly-discovered names AND auto-enable them (legacy server/index.ts:317-345); explicit user choices preserved because only NOT-known names are appended"
+    - "test: known_providers_seeded_persisted_and_patchable in crates/freshell-server/src/settings_store.rs:1859"
+    - "test: live-pinned auto-enable test (persisted knownProviders ['claude'] + boot discovering 5 → new names appended and enabled) in crates/freshell-server/src/settings_store.rs:1913-1943"
+    - "crates/freshell-protocol/src/settings.rs:107-109 — SettingsExtensions { disabled: Vec<String> } in the settings tree; generic JSON deep_merge PATCH persists extensions.disabled (settings_store.rs:311-346) and PATCH broadcasts settings.updated (settings_store.rs:1630-1647)"
+    - "crates/freshell-server/src/resolve.rs:230,641-645 — disabled providers filtered OUT of the session index; tests disabled_providers_are_reported_unsearched_never_as_errors (:1750), disabled_provider_indexed_sessions_do_not_resolve (:1782)"
+    - "picker launch-availability filtering is CLIENT-side on the retained SPA (src/components/panes/PanePicker.tsx:117,126-155 uses settings.extensions.disabled + codingCli.enabledProviders + availableClis) and updates immediately on settings.updated"
+  missing: "no zod-shape validation of the extensions patch (top-level key whitelisted at settings_store.rs:1384 but disabled: string[] shape unchecked — only the final serde from_value catches wrong types with a generic 400); no dedicated extensions.disabled round-trip/persist test; typed extension-scoped content settings (contentSchema string/number/boolean defaults, manifest-driven field validation) not modeled server-side; no test that a disabled-then-restored CLI extension keeps the user's disabled choice (migration logic implies it, untested)"
+  effort: M
+  files:
+    - crates/freshell-server/src/settings_store.rs
+    - crates/freshell-protocol/src/settings.rs
+
+- id: EXT-05
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-platform/src/cli_launch.rs:400-568 — resolve_coding_cli_command: command override via envVar (:412-416), base args/env, model/sandbox/permissionMode arg templates (:538-552), createSessionArgs fresh identity + resumeArgs first-occurrence substitution (:496-510), LaunchIntent/StartIntentUnsupported gating (:144-146,499-505)"
+    - "crates/freshell-server/src/extensions.rs:226-247 — cli_command_specs() compiles every discovered CLI manifest block into CliCommandSpec (port of server/index.ts:231-255)"
+    - "test: golden argv suite incl. g_a1_amplifier_fresh_launch_matches_manifest, g_a2_amplifier_resume_appends_resume_args, g_a3_amplifier_env_var_override, g_a4/g_a4b intent goldens in crates/freshell-platform/src/cli_launch_goldens.rs (~780-896); amplifier manifest pinned by amplifier_manifest_matches_legacy_cli_block"
+    - "explicit doc gap: cli_launch.rs:32-33 — 'permissionModeEnvVar/permissionModeEnvValues are NOT modeled: no shipped manifest sets them, so terminal-registry.ts:355-367 is inert'; re-confirmed in-code at :553"
+    - "legacy disabled-provider launch gate exists ONLY on the WS codingcli create path (server/ws-handler.ts:3286-3292 'Provider disabled'); no equivalent enabledProviders check found in crates/freshell-ws/src/terminal.rs create path"
+  missing: "permissionModeEnvVar + permissionModeValues mapping (explicitly unimplemented); server-side rejection of disabled providers at terminal/CLI launch (WS create path); native Windows quoting/argv verification is host-limited (Windows-only branches exist — ProviderTarget::Windows, cli_launch.rs:74-81 — but cannot be validated on this Linux host); cwd honoring lives in the generic spawn layer, not extension-specific (not verified here)"
+  effort: M
+  files:
+    - crates/freshell-platform/src/cli_launch.rs
+    - crates/freshell-server/src/extensions.rs
+    - crates/freshell-ws/src/terminal.rs
+
+- id: EXT-06
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/boot.rs:87-88 — the ONLY extension routes in the Rust server are GET /api/extensions and GET /api/extensions/{name}; grep confirms no /icon, no /client/*, no nested-route serving anywhere in crates/freshell-server/src"
+    - "crates/freshell-server/src/extensions.rs:290-297 — iconUrl is GENERATED ('/api/extensions/{name}/icon') but the endpoint it points to is not implemented"
+    - "reference: server/extension-routes.ts:64-125 (GET /:name/client/* with publicRoot scoping, realpath symlink + traversal protection, 404/400 errors) and :127-157 (GET /:name/icon, image/svg+xml, traversal protection)"
+  missing: "entire client-extension static serving: entry/publicRoot resolution, nested route/asset serving with content types, icon endpoint, realpath traversal + symlink protections, error UI handoff (404/400 payloads)"
+  effort: M
+  files:
+    - crates/freshell-server/src/boot.rs
+    - crates/freshell-server/src/extensions.rs
+
+- id: EXT-07
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/extensions.rs:110 — RegistryEntry deliberately omits the server-process handle ('this read-only port never spawns'); :286-288 — serverRunning hardcoded false, serverPort always omitted"
+    - "no POST /api/extensions/{name}/start route exists (boot.rs:87-88 is the full extension surface)"
+    - "reference: server/extension-manager.ts:218-309 (startServer: port allocation via listen(0), {{port}}/contentSchema-default env interpolation + ~/ expansion :349-378, spawn with cwd=ext dir, readyPattern/readyTimeout race, concurrent-start dedup via starting map :233-245) and server/extension-routes.ts:33-50"
+  missing: "all of it: free-port allocation, env interpolation ({{port}}, content-schema defaults, ~/ expansion), cwd=extension-dir spawn, ready-pattern/timeout health gate, concurrent-start dedup, POST :name/start route"
+  effort: L
+  files:
+    - crates/freshell-server/src/extensions.rs
+    - crates/freshell-server/src/boot.rs
+
+- id: EXT-08
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-protocol/src/server_messages.rs:54-60 — wire types extension.server.error/ready/starting/stopped declared; :363-372 payloads; shape pinned by crates/freshell-protocol/tests/activity_extension.rs — but no crate produces them"
+    - "reference: server/extension-manager.ts:249-340 (spawn-error race, kill+server.error on failure, exit-watch cleanup emitting server.stopped, stopServer SIGTERM→5s→SIGKILL, stopAll at server/index.ts:1252) and server/index.ts:473-484 (broadcast wiring)"
+    - "no process table, no health-check polling, no lifecycle events anywhere in crates/ (grep for startServer/stopServer/server.starting producers returns only protocol declarations)"
+  missing: "all of it: starting/ready/error/stopped status publication, failed-child cleanup, post-ready crash detection, targeted stop (SIGTERM grace → SIGKILL), stop-all on shutdown, POST :name/stop route, healthCheck polling"
+  effort: L
+  files:
+    - crates/freshell-server/src/extensions.rs
+    - crates/freshell-server/src/boot.rs
+
+- id: EXT-09
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-server/src/boot.rs:100-103,150-152,194-196 — every existing extension route is constant-time-token auth-gated (is_authed) before any registry access, mirroring server/auth.ts httpAuthMiddleware (boot.rs module doc :24-29)"
+    - "crates/freshell-server/src/boot.rs:145-165 — extension_by_name returns 404 {error: Extension not found: '<name>'} byte-matching extension-routes.ts:20-24; name is used only for in-memory lookup (no fs join), so name-based traversal is a no-op on this surface"
+    - "the dangerous surfaces simply do not exist in Rust yet: no icon/client file-serving (so no traversal/absolute/symlink read path), no start/stop (so no process-launch boundary), no POST bodies on extension routes (so no body-limit exposure)"
+  missing: "traversal/absolute-path/symlink file-read protections (blocked on EXT-06), category guards and command-boundary enforcement for process launch (blocked on EXT-07), oversized/malformed body limits on extension mutation routes (none exist); only the two GET routes' auth parity can be claimed today"
+  effort: M
+  files:
+    - crates/freshell-server/src/boot.rs
+    - crates/freshell-server/src/extensions.rs
+
+- id: EXT-10
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "extensions/amplifier/freshell.json — built-in manifest present in this worktree and byte-identical to origin/main (git show/ls-tree verified: only freshell.json exists under extensions/amplifier/ on origin/main; no icon asset exists there either)"
+    - "extensions/amplifier/freshell.json cli.env PROMPT_TOOLKIT_NO_CPR=1 — the CPR-warning suppression from main commit 54aeabb96, carried verbatim through Rust launch env"
+    - "test: g_a1_amplifier_fresh_launch_matches_manifest in crates/freshell-platform/src/cli_launch_goldens.rs:780 — asserts PROMPT_TOOLKIT_NO_CPR=1 reaches the launch env; g_a2/g_a3 cover resume argv and AMPLIFIER_CMD override"
+    - "test: amplifier_manifest_matches_legacy_cli_block in crates/freshell-platform/src/cli_launch_goldens.rs:912 — pins the on-disk manifest against silent drift"
+    - "test: fresh_amplifier_create_carries_launcher_assigned_session_ref_and_stub (+4 more amplifier launcher tests) in crates/freshell-ws/tests/amplifier_launcher_identity.rs:21"
+    - "manifest has picker {shortcut:A, group:agents} which Rust to_client_registry serializes (extensions.rs:305-310), so the picker entry is registry-backed"
+  missing: "icon clause: NO icon field or icon asset exists under extensions/amplifier/ even on origin/main (and no /api/extensions/{name}/icon route exists in Rust regardless — see EXT-06), so 'real icon appears in picker/history' is unprovable as written; provider activity/durability/history explicitly belong to TERM-*/SESSION-* sections and were not re-audited here"
+  effort: S
+  files:
+    - extensions/amplifier/freshell.json
+    - crates/freshell-platform/src/cli_launch_goldens.rs
+    - crates/freshell-server/src/boot.rs
diff --git a/docs/plans/parity-ledger/audit/p1-native-windows-tauri-startup-and-configuration.yaml b/docs/plans/parity-ledger/audit/p1-native-windows-tauri-startup-and-configuration.yaml
new file mode 100644
index 000000000..26e38f4a7
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p1-native-windows-tauri-startup-and-configuration.yaml
@@ -0,0 +1,111 @@
+- id: TAURI-01
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/config.rs:26-31 — config_dir() resolves FRESHELL_HOME first, then HOME; returns None if neither set"
+    - "test: config_dir_prefers_freshell_home in crates/freshell-tauri/src/config.rs:155 — asserts FRESHELL_HOME wins over HOME and HOME is the fallback"
+    - "grep for USERPROFILE over crates/ — hits only in freshell-freshagent/freshell-ws/freshell-platform/freshell-server (e.g. crates/freshell-server/src/session_directory.rs:441-457, which is the spawned SERVER's own home resolution), ZERO hits in crates/freshell-tauri/src"
+    - "grep for webview2|user_data|data_directory over crates/freshell-tauri/src and tauri.conf.json — no WebView2 user-data-folder or profile-path isolation configured anywhere (renderer_recovery.rs:33 only mentions WebView2 ProcessFailed in a doc comment)"
+  missing: "No %USERPROFILE% fallback when HOME is absent (on native Windows config_dir() returns None, so desktop.json read/writes silently disable and window state stops persisting); no isolation of WebView2 user-data/profile paths under the Freshell home"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/config.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/tauri.conf.json
+
+- id: TAURI-02
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/config.rs:41-48 — read_config_at parses desktop.json, malformed file is a hard error (not silently overwritten); missing file -> {}"
+    - "test: read_malformed_is_error, read_missing_is_empty_object, patch_round_trips_atomically in crates/freshell-tauri/src/config.rs:120-152 — merge preserves unknown keys losslessly across saves"
+    - "crates/freshell-tauri/src/lib.rs:308-318 — cold start reads desktop.json ONLY for windowState via load_persisted_window_state; window-state load/save tested by load_full_window_state_round_trips, load_partial_window_state_fills_defaults, save_preserves_other_config_keys in crates/freshell-tauri/src/window_state.rs:239-314"
+    - "crates/freshell-tauri/src/lib.rs:96-104 + provisioning.rs:162-177 — boot resolves remote ONLY from env pair or one-shot desktop.provision file; a pre-existing desktop.json with serverMode:'remote' is NOT honored on cold start"
+    - "crates/freshell-tauri/src/lib.rs:536 — global hotkey registered from hotkey::DEFAULT_HOTKEY constant ('CommandOrControl+`'), never read from desktop.json.globalHotkey"
+    - "crates/freshell-tauri/src/tray.rs:83-245 — TrayStatus carries only running+mode; minimizeToTray/startOnLogin config keys never read"
+  missing: "Cold-start honoring of serverMode/port/remoteUrl+remoteToken from an existing desktop.json (only provisioning path reads them); knownServers never read; chooser policy (alwaysAskOnLaunch) not consumed at boot (only inside get_launch_options command, commands.rs:366-386); globalHotkey and tray behavior (minimizeToTray/startOnLogin) not honored; no schema validation equivalent to DesktopConfigSchema (no version/type rejection); no backup/reset/edit recovery path for malformed/unsupported config — malformed falls back to window-state defaults silently and launch proceeds"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/config.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/hotkey.rs
+    - crates/freshell-tauri/src/tray.rs
+    - crates/freshell-tauri/src/state_machine.rs
+
+- id: TAURI-03
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/state_machine.rs:18-122 — explicit Boot->(Wizard|Chooser|Main)->Exiting state machine: decide_initial_phase (no config/setupCompleted=false -> Wizard; app-bound/daemon/remote -> Main), pure transition(), and runnable_phase() which DEGRADES Wizard/Chooser to Main because their windows are 'deferred to Phase 3.14'"
+    - "test: unconfigured_app_decides_wizard, configured_app_bound_decides_main, configured_daemon_and_remote_also_reach_main, boot_resolves_to_decided_phase, aux_windows_return_to_boot_not_recursion, chooser_selection_advances_to_main, quit_from_any_phase_exits, main_ignores_aux_close in crates/freshell-tauri/src/state_machine.rs:124-215"
+    - "crates/freshell-tauri/src/lib.rs:136-148 — the ONLY wired path forces BootInputs{setup_completed:true, AppBound} and debug_asserts the phase is Main; an unconfigured desktop boots straight to Main (Wizard never constructed)"
+    - "crates/freshell-tauri/src/windows.rs:40-78 — wizard/chooser window specs + per-window trust gating exist; commands.rs registers complete_setup/get_launch_options/choose_launch_option; but restartMain re-derive and window construction are documented as deferred (commands.rs:19-24)"
+  missing: "Actual routing execution: no config->wizard never constructs the wizard window (exprunnable degrades to Main); forced/ambiguous/unavailable target->chooser window not constructed (chooser + restartMain transition deferred); daemon->service path absent entirely (daemon managers unwired, ServerMode::Daemon is decision-only); 'exactly one appropriate window' unverifiable headlessly and not enforced"
+  effort: L
+  files:
+    - crates/freshell-tauri/src/state_machine.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/windows.rs
+    - crates/freshell-tauri/src/commands.rs
+
+- id: TAURI-04
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/shim.rs:65-90 — build_load_url percent-encodes the token with an exact encodeURIComponent clone; tests load_url_percent_encodes_special_token ('a b+c&d#e/f' -> a%20b%2Bc%26d%23e%2Ff) and encode_uri_component_matches_js_semantics cover +, &, #, /, spaces, UTF-8"
+    - "src/lib/auth.ts:31-49 (shared SPA, loaded unchanged by the Tauri webview) — initializeAuthToken reads ?token=, stores it, then window.history.replaceState scrubs the query; covered by replaceState assertions in test/unit/client/lib/auth.test.ts:80-202"
+    - "crates/freshell-tauri/src/lib.rs:458-464 — redact_token strips ?token= from the shell's own log lines; test redact_token_hides_the_credential (lib.rs:600)"
+    - "crates/freshell-tauri/src/server.rs:153-174 + lib.rs:150-154 — app-bound ALWAYS allocates an ephemeral port and generates a fresh 128-bit hex token per boot; no desktop-config/.env/legacy-config.json token or port precedence resolution exists (reference: electron/entry.ts:282 port = desktopConfig.port ?? 3001, electron/startup.ts:295,377 readEnvToken(.env), electron/token-resolver.ts:13-38 .env then legacy config.json)"
+  missing: "Reuse of the configured app-bound port and legacy .env config.json token with documented precedence (desktop config /.env / legacy config) — Rust deliberately regenerates both each boot (server.rs:166-167 documents the divergence); missing .env AUTH_TOKEN parsing (extractTokenFromEnv analog) and config.json authToken/token fallback; no test proving the bootstrap URL never enters retained traces/logs/history/screenshots (only the eprintln redaction is tested), no launch-recorder contract"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/server.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/config.rs
+    - crates/freshell-tauri/src/shim.rs
+
+- id: TAURI-05
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/commands.rs:331-333 — is_port_available bind-check (listen on 0.0.0.0, errs toward occupied), the analog of electron/port-check.ts:11-21"
+    - "test: is_port_available_true_for_free_port and is_port_available_false_for_occupied_port in crates/freshell-tauri/src/commands.rs:730-746"
+    - "crates/freshell-tauri/src/commands.rs:410-416 — chooser start-local path rejects an occupied port with 'Port {port} is already in use. Choose a different port, or connect to that server.' (change path exists in the chooser command)"
+    - "crates/freshell-tauri/src/server.rs:213-239 — reap only ever signals the owned Child handle; an external port occupant is never killed (no pkill anywhere)"
+    - "crates/freshell-tauri/src/health.rs:101-125 — http_probe can detect a 200 from /api/health on an occupied port (primitive for compatibility detection), tested by http_probe_reports_ready_against_a_200_stub"
+  missing: "No boot-time collision handling at all: app-bound startup always silently takes an EPHEMERAL port (server.rs:157-162 allocate_ephemeral_port, lib.rs:152) so the configured port is never probed or bound; no compatible-Freshell-occupant identification (http_probe result is never classified, no /api/health identity/version check); no connect/change/retry offer when the configured port is occupied; chooser window that would present the choices is deferred to Phase 3.14"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/commands.rs
+    - crates/freshell-tauri/src/health.rs
+    - crates/freshell-tauri/src/server.rs
+    - crates/freshell-tauri/src/lib.rs
+
+- id: TAURI-06
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/server.rs:133-151 — resolve_server_binary: FRESHELL_SERVER_BIN env override FIRST, else sibling of current_exe (works for the dev target/<profile>/ layout and any installed layout that places the server beside the app exe); sibling_server_binary handles the .exe suffix — test sibling_binary_uses_exe_suffix (server.rs:349-359)"
+    - "crates/freshell-tauri/tests/server_spawn_smoke.rs:21-37,40-106 — headless integration test discovers the binary via env-or-sibling and drives real spawn/health/reap end-to-end (soft-skips when unbuilt)"
+    - "crates/freshell-tauri/tauri.conf.json:24-28 — bundle.active is FALSE and there is no externalBin/sidecar/resources entry, so no installed-layout bundling of freshell-server is established in the Rust port"
+  missing: "Item requires resolution 'without relying on environment overrides' — FRESHELL_SERVER_BIN is the primary lookup here; installed/packaged layout (sidecar inside app resources) is not configured (bundle.active:false, no externalBin); no existence/permission preflight — resolve returns a path without checking it exists or is executable, so a missing binary surfaces only as a raw spawn io::Error, not a clear 'server binary missing at X' startup UI; no spaced/Unicode-path-specific test (PathBuf joins are space-safe by construction but nothing exercises it)"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/server.rs
+    - crates/freshell-tauri/tauri.conf.json
+    - crates/freshell-tauri/src/lib.rs
+
+- id: TAURI-07
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/server.rs:116-126 — spawn_server explicitly DISCARDS stdout (Stdio::null) and only INHERITS stderr (Stdio::inherit); no piping, capture, or structured diagnostics storage"
+    - "crates/freshell-tauri/src/health.rs:31-52,68-95 — supervision exists: fail-fast HealthError::ChildExited ('server process exited before health check succeeded') and Timeout; test poll_fails_fast_when_child_exits"
+    - "crates/freshell-tauri/src/lib.rs:178-186 — a health-gate failure reaps the child and aborts the launch with an eprintln; failed Tauri build also reaps (lib.rs:110-118)"
+    - "test: app_bound_spawn_health_reap_end_to_end in crates/freshell-tauri/tests/server_spawn_smoke.rs asserts Graceful reap and that the port is released afterward (reaping covered); reap tests in server.rs:410-451"
+  missing: "The capture half of the item: child stdout/stderr are discarded/inherited rather than piped and stored as structured diagnostics; no redaction of captured output into desktop logs; the visible startup failure contains only the generic health reason (ChildExited/Timeout), not any safe excerpt of the child's own output; child reaping + failure surface exist (that half is done)"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/server.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/health.rs
diff --git a/docs/plans/parity-ledger/audit/p1-network-management.yaml b/docs/plans/parity-ledger/audit/p1-network-management.yaml
new file mode 100644
index 000000000..90bc6a8b0
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p1-network-management.yaml
@@ -0,0 +1,168 @@
+- id: NET-01
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/network.rs:84-88 — Rust router exposes GET /api/network/status, wired in crates/freshell-server/src/main.rs:1202"
+    - "crates/freshell-server/src/network.rs:194-213 — full NetworkStatus shape built (configured, host, remoteAccess*, port, lanIps, machineHostname, firewall{platform,active,portOpen,commands,configuring}, rebinding, devMode, accessUrl)"
+    - "crates/freshell-server/src/network.rs:122-123 — reachability probe deferred: raw_port_open always None, so portOpen is always null and remoteAccessEnabled can never be true on non-WSL2 hosts"
+    - "crates/freshell-server/src/network.rs:159-160 — stale managed Windows firewall rule read deferred: stale hardcoded false"
+    - "crates/freshell-server/src/network.rs:208,210 — in-progress state (firewall.configuring, rebinding) hardcoded false; no mutation machinery exists to ever set them"
+    - "crates/freshell-server/src/main.rs:199 — NetworkState.settings is a boot-time Arc<ServerSettings> snapshot (not re-read after PATCH /api/settings), and network.rs:97-111 caches live facts in a OnceCell for process life — fields do NOT live-update while Settings stays open"
+    - "crates/freshell-server/src/network.rs:247-248 — on native Linux/macOS lanIps is always empty (interfaces edge unwired, documented)"
+    - "test: loopback_wsl2_boot_is_remote_access_off_and_shape_complete in crates/freshell-server/src/network.rs:307"
+    - "test: all_interfaces_unconfigured_requests_remote_access_and_builds_commands in crates/freshell-server/src/network.rs:365"
+    - "test: hostname_strips_dot_local_suffix in crates/freshell-server/src/network.rs:401"
+  missing: "Live port-reachability probe (portOpen forever null), stale managed-rule detection, in-progress states (always false), live refresh while Settings open (boot-time settings snapshot + process-life OnceCell fact cache), LAN IP enumeration on native Linux/macOS (always []), no /api/lan-info route."
+  effort: M
+  files:
+    - crates/freshell-server/src/network.rs
+    - crates/freshell-platform/src/network.rs
+    - crates/freshell-server/src/main.rs
+
+- id: NET-02
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/network.rs:19-21 — module doc states the mutating paths (configure / configure-firewall / disable-remote-access) are NOT wired in Rust"
+    - "grep for 'network/configure' across crates/ finds only doc comments; no POST /api/network/configure route exists (only .merge(network::router(...)) with the GET at crates/freshell-server/src/main.rs:1202)"
+    - "crates/freshell-server/src/main.rs:1356 — tokio TcpListener binds exactly once at startup; no rebind/hot-rebind machinery exists anywhere in Rust"
+    - "crates/freshell-server/src/settings_store.rs:311 — PATCH /api/settings can persist a network.host change but triggers no listener effect (no rebind side effect in patch_settings, settings_store.rs:1630-1651)"
+    - "crates/freshell-platform/src/network.rs:49 — resolve_bind_host (faithful port of get-network-host.ts) is consumed only at boot (main.rs resolve path ~1620-1640), never at runtime"
+    - "Legacy reference: server/network-manager.ts:400-438 (configure + persist + hostChanged) and :449+ (rebind with old-host listener fallback)"
+  missing: "Entire POST /api/network/configure route, transactional rebind (prove new listener before discarding old), persist-after-proof ordering, rebind-in-flight queueing, and any runtime listener change — the Rust server must restart to change bind."
+  effort: L
+  files:
+    - crates/freshell-server/src/network.rs
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-server/src/settings_store.rs
+
+- id: NET-03
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-platform/src/network.rs:227-241 — encode_uri_component implements JS encodeURIComponent unreserved set"
+    - "crates/freshell-platform/src/network.rs:245-260 — access_url builds http://<lanIp|localhost>:<port>/?token=<enc> (protocol/host/port + encoded token)"
+    - "crates/freshell-server/src/network.rs:192,212 — accessUrl included in the live status response using the real bound port and auth token"
+    - "test: access_url_lan_when_share_enabled_and_encodes_token in crates/freshell-platform/src/network.rs:694 (asserts 'a b/c' -> 'a%20b%2Fc')"
+    - "test: access_url_localhost_when_share_disabled (network.rs:686) and access_url_falls_back_to_localhost_without_lan (network.rs:702)"
+    - "crates/freshell-server/src/logging.rs:153-186,455-475 — log scrubber redacts any *token* JSON field and strips ?token= from logged request paths; startup line (main.rs:1368-1371) logs listen URL without the token"
+    - "test: scrub_redacts_the_exact_secret_value_wherever_it_appears (logging.rs:531), sanitize_route_strips_only_the_token_query_param (logging.rs:618)"
+    - "crates/freshell-server/src/network.rs:171-189 — share_route_enabled can only activate via remote_access_enabled (never true: probe deferred) or the WSL2-unknown branch; on native Windows/Linux with remote access actually working, the returned URL is still localhost"
+  missing: "Share-URL accuracy on non-WSL2 0.0.0.0 binds: the share route never activates without the deferred reachability probe, so accessUrl is always localhost even when the server is genuinely reachable on the LAN (coupled to NET-01's missing probe). Copy-from-Settings UI is the retained React client (not Rust)."
+  effort: S
+  files:
+    - crates/freshell-server/src/network.rs
+    - crates/freshell-platform/src/network.rs
+
+- id: NET-04
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-platform/src/elevated.rs:137-266 — ConfirmationGate: one-time action-bound tokens (issue_confirmation:155, matches:168, consume:177), in-progress lock (try_acquire_repair_lock:208), action metadata (windows-repair/windows-disable/wsl2-repair/wsl2-disable, powershell_command:68, response_method:81)"
+    - "crates/freshell-platform/src/firewall.rs:230 — managed_windows_firewall_rule_name gives the exact 'Freshell (port N)' managed rule name; builders at :248 (delete), :261 (add), :278 (repair with stale-delete + missing-add semantics); read-only existence probe at :313"
+    - "test: phase1_issues_token_and_never_spawns (elevated.rs:339) — cancel/no-confirm path performs zero spawns"
+    - "test: phase2_matching_token_spawns_via_injected_fake (elevated.rs:364) — exact elevated command via WSL powershell path; token consumed so replay re-issues"
+    - "test: confirm_with_wrong_token_reissues_and_does_not_spawn (elevated.rs:411) and in_flight_repair_returns_locked_409 (elevated.rs:433)"
+    - "test: managed_delete_uses_plain_dollar_null, managed_add_golden, repair_reachable_adds_only_missing_and_deletes_stale, repair_unreachable_adds_all_required (firewall.rs:391,400,419,431)"
+    - "crates/freshell-server/src/network.rs:19-21 — confirms none of this is exposed over HTTP; builders/gate are library-only, never executed (elevated commands only constructed)"
+  missing: "The /api/network/configure-firewall HTTP route itself, route-level wiring of the gate (409/reissue/consume flow incl. fresh status re-check under lock), the post-operation verification step (no verifySuccess equivalent — request_elevation releases the lock immediately after spawn with no verification), managed-port persistence (persistManagedWindowsRemoteAccessPortsForServer / clear equivalents), and stale-repair detection in status. Actual netsh/UAC execution is inherently Windows-host-only (Tauri/elevated VM) — the gate/lock/builders are tested via fakes."
+  effort: L
+  files:
+    - crates/freshell-server/src/network.rs
+    - crates/freshell-platform/src/elevated.rs
+    - crates/freshell-platform/src/firewall.rs
+
+- id: NET-05
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-platform/src/port_forward.rs:413 — build_wsl_port_forwarding_plan (full port of wsl-port-forward.ts: NotWsl2/Disabled/Error/Noop/Ready); teardown plan at :473 — plan decisions cover Freshell-owned portproxy/FreshellLANAccess-only state incl. stale cleanup"
+    - "crates/freshell-platform/src/port_forward.rs:311,327,354 — firewall-only / port-forwarding / teardown script builders with the byte-load-bearing 2>\\$null escaping; normalize_for_elevated_powershell at :264"
+    - "crates/freshell-platform/src/port_forward.rs:254 — is_wsl_port_forwarding_disabled_by_env kill-switch; read-only state reads get_wsl_ip (:549), get_existing_port_proxy_rules (:565), get_existing_firewall_ports (:578)"
+    - "test: plan_noop_when_rule_and_firewall_correct (763), plan_full_when_portproxy_missing (783), plan_firewall_only_when_proxy_ok_but_firewall_missing (804), plan_full_cleans_up_stale_owned_ports (825), teardown_plan_ready_removes_rule_and_proxy (862), kill_switch_values (749) + parser/live read-only tests (946, 964) in port_forward.rs"
+    - "crates/freshell-platform/src/port_forward.rs:18-23 — safety note: add/delete are only ever built as strings, never executed; no HTTP route or executor consumes the plans anywhere in crates/"
+    - "Native-Windows client aspect: freshell-tauri spawns the server as a child process (crates/freshell-tauri/src/lib.rs:59) with no WSLg dependency found, but no forwarding handshake wiring exists"
+  missing: "Execution + wiring: no /api/network/configure-firewall route to return/run the plans, no persistManagedWslRemoteAccessPorts/clearManagedWslRemoteAccessPorts state file, no post-run verification (verifyWslRepairSuccess equivalent), no status reconcile after repair. Live create/verify of portproxy/firewall state is Windows+WSL2-host-only (the plan logic itself is fully fake-tested)."
+  effort: M
+  files:
+    - crates/freshell-platform/src/port_forward.rs
+    - crates/freshell-server/src/network.rs
+
+- id: NET-06
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-platform/src/port_forward.rs:473 — build_wsl_port_forwarding_teardown_plan removes only Freshell-owned portproxy/firewall state (unrelated rules untouched by construction)"
+    - "test: teardown_plan_noop_when_nothing_present and teardown_plan_ready_removes_rule_and_proxy in crates/freshell-platform/src/port_forward.rs:853,862"
+    - "crates/freshell-platform/src/firewall.rs:248-258 — build_windows_firewall_delete_commands targets only the exact 'Freshell (port N)' managed names"
+    - "crates/freshell-platform/src/elevated.rs:51-53 — ConfirmationAction::WindowsDisable / Wsl2Disable exist in the (unwired) gate"
+    - "crates/freshell-server/src/network.rs:19-21 — /network/disable-remote-access is one of the explicitly NOT-wired mutating paths; no route, no rebind (main.rs:1356 binds once), no verification"
+  missing: "The disable endpoint itself (confirmation flow via accessible UI POST), loopback rebind (no runtime rebind exists at all), applyRemoteAccessDisabledState (persist loopback config + clear managed ports), verification-before-claiming-completion (verifyWslDisableSuccess/verifyWindowsDisableSuccess equivalents), and settings.updated broadcast on the disable path."
+  effort: L
+  files:
+    - crates/freshell-server/src/network.rs
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-server/src/settings_store.rs
+    - crates/freshell-platform/src/port_forward.rs
+
+- id: NET-07
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-platform/src/elevated.rs:116-128 — ElevationDecision has Locked (409 overlap) and Reissued (stale token) variants only; no denial/timeout/partial-success/verification-failure outcomes"
+    - "crates/freshell-platform/src/elevated.rs:17-18 — ELEVATED_POWERSHELL_TIMEOUT_MS constant exists but is never enforced (no async spawn/timeout machinery; runner is synchronous and only ever faked)"
+    - "crates/freshell-server/src/network.rs:19-21 — no elevated/mutating network operation is wired, so there is no runtime path that could hit elevation denial, partial success, or verification failure"
+    - "test: in_flight_repair_returns_locked_409 (elevated.rs:433) — only the overlap case is covered; grep found no tests exercising denial, timeout, or verification failure anywhere in crates/"
+    - "Legacy references the Rust lacks: server/network-router.ts:208-211 (spawn error), elevated-powershell timeout/denial, verifyWslRepairSuccess/verifyWindowsDisableSuccess (network-router.ts:380-410), error-side consume_confirmation + 500 envelopes"
+  missing: "All four failure modes (UAC denial, timeout, partial success, verification failure), the fresh-status reconcile after failure, nonstuck-UI state transitions (firewall.configuring/resetFirewallCache), and retry/repair result envelopes — none of the mutating surface exists in Rust."
+  effort: L
+  files:
+    - crates/freshell-platform/src/elevated.rs
+    - crates/freshell-server/src/network.rs
+
+- id: NET-08
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "grep across crates/: the only network mutation routes named (configure / configure-firewall / disable-remote-access) appear in doc comments — no Rust POST /api/network/* endpoints exist, so there is no mutation surface to validate (crates/freshell-server/src/network.rs:84-88 registers GET only)"
+    - "crates/freshell-server/src/settings_store.rs:1635 — the only route that can change network settings, PATCH /api/settings, is auth-gated (is_authed) and rejects unknown top-level keys strictly (:1380-1402, 'network' known at :1387)"
+    - "crates/freshell-server/src/settings_store.rs:1350-1409 — validate_patch validates allowedFilePaths/panes/codingCli/editor but performs NO field validation of the network section, diverging from the legacy strict network sub-schema {host: enum(127.0.0.1|0.0.0.0), configured: boolean} in shared/settings.ts:815-818 — arbitrary network.host values are accepted and persisted"
+    - "test: check_auth_is_constant_time_and_rejects_absent in crates/freshell-api/src/lib.rs:117 (shared gate used by all authed routers)"
+    - "crates/freshell-platform/src/elevated.rs:227-265 — token-replay/overlap rejection logic exists but only as an unwired library (tested: confirm_with_wrong_token_reissues_and_does_not_spawn, in_flight_repair_returns_locked_409)"
+  missing: "Every mutation endpoint plus its defenses: zod-equivalent body schemas for the network routes (arbitrary-host/malformed-field rejection), command-injection surface hardening at route level, route-wired token replay rejection, and overlapping-privileged-op 409s — and PATCH /api/settings lacks network sub-field validation that legacy enforces."
+  effort: M
+  files:
+    - crates/freshell-server/src/network.rs
+    - crates/freshell-server/src/settings_store.rs
+
+- id: NET-09
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/settings_store.rs:311-370 — SettingsStore::patch: validate-then-mutate, persist BEFORE committing the live tree (GAP2 fix), single shared serialized store"
+    - "crates/freshell-server/src/settings_store.rs:419-514 — persist(): advisory ConfigLock across read-modify-write (:430), fresh disk read with copy-forward of unmanaged top-level keys (completedMigrations, recentDirectories), overlay-dirty-keys merge for session/terminal overrides"
+    - "crates/freshell-server/src/settings_store.rs:1387 — 'network' is a known top-level PATCH section; patch_settings route at :1614-1651 (auth + broadcast settings.updated at :1642-1646)"
+    - "test: settings_patch_preserves_unmanaged_top_level_document_state (settings_store.rs:2614), terminal_override_patch_preserves_unmanaged_top_level_document_state (:2642), session_override_patch_preserves_unmanaged_top_level_document_state (:2669), corrupted_primary_restores_from_backup_preserving_every_last_good_value (:3251)"
+    - "test: default_plus_network_overlay_matches_captured_fixture (settings_store.rs:2335) — byte-for-byte network overlay against a captured original settings.updated payload"
+    - "Gap: legacy routes network changes through POST /network/configure -> networkManager.configure -> configStore.patchSettings (server/network-manager.ts:417); that route is absent in Rust, so a network toggle via PATCH persists losslessly but produces NO chosen network behavior (no rebind — see NET-02)"
+  missing: "The canonical network-mutation route through the store (POST /api/network/configure) including its settings.updated broadcast on that path and the resulting listener behavior; storage-level losslessness for PATCH-initiated network writes is implemented and tested."
+  effort: S
+  files:
+    - crates/freshell-server/src/settings_store.rs
+    - crates/freshell-server/src/network.rs
+
+- id: NET-10
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-platform/src/firewall.rs:93-127,175 — native Linux firewall detection (ufw status / firewall-cmd --state, read-only); tests detect_linux_ufw_active (:492), ufw_inactive_falls_through_to_firewalld (:508), linux_none (:530)"
+    - "crates/freshell-platform/src/firewall.rs:190-223 — firewall_commands emits the legacy guidance strings ('sudo ufw allow <p>/tcp', firewall-cmd joined command, macOS socketfilterfw); golden tests ufw_commands_per_port (:341), firewalld_single_joined_command (:349), macos_app_level_command (:360)"
+    - "crates/freshell-server/src/network.rs:163-169 — active-firewall commands surface in status.firewall.commands (asserted non-empty in test all_interfaces_unconfigured_requests_remote_access_and_builds_commands, :365); privileged add commands are only ever built as strings, never executed (firewall.rs:8-11)"
+    - "crates/freshell-server/src/network.rs:243-249 — on native Linux/macOS the LAN-IP edge is unwired: lanIps is always [] (documented: 'only affects the 0.0.0.0 share path'); no listener enumeration beyond the bind host"
+    - "No configure-firewall route exists, so the legacy Linux guidance response {method:'terminal', command: ...} (server/network-router.ts:316-319) has no Rust equivalent; no macOS release-boundary declaration found (detection + app-level command strings exist instead)"
+  missing: "Native Linux LAN address/listener reporting (lanIps always empty), the terminal-guidance endpoint returning exact copyable commands ({method:'terminal'} response), and the documented macOS support-boundary declaration in UI — ufw/firewalld/macos detection, guidance strings and the no-privileged-execution guarantee are implemented and tested."
+  effort: M
+  files:
+    - crates/freshell-server/src/network.rs
+    - crates/freshell-platform/src/network.rs
+    - crates/freshell-platform/src/firewall.rs
diff --git a/docs/plans/parity-ledger/audit/p1-protocol-security-and-reliability-limits.yaml b/docs/plans/parity-ledger/audit/p1-protocol-security-and-reliability-limits.yaml
new file mode 100644
index 000000000..4b8d41238
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p1-protocol-security-and-reliability-limits.yaml
@@ -0,0 +1,196 @@
+# Parity audit: P1 — Protocol, security, and reliability limits
+# Repo audited (read-only): /home/dan/code/freshell/.worktrees/parity-campaign
+# Legacy Node behavior reference: server/ ; Rust port: crates/
+# Time-boxed breadth-first triage. SAFE-01..06 fully examined; SAFE-07..13
+# interrupted mid-triage (marked UNCLEAR with partial findings recorded).
+
+- id: SAFE-01
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/main.rs:1554-1569 — validate_auth_token() rejects empty/whitespace-only, <16 chars, and DEFAULT_BAD_TOKENS {changeme,default,password,token} (case-insensitive) in the legacy message order; called from the AUTH_TOKEN env read at main.rs:92"
+    - "crates/freshell-server/src/main.rs:2822-2850 — unit tests: empty, whitespace-only (20 spaces), short, each weak/default value, and a valid token"
+    - "crates/freshell-server/src/boot.rs:686-708 — is_authed(): 'x-auth-token' header wins UNCONDITIONALLY over the 'freshell-auth' cookie (percent-decoded), empty header falls through to cookie, constant-time compare via freshell_api::constant_time_eq"
+    - "test: wrong_header_rejects_even_with_correct_cookie_present (crates/freshell-server/src/boot.rs:794)"
+    - "test: empty_header_value_falls_back_to_cookie (crates/freshell-server/src/boot.rs:810); cookie_token_authorizes_url_decoded (:782); header_token_authorizes (:767); wrong_and_absent_token_rejected (:773)"
+    - "crates/freshell-ws/src/lib.rs:517-526 — evaluate_hello(): protocolVersion checked BEFORE token; constant-time token compare; wrong OR missing token both close 4001 'Invalid token' (lib.rs:613,640) matching legacy"
+    - "test: evaluate_hello_checks_version_before_token (crates/freshell-ws/src/lib.rs:859)"
+    - "test/e2e-browser/specs/safe01-auth-matrix.spec.ts — 12 tests (empty/short/'changeme'/whitespace startup rejection; API good/wrong/missing header, cookie-only, conflicting sources, empty-header fall-through; UI ?token= query + URL strip; WS hello good/wrong/missing). Section notes record green 12/12 x2 runs on BOTH legacy-chromium and rust-chromium (2026-07-19)"
+    - "Deliberate hardening divergence asserted in the spec: whitespace-only token rejected on Rust (token.trim().is_empty()), accepted by legacy's JS-falsy check — documented as KNOWN DIVERGENCE, not a bug (safe01-auth-matrix.spec.ts:141)"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-server/src/boot.rs
+    - crates/freshell-ws/src/lib.rs
+
+- id: SAFE-02
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "crates/freshell-server/src/rate_limit.rs (527 lines) — global process-wide token bucket (capacity 300, refill 5/s = legacy's windowMs 60_000/max 300 at server/index.ts:165-166); try_acquire returns Err(retry_after_secs) when empty; warn-once-per-streak 'api_rate_limited' log (rate_limit.rs:213-254)"
+    - "crates/freshell-server/src/rate_limit.rs:262-287 — is_limited_path limits /api/* EXCEPT /api/health; ws/static exempt; enforce() middleware returns 429 with JSON {ok:false,error:'rate_limited',message} + integer Retry-After header (rate_limit.rs:301-324)"
+    - "Wired into the production app: crates/freshell-server/src/main.rs:1138-1139 constructs RateLimiter::new_system(RateLimitConfig::default_api()); main.rs:1340-1341 layers rate_limit::enforce via axum::middleware::from_fn"
+    - "test: sustained_over_limit_returns_429_with_retry_after (rate_limit.rs:370); refill_restores_service_after_synthetic_clock_advance (:388, injectable TestClock — no real sleeps); concurrent_hammering_never_exceeds_capacity_and_never_panics (:411)"
+    - "test: exceeding_limit_on_an_api_route_returns_429_with_retry_after_and_envelope (rate_limit.rs:482, real router round-trip); health_path_is_exempt_even_when_bucket_is_exhausted (:460); ws_path_is_exempt_even_when_bucket_is_exhausted (:471)"
+    - "Deliberate deviation documented in module doc (rate_limit.rs:38-50): bucket is GLOBAL (one per process), not legacy's per-IP default — chosen to honor the checklist item's own word 'global'"
+    - "No PW-RUST browser spec exists (no safe02* in test/e2e-browser/specs/); two-context boundary/clock-advance/recovery legs are covered only at crate level via the synthetic-clock tests above"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-server/src/rate_limit.rs
+    - crates/freshell-server/src/main.rs
+
+- id: SAFE-03
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-ws/src/origin.rs:104 — evaluate_origin(origin, host, allowed_origins) allow-list policy (same-origin, allow-listed remote, explicit handling of missing/null/malformed origins); 8+ unit tests in its #[cfg(test)] module (origin.rs:129-201+); resolve_allowed_origins wires ALLOWED_ORIGINS + EXTRA_ALLOWED_ORIGINS"
+    - "crates/freshell-ws/src/lib.rs:540-549 — origin evaluated BEFORE the first frame is read; Rejected -> error frame + close 4011 'Origin not allowed' (CLOSE_ORIGIN_REJECTED, lib.rs:722) and return, so no ready/settings/terminal.inventory ever reaches a rejected client (structurally — the code returns before handshake build)"
+    - "crates/freshell-ws/src/main-server wiring: crates/freshell-server/src/main.rs:1613-1618 resolve_allowed_origins() passes env-derived list into WsState.allowed_origins (lib.rs:235)"
+    - "test: hostile_origin_with_valid_token_is_rejected_before_session_state (crates/freshell-ws/tests/origin_policy.rs:194, real axum server + real tokio-tungstenite client); null_origin_is_rejected (:203); allow_listed_origin_is_accepted (:212); same_origin_matching_host_is_allowed (:181); no_origin_header_is_allowed_through_to_handshake (:172)"
+    - "test/e2e-browser/specs/safe03-origin-matrix.spec.ts — 6 raw-socket tests against a server booted with ALLOWED_ORIGINS=https://trusted.example; section notes record green 6/6 x2 runs on BOTH legacy-chromium and rust-chromium (2026-07-19), with the intentional Rust-rejects/legacy-advisory-only difference asserted as KNOWN DIVERGENCE"
+    - "Narrowed caveat (documented in section notes): a broadcast-vs-close race (background terminal.inventory/settings.updated while a rejection lands) was not attempted in the PW spec; proven instead to the next-frame-after-reject-is-close level by the crate real-socket tests"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-ws/src/origin.rs
+    - crates/freshell-ws/src/lib.rs
+    - crates/freshell-ws/tests/origin_policy.rs
+
+- id: SAFE-04
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "Legacy reference exists: server/ws-handler.ts:237 (maxConnections: Number(process.env.MAX_CONNECTIONS || 50)), :271 (CLOSE_CODES.MAX_CONNECTIONS = 4003), :1132-1135 (onConnection closes 'Too many connections' when connections.size >= maxConnections, before hello)"
+    - "Exhaustive grep across crates/ for '4003', 'MAX_CONNECTIONS', 'Too many connections', 'max_connections', 'MAX_WS_CONNECTIONS' finds NO enforcement code; freshell-ws close codes are only 4001/4002/4010/4011 (crates/freshell-ws/src/lib.rs:713-722)"
+    - "crates/freshell-terminal/src/registry.rs:745-746 exposes connection_count() (increment :737 / decrement :1280) but its ONLY consumer is diagnostics (crates/freshell-server/src/diag.rs:152 'wsConnections') — observability, not enforcement"
+    - "No safe04* spec in test/e2e-browser/specs/; no max-connections option on WsState (lib.rs:91-235 state fields carry no cap)"
+  missing: "Entire clause: MAX_CONNECTIONS env config (default 50) plumbed through main.rs into WsState; pre-session rejection close 4003 'Too many connections' when the authenticated-connection cap is reached; prompt capacity release on close; preservation of existing clients; PW max-two scenario (third socket 4003s, close-one/reconnect succeeds, survivor keeps terminal I/O)"
+  effort: M
+  files:
+    - crates/freshell-ws/src/lib.rs
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-terminal/src/registry.rs
+
+- id: SAFE-05
+  verdict: DONE
+  confidence: medium
+  evidence:
+    - "Hello timeout: crates/freshell-ws/src/lib.rs:566-590 wraps the whole pre-hello read loop in tokio::time::timeout(hello_deadline) and closes CLOSE_HELLO_TIMEOUT=4002 'Hello timeout' on elapse (constant lib.rs:716); config resolve_hello_timeout_ms() env HELLO_TIMEOUT_MS default 5000 (crates/freshell-server/src/main.rs:1591-1596) mirrors ws-handler.ts:223"
+    - "test: connection_with_no_hello_is_closed_with_hello_timeout_code (crates/freshell-ws/tests/hello_timeout.rs:122, real socket, ephemeral port); connection_with_timely_hello_stays_open_past_the_deadline (:160 — authenticated connection immune)"
+    - "JSON ping/pong: crates/freshell-ws/src/terminal.rs:861-865 — ClientMessage::Ping arm replies ServerMessage::Pong(Pong{ timestamp: crate::now_iso() }) (types: crates/freshell-protocol/src/client_messages.rs:22, crates/freshell-protocol/src/server_messages.rs:87,764), byte-parity with legacy ws-handler.ts ping/pong"
+    - "test: test/e2e-browser/specs/ws-ping-pong-matrix.spec.ts — 2 tests (exact {type:'pong',timestamp} shape; fresh timestamp per ping); section notes record green 2/2 x2 on BOTH legacy-chromium and rust-chromium after the Ping-arm fix (2026-07-18 follow-on)"
+    - "Transport heartbeat, independent of app ping: crates/freshell-ws/tests/keepalive.rs — idle_connection_receives_periodic_pings_and_survives_multiple_cycles (:162) and connection_with_no_pong_reply_is_terminated_after_one_missed_cycle (:266), both real-socket; PING_INTERVAL_MS plumbing at main.rs:1581-1586 mirrors ws-handler.ts:224"
+    - "Detached-terminal survival is structural (terminals owned by freshell-terminal TerminalRegistry, not the socket) and exercised by the restore e2e suites (e.g. server-restart-recovery.spec.ts, double-restart-terminal-restore-rust.spec.ts present in test/e2e-browser/specs/)"
+    - "Caveat: no PW controllable-proxy leg for control-frame heartbeat suppression (per section notes; covered by the keepalive.rs real-socket tests instead), and no single test explicitly asserting 'heartbeat close never kills a detached terminal' — hence medium, not high"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-ws/src/lib.rs
+    - crates/freshell-ws/src/terminal.rs
+    - crates/freshell-server/src/main.rs
+
+- id: SAFE-06
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "DONE clause — WS inbound frame/message bound at legacy parity: WS_MAX_PAYLOAD_BYTES env, default 16 MiB (crates/freshell-server/src/main.rs:1601-1607 -> ws_max_payload_bytes passed at main.rs:765), mirroring ws-handler.ts:226"
+    - "test: message_at_exactly_the_limit_is_accepted_and_processed (crates/freshell-ws/tests/max_payload.rs:201, real socket); message_one_byte_over_the_limit_is_rejected_without_affecting_other_clients (:223 — rejection does not cross-contaminate a second client)"
+    - "MISSING — HTTP body bound at legacy parity: legacy is express.json({limit:'1mb'}) (server/index.ts:185); Rust has NO explicit DefaultBodyLimit anywhere in crates/ (grep confirms) and only inherits axum 0.8's implicit 2 MB Json-extractor limit — recorded as an 'accepted deviation' comment (crates/freshell-server/src/resolve.rs:113) but never implemented to 1mb and never tested"
+    - "MISSING — bootstrap payload budget: legacy rejects an oversized bootstrap with 500 'Bootstrap payload exceeds budget' when > MAX_BOOTSTRAP_PAYLOAD_BYTES (12*1024, shared/read-models.ts:4; check server/shell-bootstrap-router.ts:85-88); the Rust handler (crates/freshell-server/src/boot.rs:100-126) has no byte-count/budget check at all"
+    - "MISSING — outbound chunk knob: legacy declares maxChunkBytes (MAX_WS_CHUNK_BYTES, default 500KiB) at server/ws-handler.ts:132,244 (it is declared-but-never-consumed even in legacy); no Rust equivalent declaration exists; no PW just-below/just-above matrix, bounded-RSS, or chunk-reassembly assertions found (no safe06* spec)"
+  missing: "HTTP body limit at 1mb legacy parity (explicit DefaultBodyLimit + rejection test); bootstrap payload budget check (MAX_BOOTSTRAP_PAYLOAD_BYTES 12KiB -> 500 'Bootstrap payload exceeds budget') in the Rust /api/bootstrap handler; outbound chunk byte cap plumbing (MAX_WS_CHUNK_BYTES) if non-dead behavior is desired; PW just-below/just-above-limit HTTP+WS matrix with RSS and second-client-health assertions"
+  effort: S
+  files:
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-server/src/boot.rs
+    - crates/freshell-ws/src/lib.rs
+
+- id: SAFE-07
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "Schema side exists: crates/freshell-protocol/src/client_messages.rs defines a ~30-variant ClientMessage enum (hello, ping, client.diagnostic, terminal.create/attach/detach/input/resize/kill/autoResumeCancel, terminal.codex.candidate.persisted, codex/opencode/claude/amplifier activity.list, ui.layout.sync, ui.screenshot.result, codingcli.create/input/kill, freshAgent create/attach/send/interrupt/compact/approval.respond/question.respond/kill/fork, pane.reconcile.request)"
+    - "Handler dispatch exists: crates/freshell-ws/src/terminal.rs handle_client_text with explicit arms (e.g. ClientMessage::Ping -> Pong at :861-865); many WS behavior tests exist in crates/freshell-ws/tests/ (create_protection.rs, unknown_terminal_reply.rs, create_limit.rs-adjacent suites, term09_output_queue.rs, rest_ws_shared_gate.rs, etc.)"
+    - "NOT verified: per-variant valid-minimum and negative-payload coverage, request-correlation on errors, the schema-row-to-scenario table, or a PW-RUST matrix generator — no safe07* spec in the test/e2e-browser/specs/ listing"
+  missing: "Unknown whether every ClientMessage variant has its correct success effect/response and graded negative-case errors; the PW-RUST generated-table spec (valid minimum + negative payload per variant, follow each nonfatal negative with a valid terminal command) was not found. Audit was interrupted before handler-coverage mapping could be completed."
+  effort: L
+  files:
+    - crates/freshell-protocol/src/client_messages.rs
+    - crates/freshell-ws/src/terminal.rs
+
+- id: SAFE-08
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "A test file named for this exact item exists: crates/freshell-ws/tests/safe08_restore_diagnostics.rs (present in the tests/ listing) — its contents were not read"
+    - "Adjacent restore infrastructure exists in the same crate: restore_spawn_gate.rs, restore_storm.rs, restore_plan_queue_cap.rs, pane_ledger_restore.rs, resume_validation_gate.rs tests; restore/ledger/reconcile modules in crates/freshell-ws/src/ (pane_ledger.rs, reconcile.rs, resume_validation.rs, restore-gate files)"
+    - "No safe08* PW spec in test/e2e-browser/specs/ listing"
+  missing: "Unverified: structured diagnostics recording, one-shot stale-handle reconcile (repair/recreation) semantics, restore-loop prevention, and the PW seed-dead/mismatched-handles scenario. Audit interrupted before safe08_restore_diagnostics.rs or the implementation modules were read."
+  effort: M
+  files:
+    - crates/freshell-ws/src/reconcile.rs
+    - crates/freshell-ws/tests/safe08_restore_diagnostics.rs
+
+- id: SAFE-09
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "No search was performed for cancellation of abandoned work (snapshot, wait-for, exec, provider start, search, bootstrap) before the audit was interrupted"
+    - "No safe09* PW spec in the test/e2e-browser/specs/ listing"
+  missing: "Everything unverified: abort/cancellation wiring (CancellationToken/AbortHandle-equivalents) for snapshot/wait-for/exec/provider-start/search/bootstrap work, task-count-returns-to-zero assertions, and the PW abort/navigate-away scenario"
+  effort: M
+  files:
+    - crates/freshell-server/src/boot.rs
+    - crates/freshell-ws/src/terminal.rs
+
+- id: SAFE-10
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "crates/freshell-ws/src/backpressure.rs exists (not read)"
+    - "test: slow_broadcast_consumer_lag_closes_with_backpressure_code_for_resync (crates/freshell-ws/tests/keepalive.rs:206, real-socket) — the test NAME suggests lag -> close-with-backpressure-code-for-resync behavior, matching legacy's CLOSE_CODES.BACKPRESSURE=4008 (server/ws-handler.ts:272, backpressureCloseCode at :1656); contents not verified"
+    - "NOT verified: the resync/snapshot side for the named families (settings, lifecycle, activity, association, materialization, extensions, tabs), i.e. whether a resumed lagged client is made equal to authoritative API state; no safe10* PW spec in the specs listing"
+  missing: "Unverified resync/recovery surface per event family and the PW freeze-context/burst-events/resume/equality-to-authoritative-state scenario (no ghost terminals, no stale busy state)"
+  effort: M
+  files:
+    - crates/freshell-ws/src/backpressure.rs
+    - crates/freshell-ws/src/terminal.rs
+
+- id: SAFE-11
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "crates/freshell-server/src/shutdown_forensics.rs exists (name implies shutdown logging/forensics; not read)"
+    - "WsState carries shutdown: Arc<Notify> (crates/freshell-ws/src/lib.rs:815) and lib.rs:208 documents the client observing close code 4009 'Server shutting down' — legacy CLOSE_CODES.SERVER_SHUTDOWN=4009 (server/ws-handler.ts:272, used at :1129); enforcement path not read"
+    - "crates/freshell-tauri crate exists for the Windows/Tauri host (relevant to the 'ownership-safe / never touch unrelated processes' clause)"
+    - "The checklist's PW validation is explicitly a native Windows project (kill exact server, sentinel PID safety) — that layer is host-specific and was not assessable here; no safe11*-style spec was observed in the specs listing"
+  missing: "Unverified: stop-accepting-work gate, state/log flush, exact terminal/provider/extension process-tree termination, full-grace-period usage, unrelated-process safety, and the Windows-host PW scenario. Audit interrupted before shutdown_forensics.rs / main.rs shutdown wiring were read."
+  effort: L
+  files:
+    - crates/freshell-server/src/main.rs
+    - crates/freshell-server/src/shutdown_forensics.rs
+    - crates/freshell-ws/src/lib.rs
+
+- id: SAFE-12
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "test/e2e-browser/specs/stress.spec.ts exists (not read); whether it is a bounded MIXED-load soak (terminals + agent events + tab sync + proxy traffic + indexing + API captures + reconnects together) or runs against rust-chromium is unverified"
+    - "No evidence gathered for the 30-minute CI / 2-hour release-gate soak runner, latency/RSS/handle/queue ceiling assertions, cross-session leak checks, or zero-owned-PID teardown assertions"
+  missing: "Unverified: bounded mixed-load soak scenario, duration tiers (30 min CI / 2 h gate), marker/latency/RSS/handle/queue ceilings, cross-session leakage assertions, and post-teardown owned-PID accounting"
+  effort: L
+  files:
+    - test/e2e-browser/specs/stress.spec.ts
+
+- id: SAFE-13
+  verdict: UNCLEAR
+  confidence: low
+  evidence:
+    - "Server-message schema exists: crates/freshell-protocol/src/server_messages.rs defines the ServerMessage enum (e.g. Pong at :87; Pong struct at :764) — full variant inventory not read"
+    - "NOT verified: per-variant semantic owner map, payload/correlation contracts, reconnect behavior, retirement inventory, or a PW-RUST generated coverage manifest; no safe13* spec in the test/e2e-browser/specs/ listing"
+  missing: "Unverified: complete server-to-client event inventory with owners/contracts/reconnect behavior, the PW coverage-manifest generator (raw-frame capture + user-visible effect assertion per variant, fail-on-unowned-type)"
+  effort: L
+  files:
+    - crates/freshell-protocol/src/server_messages.rs
diff --git a/docs/plans/parity-ledger/audit/p1-tab-pane-cli-and-mcp-automation.yaml b/docs/plans/parity-ledger/audit/p1-tab-pane-cli-and-mcp-automation.yaml
new file mode 100644
index 000000000..b41c801c9
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p1-tab-pane-cli-and-mcp-automation.yaml
@@ -0,0 +1,235 @@
+- id: AUTO-01
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-protocol/src/client_messages.rs:52-53,332 — `ui.layout.sync` / `UiLayoutSync` exist only as protocol types"
+    - "test: ui.layout.sync roundtrips in crates/freshell-protocol/tests/roundtrip.rs:352-377 — wire-shape tests only, no server behavior"
+    - "crates/freshell-ws/src/terminal.rs:524-949 — `handle_client_text` dispatch has NO `ClientMessage::UiLayoutSync` arm; it falls to `_ => true` at :949 (accept-and-strip, silently ignored)"
+    - "crates/freshell-freshagent/src/pane_ops.rs:791-815 — resize deferral comment states verbatim: 'the one channel that could report it back (ui.layout.sync, the client->server layout mirror) is not consumed anywhere in this port' and 'Deferred pending ui.layout.sync ingestion (AUTO-01)'"
+    - "crates/freshell-freshagent/src/pane_ops.rs:517-532 — GET /api/layout/snapshot doc comment admits it is NOT the connected-UI layout: no server-side layout tree, activeTabId always null, multi-pane tabs get {type:'unknown'} markers"
+  missing: "Entire core mechanic: no WS handler ingests ui.layout.sync; there is no authoritative connected-UI layout store shared by browser/REST/CLI/MCP; REST/MCP see only the server's own in-memory bookkeeping (activeTabId hard-coded null). Only protocol scaffolding exists."
+  effort: L
+  files:
+    - crates/freshell-ws/src/terminal.rs
+    - crates/freshell-ws/src/lib.rs
+    - crates/freshell-freshagent/src/pane_ops.rs
+    - crates/freshell-protocol/src/client_messages.rs
+
+- id: AUTO-02
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-freshagent/src/terminal_tabs.rs:196-269 — create_terminal_or_content_tab dispatches browser/editor/terminal shapes (legacy router.ts:695-831 parity)"
+    - "crates/freshell-freshagent/src/terminal_tabs.rs:828-890 — spawn_terminal_pane: mode_is_known gate (:841), cwd pre-validation for atomic rollback (:882-889 'keeps the atomic-rollback contract honest and testable'), model/sandbox/permissionMode body options honored at :1479-1490"
+    - "crates/freshell-freshagent/src/terminal_tabs.rs:824-827 — doc: 'atomic rollback by construction' (nothing recorded before spawn succeeds), matching legacy cleanup contract"
+    - "crates/freshell-freshagent/src/lib.rs:1483-1504 — agent dispatch: only agent:'opencode' accepted; any other agent -> 400 'unknown agent' (legacy router.ts:540-544 AGENT_SESSION_TYPES also supports claude/codex)"
+    - "crates/freshell-freshagent/src/lib.rs:1506-1579 — opencode fresh-agent create carries cwd/model/effort/name options + placeholder pane + ui.command{tab.create}"
+    - "test: create_tab_rollback_on_spawn_failure_leaves_no_tab_or_pane_or_registry_entry in crates/freshell-freshagent/src/terminal_tabs.rs:3034"
+    - "test: create_browser_tab_attaches_browser_pane_content_and_no_terminal (:3066), create_editor_tab_attaches_editor_pane_content (:3092), create_tab_defaults_to_shell_mode_when_mode_absent (:3002), create_tab_unregistered_terminal_mode_is_400 (:3010), create_tab_without_registry_wired_is_503 (:3023)"
+    - "test: rich terminal providers — create_amplifier_tab_fresh_mints_identity_prestubs_and_spawns_resume_argv (:3558), create_opencode_tab_fresh_spawns_with_hostname_port_args_and_arms_locator (:3759), create_fresh_claude_tab_preallocates_session_identity (:3877), create_codex_tab_accepts_session_ref_and_derives_resume_args (:4388), create_codex_tab_rejects_raw_resume_session_id_without_session_ref (:4365)"
+    - "e2e: test/e2e-browser/specs/mcp-qa-smoke-rust.spec.ts:68 — unmodified legacy MCP binary creates shell/amplifier(fresh+resume)/opencode/codex(fresh+sessionRef)/browser/editor tabs against the release Rust server"
+  missing: "Fresh-agent (non-terminal) REST tab creation for agent:'claude' and agent:'codex' — only opencode is wired (lib.rs:1499-1504 400s the rest), while legacy supports all three via AGENT_SESSION_TYPES. (claude/codex/kimi/gemini are creatable only as terminal-mode CLI tabs.)"
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/lib.rs
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+
+- id: AUTO-03
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-freshagent/src/terminal_tabs.rs:2244-2259 — GET /api/tabs list (reduced shape: id/title/paneId/kind, activeTabId hard-coded null)"
+    - "crates/freshell-freshagent/src/pane_ops.rs:330-351 — POST /api/tabs/:id/select (legacy-exact unconditional broadcast, router.ts:834-838)"
+    - "crates/freshell-freshagent/src/pane_ops.rs:358-386 — PATCH /api/tabs/:id rename (+ui.command{tab.rename})"
+    - "crates/freshell-freshagent/src/pane_ops.rs:397-445 — DELETE /api/tabs/:id removes tab + every owned pane's bookkeeping, no PTY kill (documented legacy detach-don't-kill parity :15-32)"
+    - "crates/freshell-freshagent/src/pane_ops.rs:456-467 — GET /api/tabs/has exists-check (id-only matching, :449-455)"
+    - "test: select_known_tab_succeeds (:1336), select_unknown_tab_still_broadcasts_but_reports_not_found (:1318), rename_known_tab_broadcasts_tab_rename (:1379), delete_tab_removes_tab_and_every_owned_pane_without_killing_ptys (:1420), tabs_has_true_for_known_tab_id (:1495), tabs_has_false_for_unknown_tab_id (:1508) in crates/freshell-freshagent/src/pane_ops.rs"
+    - "test: get_tabs_lists_every_created_tab_kind in crates/freshell-freshagent/src/terminal_tabs.rs:3150"
+    - "crates/freshell-freshagent/src/pane_ops.rs:469-513 — POST /api/tabs/next and /prev are EXPLICIT honest-400 deferrals (TAB_CYCLE_DEFERRAL_MESSAGE): FreshAgentState is an unordered HashMap with no active-tab concept"
+    - "test: tabs_next_is_honest_400_deferral (:1526), tabs_prev_is_honest_400_deferral (:1542) — tests pin the FAILURE, not the behavior"
+  missing: "next/previous (always 400); tab ORDER preservation (unordered HashMap — list order is arbitrary, so 'preserve order' is unmeetable today); activeTabId tracking (always null, so selection is not observable via GET /api/tabs); title-based exists lookup (legacy hasTab matches id OR title, pane_ops.rs:449-455)."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/pane_ops.rs
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+    - crates/freshell-freshagent/src/lib.rs
+
+- id: AUTO-04
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-freshagent/src/pane_ops.rs:533-613 — GET /api/layout/snapshot?tabId= with legacy top-level keys {tabs, activeTabId, layouts, activePane, paneTitles, paneTitleSetByUser} and tabId filter (:541, :558-560, :569)"
+    - "crates/freshell-freshagent/src/pane_ops.rs:591-598 — single-pane tabs produce a real {type:'leaf', id, content:{kind, terminalId}} node; multi-pane tabs produce only a self-describing {type:'unknown', paneIds:[...]} marker — direction/sizes/ratios are explicitly NOT returned (doc :517-532: 'Rather than fabricate split geometry... this port never tracked')"
+    - "crates/freshell-freshagent/src/terminal_tabs.rs:2280-2330 — GET /api/panes with ?tabId= filter; per-pane id/tabId/title/kind/terminalId resolved from terminal_panes/content_panes/fresh-agent panes maps (:2306-2319)"
+    - "test: layout_snapshot_empty_state_has_legacy_exact_top_level_keys (:1559), layout_snapshot_single_pane_tab_is_a_real_leaf_node (:1573), layout_snapshot_multi_pane_tab_is_an_honest_unknown_marker_not_a_fabricated_split (:1592), layout_snapshot_tab_id_filter_narrows_to_one_tab (:1633) in crates/freshell-freshagent/src/pane_ops.rs"
+    - "test: get_panes_lists_created_panes_with_id_and_terminal_id in crates/freshell-freshagent/src/terminal_tabs.rs:3127"
+  missing: "The 'exact current layout': no split tree (direction/sizes/ratios) for multi-pane tabs; paneTitles/paneTitleSetByUser always {} (:607-609); activeTabId always null (:605); leaf content carries only kind+terminalId — provider/session identities (sessionRef/resumeSessionId) are NOT included in the snapshot content. True exactness is blocked on AUTO-01 (no connected-UI layout source)."
+  effort: L
+  files:
+    - crates/freshell-freshagent/src/pane_ops.rs
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+
+- id: AUTO-05
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-freshagent/src/pane_ops.rs:89-195 — POST /api/panes/:id/split: direction param honored (default 'horizontal', :118-123), browser URL (:127-138), editor file (:139-154), terminal via the shared spawn_terminal_pane pipeline (:156-159, inherits provider params model/sandbox/permissionMode/cwd), unknown pane 404 (:105-107)"
+    - "crates/freshell-freshagent/src/pane_ops.rs:109-116 — agent-based (rich-agent) splits are an explicit honest-400 deferral"
+    - "test: split_terminal_pane_spawns_real_pty_and_broadcasts_pane_split (:1102, vertical direction + real PTY + exact pane.split payload), split_browser_pane_registers_cheap_content_no_terminal (:1152), split_agent_pane_is_honest_400 (:1084), split_unknown_pane_is_404 (:1070) in crates/freshell-freshagent/src/pane_ops.rs"
+    - "test: split_pane_claude_preallocates_fresh_session_identity (crates/freshell-freshagent/src/terminal_tabs.rs:4018), rest_split_resume_onto_live_session_is_refused_409 (:4864)"
+    - "rollback: pane_ops.rs:156-169 — spawn failure returns before ANY pane_tabs/content_panes/terminal map insert (rollback by construction)"
+    - "e2e: test/e2e-browser/specs/mcp-qa-smoke-rust.spec.ts:336-344 — MCP split-pane round trip + list-panes assertion"
+  missing: "Rich-agent (agent) splits (400 deferral). 'position' parameter: no handling observed (payload carries only direction/newPaneId/newContent, :173-184). 'ratio': no server-side sizes/ratio support (resize itself is deferred — AUTO-06). Editor-split and spawn-failure-injection rollback have no dedicated split-scoped test (rollback is by construction + create-path test only). "
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/pane_ops.rs
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+
+- id: AUTO-06
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "close: crates/freshell-freshagent/src/pane_ops.rs:210-273 (+ cannot-close-only-pane guard :240-245); tests close_only_pane_in_tab_is_refused (:1186), split_then_close_removes_bookkeeping_but_keeps_pty_alive_no_orphan (:1218)"
+    - "select: pane_ops.rs:283-313; tests select_pane_resolves_tab_via_pane_tabs_and_broadcasts (:1288), select_unknown_pane_is_ok_with_not_found_message_and_no_broadcast (:1269)"
+    - "swap: pane_ops.rs:838-933 (content exchange across terminal/content buckets, stable pane IDs); tests swap_two_terminal_panes_in_same_tab_exchanges_bookkeeping_and_broadcasts (:1929), swap_cross_tab_panes_reports_panes_not_found (:1907)"
+    - "respawn: pane_ops.rs:696-742 (same pane id, new terminal, pane.attach broadcast, createRequestId rotation); tests respawn_pane_replaces_terminal_in_place_and_broadcasts_pane_attach (:1741), respawn_pane_claude_ends_with_session_identity (terminal_tabs.rs:4090), rest_respawn_resume_onto_live_session_is_refused_409 (:4748)"
+    - "rename: crates/freshell-freshagent/src/lib.rs:1627-1651 (name validation + 500-char cap :1584/:1640-1645); tests at lib.rs:3103-3134 (PATCH /api/panes/:id section)"
+    - "resize: pane_ops.rs:799-818 — EXPLICIT honest-400 deferral (server never learns splitId; needs ui.layout.sync/AUTO-01); test resize_pane_is_honest_400_deferral (:1841) pins the failure"
+  missing: "resize (always 400 — blocked on AUTO-01 splitId channel). rename is reduced-fidelity: returns 200 for ANY pane id (disclosed deviation lib.rs:1616-1626 — no pane resolution, no 404/409) and emits NO ui.command{pane.rename} broadcast (lib.rs:1602-1607), so title updates are not server-propagated. swap cannot touch fresh-agent panes (graceful not-found, pane_ops.rs:833-837)."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/pane_ops.rs
+    - crates/freshell-freshagent/src/lib.rs
+
+- id: AUTO-07
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-freshagent/src/pane_ops.rs:760-779 — POST /api/panes/:id/attach is a permanent honest-400 stub: 'pane attach is not implemented on this server...' — the legacy identity guard (terminalMatchesExpectedSession, router.ts:1619-1652) needs TerminalIdentityRegistry in freshell-ws, unreachable without a circular dependency (:746-759)"
+    - "test: attach_pane_requires_auth (:1816), attach_pane_is_honest_400_deferral (:1823) — the only tests prove the route always refuses"
+    - "e2e smoke test/e2e-browser/specs/mcp-qa-smoke-rust.spec.ts:330-334 — 'attach is skipped' explicitly documented as out of scope"
+  missing: "Everything: no PTY re-bind mechanics, no expected-session/identity-mismatch 409 check, no buffered-output replay to the pane, no 'one PID' enforcement. Route exists solely to return 400."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/pane_ops.rs
+    - crates/freshell-ws/src/identity.rs
+
+- id: AUTO-08
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-freshagent/src/pane_ops.rs:624-677 — POST /api/panes/:id/navigate: url/url-or-target required (:634-641, 400 'url required'), unknown pane 404 (:643-651), REPLACES pane content with browser kind while RETAINING the pane id (:653-658), clears any terminal registration (:659-663), broadcasts ui.command{pane.attach} with the same paneId (:665-668), legacy-exact 'navigate requested' message (:670-676)"
+    - "test: navigate_pane_requires_auth (:1654), navigate_pane_missing_url_is_400 (:1661), navigate_unknown_pane_is_404 (:1669), navigate_pane_success_sets_browser_content_and_broadcasts_pane_attach (:1683) in crates/freshell-freshagent/src/pane_ops.rs"
+    - "e2e: test/e2e-browser/specs/mcp-qa-smoke-rust.spec.ts:319-323 — MCP navigate against a browser pane returns non-error"
+    - "pane_ops.rs:617-623 — doc claims legacy's attachPaneContent is unconditional, so converting a terminal/editor pane into a browser is deliberate legacy parity"
+  missing: "History rules: no navigation-history tracking observed anywhere in the Rust port. Forwarding-state retention: no forwarding concept observed attached to browser panes. Invalid/nonbrowser-target rejection: the implementation converts ANY pane kind to browser content (there is no error branch for a 'documented' invalid target beyond 404/400) — could not fully verify against legacy router.ts:1654-1667 within the time budget whether legacy has a nonbrowser error this port skips."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/pane_ops.rs
+
+- id: AUTO-09
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "send (terminal): crates/freshell-freshagent/src/terminal_tabs.rs:2340-2411 maybe_send_keys — real PTY write via shared registry (:2392), text required 400 (:2365), terminal not found 404 (:2371-2376), codex/opencode locator feeding on submit (:2387-2409)"
+    - "send (rich agent): crates/freshell-freshagent/src/lib.rs:1655-1865 — opencode placeholder-pane cold-start, durable session materialization, run_turn to idle edge, timeout -> approx (:1852-1862)"
+    - "capture (terminal): terminal_tabs.rs:2429-2455 — scrollback snapshot with S start-line semantics (:2490-2505)"
+    - "capture (editor, capture-only): terminal_tabs.rs:2468-2474 — returns editor content as text"
+    - "capture (browser, explicit rejection): terminal_tabs.rs:2476-2481 — 422 'pane kind \"browser\" does not support capture-pane; use screenshot-pane' (legacy-exact wording router.ts:947-949)"
+    - "test: send_keys_then_wait_for_then_capture_round_trips_a_real_shell_command (:3199), capture_editor_pane_returns_content_text (:3357), capture_browser_pane_is_422_use_screenshot_pane (:6223), send_keys_unknown_pane_falls_through_to_pane_not_found_404 (:3303), send_keys_enter_feeds_codex_locator (:5939) in crates/freshell-freshagent/src/terminal_tabs.rs"
+  missing: "Browser/editor INPUT rejection is not type-correct: send-keys to a browser/editor pane falls through maybe_send_keys -> fresh-agent map lookup -> generic 404 'pane not found' (lib.rs:1690), not a kind-specific explicit rejection, and no test covers that pair. ANSI-inclusive capture (e flag) and wrapped-line join (J flag) are documented no-ops (terminal_tabs.rs:2424-2426). Rich-agent send/capture covers ONLY opencode REST panes (claude/codex fresh-agent panes unreachable on this surface). The full unsupported-pane/operation error matrix is untested."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/lib.rs
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+
+- id: AUTO-10
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-freshagent/src/terminal_tabs.rs:2514-2585 — GET /api/panes/:id/wait-for terminal branch: pattern/p regex via fancy_regex (:2540-2547), invalid regex -> 400 'invalid pattern' (:2544), T/timeout seconds with 30s default (:2559-2564), 100ms poll loop against THIS terminal's snapshot only (:2567-2583 — pane isolation by construction), {matched:true, reason:'pattern'} (:2575), timeout -> approx {matched:false} (:2581)"
+    - "terminal_tabs.rs:2548-2556 — no pattern -> explicit 400 naming the deferred stable/exit/prompt modes rather than silently succeeding"
+    - "test: wait_for_requires_auth (:3317), wait_for_unknown_pane_is_404_terminal_not_found (:3324), wait_for_never_matching_pattern_times_out_as_approx (:3337) in crates/freshell-freshagent/src/terminal_tabs.rs; real-PTY round trip in send_keys_then_wait_for_then_capture_round_trips_a_real_shell_command (:3199)"
+  missing: "stable/exit/prompt modes (explicit deferral, terminal_tabs.rs:2507-2513); rich-agent idle/waiting modes (wait_for 404s on fresh-agent panes at :2530-2531); explicit request-cancellation API (no cancellation channel in the loop; only implicit drop-on-disconnect). Text (non-regex) matching is only via regex pattern — no separate plain-text mode observed."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+
+- id: AUTO-11
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "grep for 'api/run' and '/api/run' across all of crates/ returned ZERO matches — no route, no handler"
+    - "legacy reference exists: server/agent-api/router.ts:1154 router.post('/run', ...)"
+    - "crates/freshell-freshagent/src/lib.rs:1367-1372 and pane_ops.rs:56-73 route tables contain every other agent-API route but no /api/run"
+  missing: "The entire route: no POST /api/run handling command/title/name/mode/shell/cwd/capture/detached/timeout, no always-new-tab creation from it, no legacy response/capture envelope."
+  effort: M
+  files:
+    - crates/freshell-freshagent/src/lib.rs
+    - crates/freshell-freshagent/src/terminal_tabs.rs
+
+- id: AUTO-12
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-protocol/src/client_messages.rs:56-61,427-463 — codingcli.create/input/kill message types exist in the protocol crate"
+    - "crates/freshell-protocol/src/server_messages.rs:35-43 — codingcli.created/event/exit/killed/stderr response types exist"
+    - "test: codingcli.create wire roundtrip in crates/freshell-protocol/tests/roundtrip.rs:308-310 — shape-only"
+    - "crates/freshell-ws/src/terminal.rs:524-949 — WS dispatch has NO CodingCliCreate/CodingCliInput/CodingCliKill arm; all three hit `_ => true` (:949) and are silently ignored (accept-and-strip)"
+    - "crates/freshell-ws/src/terminal.rs:711-819 — the provider-registry routing exists only under the freshAgent.create/send/interrupt/kill surface (codex/claude/opencode), NOT the legacy codingcli.* frame family"
+  missing: "The entire runtime API: no handler routes codingcli.create/input/kill to any provider; no correlated codingcli.created/event/exit/killed/stderr frames are ever emitted; an unsupported provider gets silence, not an explicit error. Only protocol envelopes exist."
+  effort: M
+  files:
+    - crates/freshell-ws/src/terminal.rs
+    - crates/freshell-protocol/src/client_messages.rs
+
+- id: AUTO-13
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "test/e2e-browser/specs/mcp-bridge-rust.spec.ts:48 — 'unmodified legacy MCP stdio binary drives an ephemeral Rust server end-to-end' (new-tab -> send-keys -> wait-for -> capture-pane, shell)"
+    - "test/e2e-browser/specs/mcp-qa-smoke-rust.spec.ts:68-417 — unmodified legacy MCP stdio binary (dist/server/mcp/server.js, FRESHELL_URL/FRESHELL_TOKEN only) against the Rust release binary covers: new-tab (shell, amplifier fresh+resume, opencode, codex fresh + sessionRef-resume + raw-resume-reject :288-291), send-keys, wait-for, capture-pane, list-panes, navigate, split-pane, resize-pane (non-error only :346-347), swap-pane, respawn-pane, select-pane, select-tab, has-tab, kill-pane, kill-tab, list-tabs, plus restart-isolation assertions"
+    - "registered inventory (legacy server/mcp/freshell-tool.ts:631-923) includes ~30 actions: fresh-send, new-tab, list-tabs, select-tab, kill-tab, rename-tab, has-tab, next-tab, prev-tab, split-pane, list-panes, select-pane, rename-pane, kill-pane, resize-pane, swap-pane, respawn-pane, send-keys, capture-pane, wait-for, run, summarize, display, list-terminals, attach, open-browser, navigate, screenshot, list-sessions, search-sessions, lan-info, health, help"
+    - "MCP server itself remains the legacy Node binary; Rust injects it via crates/freshell-platform/src/mcp_inject.rs:535 (generate_mcp_injection) with golden tests in mcp_inject_tests.rs"
+  missing: "Coverage is a hand-written scenario list, NOT a table generated from the registered tool definitions — nothing fails if a command is added/renamed. Unexercised commands include: fresh-send, rename-tab, rename-pane, next-tab, prev-tab, run, summarize, display, list-terminals, attach, open-browser, screenshot, list-sessions, search-sessions, lan-info, health, help; resize-pane/next-style rows that 400 would need expected-error scenarios. No per-command tool-result + DOM/API/process-effect triple assertion for most rows."
+  effort: M
+  files:
+    - test/e2e-browser/specs/mcp-qa-smoke-rust.spec.ts
+    - test/e2e-browser/specs/mcp-bridge-rust.spec.ts
+    - test/e2e-browser/helpers/mcp-stdio-client.js
+
+- id: AUTO-14
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-ws/src/screenshot.rs:54-57,107-121,131-143 — ScreenshotBroker supports expected_client_id binding: resolve_from (:131) IGNORES a ui.screenshot.result from a connection that is not the expected client — the 'never accept the first unrelated reply' mechanism EXISTS"
+    - "crates/freshell-ws/src/screenshot.rs:79-100 — per-connection capability tracking (add_capable_client/remove_capable_client on capabilities.uiScreenshotV1, wired at crates/freshell-ws/src/lib.rs:685-701 and terminal.rs:197)"
+    - "crates/freshell-server/src/screenshots.rs:62-178 — POST /api/screenshots faithful port (scope validation, 503 no-capable-client, 10s timeout, atomic file write); BUT :113 calls broker.register() with expected=None and :114-116 send_capture BROADCASTS to all clients (screenshot.rs:163-185) — the first reply from ANY window wins; the binding machinery is unused by this route"
+    - "grep found no register_expected caller outside screenshot.rs itself — target-bound registration appears unwired (doc at screenshot.rs:145-146 references target-bound restore requests, but no call site was located in the time budget)"
+    - "crates/freshell-freshagent/src/pane_ops.rs:330-351 — tab select broadcasts ui.command{tab.select} to ALL clients; no per-window targeting for select/rename either"
+    - "test: capability_count_tracks_and_clamps (:197), register_resolve_delivers_result (:214), resolve_unknown_request_is_ignored (:235), send_capture_frame_matches_ui_command_shape (:259) in crates/freshell-ws/src/screenshot.rs"
+  missing: "Authoritative-layout-source tracking (blocked on AUTO-01 — the server cannot know which window owns a pane). POST /api/screenshots registers unbound (expected_client_id=None) so a two-context race resolves to whichever replies first. select/rename/capture commands are broadcast to every client (no per-window addressing), so 'only B changes' is not server-enforceable. No two-context test exists."
+  effort: L
+  files:
+    - crates/freshell-server/src/screenshots.rs
+    - crates/freshell-ws/src/screenshot.rs
+    - crates/freshell-freshagent/src/pane_ops.rs
+
+- id: AUTO-15
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-ws/src/terminal.rs:509-519 — WS dispatch handles the tabs.sync.push / tabs.sync.query / tabs.sync.client.retire family (handle_tabs_push :4481, handle_tabs_query :5000, handle_tabs_retire :5023)"
+    - "crates/freshell-ws/src/terminal.rs:4565-4650 — validate_tabs_push: strict envelope + per-record schema validation, snapshotRevision must be a non-negative integer (:4586-4592), open/closed record statuses (:4601-4612), candidate pre-validated against the snapshot-reader schema (:4640)"
+    - "crates/freshell-ws/src/tabs_persist.rs:150-168,227-262 — per-client revision-ordered generations; ordering read from (snapshotRevision, capturedAt) so clock rollback cannot make a later revision look stale"
+    - "crates/freshell-ws/src/tabs_persist.rs:299-336 — device-identity conflict is a hard ERROR ('generations in one device dir disagree on deviceId; refusing to guess an identity')"
+    - "crates/freshell-ws/src/tabs_persist.rs:667-740 — retention: .tmp reap before cap accounting, oversize-skip, per-(device,client) generation cap, global per-device file cap, device-count cap, whole cycle serialized under PERSIST_LOCK"
+    - "crates/freshell-ws/src/tabs_persist.rs:434-559 — union_of_newest_per_client + per-record dedupe by (revision, updatedAt, clientInstanceId, generationId) for restore"
+    - "test files exist: crates/freshell-ws/src/tabs_persist_tests.rs, tabs_persist_validation_tests.rs (+ crates/freshell-server/src/tabs_snapshots_tests.rs) — individual test names not enumerated within the time budget"
+  missing: "Unverified/likely-absent clauses: TTLs/expiry (only file-count/size caps observed; no time-based expiry seen in tabs_persist.rs); tombstone non-resurrection semantics (closed-record status exists at validate time, but retire/tombstone handling in handle_tabs_retire :5023 and resurrection guards were not read); idempotent-retry assertion (replace_client_snapshot via spawn_blocking :4537-4546 exists but same-revision replay behavior unconfirmed); exact accept/reject revision contract tests unnamed; CFG-08 restart-persistence interplay unassessed (out of this section's owned scope)."
+  effort: M
+  files:
+    - crates/freshell-ws/src/tabs_persist.rs
+    - crates/freshell-ws/src/tabs.rs
+    - crates/freshell-ws/src/terminal.rs
+    - crates/freshell-server/src/tabs_snapshots.rs
diff --git a/docs/plans/parity-ledger/audit/p1-tauri-owned-server-and-daemon-lifecycle.yaml b/docs/plans/parity-ledger/audit/p1-tauri-owned-server-and-daemon-lifecycle.yaml
new file mode 100644
index 000000000..b2f3d4f3e
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p1-tauri-owned-server-and-daemon-lifecycle.yaml
@@ -0,0 +1,66 @@
+- id: TAURI-17
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/server.rs:200-208 — pure reap_step escalation decision: WaitMore within grace, Escalate only at elapsed >= grace, Done on exit (mirrors server-spawner.ts:168-188, 5s grace)"
+    - "crates/freshell-tauri/src/server.rs:214-239 — reap_child: SIGTERM (unix), waits the full grace interval polling, escalates to kill only after grace elapses; escalate targets only the owned Child handle (no pkill)"
+    - "crates/freshell-tauri/src/lib.rs:55-57 — REAP_GRACE = 5s, REAP_POLL = 50ms, 1:1 with the legacy 5000ms kill timeout (server-spawner.ts:178)"
+    - "crates/freshell-tauri/src/lib.rs:121-125 + 444-456 — RunEvent::Exit triggers reap_server; take()-based exactly-once reap of the owned server"
+    - "test: reap_step_transitions in crates/freshell-tauri/src/server.rs:383"
+    - "test: reap_terminates_a_long_sleeper_gracefully (SIGTERM'd child exits gracefully within grace) in crates/freshell-tauri/src/server.rs:435 (cfg(unix))"
+    - "test: reap_already_exited_short_process in crates/freshell-tauri/src/server.rs:411"
+    - "test: app_bound_spawn_health_reap_end_to_end in crates/freshell-tauri/tests/server_spawn_smoke.rs:39 — real freshell-server binary driven through spawn -> health -> reap"
+  missing: "Windows process-tree close: request_terminate under #[cfg(not(unix))] returns false so reap_child jumps straight to Child.kill() = TerminateProcess of the DIRECT pid only (server.rs:259-264); no job object or taskkill /T anywhere in crates (grep for taskkill/JobObject finds only a comment), so the full grace interval is never applied on Windows and the server's own children (PTY/terminal grandchildren) are never signaled. Flush state/logs on quit: RunEvent::Exit only reaps (lib.rs:121-125); window-state save is debounced 500ms (lib.rs:344-371) with no final flush at exit, and the server's stderr is merely inherited / stdout discarded (server.rs:122-124) with no log stream to flush (legacy pipes child output to ~/.freshell/logs/server.log, server-spawner.ts:142-149). No test covers the Forced escalation outcome (SIGTERM-ignoring child killed only after the full grace). The PW-TAURI-WIN Playwright validation itself is Windows-host-only."
+  effort: M
+  files:
+    - crates/freshell-tauri/src/server.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/tests/server_spawn_smoke.rs
+
+- id: TAURI-18
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/lib.rs:103 + 132-210 — only the app-bound boot path spawns/owns a server child; it is stored in server_slot and reaped on RunEvent::Exit (lib.rs:121-125, 444-456), idempotent via take()"
+    - "crates/freshell-tauri/src/lib.rs:218-255 — setup_remote spawns NO child; the health gate's child-dead fail-fast is `|| false` (lib.rs:240), so quit can never kill a remote server (safe by construction)"
+    - "crates/freshell-tauri/src/state_machine.rs:52-60 — ServerMode::{AppBound,Daemon,Remote} modeled; Daemon explicitly 'Deferred (daemon managers -> 3.14, CD-5)'"
+    - "crates/freshell-tauri/src/lib.rs:239 — remote mode logs 'no app-bound spawn'"
+    - "test: empty_slot_reports_exited_and_reap_is_noop in crates/freshell-tauri/src/lib.rs:613 — a quit with no owned child reaps nothing (the remote-mode quit case)"
+    - "test: reap_terminates_a_long_sleeper_gracefully in crates/freshell-tauri/src/server.rs:435 — the app-bound-owned child IS stopped on reap"
+  missing: "Daemon-owned servers: daemon mode is entirely unimplemented (deferred per state_machine.rs:56 and commands.rs:26-30 CD-5 note), so 'never stop daemon-owned servers' is vacuous rather than implemented/verified. No test asserts that remote/daemon fixture servers remain reachable/reconnectable across an app quit. The PW-TAURI-WIN per-mode quit validation is Windows-host-only."
+  effort: M
+  files:
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/state_machine.rs
+
+- id: TAURI-19
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No Rust daemon manager exists: grep for schtasks|Task Scheduler|DaemonManager|windows-service across crates/ returns zero matches; TASK_NAME/task-XML/install/uninstall/start/stop/status code is absent"
+    - "crates/freshell-tauri/src/state_machine.rs:56 — 'Server is an OS service. Deferred (daemon managers -> 3.14, CD-5)'"
+    - "crates/freshell-tauri/src/commands.rs:26-30 — CD-5 note: daemon install/uninstall are never wired even in the reference; the Rust wizard can persist serverMode:'daemon' but cannot install anything"
+    - "crates/freshell-tauri/Cargo.toml:26 — crate description states 'daemon deferred to 3.14'"
+    - "Legacy reference to port: electron/daemon/windows-service.ts:30-159 (schtasks /Create from freshell-task.xml template, /Run, stop via wmic PID match + taskkill /PID, /Query CSV status, /Delete uninstall, /RL LIMITED least-privilege)"
+  missing: "Everything: one Freshell-owned scheduled task creation with safely quoted installed paths, stable task identity, noninteractive startup, install/start/stop/status/uninstall, exact ownership checks, and the PW-TAURI-WIN-PACKAGED packaged-app validation (Windows elevated VM only)."
+  effort: L
+  files:
+    - crates/freshell-tauri/src/daemon/mod.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/commands.rs
+
+- id: TAURI-20
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No daemon status/failure handling exists in Rust because there is no daemon manager at all (see TAURI-19 greps: zero schtasks/DaemonManager matches in crates/)"
+    - "crates/freshell-tauri/src/lib.rs:138-143 — the app-bound boot hardcodes ServerMode::AppBound and never reads a configured serverMode:'daemon', so no daemon failure can even be detected, let alone handled"
+    - "crates/freshell-tauri/src/state_machine.rs:106-110 — decide_initial_phase maps a configured Daemon mode straight to Main without any status check or fallback"
+    - "Legacy reference to port: electron/startup.ts:345-352 — daemon mode calls daemonManager.status(), throws 'Daemon service is not installed. Please re-run setup...' when !installed, starts the service when !running"
+  missing: "All failure/stale-registration handling: access denied, missing binary, stopped service, stale config detection, repair flow, and app-bound fallback; plus the wizard/chooser surfacing with no premature success. Only TD residue exists: wizard validation accepts serverMode:'daemon' (commands.rs:81) per CD-5 parity with the reference dead-end. The PW-TAURI-WIN fault-fixture validation is Windows-host-only. Blocked by TAURI-19 (no daemon manager to query)."
+  effort: M
+  files:
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/state_machine.rs
+    - crates/freshell-tauri/src/commands.rs
+    - crates/freshell-tauri/src/daemon/mod.rs
diff --git a/docs/plans/parity-ledger/audit/p1-tauri-setup-wizard-and-launch-chooser.yaml b/docs/plans/parity-ledger/audit/p1-tauri-setup-wizard-and-launch-chooser.yaml
new file mode 100644
index 000000000..a7ea9fdd6
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p1-tauri-setup-wizard-and-launch-chooser.yaml
@@ -0,0 +1,142 @@
+- id: TAURI-08
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/commands.rs:59-84 — validate_port/validate_url/validate_wizard_config ported from wizard-logic.ts (remote→URL, daemon/app-bound→port; unknown mode rejected)"
+    - "crates/freshell-tauri/src/commands.rs:89-104 — build_wizard_patch writes serverMode/port/globalHotkey/setupCompleted:true, omits empty remoteUrl/remoteToken (entry.ts:513-528 analog)"
+    - "crates/freshell-tauri/src/commands.rs:344-360 — #[tauri::command] complete_setup: wizard-window-gated, validates, then persists patch to desktop.json (written only at confirmation seam)"
+    - "crates/freshell-tauri/src/windows.rs:40-50 — wizard_spec 640x500 resizable:false centered (wizard-window.ts:14-40 analog); crates/freshell-tauri/capabilities/wizard.json grants wizard window core invoke"
+    - "test: validate_port_bounds, validate_url_accepts_and_rejects, wizard_config_validation_by_mode, wizard_patch_sets_setup_completed_and_omits_empty_remote, wizard_patch_includes_remote_when_present in crates/freshell-tauri/src/commands.rs"
+  missing: "No wizard UI/screens at all (crates/freshell-tauri/frontend/index.html is a 'Starting Freshell…' placeholder; no wizard React bundle). Wizard window is never constructed: lib.rs builds only the 'main' window (lib.rs:272) and setup_app_bound hardcodes setup_completed:true (lib.rs:138-148); state_machine::runnable_phase degrades ShellPhase::Wizard→Main (state_machine.rs:116-122). No wizard→main transition wiring. No token or hotkey validation in validate_wizard_config (only port/URL; commands.rs:78-84). Daemon-mode setup has no install path (CD-5 dead-end noted at commands.rs:26-30, matches reference). PW-TAURI-WIN lane (roles/keyboard, desktop.json timing) not runnable here."
+  effort: L
+  files:
+    - crates/freshell-tauri/src/commands.rs
+    - crates/freshell-tauri/src/windows.rs
+    - crates/freshell-tauri/src/state_machine.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/frontend/index.html
+
+- id: TAURI-09
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/windows.rs:98-122 — PrivilegedCommand enum, owner_window mapping, is_allowed_window gate (Rust analog of entry.ts:538-541 isAllowedSender / electron webContents.id check, replacing it with window labels)"
+    - "crates/freshell-tauri/src/commands.rs:349-351 (complete_setup), 367-369 (get_launch_options), 397-399 (choose_launch_option) — each privileged command rejects non-owner windows before any config change; rejections return errors / LaunchChoiceResult::err"
+    - "crates/freshell-tauri/capabilities/wizard.json and chooser.json — per-window capability files scoping invoke to 'wizard'/'chooser' windows; main window (capabilities/default.json) has no grant for setup/choice commands"
+    - "test: per_window_command_gate in crates/freshell-tauri/src/windows.rs:175-211 — asserts each privileged command accepts only its owning window and rejects main/wizard/chooser mismatches"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-tauri/src/windows.rs
+    - crates/freshell-tauri/src/commands.rs
+    - crates/freshell-tauri/capabilities/wizard.json
+    - crates/freshell-tauri/capabilities/chooser.json
+
+- id: TAURI-10
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/commands.rs:133-152 — build_launch_options (launch-options.ts:16-27 analog) returns candidates/reason/alwaysAskOnLaunch/port/remoteUrl"
+    - "crates/freshell-tauri/src/commands.rs:246-327 — plan_launch_choice: connect/remote and start-local accept/reject matrix, token-required gate, remember→persist serverMode/remoteUrl/remoteToken or app-bound/port, always persists alwaysAskOnLaunch; forced-launch plan"
+    - "crates/freshell-tauri/src/commands.rs:365-428 — get_launch_options reads desktop.json; choose_launch_option validates, runs authoritative port bind-check (is_port_available, commands.rs:331-333, 411-417), persists patch"
+    - "crates/freshell-tauri/src/windows.rs:54-64 — chooser_spec 760x720 shown-after-load (entry.ts:588-608 analog); capabilities/chooser.json"
+    - "test: launch_options_defaults_when_no_pending, launch_options_uses_pending_candidates, connect_requires_token_when_auth_required, connect_remember_persists_remote_defaults, connect_no_remember_only_persists_always_ask, start_local_validates_port_and_requests_bind_check, is_port_available_true_for_free_port / _false_for_occupied_port in crates/freshell-tauri/src/commands.rs"
+  missing: "Candidate list is always empty — live discovery explicitly deferred (commands.rs:20-24, 363-364; get_launch_options passes None). 'Transitioning cleanly' not wired: restartMain/forced-launch handoff deferred (commands.rs:390-391), state_machine LaunchChosen event exists+tested but nothing fires it at runtime. Chooser window never constructed (lib.rs:272 builds only 'main'; runnable_phase, state_machine.rs:116-122, degrades Chooser→Main). No chooser UI/frontend. Auth_check (live token validation) deferred (commands.rs:418-419). No no-duplicate-process guarantee exercised."
+  effort: L
+  files:
+    - crates/freshell-tauri/src/commands.rs
+    - crates/freshell-tauri/src/windows.rs
+    - crates/freshell-tauri/src/state_machine.rs
+    - crates/freshell-tauri/src/lib.rs
+
+- id: TAURI-11
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/commands.rs:20-24 — module doc explicitly defers live server discovery feeding the chooser's candidate list"
+    - "crates/freshell-tauri/src/commands.rs:363-364,385 — get_launch_options returns empty candidate list ('candidate discovery is deferred')"
+    - "Only normalize_server_url (commands.rs:203-205, provisioning.rs:140-142) is ported from launch-discovery.ts; grep across crates/ found no buildLocalProbeUrls/discoverLocalServers/isLoopbackUrl/HealthPayload analog (reference: electron/launch-discovery.ts:31-96)"
+    - "Reference behavior absent: probe of configured port + 3001-3010 range + knownServers (launch-discovery.ts:31-52), freshness-based ordering/dedup and offline remembered entries (launch-policy.ts) — no Rust module implements any of it; desktop.json 'knownServers' is never read anywhere in crates/freshell-tauri/src"
+    - "No Rust tests for discovery/dedup/ordering exist"
+  missing: "All clauses: probing configured ports and remembered knownServers entries, deduplication, offline-candidate retention, predictable ordering, labels/status population. No Rust implementation found."
+  effort: M
+  files:
+    - crates/freshell-tauri/src/commands.rs
+    - crates/freshell-tauri/src/health.rs
+
+- id: TAURI-12
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "Reference precedence resolver is electron/token-resolver.ts:52-74 (resolveCandidateToken): saved remoteUrl match→remoteToken, else loopback→.env AUTH_TOKEN (extractTokenFromEnv), else config.json authToken/token (extractTokenFromConfigJson); user entry supplies the rest via the chooser"
+    - "Grep across crates/ for resolveCandidateToken/extractTokenFromEnv/extractTokenFromConfigJson: no analog found (only unrelated AUTH_TOKEN doc in crates/freshell-api/src/lib.rs:34); crates/freshell-tauri never reads .env or legacy config.json"
+    - "crates/freshell-tauri/src/commands.rs:262-272 — plan_connect uses ONLY the user-entered choice.token; no candidate-token resolution or precedence anywhere"
+    - "Secrecy aid already present: redact_token in crates/freshell-tauri/src/lib.rs:459-464 strips ?token= from logs"
+    - "test: redact_token_hides_the_credential in crates/freshell-tauri/src/lib.rs:600-610 (secrecy of logged load URL only — not a precedence test)"
+  missing: "Entire precedence chain (saved remoteToken for matching URL → .env AUTH_TOKEN → legacy config.json authToken/token) and its wiring into chooser candidate selection; user entry exists only as a pass-through of choice.token. Log/UI secrecy verified only for the launch-load URL log line."
+  effort: M
+  files:
+    - crates/freshell-tauri/src/commands.rs
+    - crates/freshell-tauri/src/config.rs
+
+- id: TAURI-13
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/lib.rs:240-246 — setup_remote only health-gates via wait_for_health (public, unauthenticated /api/health probe, health.rs:97-125) before creating the main window at the ?token= URL; a 200 health response with a wrong token would still load main"
+    - "crates/freshell-tauri/src/commands.rs:418-419 — choose_launch_option: 'live token validation (auth_check) against {url}/api/settings is deferred (needs an HTTP client + a running target) — flow-gated'; plan_launch_choice surfaces auth_check (commands.rs:234-239, 293-297) but the wrapper never executes it"
+    - "Reference gate: electron/launch-choice-handler.ts:62-72 calls validateServerAuth before restartMain and returns 'The server rejected that token.' — no Rust equivalent (no HTTP client dependency in crates/freshell-tauri/Cargo.toml)"
+    - "No Rust test exists asserting rejection-on-bad-token after a healthy server"
+  missing: "Settings-endpoint authentication check before opening main, both in the chooser command path and in the remote/provisioned boot path; auth error surface that keeps the chooser open; retry after token correction."
+  effort: M
+  files:
+    - crates/freshell-tauri/src/commands.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/health.rs
+
+- id: TAURI-14
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/health.rs:101-125 — http_probe_inner opens a plain TcpStream and sends cleartext 'GET /api/health HTTP/1.1'; against any TLS port this can never succeed"
+    - "crates/freshell-tauri/Cargo.toml — no reqwest/rustls/native-tls/openssl dependency; no TLS client exists anywhere in the crate"
+    - "https is accepted only at URL-validation level (validate_remote_launch_url, commands.rs:208-213; validate_url, commands.rs:68-73) and the webview would load an https URL, but the health/auth gates preceding it are plaintext-only; no trusted/untrusted certificate handling and no explicit never-downgrade guard in the Rust network path"
+    - "test: http_probe_reports_ready_against_a_200_stub and http_probe_reports_notready_on_closed_port in health.rs exercise plain HTTP only — no TLS tests"
+  missing: "All clauses: TLS for health + authentication probes, trusted/untrusted certificate handling with a clear chooser error, and a guaranteed no-plain-HTTP-fallback path. The current probe speaks only plain HTTP."
+  effort: L
+  files:
+    - crates/freshell-tauri/src/health.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/commands.rs
+    - crates/freshell-tauri/Cargo.toml
+
+- id: TAURI-15
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/lib.rs:216-217,240-243 — setup_remote on health-gate failure logs and returns Err; comment: 'the reference would fall back to the launch chooser — a deferred 3.14 window — so fail-fast is the honest analog'"
+    - "crates/freshell-tauri/src/lib.rs:110-119 — a failed build/setup path reaps the server and calls std::process::exit(1): the desktop does NOT stay alive"
+    - "Reference chooser fallback reasons: electron/launch-policy.ts ('saved-remote-unreachable', 'saved-remote-token-invalid', retry/change/start-local via chooser) — no Rust port of chooseLaunchAction; grep found no launch-policy analog"
+    - "No chooser window exists to fall back to (lib.rs:272 builds only 'main'; state_machine.rs:116-122 degrades Chooser→Main); no Rust test for saved-target-unavailable retry"
+  missing: "All clauses: chooser fallback with the failed candidate shown as offline, retry/change/start-local choices, keeping the desktop alive instead of aborting, and the Retry→single authenticated main window flow."
+  effort: M
+  files:
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/state_machine.rs
+    - crates/freshell-tauri/src/windows.rs
+
+- id: TAURI-16
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/provisioning.rs:55-70 — parse_provisioning 1:1 with desktop-provisioning.ts:23-34 (KEY=value, verbatim value after first '=', unknown/malformed lines ignored)"
+    - "crates/freshell-tauri/src/provisioning.rs:92-122 — apply_provisioning_file: one-shot (provision file always deleted, incl. unreadable case), persists {serverMode:'remote', remoteUrl, remoteToken, setupCompleted:true} via atomic patch, persist errors swallowed so a malformed file cannot brick startup (desktop-provisioning.ts:48-81 analog)"
+    - "crates/freshell-tauri/src/config.rs:69-94 — write_config_atomic (tmp+rename) + patch_config_at (merge-preserving); wired at boot in lib.rs:96-104 (env pair FRESHELL_REMOTE_URL/FRESHELL_TOKEN first, then provision file)"
+    - "test: apply_patches_config_and_deletes_file, apply_incomplete_file_deletes_without_patching (invalid input cannot partially overwrite config), apply_preserves_other_desktop_json_keys, apply_missing_file_returns_false, env_pair_resolves_only_when_both_nonempty, desktop_config_remote_roundtrip in crates/freshell-tauri/src/provisioning.rs:179-327"
+    - "Second-boot reuse helper exists: remote_from_desktop_config (provisioning.rs:126-136) extracts a saved remote from desktop.json"
+  missing: "'Use saved configuration on later cold starts' is not wired: resolve_provisioned_remote (provisioning.rs:162-177) returns None when neither the env pair nor a provision file exists, so setup() proceeds app-bound (lib.rs:100-104) and never consults desktop.json's saved serverMode/remoteUrl/remoteToken on a later cold start — remote_from_desktop_config is only read within the same boot that consumed a provision file. 'Preserve diagnostics for invalid/interrupted input': errors are silently swallowed (let _ = …, provisioning.rs:99,109,120) matching the reference's swallow, so nothing is surfaced/recorded. PW-TAURI-WIN packaged lane (valid/malformed/wrong-token/interrupted matrix) not runnable on this host."
+  effort: S
+  files:
+    - crates/freshell-tauri/src/provisioning.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/config.rs
diff --git a/docs/plans/parity-ledger/audit/p1-tauri-window-tray-shortcut-menu-and-recovery.yaml b/docs/plans/parity-ledger/audit/p1-tauri-window-tray-shortcut-menu-and-recovery.yaml
new file mode 100644
index 000000000..b2add4e31
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p1-tauri-window-tray-shortcut-menu-and-recovery.yaml
@@ -0,0 +1,149 @@
+- id: TAURI-21
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/lib.rs:354-371 — the only on_window_event handler matches Moved|Resized for state save; there is NO CloseRequested interception / close-to-tray anywhere in the crate"
+    - "crates/freshell-tauri/src/window_state.rs:273,288 — 'minimizeToTray' appears ONLY as fixture data in a config-preservation test, never as implemented behavior"
+    - "rg 'minimizeToTray|minimize_to_tray|CloseRequested' across crates/ finds no implementation; reference behavior is electron/main.ts:40-48 (close → preventDefault + hide when minimizeToTray)"
+    - "crates/freshell-tauri/src/config.rs — no field/handler for minimizeToTray; config is raw serde_json with no typed DesktopConfig"
+  missing: "all clauses — no minimizeToTray read from desktop.json, no native-close hides-when-enabled, no exits-when-disabled (isQuitting flag) semantics, no restoring-same-page/state guarantee wiring, no tests"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/config.rs
+
+- id: TAURI-22
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/tray.rs:83-121 — build_menu_model produces Show/Hide, sep, 'Server: Running|Stopped' + 'Mode: {mode}' (disabled), sep, Settings, Check for Updates, Quit"
+    - "crates/freshell-tauri/src/tray.rs:125-133,199-208 — action_for_menu_id routing + dispatch (Show/Settings→show_and_focus_main, CheckUpdates→emit event, Quit→app.exit(0)); build_tray seam at :214-241 incl. left-click show/focus"
+    - "test: menu_model_has_the_reference_shape, status_line_reflects_running_state, mode_line_shows_the_mode, status_lines_are_disabled, menu_ids_route_to_actions, disabled_and_unknown_ids_have_no_action in crates/freshell-tauri/src/tray.rs"
+    - "crates/freshell-tauri/src/lib.rs:552-554,573-593 — tray Check-for-Updates listener → report_updater_state reading real tauri.conf.json plugins.updater config; updater decision logic tested (should_update_offers_newer_only, check_manifest_offers_for_matching_platform, updater_state_reflects_config_presence in updater.rs)"
+    - "crates/freshell-tauri/src/tray.rs:245-254 — refresh_tray_status defined but grep finds NO caller outside tray.rs; lib.rs:541-544 builds the tray ONCE with hardcoded running:true → status/mode frozen at boot, no crash/restart live update"
+    - "crates/freshell-tauri/src/tray.rs:43-53,27-31 — no Hide action (Show only, flagged as faithful replication of the reference's show-only quirk); Settings only shows+focuses (matches reference electron/entry.ts:429-435 which also only shows+focuses)"
+  missing: "live status refresh — refresh_tray_status never driven by any server-liveness watcher so 'reflect current state' after child crash/restart is absent; no Hide handler; live updater check/apply is signing-gated (placeholder pubkey in tauri.conf.json:18 → DisarmedNoPubkey)"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/tray.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/updater.rs
+
+- id: TAURI-23
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/hotkey.rs:172-194 — translate_accelerator (Electron→tauri-plugin-global-shortcut grammar) heavily tested: translates_the_reference_default, command_or_control_variants_canonicalize, maps_modifiers_to_tauri_spelling, maps_letters_digits_and_function_keys, maps_named_and_arrow_keys, maps_punctuation_keys, rejects_modifier_only_and_empty, reference_table_all_translate"
+    - "crates/freshell-tauri/src/hotkey.rs:219-279 — HotkeyManager register/unregister/update state machine, tested with FakeBackend: manager_registers_translated_accelerator, manager_update_unregisters_old_then_registers_new, manager_unregister_clears_current_and_is_idempotent"
+    - "crates/freshell-tauri/src/lib.rs:476-500,506-528,536 — plugin registered with toggle handler; register_global_hotkey called with hotkey::DEFAULT_HOTKEY (hardcoded, never reads desktop.json globalHotkey)"
+    - "crates/freshell-tauri/src/lib.rs:86-92 — invoke_handler has no set-global-hotkey analog; HotkeyManager has NO instantiation outside its own tests (grep: no callers in lib.rs)"
+    - "crates/freshell-tauri/src/hotkey.rs:258-273 + test manager_update_failure_clears_current — on new-registration failure the manager CLEARS current (mirrors hotkey.ts), i.e. NO retain-old-working-key rollback"
+  missing: "honor the SAVED shortcut (desktop.json globalHotkey never read at boot — always default); no change-shortcut IPC command so safe change is unreachable from UI; no retain-old-working-key-on-conflict rollback; error only eprintln'd (lib.rs:524-526), no user-visible error"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/hotkey.rs
+    - crates/freshell-tauri/src/commands.rs
+    - crates/freshell-tauri/src/config.rs
+
+- id: TAURI-24
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "rg 'startOnLogin|start_on_login|Task Scheduler|schtasks|Startup' across crates/freshell-tauri finds no implementation (only unrelated 'startup.ts' doc comments and a state-machine 'Startup' phase comment)"
+    - "crates/freshell-tauri/Cargo.toml — no launch-at-login/auto-start dependency; tauri.conf.json:1-29 has no autostart plugin"
+    - "reference electron/types.ts:17 + desktop-config.ts:23 only declare the schema default; the legacy control was unimplemented per the item text itself"
+    - "validation (per-user Windows Task Scheduler entry, sign-in simulation) can only run on a Windows host — none exists in repo either"
+  missing: "all clauses — no per-user Windows Task Scheduler entry creation/removal, no safely quoted task, no enable/disable wiring, no config read/write, no tests; new product work not started"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/config.rs
+    - crates/freshell-tauri/Cargo.toml
+
+- id: TAURI-25
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/window_state.rs:74-100 — load_from_config (per-field defaults, window-state.ts parity); :108-117 save_into_config preserves every other desktop.json key; :167-217 clamp_to_monitors (off-screen recenter, edge shift, shrink-to-fit, empty-monitor no-op)"
+    - "crates/freshell-tauri/src/lib.rs:270-306 — restore saved x/y/w/h clamped on-screen + maximize if saved maximized; :350-371 debounced 500 ms save on Moved/Resized; :376-414 save via inner_position/inner_size with scale_factor logical conversion (DPI) and atomic write through config.rs:69-82"
+    - "test: load_missing_window_state_returns_defaults, load_full_window_state_round_trips, load_partial_window_state_fills_defaults, save_preserves_other_config_keys, save_then_load_is_stable, on_screen_window_is_unchanged, off_screen_window_recenters_on_primary, partially_off_right_edge_shifts_into_view, window_larger_than_monitor_is_shrunk, window_on_secondary_monitor_stays_there, empty_monitor_list_returns_bounds_unchanged (window_state.rs); patch_round_trips_atomically (config.rs:135)"
+    - "crates/freshell-tauri/src/config.rs:54-64,86-94 — merge_patch/patch_config_at give single-writer merge-preserving semantics; concurrency vs other desktop keys = atomic tmp+rename write, tested"
+    - "note: live multi-monitor/DPI-tolerance Playwright validation (PW-TAURI-WIN) is Windows host-limited and not present; code+unit coverage of every listed clause exists"
+  missing: ""
+  effort: S
+  files:
+    - crates/freshell-tauri/src/window_state.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/config.rs
+
+- id: TAURI-26
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-tauri/src/lib.rs:69-80 — tauri_plugin_single_instance::init registered FIRST with focus callback (unminimize+show+set_focus on the main window); comment :69-70 documents second-launch focus-first semantics (main.ts:24-28,63-70 parity)"
+    - "crates/freshell-tauri/src/lib.rs:132-176 — server spawn happens only in setup(), which the plugin prevents from running in the second process → never spawns another server (by plugin-contract, not verified by test on this host)"
+    - "crates/freshell-tauri/tests/server_spawn_smoke.rs is the only integration test and covers spawn/health/reap only — grep shows NO test exercising single-instance or the focus callback"
+  missing: "no test coverage at all for the single-instance behavior or focus callback (DONE requires at least one test); second-process prompt-exit assertion unproven; live focus/hide-relaunch validation is Windows host-limited"
+  effort: S
+  files:
+    - crates/freshell-tauri/src/lib.rs
+
+- id: TAURI-27
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "rg -i 'submenu|about|zoom|fullscreen|devtool|set_menu|app.menu' across crates/freshell-tauri/src finds only the TRAY menu (tray.rs:142-187) — no application menu built; lib.rs never calls set_menu"
+    - "crates/freshell-tauri/tauri.conf.json:1-29 — no menu configuration"
+    - "crates/freshell-tauri/src/tray.rs:142-187 — the only Menu construction is the tray context menu (Show/Hide, Settings, Check for Updates, Quit), not Edit/View/Window/Help"
+    - "no Rust counterpart of any Edit/View/Window/Help/Preferences/reload/zoom/fullscreen/DevTools gating exists; PW-TAURI-WIN harnesses absent from scripts/ and test/"
+  missing: "all clauses — Edit, View, Window, Help/About, Preferences, update, reload, zoom (in/out/reset), fullscreen, development-tools gating: no desktop application menu module, no handlers, no tests"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/Cargo.toml
+
+- id: TAURI-28
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/external_url.rs:53-69 — canonicalize_external_url: control-char reject → absolute-parse → http/https-only → embedded-credentials reject, order-matching external-url.ts:6-20; :82-86 open via single tauri_plugin_opener::open_url call (open exactly once, system browser)"
+    - "test: accepts_canonical_http_and_https, rejects_non_http_schemes (file:/javascript:/data:/ftp:/mailto:), rejects_embedded_credentials, rejects_control_characters, rejects_relative_or_garbage, control_char_predicate_boundaries in crates/freshell-tauri/src/external_url.rs"
+    - "crates/freshell-tauri/src/shim.rs:30-50 — SPA window.freshellDesktop.openExternal routes to the open_external_url command; shim tests shim_exposes_exactly_the_two_touchpoints, shim_openexternal_returns_a_promise"
+    - "crates/freshell-tauri/src/windows.rs:107-122,174-211 — is_allowed_window(OpenExternalUrl→main) exists and is tested (per_window_command_gate) but is NOT called by the command: external_url.rs:83-86 takes only `url`, no window label checks"
+    - "crates/freshell-tauri/capabilities/default.json:4-6 — only core:default granted; the file's own description states app-defined commands are NOT permission-gated, so the untrusted-frame/sender rejection has no enforcement seam"
+  missing: "untrusted-frame rejection is defined-but-not-enforced — open_external_url ignores the caller window/frame (is_allowed_window(OpenExternalUrl) never called; hostile-iframe call not blocked); exact-once + Windows Shell delivery validation is packaged-host-limited but the single-open code path is trivially satisfied"
+  effort: S
+  files:
+    - crates/freshell-tauri/src/external_url.rs
+    - crates/freshell-tauri/src/windows.rs
+
+- id: TAURI-29
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/renderer_recovery.rs:39-49 — byte-faithful constants (60 s circuit window, 3 attempts/window, 250/1000/3000 ms backoff, 15 s unresponsive threshold, -3 aborted filter); :80-95 recovery_mode_for; :123-208 RecoveryCircuit state machine"
+    - "test: did_fail_load_filters_subframe_and_aborted, crash_and_unresponsive_reload, first_recovery_is_immediate, backoff_grows_with_consecutive_failures, success_resets_backoff, after_a_failure_next_plan_is_delayed, circuit_opens_after_three_attempts_in_window, circuit_reopens_after_window_elapses, in_flight_and_scheduled_skip, watchdog_triggers_after_sustained_outage etc. in crates/freshell-tauri/src/renderer_recovery.rs"
+    - "crates/freshell-tauri/src/lib.rs:32 — 'pub mod renderer_recovery;' is the ONLY reference in lib.rs: RecoveryCircuit and ReachabilityWatchdog are never instantiated or driven by the app (grep confirms zero callers outside the module)"
+    - "crates/freshell-tauri/src/renderer_recovery.rs:5-35 — module doc itself documents the platform gap: WRY exposes no render-process-gone/did-fail-load/unresponsive signals; no WebView2 ProcessFailed hook exists anywhere in the crate"
+  missing: "real wiring — no native WebView2 renderer-failure/unresponsive event connection (platform gap, Windows host-limited by nature), no reachable driver for the watchdog/circuit, no recovery actuation (reload/load-url on the live window), no window-replacement/state-transfer, no explanation-or-manual-retry surface, no authenticated-tab restore assertion; only the pure decision core exists and is tested"
+  effort: L
+  files:
+    - crates/freshell-tauri/src/renderer_recovery.rs
+    - crates/freshell-tauri/src/lib.rs
+
+- id: TAURI-30
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-tauri/src/health.rs:20-98 — bounded boot-time health gate (100 ms→5 s backoff, 30 s cap; test backoff_doubles_then_caps_at_5s); lib.rs:179-186 (app-bound) and :240-243 (remote) use it at boot"
+    - "crates/freshell-tauri/src/renderer_recovery.rs:216-265 — ReachabilityWatchdog implemented + tested (watchdog_ignores_outage_before_first_reachable, watchdog_triggers_after_sustained_outage, watchdog_recovery_clears_on_reachable_again) but NEVER driven at runtime (no poller in lib.rs)"
+    - "crates/freshell-tauri/src/lib.rs:216-217 — comment states the reference's chooser fallback is a deferred 3.14 window and the port fail-fasts instead: an unreachable/again-unreachable remote aborts launch, no chooser routing"
+    - "crates/freshell-tauri/src/state_machine.rs:13-16,27 — Chooser phase exists in the machine but is deferred/unconstructable (runnable_phase maps it to Main)"
+  missing: "runtime disconnected-state surface after main loads; reconnect app-bound at same URL/token (no restart-respawn of the child or reload-on-recovery wiring); bounded retries with chooser/error actions for a permanently unavailable remote (currently aborts at boot only); driving the ReachabilityWatchdog at runtime; chooser window for fallback"
+  effort: L
+  files:
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/health.rs
+    - crates/freshell-tauri/src/renderer_recovery.rs
+    - crates/freshell-tauri/src/state_machine.rs
diff --git a/docs/plans/parity-ledger/audit/p2-current-main-catch-up-not-otherwise-covered-abo.yaml b/docs/plans/parity-ledger/audit/p2-current-main-catch-up-not-otherwise-covered-abo.yaml
new file mode 100644
index 000000000..bf0d083e3
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p2-current-main-catch-up-not-otherwise-covered-abo.yaml
@@ -0,0 +1,102 @@
+- id: SYNC-00
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "git state: branch parity-campaign-20260805 is current main + 1 docs commit (`git rev-list --count main..HEAD` = 1, `HEAD..main` = 0; HEAD 47f5c68f5 on top of main 4eecc456c) — so 'reconciled with current main' is true TODAY by construction, not via a recorded reconciliation process"
+    - "port/contract/ws-message-inventory.json + port/contract/generate-ws-contract.ts — a generated WS-message inventory exists, regenerated and byte-pinned"
+    - "test: 'committed message inventory deep-equals a fresh regeneration' in test/unit/port/ws-contract-freeze.test.ts:61 (plus counts/consistency guards at :79-84)"
+    - "Canonical checklist docs/plans/2026-07-14-rust-tauri-parity-completion-checklist.md:783 still lists SYNC-00 unchecked with no completion notes"
+  missing: "The 'generated inventory test that asserts every client-used route/message/provider on current main has a Rust capability or an unchecked checklist ID' (no-unowned-gaps gate) does not exist — the ws-contract-freeze inventory is WS-messages-only and maps to no Rust-capability/checklist-ID ownership; no REST-route or provider drift-inventory/gate was found anywhere under test/ or scripts/ (searched 'unowned', 'checklist ID', 'drift-inventory', route-inventory patterns). The 'again before release' reconciliation is also inherently not yet done."
+  effort: M
+  files:
+    - port/contract/generate-ws-contract.ts
+    - test/unit/port/ws-contract-freeze.test.ts
+    - scripts/
+
+- id: SYNC-01
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "shared/settings.ts:43-44 — UI_SCALE_MIN = 0.75, UI_SCALE_MAX = 4 (400%); :47 UI_SCALE_PERCENT_OPTIONS"
+    - "test: 'clamps oversized uiScale to the 400% maximum when extracting a legacy seed' and 'pins the UI scale percent options: 5% steps to 200, 25% steps to 400' in test/unit/shared/settings.test.ts:448-465"
+    - "src/components/settings/AppearanceSettings.tsx — consumes UI_SCALE_PERCENT_OPTIONS (settings UI control)"
+    - "uiScale is a LOCAL (client-side) setting — shared/settings.ts:194 `uiScale: number` inside LocalSettings; zero hits for uiScale/ui_scale anywhere under crates/ (grep-verified), and the Rust ServerSettings model (crates/freshell-server/src/settings.rs:32-85) correctly omits it — no Rust server implementation exists or is needed; the Rust server serves the same shared client"
+  missing: "The item-named Playwright validation was never authored/run against the Rust server: no e2e spec exercises boundary/invalid/400% uiScale values or reload-persistence/clamping (no uiScale hits in test/e2e-browser; settings.spec.ts is not in MATRIX_SPECS so it runs legacy-only), and the PW-TAURI-WIN leg is unrunnable on this Linux host. Keyboard accessibility at 400% is unverified by any test."
+  effort: S
+  files:
+    - shared/settings.ts
+    - src/components/settings/AppearanceSettings.tsx
+    - test/e2e-browser/specs/settings.spec.ts
+
+- id: SYNC-02
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "shared/settings.ts:52-53 — TERMINAL_FONT_SIZE_MIN = 12, TERMINAL_FONT_SIZE_MAX = 64; :57 TERMINAL_FONT_SIZE_PX_OPTIONS"
+    - "test: 'pins the terminal font size px options: 1px steps to 32, 2px to 48, 4px to 64' (:475), 'keeps the terminal font size px options strictly ascending integers spanning 12..64' (:483), 'clamps legacy-seed font sizes at the 64px boundary with round-after-clamp' (:508) in test/unit/shared/settings.test.ts"
+    - "test: 'terminal font size input changes setting' (direct spinbutton input) in test/e2e-browser/specs/settings.spec.ts:35-58 — but settings.spec.ts is NOT in MATRIX_SPECS (playwright.config.ts:13-82), so it runs against the legacy server only"
+    - "Client-local setting (LocalSettings.terminal.fontSize, shared/settings.ts:195-196); zero font-size handling under crates/ outside unrelated contexts — no Rust server work exists or is needed"
+  missing: "No rust-chromium e2e coverage of boundary/invalid/64px entry, live xterm cell/canvas metric change, or reload persistence against the Rust server (no dedicated spec; settings.spec.ts excluded from MATRIX_SPECS); PW-TAURI-WIN leg unrunnable on this Linux host."
+  effort: S
+  files:
+    - shared/settings.ts
+    - src/components/settings/AppearanceSettings.tsx
+    - test/e2e-browser/specs/settings.spec.ts
+
+- id: SYNC-03
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "src/components/panes/EditorPane.tsx:174 — `const editorFontSize = useAppSelector((s) => s.settings.settings.terminal?.fontSize) ?? 16`; applied to the editor at :1034 (`fontSize: editorFontSize`) — editor font follows the terminal font-size setting via Redux, so existing panes update live without remount and new panes get the current value"
+    - "test: describe 'EditorPane font size follows settings.terminal.fontSize' in test/unit/client/components/panes/EditorPane.fontSize.test.tsx:73 — 'defaults to the terminal font size setting (16, not the old hard-coded 14)' (:85), 'uses the current setting when the editor mounts after the setting changed' (:96), 'updates live without remounting when the setting changes (slider drag)' (:104), 'receives the clamped value for out-of-range settings (999 -> 64)' (:120)"
+    - "Pure client-side wiring; no server involvement in either implementation (nothing under crates/ touches editor fonts)"
+  missing: "The 'after restart' leg and the side-by-side terminal-vs-editor computed-text-size comparison are not covered by any Playwright spec (editor-pane.spec.ts has no font-follows assertion), so neither the PW-RUST nor PW-TAURI-WIN validation leg exists; after-restart behavior rests on generic localStorage persistence only (settings-persistence-split.spec.ts covers theme, not font size)."
+  effort: S
+  files:
+    - src/components/panes/EditorPane.tsx
+    - test/unit/client/components/panes/EditorPane.fontSize.test.tsx
+    - test/e2e-browser/specs/editor-pane.spec.ts
+
+- id: SYNC-04
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "grep-verified: zero hits for `fontScale`/`font_scale` in shared/, server/, src/, and all of crates/ — the deprecated key is fully removed from production code on both implementations (remaining hits are docs and regression tests only); spec record: docs/plans/2026-07-08-remove-freshagent-fontscale-spec.md"
+    - "test: e2e 'freshAgent.fontScale setting is ignored when present in a dispatched patch' in test/e2e-browser/specs/fresh-agent.spec.ts:880-884 (dispatches `{ freshAgent: { fontScale: 2 } }` and asserts no effect)"
+    - "test: test/unit/client/browser-preferences.fresh-agent-settings.test.ts — persistence layer never round-trips fontScale"
+    - "No Rust surface: grep of crates/ for fontScale/font_scale returns zero — nothing to port, nothing stale to override on the Rust server"
+  missing: "The item-named PW-RUST validation leg (seed deprecated property, launch, settings save, verify removed) is not wired against the Rust server — fresh-agent.spec.ts is not in MATRIX_SPECS (playwright.config.ts:13-82), so the seed-and-save regression runs legacy-only. Implementation itself is complete on both stacks."
+  effort: S
+  files:
+    - shared/settings.ts
+    - src/store/browserPreferencesPersistence.ts
+    - test/e2e-browser/specs/fresh-agent.spec.ts
+
+- id: SYNC-05
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "test/e2e-browser/specs/restore-sync05.spec.ts exists and is registered in MATRIX_SPECS (test/e2e-browser/playwright.config.ts:34, with the SYNC-05 comment at :31-33) — so the quiet-expected-restart suite runs on BOTH legacy-chromium and rust-chromium against the real Rust server restart path"
+    - "Section-file recorded greens (2026-07-18/19): general-leg test 'a live terminal pane reconnects quietly after a deliberate server restart, with no user-facing error noise' and FreshCodex-leg test 'a FreshCodex pane reconnects quietly after a deliberate server restart, targeting the same durable session with no user-facing error noise', both green 2x on both projects (not re-executed in this audit)"
+    - "Dependencies TERM-22/SAFE-11/TAURI-30 are owned by other checklist sections (not audited here)"
+  missing: "The equivalent-crash / diagnostics-retained leg (unexpected crash must retain actionable diagnostics) has no spec, and the PW-TAURI-WIN half of the validation is absent (native Windows WebView2, unrunnable on this host) — both explicitly deferred per the spec file's own doc comment."
+  effort: M
+  files:
+    - test/e2e-browser/specs/restore-sync05.spec.ts
+    - crates/freshell-server/src/boot.rs
+
+- id: SYNC-06
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/resolve.rs:1 — '`POST /api/sessions/resolve` — SYNC-06 port of the resolve route'; route mounted at resolve.rs:256 (`.route(\"/api/sessions/resolve\", post(resolve_session))`); module doc (resolve.rs:11) states the `sessionResolve` capability flag is declared `true` in main.rs; `homeDir` wire helper at crates/freshell-server/src/main.rs:1715; in-crate route tests exercise the endpoint (resolve.rs:992, :1616)"
+    - "crates/freshell-sessions/src/resume_input.rs (parser port) and crates/freshell-sessions/src/resume_resolve.rs (matching core + budgeted shape-gated fallbacks) exist; test crates: crates/freshell-sessions/tests/resume_input_parser_parity.rs and crates/freshell-sessions/tests/resume_resolve.rs, plus fixture-driven cross-language parity via test/fixtures/resume-input/parser-cases.json"
+    - "test/e2e-browser/specs/resume-button.spec.ts is registered in MATRIX_SPECS (test/e2e-browser/playwright.config.ts:58; comment at :54-57 confirms the Rust server has the hardened response surface: degraded/providerErrors/unsearchedProviders/homeDir) — runs on both Node and Rust"
+    - "Section-file record (2026-07-30, commit 22022a848): hardened-contract follow-up green — `cargo test --workspace` 2368 passed / 0 failed, resume-button.spec.ts green on both projects, flag restored (not re-executed in this audit)"
+  missing: "Only the PW-TAURI-WIN validation half (native Windows WebView2 sidebar Resume button + paste-resume flow) — deliberately out of scope per the SYNC-05/SAFE-11 PARTIAL convention; implementation and logic/e2e coverage on the Rust server are otherwise recorded as at hardened parity with current main."
+  effort: S
+  files:
+    - crates/freshell-server/src/resolve.rs
+    - crates/freshell-sessions/src/resume_resolve.rs
+    - crates/freshell-sessions/src/resume_input.rs
+    - test/e2e-browser/specs/resume-button.spec.ts
diff --git a/docs/plans/parity-ledger/audit/p2-packaging-installer-updater-and-upgrade-path.yaml b/docs/plans/parity-ledger/audit/p2-packaging-installer-updater-and-upgrade-path.yaml
new file mode 100644
index 000000000..538ac7c81
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/p2-packaging-installer-updater-and-upgrade-path.yaml
@@ -0,0 +1,291 @@
+- id: PACKAGE-01
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/tauri.conf.json:25 — `\"bundle\": { \"active\": false }`: Tauri bundling is explicitly disabled; no resource/app-bundle packaging exists"
+    - "crates/freshell-tauri/tauri.conf.json:6 — frontendDist is `frontend`; crates/freshell-tauri/frontend/ contains only a placeholder index.html splash page"
+    - "tauri.conf.json has no bundle.resources at all: no extension manifests/assets, no wizard/chooser bundles, no provider resources are packaged"
+    - "crates/freshell-tauri/capabilities/{default,chooser,wizard}.json and crates/freshell-tauri/icons/{icon.png,icon.ico} exist in the source tree but are not assembled into any bundle"
+    - "crates/freshell-tauri/src/server.rs:5-9 — design replaces bundled Node with the cargo-built freshell-server binary, but nothing packages/installs it"
+    - "No Tauri packaging/bundling workflow; .github/workflows/ contains only electron-build/electron-release for the legacy app"
+  missing: "Everything: bundling is disabled (bundle.active=false); no packaged artifact containing server binary, client, wizard, chooser, capability files, extension manifests/assets/icons, or provider resources; no install-tree independence from source/Node/Cargo; PW-TAURI-WIN-PACKAGED validation absent"
+  effort: L
+  files:
+    - crates/freshell-tauri/tauri.conf.json
+    - crates/freshell-tauri/src/server.rs
+    - crates/freshell-tauri/build.rs
+
+- id: PACKAGE-02
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/tauri.conf.json:25 — bundle.active=false and targets:'all' placeholders; no NSIS target configured for Tauri"
+    - "installers/windows/ contains only freshell-task.xml.template (scheduled-task template), no NSIS script"
+    - "Legacy reference only: config/electron-builder.yml:81,92-96 (nsis oneClick/runAfterFinish, include assets/electron/installer.nsh) — Electron builder config, not the Rust/Tauri port"
+    - "No running-process detection, close/retry/cancel, silent-mode, or profile-retention code anywhere in crates/ (grep of crates/ for nsis/installer shows none)"
+  missing: "Everything: no Tauri NSIS installer config or script; no interactive/unattended modes; no running-process detection with exact-name matching; no close/retry/cancel or silent-nonzero-no-mutation behavior; no uninstall registration or profile-retention opt-in; PW validation absent (Windows-host gated regardless)"
+  effort: L
+  files:
+    - crates/freshell-tauri/tauri.conf.json
+    - installers/windows/
+
+- id: PACKAGE-03
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/tauri.conf.json:2-4 — productName 'Freshell', version '0.7.0', identifier 'com.freshell.desktop' (identifier matches legacy appId in config/electron-builder.yml:2)"
+    - "crates/freshell-tauri/tauri.conf.json:27 — icon list ['icons/icon.png','icons/icon.ico']; both files exist in crates/freshell-tauri/icons/"
+    - "crates/freshell-tauri/src/tray.rs:236-237 — tray uses app.default_window_icon() at runtime"
+    - "package.json:4 — legacy version is '0.7.5' vs tauri.conf.json '0.7.0': versions are INCONSISTENT"
+    - "No test anywhere asserts identity/icon/metadata consistency (grep found none)"
+  missing: "Version consistency (0.7.0 vs legacy 0.7.5); executable metadata (versioninfo) consistency; any test gating name/identifier/icon consistency; packaged window/taskbar/tray icon validation (PW-TAURI-WIN, Windows-host); permanent gate for the local icon fix"
+  effort: S
+  files:
+    - crates/freshell-tauri/tauri.conf.json
+    - crates/freshell-tauri/src/tray.rs
+
+- id: PACKAGE-04
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/tauri.conf.json — no signing configuration of any kind"
+    - "crates/freshell-tauri/src/updater.rs:19-24 — module docs state signed release bundle / live feed are 'NOT verifiable on this headless host' and signing-gated"
+    - "crates/freshell-tauri/src/updater.rs:64 — PLACEHOLDER_PUBKEY_MARKER 'REPLACE_WITH_REAL_ED25519_PUBKEY' is what tauri.conf.json:18 ships"
+    - ".github/workflows/ has no Tauri build/sign workflow; no Authenticode/signtool config found"
+  missing: "Everything: no code-signing of executable/server/installer/update artifacts, no publisher/trust-chain/timestamp configuration, no Windows signature-verification step (PW validation inherently Windows-host)"
+  effort: L
+  files:
+    - crates/freshell-tauri/tauri.conf.json
+    - .github/workflows/
+
+- id: UPDATE-01
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/tauri.conf.json:17 — production endpoint 'https://releases.freshell.app/latest.json' is present"
+    - "crates/freshell-tauri/tauri.conf.json:18 — pubkey is the literal placeholder 'REPLACE_WITH_REAL_ED25519_PUBKEY': the production public key is NOT registered"
+    - "crates/freshell-tauri/src/updater.rs:91-101 — updater_state() treats placeholder/empty pubkey as DisarmedNoPubkey (no silent arm with a fake key)"
+    - "crates/freshell-tauri/src/lib.rs:574-591 — runtime reports armed/disarmed from the real tauri.conf.json plugins block"
+    - "test: parse_updater_config_reads_plugins_block in crates/freshell-tauri/src/updater.rs:362 — asserts shipped placeholder => DisarmedNoPubkey"
+    - "test: updater_state_reflects_config_presence in crates/freshell-tauri/src/updater.rs:260"
+  missing: "The real production Ed25519 public key in the shipped config (still a placeholder); proof the endpoint is unchanged in the shipped artifact; wrong-key-manifest rejection test run against a packaged artifact; Playwright artifact fingerprint/endpoint validation (PW-TAURI-WIN-PACKAGED, Windows-host) — no packaged artifact exists to inspect"
+  effort: M
+  files:
+    - crates/freshell-tauri/tauri.conf.json
+    - crates/freshell-tauri/src/updater.rs
+
+- id: UPDATE-02
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/updater.rs:122-134 — should_update(): strictly-newer offer / UpToDate / NoDowngrade / Unparseable (the check-decision + no-downgrade clause)"
+    - "crates/freshell-tauri/src/updater.rs:181-196 — check_manifest(): platform-target matching with NoArtifactForPlatform (platform matching clause)"
+    - "crates/freshell-tauri/src/updater.rs:159-165 — platform_target() windows-x86_64/darwin-aarch64 mapping"
+    - "crates/freshell-tauri/src/updater.rs:37 — FIRST_CHECK_DELAY 10s matching legacy startup.ts:219-221"
+    - "tests: should_update_offers_newer_only (:203), should_update_up_to_date_and_downgrade (:219), should_update_tolerates_v_prefix (:226), should_update_prerelease_ordering (:235), should_update_unparseable (:248), check_manifest_offers_for_matching_platform (:318), check_manifest_up_to_date (:331), check_manifest_no_artifact_for_platform (:344), platform_target_maps_macos_to_darwin (:296), first_check_delay_is_ten_seconds (:357)"
+    - "crates/freshell-tauri/Cargo.toml — tauri-plugin-updater is NOT a dependency; no `.plugin(tauri_plugin_updater...)` registration exists (grep: tauri_plugin_updater absent), so checking/downloaded/install/restart never execute at runtime; the plugins.updater config block is inert except for the state report"
+    - "crates/freshell-tauri/src/updater.rs:18-24 — module documents live update as not implemented/verifiable headlessly"
+  missing: "Runtime wiring of tauri-plugin-updater (not even a dependency); actual checking/downloaded states, download, error state, install/restart actions; tray 'Check for Updates' only reports armed/disarmed (lib.rs:548-591) and never performs a check; no-downgrade/platform-matching exist headlessly but no E2E through feeds; PW-TAURI-WIN/PW-TAURI-WIN-PACKAGED validations absent"
+  effort: L
+  files:
+    - crates/freshell-tauri/src/updater.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/Cargo.toml
+    - crates/freshell-tauri/src/tray.rs
+
+- id: UPDATE-03
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "grep of crates/freshell-tauri/src/*.rs for relaunch/restart (excluding chooser restartMain references) finds no update-restart, state-migration-across-update, or process-tree replacement code; updater.rs has no install/restart path at all"
+    - "crates/freshell-tauri/src/updater.rs:20 — install/relaunch explicitly deferred ('per-OS install/relaunch ... Phase-4 manual / cross-build QA')"
+    - "No settings/tab state carry-over-across-update logic or single-process-tree guard tied to updates (tauri-plugin-single-instance exists but is launched-at-boot concern, not update handoff)"
+  missing: "Everything: settings/tab migration across update restart, old-tree close, exactly-one-new-tree guarantee, terminal recovery/explanation, orphan-listener checks, and all PW packaged validation"
+  effort: L
+  files:
+    - crates/freshell-tauri/src/updater.rs
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/src/state_machine.rs
+
+- id: UPDATE-04
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "This item owns only the NSIS property/argument plumbing; no Tauri NSIS installer exists (crates/freshell-tauri/tauri.conf.json:25 bundle.active=false; installers/windows/ has only freshell-task.xml.template)"
+    - "Legacy reference for the plumbing: assets/electron/installer.nsh:29 reads ${StdUtils.GetParameter} $0 \"FRESHELL_REMOTE_URL\" and :40 FileWrite's the desktop.provision file — Electron-builder NSIS only, not Tauri"
+    - "The TAURI-16 receiving side DOES exist and is tested (out of this item's owned scope): crates/freshell-tauri/src/provisioning.rs:55 parse_provisioning (1:1 port), :92 apply_provisioning_file (one-shot consume + desktop.json patch + always-delete), :162 resolve_provisioned_remote (env pair FRESHELL_REMOTE_URL/FRESHELL_TOKEN then provision file)"
+    - "tests (receiver only): apply_patches_config_and_deletes_file (provisioning.rs:261), apply_incomplete_file_deletes_without_patching (:286), apply_preserves_other_desktop_json_keys (:297), parses_both_keys (:186), crlf_line_endings_are_stripped (:210)"
+  missing: "The entire owned scope: a Tauri-side NSIS installer that accepts/exposes FRESHELL_REMOTE_URL + FRESHELL_TOKEN properties, writes the exact desktop.provision values, and hands them to the provisioning path; NSIS-side E2E (PW-TAURI-WIN-PACKAGED, Windows-host). No NSIS installer exists to plumb"
+  effort: M
+  files:
+    - crates/freshell-tauri/tauri.conf.json
+    - installers/windows/
+    - crates/freshell-tauri/src/provisioning.rs
+
+- id: UPDATE-05
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - ".github/workflows/electron-release.yml — the only release workflow; it runs `npm run electron:build` and uploads Electron installers via scripts/upload-electron-release-assets.ts (electron-builder latest.yml world), no Tauri build at all"
+    - ".github/workflows/rust-clippy.yml:34 — only compiles freshell-tauri for lint; no artifact/release job"
+    - "No latest.json generation, Ed25519 update-signature, or Tauri bundle publish step anywhere in scripts/ or .github/workflows/"
+    - "crates/freshell-tauri/src/updater.rs:6-8 — docs confirm the tauri-plugin-updater signed latest.json + .sig pipeline is the intended-but-unimplemented design"
+  missing: "Everything: release CI producing Tauri platform artifacts, latest.json with matching version/real URLs/hashes/Tauri Ed25519 signature, staging-feed validation, Authenticode identity assertion, and installed-prior-version update-through-UI verification"
+  effort: L
+  files:
+    - .github/workflows/
+    - scripts/
+
+- id: MIGRATE-01
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No Electron→Tauri state-policy document exists: grep across docs/ for the policy ('state policy' / 'import, merge, reset') matches only the checklist file itself (docs/plans/2026-07-14-rust-tauri-parity-completion-checklist.md), i.e. the requirement statement, not a policy definition"
+    - "No migration module/classification code anywhere in crates/ (grep for electron-profile/localStorage/WebView2 import returns nothing)"
+  missing: "Everything: the explicit per-key classify-as import/merge/reset/reject policy for tabs/layout, browser preferences, auth, recency, prompt/input history, device identity, cursors, activity/attention, and unknown/transient keys — neither documented nor implemented"
+  effort: S
+  files:
+    - docs/plans/
+
+- id: MIGRATE-02
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No export/import bridge code found anywhere in crates/ (no legacy localStorage export, no Tauri-side import, no origin/port-independent transfer mechanism)"
+    - "grep of crates/ for localStorage/webview2/electron-profile migration terms returns no matches"
+  missing: "Everything: legacy-side export, Tauri-side import, and the origin/port-independent bridge (no reliance on Chromium/WebView2 shared storage); Electron+Tauri PW validation absent"
+  effort: L
+  files:
+    - crates/freshell-tauri/src/
+
+- id: MIGRATE-03
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No Electron→Tauri tab/pane layout migration code in crates/ (only server-side tabs persistence for the Rust server's own model: crates/freshell-ws/src/tabs_persist.rs is the Rust-native store, not a legacy importer)"
+    - "No provider-identity/session-resume migration mapping found"
+  missing: "Everything: order/active tab/names/splits/ratios/pane kinds/cwd/URLs/files/agent settings/durable provider identities import from a legacy Electron profile; 16-tab PW comparison absent"
+  effort: L
+  files:
+    - crates/freshell-tauri/src/
+    - crates/freshell-ws/src/tabs_persist.rs
+
+- id: MIGRATE-04
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No browser-local preference (localStorage) migration code in crates/ — no theme/UI-scale/font/sidebar/sound/panes/rich-agent preference import path"
+    - "Rust server settings (crates/freshell-server/src/settings_store.rs) are config.json-based server settings, not browser-local preferences"
+  missing: "Everything: migration of theme, UI scale, terminal/editor fonts, sidebar visibility/width/sort, local visibility filters, sound/notifications, panes, rich-agent preferences; reimport-on-change suppression test; PW validation absent"
+  effort: L
+  files:
+    - crates/freshell-tauri/src/
+
+- id: MIGRATE-05
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No legacy-auth-secret reuse code: grep of crates/freshell-tauri/src/*.rs for legacy token/auth migration returns nothing"
+    - "crates/freshell-tauri/src/server.rs designs a freshly generated AUTH_TOKEN per app-bound spawn (uuid v4); provisioning.rs supports remoteToken for remotes — neither reads a legacy Electron token"
+    - "The TAURI-04 token bootstrap exists only as the ?token= URL load (provisioning.rs:147 remote_load_url percent-encodes into the URL); no scrubbing/exclusion audit of retained artifacts for a migrated secret"
+  missing: "Everything: reading the legacy token into approved credential storage, authenticating Tauri+Rust with it, post-bootstrap URL scrubbing, and trace/log/history redaction validation (PW-TAURI-WIN)"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/provisioning.rs
+    - crates/freshell-tauri/src/server.rs
+    - crates/freshell-tauri/src/commands.rs
+
+- id: MIGRATE-06
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "crates/freshell-server/src/settings_store.rs:371-378 — persist() copies forward all on-disk top-level keys it does not own (completedMigrations, recentDirectories, future/unknown keys round-trip untouched); doc comment cites the fixed data-loss incident"
+    - "tests: settings_patch_preserves_unmanaged_top_level_document_state (settings_store.rs:2614), terminal_override_patch_preserves_unmanaged_top_level_document_state (:2642), session_override_patch_preserves_unmanaged_top_level_document_state (:2669) — assert recentDirectories/serverSecrets/zzFutureKey round-trip through a save and no-op patches don't rewrite config.json"
+    - "test: both_primary_and_backup_corrupt_starts_fresh_but_preserves_both_forensic_copies (settings_store.rs:3460) — corrupt-config recovery"
+    - "CFG-01 lossless-writer half of this item is implemented+tested in the Rust server"
+  missing: "The migration half: no Electron→Tauri server-config migration flow exists that routes legacy config through this writer, so 'through migration' is unexercised; first-chat controls/legacy-seed migration import; PW-TAURI-WIN hash/count validation absent"
+  effort: M
+  files:
+    - crates/freshell-server/src/settings_store.rs
+    - crates/freshell-tauri/src/
+
+- id: MIGRATE-07
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No migration engine exists in crates/ at all (no Electron→Tauri import), so there is no atomic/backup/resumable/idempotent/reversible migration implementation and no rollback/result receipt"
+    - "(crates/freshell-ws/src/tabs_persist.rs rollback code is the Rust-native snapshot store's own retention rollback, unrelated to Electron→Tauri migration)"
+  missing: "Everything: phase-wise failure handling, backup creation, resume, idempotent second launch, explicit rollback with receipt; fault-fixture PW validation absent"
+  effort: L
+  files:
+    - crates/freshell-tauri/src/
+
+- id: MIGRATE-08
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No Electron→Tauri migration code exists, so there is no merge-with-existing-Tauri-profile policy (safe-default keep/unique-tab-add) and no explicit Replace-with-restorable-backup path"
+  missing: "Everything: default merge preserving existing Tauri prefs + adding only unique legacy tabs, separate explicit Replace with restorable Tauri backup receipt first, no-silent-drop guarantee; PW validation absent"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/
+
+- id: MIGRATE-09
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No legacy-profile parsing/import code exists in crates/, so there is no handling of corrupt JSON / old schema / future schema / oversized legacy storage in a migration context"
+    - "(settings_store corrupt-config recovery at settings_store.rs:3460 covers the Rust server's own config.json, not legacy Electron storage)"
+  missing: "Everything: safe-partial recovery, rejection with nontechnical explanation, backup location surfacing, launchable main/rollback path for malformed/obsolete/future/oversized legacy storage; parameterized PW validation absent"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/
+
+- id: MIGRATE-10
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-tauri/src/commands.rs:412-415 — authoritative port bind-check before committing start-local: occupied port => error 'Port {port} is already in use. Choose a different port, or connect to that server.' (the don't-steal-the-port + offer-connect/another-port clause, though generic, not legacy-Electron-specific)"
+    - "crates/freshell-tauri/src/commands.rs:331 — is_port_available() real bind probe; :159/:198/:248 — chooser kinds connect | remote | start-local with plan_connect"
+    - "tests: is_port_available_true_for_free_port (commands.rs:731), is_port_available_false_for_occupied_port (:741), connect_requires_url (:584), connect_requires_token_when_auth_required (:594)"
+    - "No code anywhere kills or mutates a foreign/legacy process (grep confirms only owned-child SIGTERM→SIGKILL of the spawned freshell-server in server.rs)"
+    - "commands.rs:418-420 — live token validation (auth_check) against a chosen URL is explicitly deferred"
+  missing: "Legacy-specific coexistence: detection that the occupying process is the legacy Electron install and its session-marker preservation; the legacy-specific connect/change UX flow; live auth validation when connecting; PW-TAURI-WIN + Electron validation that the legacy window/server/session marker survives Tauri launch/close"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/commands.rs
+    - crates/freshell-tauri/src/state_machine.rs
+
+- id: MIGRATE-11
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No Electron user-data root/origin discovery code anywhere in crates/ (no default-path enumeration, no multiple-profile/origin handling, no database-lock detection, no portable-profile detection)"
+    - "provisioning.rs reads only ~/.freshell/ (its own desktop.json/desktop.provision); nothing locates a legacy install"
+  missing: "Everything: automatic legacy profile discovery without a supplied path, multi-profile/origin candidate choice or chooser, non-destructive close-legacy-and-retry lock state, portable/moved-profile selection, clean no-profile first run; PW validation absent"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/
+
+- id: MIGRATE-12
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No migration implementation exists, so no migration result receipt (counts, backup/rollback location, skipped items, partial-recovery flag) is produced, displayed, or persisted (grep for receipt in crates/ found only unrelated WebSocket receipt-time comments)"
+  missing: "Everything: receipt content (tabs/panes/preferences/server-settings counts, backup+rollback location, skipped/unsupported items, partial-recovery indication), keyboard-accessible result screen, persistence and later review without rerunning; PW validation absent"
+  effort: M
+  files:
+    - crates/freshell-tauri/src/
+
+- id: MIGRATE-13
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No Tauri installer exists (crates/freshell-tauri/tauri.conf.json:25 bundle.active=false; installers/windows/ has only freshell-task.xml.template), so no installer-replacement upgrade/rollback path, registration replacement, or retained-hashed-legacy-data + signed-installer retention exists"
+    - "PACKAGE-02 running-process upgrade rules likewise have no implementation to apply"
+  missing: "Everything: replacement (not side-by-side) install choice, state migration during upgrade, single shortcut/uninstall/startup owner, hashed legacy user-data retention, retained verified Electron installer/version for binary+registration restore, rollback execution; disposable-VM PW validation absent (inherently Windows-host)"
+  effort: L
+  files:
+    - crates/freshell-tauri/tauri.conf.json
+    - installers/windows/
+    - crates/freshell-tauri/src/
diff --git a/docs/plans/parity-ledger/audit/required-playwright-test-lanes.yaml b/docs/plans/parity-ledger/audit/required-playwright-test-lanes.yaml
new file mode 100644
index 000000000..69dd83c5c
--- /dev/null
+++ b/docs/plans/parity-ledger/audit/required-playwright-test-lanes.yaml
@@ -0,0 +1,196 @@
+- id: HARNESS-01
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "test/e2e-browser/helpers/rust-server.ts:277 — class RustServer implements the owned fixture: builds/locates the binary via resolveRustServerBin/ensureRustServerBuilt (:82-138, cargo build --release -p freshell-server or FRESHELL_E2E_RUST_SERVER_BIN override), ephemeral port via findFreePort (:308), unique token via randomUUID (:309), isolated FRESHELL_HOME via mkdtemp + applyIsolatedHomeEnvironment (:303, :477-492), records exact PID (:513, :540), waits for /api/health plus token-gated /api/server-info identity check (:322-352, waitForHealth :659), spawns own process group (detached:true :510) and stop() group-kills only that PID + verifies owned descendants dead (ownedDescendantPids :222, killCurrentProcess :573-610, reapSurvivingChildren :619)"
+    - "test: 'boots, survives restart, and reaps only its own process group' in test/e2e-browser/specs/harness-01-rust-server.spec.ts:84 — drives /?token=...&e2e=1 (:129), creates a shell pane + echo marker (:139-141), spawns unrelated sentinel (:144-148), restart()s the same owned server against same home/port/token (:157-161), proves post-restart functional reconnect with a second marker (:206-238), captures real PTY child PIDs (server.ownedChildPids(), :248-251) and after stop() asserts server PID, process group, each child PID dead, port freed, sentinel alive (:257-277), and that real ~/.freshell was untouched (:289-314)"
+    - "test/e2e-browser/helpers/rust-server.test.ts and rust-server-bin.test.ts — unit tests for the fixture helper exist"
+    - "Matches the item's own checked-off evidence note (commits 334f834b, 2cb57287) and checklist is marked [x]"
+  missing: ""
+  effort: S
+  files:
+    - test/e2e-browser/helpers/rust-server.ts
+    - test/e2e-browser/specs/harness-01-rust-server.spec.ts
+
+- id: HARNESS-02
+  verdict: DONE
+  confidence: high
+  evidence:
+    - "test/e2e-browser/playwright.config.ts:204-211 — projects legacy-chromium (e2eServerKind:'legacy') and rust-chromium (e2eServerKind:'rust') share testMatch MATRIX_SPECS (:13-82) over the same testDir"
+    - "MATRIX_SPECS covers all five named categories: settings (settings-persistence-split.spec.ts :15, settings-live-reload.spec.ts :48), session (session-directory-matrix.spec.ts :25), terminal (terminal-lifecycle.spec.ts :17), browser-pane (browser-pane.spec.ts :23), multi-client (multi-client.spec.ts :24), plus restart/fs coverage (server-restart-recovery.spec.ts :14) routed through handle.restart()"
+    - "test/e2e-browser/helpers/fixtures.ts:5 + external-target.ts — generalized target seam with E2eServerKind routed per project option"
+    - "test: 'the fixture-owned server is the one THIS project claims to own, across a restart' in test/e2e-browser/specs/harness-02-matrix-bite.spec.ts:87 — bite proof keyed on the permanent structural discriminator GET /api/server-info (Rust emits runtime:'rust' and no nodeVersion; legacy emits real nodeVersion and no runtime, assertions at :137-146), with a documented permanent RED demonstration of a mis-wired rust project failing only the rust leg (:44-49)"
+    - "Checklist is marked [x] with matching evidence text"
+  missing: ""
+  effort: S
+  files:
+    - test/e2e-browser/playwright.config.ts
+    - test/e2e-browser/helpers/fixtures.ts
+    - test/e2e-browser/helpers/external-target.ts
+    - test/e2e-browser/specs/harness-02-matrix-bite.spec.ts
+
+- id: HARNESS-03
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "test/e2e-browser/fixtures/ contains fakes: fake-claude-cli.mjs, fake-claude-sidecar.mjs (Claude-SDK-bridge sidecar — Kilroy is a hidden claude-backed fresh-agent type, so the Kilroy/Claude-SDK lane rides this same fake), fake-codex-cli.mjs, fake-codex-terminal.mjs, fake-opencode.cjs (opencode server/sidecar with sqlite schema), fake-opencode-terminal.mjs, fake-amplifier-cli.mjs, fake-amplifier-activity-cli.mjs (activity events), fake-crashing-claude-cli.mjs (crash lane), plus test/fixtures/coding-cli/codex-app-server/fake-app-server.mjs for the Codex app-server protocol"
+    - "Argument/request recording exists per-fake: fake-amplifier-cli.mjs:26-32 appends argv JSONL to FAKE_AMPLIFIER_ARGV_LOG; fake-opencode.cjs:12-24 appendAudit() records argv+payload to FAKE_OPENCODE_AUDIT_LOG; fake-claude-sidecar.mjs logs inbound requests to FAKE_CLAUDE_SIDECAR_LOG (logRequest, ~:50)"
+    - "Fakes are exercised by many rust-lane specs (restore-matrix.spec.ts, session-directory-matrix.spec.ts, agent-continuity-matrix.spec.ts, terminal-activity-rust.spec.ts, fake-crashing-claude via agent-crash-autoresume-rust.spec.ts, etc.)"
+  missing: "No fake Gemini or fake Kimi executable exists anywhere (no fake-gemini*/fake-kimi* in test/e2e-browser/fixtures or test/fixtures). No fixture-only contract spec that invokes each executable/protocol DIRECTLY and asserts its recorded ledger/emitted events independent of server/provider parity — every fake is only driven indirectly through the server inside parity specs. Approval/question/completion/resume event coverage is not uniform across all seven named providers (e.g. no ledger-asserting contract for bel/generic or the missing two providers)."
+  effort: M
+  files:
+    - test/e2e-browser/fixtures/fake-claude-cli.mjs
+    - test/e2e-browser/fixtures/fake-claude-sidecar.mjs
+    - test/e2e-browser/fixtures/fake-opencode.cjs
+    - test/e2e-browser/fixtures/fake-amplifier-cli.mjs
+    - test/fixtures/coding-cli/codex-app-server/fake-app-server.mjs
+    - test/e2e-browser/specs/ (new fixture-only contract spec)
+
+- id: HARNESS-04
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "test/e2e-browser/specs/session-directory-matrix.spec.ts — inline multi-provider seeding: Claude JSONL corpus (buildSessionJsonl :44-97 with system/init, turn pairs, trailing summary line :98-100), Codex (CODEX_SESSION_ID :28), OpenCode sqlite rows with time_archived columns (:203-214), Amplifier (:32-35), plus a deletion leg test ('a session file removed from disk disappears from the sidebar without a reload', ~:657) and live-create mid-test"
+    - "test/e2e-browser/perf/seed-server-home.ts:4-7 — large deterministic Claude-only corpus (12 projects * 15 sessions, 36 alpha sessions, 240-turn long history) used by the visible-first perf audit"
+    - "Pagination search slice exists in session-directory-matrix.spec.ts (:602-note and the filter test :614) but is explicitly self-described as a SLICE, not the full corpus-based pagination proof"
+  missing: "No standalone multi-provider corpus BUILDER with a parseable corpus manifest + content hashes. Not evidenced: archived AND deleted sessions as canned artifacts across all four providers (only live create/delete mid-test), provider titles, nested git repositories, worktrees, fractional timestamps, or more-than-one-page-of-results in a reusable fixture. No fixture-only contract spec that parses the corpus manifest/hashes (optionally via legacy) and proves the temp home was deleted and the real home untouched."
+  effort: M
+  files:
+    - test/e2e-browser/specs/session-directory-matrix.spec.ts
+    - test/e2e-browser/perf/seed-server-home.ts
+    - test/e2e-browser/fixtures/ (new corpus builder + contract spec)
+
+- id: HARNESS-05
+  verdict: PARTIAL
+  confidence: high
+  evidence:
+    - "test/e2e-browser/helpers/ws-capture.ts:8 — WsCapture raw Node 'ws' client: completes the hello handshake and records every received JSON frame with waitFor/count accessors; used by multiple raw-socket specs"
+    - "test/e2e-browser/specs/safe01-auth-matrix.spec.ts:76-104 — ad-hoc raw WebSocket helper records closeCode/closeReason (asserts 4001 at :224-237); safe03-origin-matrix.spec.ts:63-100 similarly captures close codes (asserts 4011 at :143-171); 15+ specs open raw 'ws' WebSockets directly (ws-ping-pong-matrix, reconcile-handshake-rust, rest-spawn-gate-rust, etc.)"
+  missing: "No reusable raw HTTP/WS helper supporting: sending malformed frames, delaying reads or the hello, slow-consumer backpressure (WsCapture parses every message immediately, no pause/drain control), recorded sent/received byte counts, or abort. No deterministic echo/error socket fixture and no fixture self-test proving 'delayed receive truly stops socket draining', close-code recording, abort, and a second normal socket staying usable. Orchestration-route calls exist only via ordinary fetch/page.request inside specs, not as part of a raw-client surface."
+  effort: M
+  files:
+    - test/e2e-browser/helpers/ws-capture.ts
+    - test/e2e-browser/specs/safe01-auth-matrix.spec.ts
+    - test/e2e-browser/specs/safe03-origin-matrix.spec.ts
+    - test/e2e-browser/helpers/ (new raw HTTP/WS client helper + echo/error fixture)
+
+- id: HARNESS-06
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "Direct evidence of absence in-repo: test/e2e-browser/specs/ws-ping-pong-matrix.spec.ts:30-31 — spec comment names the needed suppression/reply-to-pings behavior as 'via a controllable proxy ... needs proxy infrastructure this lane does not own'; test/e2e-browser/specs/safe01-auth-matrix.spec.ts:36 — the 'proxy' auth leg is noted as needing controllable proxy infrastructure 'this lane does not' have"
+    - "Search of test/ found no SMB/Windows-share fixture, no fake external-editor executable or invocation recorder (FAKE_EDITOR/EDITOR= searches empty), no signed update-feed fixture (no latest.json fixture under test/), and no trusted-HTTPS/test-certificate fixture under test/e2e-browser"
+    - "Only tangential building blocks exist: browser-pane*.spec.ts small local HTTP canary servers for the /api/proxy path and editor-pane.spec.ts's route.delay trick — these are per-spec scaffolding, not the deterministic proxy/file/SMB/editor/AI/Kilroy/update/HTTPS fixture set"
+    - "2026-07-18 reconciliation doc (docs/plans/2026-07-18-checklist-reconciliation.md:70) also graded this N (absent)"
+  missing: "All clauses: controllable proxy (HTTP+WS), hot-reload file trees, local/Windows-share (SMB) trees, fake editor with recorded invocations, summary-AI and full Kilroy runtime fixtures, signed update feed, trusted-HTTPS test certificate; plus the fixture smoke that reaches every target directly, mounts/reads the disposable SMB share on Windows, records editor/Kilroy invocations, returns fixed AI output, downloads a signed artifact, and verifies the test certificate."
+  effort: L
+  files:
+    - test/e2e-browser/fixtures/ (new deterministic fixture set)
+    - test/e2e-browser/helpers/
+
+- id: HARNESS-07
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "No occurrence of connectOverCDP, freshell-tauri.exe launching, WebView2 user-data isolation, or USERPROFILE/LOCALAPPDATA/APPDATA isolation anywhere in test/ (grep across test/**/*.ts found zero matches for connectOverCDP/tauri fixtures)"
+    - "test/e2e-electron/electron-app.test.ts exists but launches the LEGACY Electron app, not the Tauri app, and has no CDP-over-WebView2 attach"
+    - "The item is inherently Windows-only (native freshell-tauri.exe + WebView2 remote-debugging port), but no fixture code exists at all — the gap is missing code, not merely host validation. 2026-07-18 reconciliation doc (:71) graded it H (host-limited); current tree shows no new fixture"
+  missing: "Everything: Playwright fixture launching the exact freshell-tauri.exe with unique USERPROFILE/LOCALAPPDATA/APPDATA/WebView2 user-data paths, optional FRESHELL_HOME, ephemeral WebView2 remote-debugging port, chromium.connectOverCDP attach, two-isolated-runs leakage assertions, and owned-PID reaping proof. Requires a Windows host to implement and validate."
+  effort: L
+  files:
+    - test/e2e-browser/helpers/ (new tauri fixture)
+    - test/ (new native Tauri playwright config/spec)
+
+- id: HARNESS-08
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "crates/freshell-tauri/src/ contains only production modules (commands.rs, tray.rs, hotkey.rs, updater.rs, renderer_recovery.rs, server.rs, state_machine.rs, window_state.rs, windows.rs, provisioning.rs); grep for 'control plane|control-plane|e2e|E2E' across crates/freshell-tauri/src returned no files"
+    - "Feature flags in the crate are limited to 'tray' (tray.rs:139, :257; lib.rs:539-573) — no e2e/test-only build flag and no test-only command surface exists"
+    - "No Tauri e2e spec exists anywhere in test/ (see HARNESS-07), so the 'bridge present in E2E build / absent in release build' validation spec is also absent"
+    - "Validation is inherently Windows/Tauri-host-bound, but the blocking gap is absent code, not the host"
+  missing: "All clauses: an explicit E2E-build-flag-gated control plane exposing read-only desktop state and safe triggers for the production close/tray/hotkey/updater/renderer-failure/daemon handlers; compile-time exclusion from release; a spec proving presence in the E2E build and absence in a release build. Requires Windows Tauri host to validate."
+  effort: L
+  files:
+    - crates/freshell-tauri/src/lib.rs
+    - crates/freshell-tauri/Cargo.toml (new e2e feature gate)
+    - crates/freshell-tauri/src/ (new control-plane module)
+
+- id: HARNESS-09
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "Grep across test/ for SMB/firewall/scheduled-task/shortcut/window-visibility helpers found only legacy server-side API tests with mocked detection (test/integration/server/network-api.test.ts uses vi.mock of server/firewall.js) — these are not Playwright-runner host-assertion helpers and are not ownership-safe disposable-fixture driven"
+    - "No helper module for Windows process trees, listening ports, desktop shortcuts, scheduled tasks/services, firewall rules, installed-application metadata, or window visibility exists under test/e2e-browser/helpers/ or test/helpers/"
+    - "Inherently Windows-host-bound (and mutating checks would need a disposable VM/Sandbox), but no code exists"
+  missing: "All clauses: ownership-safe helpers for process trees, listening ports, files, shortcuts, scheduled tasks/services, firewall rules, installed application metadata, and window visibility in the Playwright runner; plus the self-test creating uniquely named disposable process/file/firewall fixtures with sentinel preservation. Windows host required."
+  effort: M
+  files:
+    - test/e2e-browser/helpers/ (new windows host-assertion helpers)
+
+- id: HARNESS-10
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "test/e2e-electron/electron-app.test.ts:1-50 — the existing Playwright Electron launcher does start the legacy Electron app against a temp HOME with an optional minimal desktop.json, and could host this item's validation; but it contains no synthetic profile construction"
+    - "Grep across test/ for 'migrate|migration|legacy.?profile|electron profile|MIGRATE' returned no synthetic-profile/manifest/hash fixture or spec"
+    - "Electron desktop config code (electron/ and crates/freshell-tauri config/desktop.json handling) exists as port targets, but no fixture builds the synthetic 16-tab profile"
+  missing: "All fixture clauses: a synthetic Electron profile containing server/desktop config, browser preferences, 16 representative tabs/panes, overrides, recents, colors, and migration markers; plus the validation spec asserting legacy Electron source manifest, hashes, visible state, and safe copy behavior against it. (Electron launch against it runs headless-Linux today, but the 'Tauri import' half is Windows-host-deferred per the item itself.)"
+  effort: M
+  files:
+    - test/e2e-electron/electron-app.test.ts
+    - test/fixtures/ (new synthetic legacy-profile builder)
+
+- id: HARNESS-11
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "Grep of test/e2e-browser/helpers/ for a11y helpers ('a11y|accessib|aria-') returned no files; role/label usage is ad hoc per-spec (e.g. helpers/pane-picker.ts:5-26 getByRole('toolbar'/'menuitem'/'button'), harness-01-rust-server.spec.ts:48)"
+    - "Specs routinely drive CSS implementation details (page.locator('.xterm') throughout, e.g. harness-01-rust-server.spec.ts:38-50), opposite to the 'must not rely on CSS' gate"
+    - "eslint.config.js:5 includes eslint-plugin-jsx-a11y, but that is a source-tree lint plugin, not the required Playwright-side reusable helpers/lint gate with a self-test that fails on an inaccessible fixture control"
+    - "2026-07-18 reconciliation doc (:75) graded this N (absent); nothing new found in the current tree"
+  missing: "All clauses: reusable accessibility-selector helpers/lint assertions requiring stable roles and accessible names, the self-test that uses only roles/labels/keyboard on main UI controls and deliberately fails on an inaccessible fixture control, and enforcement that feature tests avoid CSS implementation details."
+  effort: M
+  files:
+    - test/e2e-browser/helpers/ (new a11y gate helpers)
+    - eslint.config.js (optional lint rule wiring)
+
+- id: HARNESS-12
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "test/e2e-browser/specs/stress.spec.ts:6-25 — repeated create stress scenarios exist ('handles 6+ panes simultaneously', 'handles 10 tabs')"
+    - "test/e2e-browser/perf/run-sample.ts:259-311 — process-forest enumeration (ps -eo pid,ppid BFS incl. chromium process tree) used for audit teardown; perf/ visible-first audit captures latency/transport/network metrics (run-sample.ts, network-recorder.ts, derive-visible-first-metrics.ts) but contains no RSS/handle measurement (grep for memoryUsage/rss/heapUsed across test/e2e-browser returned nothing)"
+  missing: "All measurement clauses: no capture of server/Tauri/provider child PIDs as metrics, handles, RSS, queue sizes, or listening ports before/after stress; no repeated create/send/close/restart loop asserting return to a bounded resource baseline; no 'no owned process or port left behind' post-condition wired to the owned fixtures; no retained process-tree artifact on bound breach."
+  effort: M
+  files:
+    - test/e2e-browser/perf/ (extend with resource collectors)
+    - test/e2e-browser/specs/stress.spec.ts
+    - test/e2e-browser/helpers/ (new leak/resource measurement helper)
+
+- id: HARNESS-13
+  verdict: MISSING
+  confidence: high
+  evidence:
+    - "Grep across the whole repo for 'uiautomation|UI Automation|SendInput|keybd_event|robotjs|nut.js' returned no files — no Windows UI Automation or real-OS-keystroke automation exists"
+    - "No packaged-release Tauri install/launch spec exists in test/ at all (see HARNESS-07)"
+    - "scripts/assert-native-windows-build.ts exists as a build assertion script, but it does not drive tray/menu/hotkey via OS automation"
+    - "Inherently Windows-host-bound (installed release build + UI Automation), and no code exists"
+  missing: "All clauses: Windows UI Automation + real OS keystrokes/clicks from the Playwright runner; installing an exact release build; opening the real tray menu and native app menu; invoking a production action from each plus the saved global hotkey; asserting the real WebView/process effect with no E2E control plane in the build. Windows host with packaged release required."
+  effort: L
+  files:
+    - test/e2e-browser/helpers/ (new UI-automation bridge)
+    - scripts/assert-native-windows-build.ts
+
+- id: HARNESS-14
+  verdict: PARTIAL
+  confidence: medium
+  evidence:
+    - "crates/freshell-server/src/rate_limit.rs:111 — pub struct TestClock implementing the crate's Clock trait (:129 impl Clock for TestClock), with explicit advance (:105, advance_ms) used by rate-limiter unit tests (:335-351) — a real in-crate fake-clock seam, but scoped to the rate limiter only"
+    - "Grep for a server-exposed test clock found nothing: no '/api/test' clock route and no test-clock surface anywhere in crates/ (searched 'test/clock|test-clock|/api/test' — no files), so Playwright cannot advance/freeze/reset time on a running server"
+  missing: "A shared test clock across idle cleanup, rate windows, tab/device TTLs, retention, and timeout paths (TestClock today is rate_limit.rs-local); any Playwright-reachable control surface (build-flag-gated) to advance/freeze/reset it; the serial spec asserting deterministic fixture-timer ordering; and a normal-build absence proof of the control surface."
+  effort: M
+  files:
+    - crates/freshell-server/src/rate_limit.rs (existing TestClock to generalize)
+    - crates/freshell-server/src/ (gate/idle/ttl call sites to wire onto a shared clock)
+    - test/e2e-browser/specs/ (new serial clock spec)
