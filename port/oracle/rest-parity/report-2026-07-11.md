# REST surface parity sweep — report (2026-07-11)

**Task:** HANDOFF §7.C REST surface parity sweep (work-queue item 4) — ORIGINAL
node freshell vs Rust port, byte-identical requests, compared as
status + headers-of-interest + normalized-deep-equal body (§8.1 pass
definition: deep-equal after the DECLARED normalization list only).

| | |
|---|---|
| Host / branch | SurfaceBookPro9 (WSL2), `feat/rust-tauri-port` @ `14b756d3` |
| ORIGINAL | `dist/server/index.js` (built 2026-07-11 09:19), port 17871, §5.1 recipe |
| RUST PORT | `target/release/freshell-server` (built 2026-07-11 09:14), port 17872, §5.2 recipe |
| Harness | `port/oracle/rest-parity/sweep.mjs` (self-contained, rerunnable) |
| Machine results | `port/oracle/rest-parity/results-2026-07-11.json` |
| Reproduce | `node port/oracle/rest-parity/sweep.mjs --out results-<date>.json` |

## Summary

**111 cases — 6 PASS, 105 DIVERGENCE, 0 deferred.**

Of the 105 divergences, **63 are "charset-only"**: their ONLY diff is the
`Content-Type` charset suffix (systematic family S1 below). The remaining
**42 substantive divergences** collapse into **14 root-cause findings**
(R1–R14). Nothing was fixed and nothing was normalized away; every row is in
the results JSON with both raw-normalized responses.

The `ui.command`/`ui.screenshot.result` WS round-trip was driven for real with
a participating WS client on both servers (no deferral needed): the ok
envelope, the 422 failure envelope, the overwrite path, the `ui.command`
frame itself, and the written PNG bytes (sha256-equal) were all compared.

## Comparison contract

- **Requests:** byte-identical against both servers (same token, same paths,
  same bodies). Where both servers share a filesystem side-effect location
  (screenshot outputs under the shared checkout cwd), per-side hooks reset the
  filesystem between the two sends — the requests themselves never differ.
- **Compared:** HTTP status; headers-of-interest (`content-type`,
  `cache-control`, `x-frame-options`, `content-security-policy`,
  `content-security-policy-report-only`, `x-qa-marker`); body as normalized
  deep-equal (JSON), scrubbed text, or sha256 of raw bytes. Body
  representation is content-type-independent so a header divergence cannot
  skew the body comparison.
- **WS cases** compare the full inbound frame transcript + close code/reason.

## Declared normalization list (complete)

Field-name based, any depth, mirroring `port/oracle/harness/normalize.ts`.
A value is masked ONLY if it also matches the family's expected shape; a
mis-shaped value stays raw and surfaces as a divergence.

| Field(s) | Family | Justification |
|---|---|---|
| `instanceId`, `serverInstanceId`, `bootId`, `connectionId`, `requestId` | id | per-boot generated identifiers (`instanceId == ready.serverInstanceId` cross-check is owned by T0, per task charter) |
| `startedAt`, `timestamp`, `modifiedAt`, `generatedAt`, `lastActivityAt`, `createdAt`, `updatedAt`, `checkedAt`, `turnCompletedAt`, `at` | timestamp | wall-clock values |
| `version`, `currentVersion`, `latestVersion`, `cliVersion` | version | build/version strings (declared volatile by the task) |
| `revision` | seq | run-monotonic read-model revision |
| `cursor`, `nextCursor` | cursor | opaque pagination cursors |
| `updateCheck` | opaque | **registry addition, justified:** derived from a LIVE GitHub API call (`server/updater/version-checker.ts`) — third-party network data that can change between the two requests. Its presence/nullness still participates (and did diverge — R5). |
| `token` | opaque | auth credential must never appear in committed artifacts |
| `codexDisplayIdSecret` | opaque | **registry addition, justified:** per-boot generated secret material persisted by the node config-store; never value-comparable, presence still compared (visible in R2 evidence) |
| `port` (only when the value is 17871/17872) | selfport | **registry addition, justified:** the two systems necessarily listen on different ports; the port value is a sweep-environment artifact, not behavior. Foreign ports (proxy target 17876, dead port 17877) compare literally. |

String scrubbers applied to every string value: the two scratch-home absolute
paths → `<SCRATCH_HOME>`, the checkout root → `<REPO>`, `os.tmpdir()` →
`<TMP>`, the auth token → `<TOKEN>`, and `:17871`/`:17872` → `:<SELF_PORT>`
(self-port inside URLs, e.g. `network.status.accessUrl`).

## Environment/harness notes (honest disclosures)

1. **cwd = checkout for both servers.** The §5.1/§5.2 recipes run from the
   checkout; this is load-bearing for the ORIGINAL — its extension registry
   scans `$CWD/extensions` (`server/index.ts:224-228`) and the
   `availableClis` detection specs derive from those manifests. A first sweep
   run with an isolated cwd made the original return an EMPTY extension
   registry and `availableClis: {}` — a harness artifact, corrected.
2. **Checkout `.env` neutralized** for the node server via
   `DOTENV_CONFIG_PATH` pointing at a nonexistent file (explicit env always
   wins anyway; this prevents asymmetric keys like `GEMINI_API_KEY` flipping
   `featureFlags.aiEnabled` on one side only). `KILROY_ENABLED=0` pinned on
   both sides.
3. **Session seeding:** both homes were seeded identically from
   `test/fixtures/sessions/healthy.jsonl` (per task charter) **plus one
   synthesized realistic Claude session**. Reason: the repair fixtures are not
   valid coding-cli sessions (plain-string `message` fields, non-UUID
   `session_id`) — verified empirically with an at-boot probe: the node
   original never surfaces them in `/api/session-directory` under any flag.
   Without a realistic session the seeded entries/filters/revision path would
   compare empty-vs-empty and prove nothing. The fixture seeding is retained
   and pinned a real divergence (R10b: the rust port DOES surface the fixture
   under `includeNonInteractive`/`includeEmpty`).
4. **`settings.updated-broadcast.put` row caveat:** both servers emit a
   `settings.updated`-typed frame during the post-hello handshake snapshot;
   the PUT-row broadcast capture picked up that handshake frame (values are
   pre-PUT on both sides). The clean PUT-triggered-broadcast comparison is the
   **`.patch` row** (observers cleared between). The row verdicts stand as
   recorded; interpret the `.put` row as handshake-snapshot parity evidence.

## Systematic divergence families

### S1 — JSON `Content-Type` charset suffix (63 cases where it is the ONLY diff)

- node/Express: `application/json; charset=utf-8`
- rust: `application/json`

Same JSON bodies after normalization. One root cause, repeated across nearly
every JSON endpoint. Every affected row is still recorded as DIVERGENCE (the
comparison contract includes `content-type`); adjudication of whether the port
must emit the charset suffix is out of scope for this task.

### S2 — Static/SPA `Content-Type` differences

| Case | node | rust |
|---|---|---|
| `spa.root`, `spa.deeplink`, `spa.nonasset.missing` (SPA html) | `text/html; charset=UTF-8` | `text/html; charset=utf-8` (case) |
| `spa.asset.real` (`.js` asset — bytes sha256-equal: `184c0414…`) | `application/javascript; charset=UTF-8` | `text/javascript; charset=utf-8` |
| `spa.asset.missing` (404 body `Not found`) | `text/html; charset=utf-8` | `text/plain; charset=utf-8` |

Bodies were byte-identical in all SPA cases (index.html, assets, 404 text);
only the header differs. Cache-control headers (no-store for index, immutable
for hashed assets) MATCHED.

### S3 — WS close semantics (3 cases: `ws.badtoken`, `ws.notoken`, `ws.protomismatch`)

Error FRAMES are identical (`NOT_AUTHENTICATED`/`Invalid token`,
`PROTOCOL_MISMATCH`/`Expected protocol version 7. Please reload the page.`).
But the node original then closes with a proper WebSocket close frame
(code **4001** reason `Invalid token`; code **4010** reason
`Protocol version mismatch`), while the rust port drops the connection without
a close frame — the client observes **1006** (abnormal closure), empty reason.

## Substantive findings (R1–R14)

### R1 — `PUT /api/settings` is unimplemented on the rust port (405, empty body)

The original treats PUT and PATCH identically (`server/settings-router.ts:151-152`).
The port returns **405 with an empty body** for every PUT — including all
validation-error requests, so the enum/type/strict-schema error shapes could
only be exercised via node:

| Case | node | rust |
|---|---|---|
| `settings.put.happy` (`{safety:{autoKillIdleMinutes:20}}`) | 200 + updated settings | **405**, empty |
| `settings.put.enum-invalid` (`editor.externalEditor='bogus'`) | 400 `{error:'Invalid request', details:[zod invalid_value…]}` | **405**, empty |
| `settings.put.type-invalid`, `.nested-enum-invalid`, `.unknown-key`, `.client-key-rejected` | 400 + zod details | **405**, empty |
| `settings.put.agentchat-migrated` | 400 `agentChat settings have been migrated; use freshAgent` | **405**, empty |

### R2 — rust settings PATCH does not reach the served/persisted settings state

Observed chain (all from the results JSON, in execution order):

1. `settings.patch.happy` (`autoKillIdleMinutes:25`): **both 200**, rust
   response body reflects 25 and rust broadcasts `settings.updated` with 25
   (`settings.updated-broadcast.patch`).
2. `settings.get.after-put`: node `safety.autoKillIdleMinutes=25`, **rust=15
   (default)** — the GET does not see the patch the PATCH response reported.
3. `settings.configjson-shape` (direct scratch-home read): node config.json
   contains the full settings tree + `projectColors`/`recentDirectories`/
   `sessionOverrides`/`terminalOverrides`/`serverSecrets`; **rust config.json
   still contains only the pre-seeded `network` block** — no patched settings
   persisted.
4. `settings.persist.restart` (after full restart): node still `25` +
   `allowedFilePaths:[]`; **rust back to `15`, no `allowedFilePaths`** — the
   patch did not survive restart.

### R3 — `allowedFilePaths` sandbox is not enforced by the rust port (security-relevant)

After `settings.patch.sandbox-on` (`allowedFilePaths:['~/qa-files']`, both 200,
both responses echoing the sandbox):

| Case | node | rust |
|---|---|---|
| `files.sandbox.allowed` (inside) | 200 | 200 (parity) |
| `files.sandbox.denied-read` (`~/outside.txt`) | **403** `{error:'Path not allowed'}` | **200 + file content** |
| `files.sandbox.denied-complete` (`~/`) | **403** | **200 + directory listing** |
| `files.sandbox.denied-write` (`~/outside2.txt`) | **403** | **200, file written** |
| `files.sandbox.denied-mkdir` (`~/qa-outside-dir`) | **403** | **200, dir created** |

Given R2, it is not distinguishable from the REST surface alone whether
enforcement is missing or the patched sandbox config simply never reaches the
enforcement path — either way the served behavior diverges and reads/writes
outside the sandbox succeed on the port.

### R4 — `codingCli.knownProviders` not tracked by the rust port

Node populates `knownProviders: ["claude","codex","gemini","kimi","opencode"]`
(from the extension manifests) in GET/PATCH responses, broadcasts, and
persisted config; rust always reports `knownProviders: []`. Affects
`settings.get.default`, `.get.after-put`, `.patch.*`, both broadcast rows,
`settings.persist.restart`, `settings.configjson-shape`.

### R5 — `GET /api/version` `updateCheck`: rust is always `null`

Node returns a live update-check object (masked `<OPAQUE>` — live GitHub
data); rust returns `null`. `currentVersion` itself is normalized (`version`
family). Cases: `version.happy`, `version.cookie-auth`.

### R6 — `GET /api/extensions/:name` unimplemented on the rust port

- `extensions.single` (`/api/extensions/claude`): node 200 + full
  `ClientExtensionEntry`; rust **404 `{error:'Not found'}`**.
- `extensions.single.missing` (`/api/extensions/does-not-exist`): both 404,
  but node `{error:"Extension not found: 'does-not-exist'"}` vs rust
  `{error:'Not found'}`.
- The LIST route (`extensions.list`, the §7.C "5-entry registry") matched
  exactly (S1-only) once the recipe cwd was used.

### R7 — `POST /api/files/mkdir` `existed` flag semantics

Re-creating an existing directory (`files.mkdir.again`): node
`{created:true, existed:false}` (recursive mkdir cannot detect pre-existence);
rust `{created:true, existed:true}`. First creation and the 409-over-file
case matched (S1-only).

### R8 — `GET /api/files/candidate-dirs`: rust adds the home directory

Empty-state candidates: node `{directories:[]}`; rust
`{directories:["<SCRATCH_HOME>"]}`. (Same divergence pre/post session seed.)

### R9 — `/api/session-directory` query validation: node strict, rust lenient

| Case | node | rust |
|---|---|---|
| `sd.nopriority` (no `priority`) | **400** `{error:'Invalid request', details:[priority invalid_value]}` — priority is REQUIRED | **200** empty page (defaults) |
| `sd.badlimit` (`limit=abc`) | **400** zod invalid_type | **200** (ignores) |
| `sd.badpriority` (`priority=bogus`) | **400** | **200** |
| `sd.badcursor` (`cursor=garbage`) | 400 `{error:'Invalid session-directory cursor'}` | 400 same message — **parity** (S1-only) |

### R10 — seeded session-directory entries

With the realistic seeded session surfaced on BOTH sides (via live watcher,
no restart needed — `seeded session surfaced via watcher: node=true rust=true`):

- **R10a** `items[0].archived`: node emits `archived:false`; rust omits the
  key (`sd.seeded.happy`, `.query`, `.limit`, `.include-flags`).
- **R10b** `sd.seeded.include-flags`
  (`includeSubagents&includeNonInteractive&includeEmpty=true`): rust
  additionally surfaces the repair fixture as
  `{sessionId:'11111111-…', projectPath:'unknown', isNonInteractive:true}`;
  node never surfaces it under any flag combination.
- Everything else in the seeded pages matched: sessionId, provider,
  projectPath `/home/qa/demo`, title/firstUserMessage, messageCount,
  query filtering (`sd.seeded.query`/`query-nomatch`), `limit=1` slicing.

### R11 — `POST /api/screenshots` error precedence

The rust port checks for a screenshot-capable client BEFORE name/path
validation and the no-overwrite conflict; the original validates first:

| Case | node | rust |
|---|---|---|
| `shots.emptyname` | **400** `name required` | **503** `No screenshot-capable UI client connected` |
| `shots.sep-in-name` | **400** `name must not contain path separators` | **503** (same) |
| `shots.dup409` (pre-existing output, no overwrite) | **409** `output file already exists (use --overwrite)` | **503** (same) |

With a participating client connected, the full round-trip MATCHED: ok
envelope (incl. `path`/`width`/`height`/focus flags), the `ui.command`
frame, written PNG bytes (sha256-equal), overwrite path, and the 422
failure envelope (`qa-forced-failure`). Scope-validation 400s
(`shots.badscope`, `.pane-no-paneid`, `.tab-no-tabid`) matched (S1-only),
as did the no-client 503 for a valid request (`shots.noclient503`).

### R12 — auth vs routing precedence for unknown `/api` routes

`GET /api/definitely-not-a-route` without credentials: node **401
`{error:'Unauthorized'}`** (auth middleware runs first); rust **404
`{error:'Not found'}`** — the port discloses route existence to
unauthenticated callers. With credentials both return the same JSON 404
(`api.unknown-route` PASS).

### R13 — WS close frames (see S3) — counted once as a finding

### R14 — `settings.updated` broadcast content

The PATCH-triggered broadcast fires on BOTH sides with the same frame type
and patched value; contents diverge only by R4 (`knownProviders`). Recorded
in `settings.updated-broadcast.patch`.

## What matched (beyond the 6 all-green rows)

Because S1 poisons the strict verdict of almost every JSON case, it is worth
stating what was body-and-status equal (charset-only rows): `/api/health`
(all 7 fields, both before and after restart, incl. bad-token bypass),
`/api/platform` (platform/`availableClis` — same host truth —
hostName/featureFlags), `/api/extensions` list, the entire `/api/files/*`
happy+error matrix except R7 (read/stat/write+read-back atomic bytes/
complete/mkdir/validate-dir, 404-vs-400 semantics, directory-vs-file
errors), session-directory happy/background/badcursor/seeded-query paths,
`/api/network/status` (full NetworkStatus incl. accessUrl modulo self-port),
all proxy cases (`proxy.happy` and `proxy.cookie-auth` fully green:
X-Frame-Options/CSP stripped, content-type + custom header preserved,
identical 400/502 error shapes), screenshots validation/503/round-trip rows,
SPA cache-control behavior, `?token=` handling, deep-link fallback, and every
401 `{"error":"Unauthorized"}` auth-missing/auth-bad case.

## Deferred

None. (Two intermediate-run deferrals — extension name derivation and seeded
watcher pickup — were resolved by the recipe-cwd fix and the realistic seeded
session; the final run has 0 deferred rows.)

## Teardown / purity evidence

- Sweep self-teardown (final run): `orphan check: rust='' nodeDist='' ss=''`
  (`pgrep -x freshell-server`, `pgrep -f 'dist/server/index[.]js'`,
  `ss -ltnp | grep 1787` all empty); scratch homes, shots dir, and logs dir
  removed.
- One intermediate debugging probe (session-seeding investigation) briefly
  leaked a node server on 17878 + its scratch home when the shell split a
  backgrounded compound command; it was ownership-verified (`ps -fp`), killed
  by PID, and its home removed. Final orphan sweep is clean.
- Purity: `git diff --name-only server/ shared/ src/` → empty (verified after
  the final run; the sweep never touches those trees).

## Per-case appendix

`diff class` legend: `charset-only` = S1 is the only diff; `substantive` =
see R-findings; blank = PASS/parity.

| # | case | group | verdict | diff class / finding |
|---|---|---|---|---|
| 1 | `health.happy` | health | DIVERGENCE | charset-only (S1) |
| 2 | `health.bad-auth` | health | DIVERGENCE | charset-only (S1) |
| 3 | `version.happy` | version | DIVERGENCE | R5 + S1 |
| 4 | `version.cookie-auth` | version | DIVERGENCE | R5 + S1 |
| 5 | `version.no-auth` | version | DIVERGENCE | charset-only (S1) |
| 6 | `version.bad-auth` | version | DIVERGENCE | charset-only (S1) |
| 7 | `version.bad-cookie` | version | DIVERGENCE | charset-only (S1) |
| 8 | `platform.happy` | platform | DIVERGENCE | charset-only (S1) |
| 9 | `platform.no-auth` | platform | DIVERGENCE | charset-only (S1) |
| 10 | `platform.bad-auth` | platform | DIVERGENCE | charset-only (S1) |
| 11 | `extensions.list` | extensions | DIVERGENCE | charset-only (S1) |
| 12 | `extensions.no-auth` | extensions | DIVERGENCE | charset-only (S1) |
| 13 | `extensions.single` | extensions | DIVERGENCE | R6 |
| 14 | `extensions.single.missing` | extensions | DIVERGENCE | R6 |
| 15 | `files.read.happy` | files | DIVERGENCE | charset-only (S1) |
| 16 | `files.read.missing` | files | DIVERGENCE | charset-only (S1) |
| 17 | `files.read.directory` | files | DIVERGENCE | charset-only (S1) |
| 18 | `files.read.nopath` | files | DIVERGENCE | charset-only (S1) |
| 19 | `files.read.no-auth` | files | DIVERGENCE | charset-only (S1) |
| 20 | `files.stat.happy` | files | DIVERGENCE | charset-only (S1) |
| 21 | `files.stat.missing` | files | DIVERGENCE | charset-only (S1) |
| 22 | `files.stat.directory` | files | DIVERGENCE | charset-only (S1) |
| 23 | `files.stat.nopath` | files | DIVERGENCE | charset-only (S1) |
| 24 | `files.write.happy` | files | DIVERGENCE | charset-only (S1) |
| 25 | `files.write.readback` | files | DIVERGENCE | charset-only (S1) |
| 26 | `files.write.nopath` | files | DIVERGENCE | charset-only (S1) |
| 27 | `files.write.nocontent` | files | DIVERGENCE | charset-only (S1) |
| 28 | `files.complete.dir` | files | DIVERGENCE | charset-only (S1) |
| 29 | `files.complete.partial` | files | DIVERGENCE | charset-only (S1) |
| 30 | `files.complete.dirsonly` | files | DIVERGENCE | charset-only (S1) |
| 31 | `files.complete.noprefix` | files | DIVERGENCE | charset-only (S1) |
| 32 | `files.complete.missingdir` | files | DIVERGENCE | charset-only (S1) |
| 33 | `files.mkdir.happy` | files | DIVERGENCE | charset-only (S1) |
| 34 | `files.mkdir.again` | files | DIVERGENCE | R7 + S1 |
| 35 | `files.mkdir.overfile` | files | DIVERGENCE | charset-only (S1) |
| 36 | `files.mkdir.nopath` | files | DIVERGENCE | charset-only (S1) |
| 37 | `files.validate-dir.happy` | files | DIVERGENCE | charset-only (S1) |
| 38 | `files.validate-dir.missing` | files | DIVERGENCE | charset-only (S1) |
| 39 | `files.validate-dir.file` | files | DIVERGENCE | charset-only (S1) |
| 40 | `files.validate-dir.blank` | files | DIVERGENCE | charset-only (S1) |
| 41 | `files.validate-dir.nopath` | files | DIVERGENCE | charset-only (S1) |
| 42 | `files.candidate-dirs` | files | DIVERGENCE | R8 + S1 |
| 43 | `sd.empty.happy` | session-directory | DIVERGENCE | charset-only (S1) |
| 44 | `sd.empty.background` | session-directory | DIVERGENCE | charset-only (S1) |
| 45 | `sd.nopriority` | session-directory | DIVERGENCE | R9 + S1 |
| 46 | `sd.badlimit` | session-directory | DIVERGENCE | R9 + S1 |
| 47 | `sd.badpriority` | session-directory | DIVERGENCE | R9 + S1 |
| 48 | `sd.badcursor` | session-directory | DIVERGENCE | charset-only (S1) |
| 49 | `sd.no-auth` | session-directory | DIVERGENCE | charset-only (S1) |
| 50 | `sd.seeded.happy` | session-directory | DIVERGENCE | R10a + S1 |
| 51 | `sd.seeded.query` | session-directory | DIVERGENCE | R10a + S1 |
| 52 | `sd.seeded.query-nomatch` | session-directory | DIVERGENCE | charset-only (S1) |
| 53 | `sd.seeded.include-flags` | session-directory | DIVERGENCE | R10a, R10b + S1 |
| 54 | `sd.seeded.limit` | session-directory | DIVERGENCE | R10a + S1 |
| 55 | `network.status.happy` | network | DIVERGENCE | charset-only (S1) |
| 56 | `network.status.no-auth` | network | DIVERGENCE | charset-only (S1) |
| 57 | `proxy.happy` | proxy | PASS | — |
| 58 | `proxy.cookie-auth` | proxy | PASS | — |
| 59 | `proxy.no-auth` | proxy | DIVERGENCE | charset-only (S1) |
| 60 | `proxy.badport.oversize` | proxy | DIVERGENCE | charset-only (S1) |
| 61 | `proxy.badport.nan` | proxy | DIVERGENCE | charset-only (S1) |
| 62 | `proxy.deadport` | proxy | DIVERGENCE | charset-only (S1) |
| 63 | `shots.badscope` | screenshots | DIVERGENCE | charset-only (S1) |
| 64 | `shots.pane-no-paneid` | screenshots | DIVERGENCE | charset-only (S1) |
| 65 | `shots.tab-no-tabid` | screenshots | DIVERGENCE | charset-only (S1) |
| 66 | `shots.emptyname` | screenshots | DIVERGENCE | R11 + S1 |
| 67 | `shots.sep-in-name` | screenshots | DIVERGENCE | R11 + S1 |
| 68 | `shots.dup409` | screenshots | DIVERGENCE | R11 + S1 |
| 69 | `shots.noclient503` | screenshots | DIVERGENCE | charset-only (S1) |
| 70 | `shots.no-auth` | screenshots | DIVERGENCE | charset-only (S1) |
| 71 | `shots.roundtrip.ok` | screenshots | DIVERGENCE | charset-only (S1) |
| 72 | `shots.roundtrip.ui-command-frame` | screenshots | PASS | — |
| 73 | `shots.roundtrip.file-bytes` | screenshots | PASS | — |
| 74 | `shots.roundtrip.overwrite` | screenshots | DIVERGENCE | charset-only (S1) |
| 75 | `shots.roundtrip.422` | screenshots | DIVERGENCE | charset-only (S1) |
| 76 | `settings.get.default` | settings | DIVERGENCE | R4 + S1 |
| 77 | `settings.no-auth` | settings | DIVERGENCE | charset-only (S1) |
| 78 | `settings.put.happy` | settings | DIVERGENCE | R1 |
| 79 | `settings.updated-broadcast.put` | settings | DIVERGENCE | R4 (see caveat) |
| 80 | `settings.patch.happy` | settings | DIVERGENCE | R4 + S1 |
| 81 | `settings.updated-broadcast.patch` | settings | DIVERGENCE | R4, R14 |
| 82 | `settings.get.after-put` | settings | DIVERGENCE | R2, R4 + S1 |
| 83 | `settings.put.enum-invalid` | settings | DIVERGENCE | R1 |
| 84 | `settings.put.type-invalid` | settings | DIVERGENCE | R1 |
| 85 | `settings.put.agentchat-migrated` | settings | DIVERGENCE | R1 |
| 86 | `settings.put.nested-enum-invalid` | settings | DIVERGENCE | R1 |
| 87 | `settings.put.client-key-rejected` | settings | DIVERGENCE | R1 |
| 88 | `settings.put.unknown-key` | settings | DIVERGENCE | R1 |
| 89 | `settings.patch.sandbox-on` | settings | DIVERGENCE | R4 + S1 |
| 90 | `files.sandbox.allowed` | files | DIVERGENCE | charset-only (S1) |
| 91 | `files.sandbox.denied-read` | files | DIVERGENCE | R3 + S1 |
| 92 | `files.sandbox.denied-complete` | files | DIVERGENCE | R3 + S1 |
| 93 | `files.sandbox.denied-write` | files | DIVERGENCE | R3 + S1 |
| 94 | `files.sandbox.denied-mkdir` | files | DIVERGENCE | R3 + S1 |
| 95 | `settings.patch.sandbox-off` | settings | DIVERGENCE | R4 + S1 |
| 96 | `files.sandbox.cleared` | files | DIVERGENCE | charset-only (S1) |
| 97 | `settings.configjson-shape` | settings | DIVERGENCE | R2, R4 |
| 98 | `spa.root` | spa | DIVERGENCE | charset-only (S1) |
| 99 | `spa.root.token-query` | spa | DIVERGENCE | charset-only (S1) |
| 100 | `spa.deeplink` | spa | DIVERGENCE | charset-only (S1) |
| 101 | `spa.asset.real` | spa | DIVERGENCE | S2 |
| 102 | `spa.asset.missing` | spa | DIVERGENCE | S2 |
| 103 | `spa.nonasset.missing` | spa | DIVERGENCE | charset-only (S1) |
| 104 | `spa.favicon` | spa | PASS | — |
| 105 | `ws.badtoken` | ws-auth | DIVERGENCE | S3/R13 |
| 106 | `ws.notoken` | ws-auth | DIVERGENCE | S3/R13 |
| 107 | `ws.protomismatch` | ws-auth | DIVERGENCE | S3/R13 |
| 108 | `api.unknown-route` | api-404 | PASS | — |
| 109 | `api.unknown-route.no-auth` | api-404 | DIVERGENCE | R12 |
| 110 | `settings.persist.restart` | settings | DIVERGENCE | R2, R4 + S1 |
| 111 | `health.after-restart` | health | DIVERGENCE | charset-only (S1) |

---

## Resolution (post-fix pass, same day)

Continuation of the fix work: verified the prior worker's uncommitted diff (it
did not boot — see the axum-routing crash below), completed R5/R9/R10b, fixed
the fallout the R1-R4 refactor unmasked once PUT/PATCH actually worked, and
re-swept. Final: **`results-2026-07-11-postfix.json`, 110/111 PASS** (the one
remaining row is the pre-existing, already-adjudicated handshake-snapshot
caveat, not a new divergence — see below). T1 10/10. T0 4/5 (one pre-existing
harness asymmetry, not a port behavior bug — see "T0 residue" below).

### Showstopper found before any sweep could run

The uncommitted diff had **never been booted**. `crates/freshell-server/src/boot.rs`
registered `GET /api/extensions/:name` in axum-0.8 syntax (`:name`); axum 0.8
requires `{name}` and panics at router-build time with `Path segments must not
start with ':'`. Every route after it in the merged app was unreachable —
`cargo test` never caught this because the unit tests build sub-routers in
isolation, not the full merged app. Fixed: `"/api/extensions/{name}"`. This is
why step 1's `cargo test --workspace` being green was NOT sufficient evidence
the server actually ran; the sweep (which boots the real binary) is what
caught it.

### Per-finding resolution

| Finding | Root cause | Fix (file) | Regression test | Sweep status |
|---|---|---|---|---|
| **R9** session-directory validation lenient | `parse_query` silently defaulted/ignored `priority`/`limit` instead of validating the zod schema (`SessionDirectoryQuerySchema`: `priority` required, `limit: int().positive().max(50)`) | `session_directory.rs`: new `validate_query`/`validate_limit`/`js_number` (JS `Number()` coercion semantics), 400 body byte-matched against a LIVE probe of the original (zod v4 issue shapes for missing/bogus priority, NaN/fractional/out-of-range limit, and multi-issue collection order) | 11 new tests in `session_directory::tests` (`missing_priority_is_400_invalid_value`, `non_numeric_limit_is_400_invalid_type_nan`, `multiple_violations_collect_into_one_details_array_priority_then_limit`, etc.) | `sd.nopriority`/`sd.badlimit`/`sd.badpriority` PASS |
| **R10b** repair fixture leaks under include flags | The port's `list_claude_sessions` never applied the original's discovery-time gate: `session-indexer.ts:756,1124` (`if (!meta.cwd) continue`) drops any session file with no resolvable `cwd` BEFORE it ever reaches the visibility-flag filters, so a non-coding-cli file (e.g. plain-string `message` fields, no `cwd`) is invisible under every flag combination — not merely hidden by default filters | `session_directory.rs`: `parse_claude_file` now returns `None` when `meta.cwd` is absent | `r10b_cwdless_repair_fixture_never_surfaces_under_any_flags` (reproduces the exact sweep seed: `healthy.jsonl` renamed to a canonical-UUID filename) + updated `default_query_hides_non_interactive_fixtures`/`include_empty_surfaces_untitled_sessions_sorted_desc` (list count now 1, not 2) | `sd.seeded.include-flags` PASS |
| **R5** `updateCheck` always `null` | Static `null`; the original does a LIVE `https://api.github.com/.../releases/latest` call (`version-checker.ts`) and in this environment (real internet egress, verified by probe) returns a real non-null object — a static `null` was an honest behavior gap, not an environment limitation | New `updater.rs`: `reqwest` + `rustls` (crypto-provider-bearing feature, not `-no-provider`), 1:1 port of `parseVersion`/`isMinorOrMajorNewer`/`checkForUpdate`, 10-minute success-only cache (`createCachedUpdateChecker`). Wired into `boot.rs`'s `version` handler via new `BootState.update_checker` | `updater::tests` (6 tests: pure semver-compare logic network-free; live-call shape invariants; cache-reuse) | `version.happy`/`version.cookie-auth` PASS |
| Dead code | `boot::get_settings` (superseded by `settings_store::get_settings`) and `settings::load_server_settings` (superseded by `settings_store::load_full_settings`) were unreachable after the R2 refactor | Removed both; migrated the golden-fixture parity test (`default_plus_network_overlay_matches_captured_fixture`) onto the LIVE `load_full_settings` path instead of deleting its coverage | `settings_store::tests::default_plus_network_overlay_matches_captured_fixture` | n/a (no wire behavior) |
| **Fallout A** wrong enum values in `PUT`/`PATCH` validation | `settings_store.rs`'s `validate_patch` hardcoded `externalEditor` values as `["auto","vscode","custom"]` and `defaultNewPane` as `["ask","terminal","browser"]` — both wrong (real: `EXTERNAL_EDITOR_VALUES = [auto,cursor,code,custom]`, `DEFAULT_NEW_PANE_VALUES = [ask,shell,browser,editor]`, `shared/settings.ts:25,33`). Masked before R1 landed (PUT was a blanket 405, so these branches never ran) | Corrected both VALID arrays | `enum_values_and_unrecognized_key_message_match_the_original` | `settings.put.enum-invalid`/`.nested-enum-invalid` PASS |
| **Fallout B** wrong "unrecognized key" message + spurious `received` field | `validate_patch`'s unknown-key message was zod's stock plural wording (`"Unrecognized key(s) in object: 'x'"`); the original overrides it to singular (`"Unrecognized key: \"x\""`, `buildServerSettingsPatchSchema`). `enum_details` also added a `received` field the original's zod enum issue never carries | Fixed message string; dropped `received` from `enum_details` | (same test as above) | `settings.put.client-key-rejected`/`.unknown-key` PASS |
| **Fallout C** `serverSecrets.codexDisplayIdSecret` never persisted | `SettingsStore::persist()` wrote `sessionOverrides`/`terminalOverrides`/`projectColors`/`recentDirectories` but no `serverSecrets` sibling; the original always persists a per-boot generated secret there (`config-store.ts`) | Added `codex_display_id_secret` (minted once, read back from an existing `config.json` so it survives restart) to `SettingsStore` + `persist()` | (covered implicitly by the sweep; value itself is oracle-`opaque`-normalized, only presence matters) | `settings.configjson-shape` PASS |
| **Fallout D** T0 handshake `knownProviders` divergence | `known_providers` (fed into `SettingsStore::load`) was derived via `cli_detection_specs()`, which — correctly, for `availableClis` CLI-probing — falls back to `DEFAULT_CLI_DETECTION_SPECS` when no extension manifests are found. Reusing that SAME fallback-including value for `codingCli.knownProviders` is wrong: the original seeds `knownProviders` strictly from genuinely-discovered extension manifests (`server/index.ts:276-294`), with NO fallback, so it's genuinely empty when none are found | `extensions.rs`: new `discovered_cli_names()` (no fallback). `main.rs`: `known_providers` now scanned from a `known_provider_dirs` list mirroring the original's cwd/home-relative discovery ONLY (`userExtDir`/`localExtDir`/`builtinExtDir`, `server/index.ts:225-227`) — excluding the Rust-only compiled-in `CARGO_MANIFEST_DIR` dev/test fallback that `resolve_extension_dirs` also carries (kept, unchanged, for `availableClis`) | (behavior change only; covered by the sweep's `settings.get.default` et al., which run with cwd=checkout so both paths agree there) | sweep unaffected (cwd=checkout); see **T0 residue** below |

### `.put`-broadcast row — CORRECTED (additive, does not delete the history above)

> **Correction below per antagonist adjudication
> `0000000000000000-dc849de1bd584a39_self-driving-reviewer` (2026-07-11,
> RULING 3(b)): the claim immediately above (in the original "Resolution"
> pass) — "`settings.updated-broadcast.put` still shows `node=20 rust=15` …
> unchanged behavior" — is FACTUALLY WRONG.** It was written by generalizing
> from the FIRST sweep run's `.put` row (which genuinely did diff on
> `knownProviders[0..4]`, a real pre-R1-R4-fix divergence) without re-checking
> what the **postfix** run's `.put` row actually diffed on. They are not the
> same divergence:
> - **Original run** (`results-2026-07-11.json`): `.put` row diverged on
>   `codingCli.knownProviders` (node `[]` vs rust `["claude","codex","gemini","kimi","opencode"]`)
>   — a real behavior gap, R4, fixed in this same pass.
> - **Postfix run** (`results-2026-07-11-postfix.json`): R4 was already fixed
>   by then, so `knownProviders` agreed on both sides. The row instead diverged
>   on `safety.autoKillIdleMinutes` (**node `20`, rust `15`** — the literal
>   values the "corrected" sentence quoted, but misattributed to
>   `knownProviders`). Root cause (confirmed by re-reading the pre-fix
>   `sweep.mjs`, §"Environment/harness notes" #4 above): the `.put` row's
>   `obsNode.clear()`/`obsRust.clear()` ran immediately after `connect()`,
>   racing each side's own still-in-flight post-hello handshake `settings.updated`
>   snapshot frame (value `15`, the pre-PUT default). Depending on exact
>   arrival timing, `waitForType('settings.updated')` could return that stale
>   handshake snapshot instead of the PUT-triggered broadcast (value `20`) —
>   and it did, asymmetrically, on the postfix run's node leg. This was a
>   **capture-timing race in the harness**, not a served-behavior divergence.
>
> **Fix applied (RULING 3(a), `sweep.mjs` ~lines 936-951):** before the `.put`
> row's `clear()`, explicitly wait for and consume each side's post-hello
> handshake `settings.updated` frame first — mirroring the `.patch` row's own
> protocol (which only clears AFTER the prior expected frame has been
> observed) — so the subsequent `clear()` drains a frame known to have already
> arrived instead of racing one still in flight.
>
> **Re-run (RULING 3(c), `results-2026-07-11-postfix2.json`): `settings.updated-broadcast.put`
> now PASSES deterministically** (node and rust both report `20`, the
> PUT-triggered value) — full sweep **111/111 PASS**. See "Postfix2" section
> below for the run transcript.

Original (now-corrected) claim, retained for history: `settings.updated-broadcast.put`
"still shows `node=20 rust=15`". As documented above (§"`settings.updated-broadcast.put`
row caveat"), both servers' `.put`-row capture actually caught the POST-HELLO
HANDSHAKE snapshot frame, not a PUT-triggered broadcast — the clean comparison
was said to be the `.patch` row (`settings.updated-broadcast.patch`), which
PASSES. **This framing conflated the two different sweep runs' distinct
divergences (see the correction above) and is superseded by it.**

### T0 residue: `knownProviders` harness asymmetry — RESOLVED (RULING 1)

> **Update per antagonist adjudication
> `0000000000000000-dc849de1bd584a39_self-driving-reviewer` (2026-07-11):
> RULING 1 authorized the harness-symmetry fix option (a) proposed just below
> — `startRustServer` now spawns from the SAME kind of isolated scratch root
> `startNodeServer` uses (mkdtemp under `<PROJECT_ROOT>/.worktrees/`, copying
> only `package.json` + `dist/`, no `extensions/`), cleaned up in `stop()`
> alongside `homeDir`/`probeHome`. `t0-equivalence-rust.test.ts` now passes
> case (c) and is **5/5**.**
>
> RULING 2 additionally required proof the comparator would still catch a
> real `knownProviders` divergence rather than passing vacuously on `[]≡[]`:
> the new `test/unit/port/oracle/t0-known-providers-discovery-rust.test.ts`
> boots BOTH targets with the identical cwd semantics pointed at the REAL
> checkout instead (a new `cwdMode: 'project'` option on
> `startExternalServer`, exercising the genuinely non-empty
> `extensions/` dir on both sides) and asserts (i) the normalized handshakes
> still deep-equal there too, and (ii) the node reference's
> `codingCli.knownProviders` is non-empty (a rot-guard against a
> vacuous `[]≡[]` pass).
>
> **Mutation demonstration (temporary, not left in the tree):** reverted
> `crates/freshell-server/src/main.rs`'s `known_providers` derivation to
> `Vec::new()` (the pre-R4-fix HEAD behavior), rebuilt
> (`cargo build --release -p freshell-server`), and re-ran the new case:
>
> ```
> $ cargo build --release -p freshell-server   # with known_providers = Vec::new()
>    Finished `release` profile [optimized] target(s) in 9.34s
> $ npx vitest run --config config/vitest/vitest.oracle.config.ts \
>     test/unit/port/oracle/t0-known-providers-discovery-rust.test.ts
> [T0-knownProviders] original pid=98411 knownProviders=["claude","codex","gemini","kimi","opencode"]
> [T0-knownProviders] rust     pid=98442 knownProviders=[]
> [T0-knownProviders] original≡rust deep-equal: false
>  ❯ (i) ... DEEP-EQUAL after normalization (real, non-empty extensions/)
>    → expected false to be true
>  Test Files  1 failed (1)
>       Tests  1 failed | 3 passed (4)
> ```
>
> Restored the fix (`known_providers` back to
> `extensions::ExtensionRegistry::scan(&known_provider_dirs).discovered_cli_names()`),
> rebuilt, and re-ran:
>
> ```
> $ cargo build --release -p freshell-server   # fix restored
>    Finished `release` profile [optimized] target(s) in 7.19s
> $ npx vitest run --config config/vitest/vitest.oracle.config.ts \
>     test/unit/port/oracle/t0-known-providers-discovery-rust.test.ts \
>     test/unit/port/oracle/t0-equivalence-rust.test.ts
> [T0-knownProviders] original≡rust deep-equal: true
>  ✓ t0-known-providers-discovery-rust.test.ts (4 tests)
> [T0-eqv] original≡rust deep-equal: true
>  ✓ t0-equivalence-rust.test.ts (5 tests)
>  Test Files  2 passed (2)
>       Tests  9 passed (9)
> ```
>
> Proof: the new case is not a tautology — it FAILS when the port's
> `knownProviders` derivation regresses, and PASSES with the fix in place. The
> mutation was reverted before finishing; `git diff` on `main.rs` shows only
> the durable R4/Fallout-D fix, no trace of the temporary `Vec::new()`.

The section below is retained for history (it predates RULING 1 and correctly
diagnosed the root cause, but its "Not fixed" framing is superseded):

### T0 residue: `knownProviders` harness asymmetry (not fixed — reported, not papered over)

`test/unit/port/oracle/t0-equivalence-rust.test.ts` still fails case (c) on
`settings.codingCli.knownProviders`: original `[]`, rust
`["claude","codex","gemini","kimi","opencode"]`. Root cause, traced (not
guessed): `port/oracle/harness/external-server.ts`'s `startRustServer` spawns
the rust binary with `cwd: PROJECT_ROOT` (the real checkout, whose real
`extensions/` dir the fix above now faithfully discovers), while
`startNodeServer` boots the original through `TestServer`
(`test/e2e-browser/helpers/test-server.ts`), which spawns with
`cwd: runtimeRoot` — an **isolated** scratch root with no `extensions/` dir.
Both servers are faithfully cwd-relative (matching `server/index.ts:225-227`
exactly, per Fallout D above); the two harness functions simply hand them
**different cwds**. This asymmetry pre-dates this pass (`startRustServer`'s
`cwd: PROJECT_ROOT` looks deliberate, per its own doc comment, to make the
retained SPA/other paths resolvable) and was invisible before because rust's
`knownProviders` was unconditionally `[]` (the R1-R4 bug) — coincidentally
matching whatever the isolated-cwd original showed. Now that the port
correctly discovers real extensions when its cwd actually has them, the
harness's cwd asymmetry surfaces as a spurious T0 divergence.

**Not fixed, by design of this task's rules**: `port/oracle/harness/*` is
oracle-harness infrastructure ("never weaken an oracle assertion"; the
explicit instruction not to edit oracle test files extends in spirit to the
harness it depends on), and I was not asked to adjudicate or repair the
harness itself. Reported here as a **deviation candidate** for adversarial
review: either (a) `startRustServer` should spawn with the same isolated
`runtimeRoot` cwd `startNodeServer` uses (true harness-symmetry fix), or (b)
if `cwd: PROJECT_ROOT` is intentional for another reason, T0's normalization
registry should mask `knownProviders` the same way `codingCli.knownProviders`
elsewhere already tolerates environment-dependent content. The REST-parity
sweep itself is unaffected (both servers share `cwd = checkout` there by
recipe, so both genuinely see the same real extensions and agree) — this is
strictly a T0-harness-specific artifact, not a rust-server behavior bug.

### Final verification evidence

- `cargo test --workspace --exclude freshell-tauri`: all green (freshell-server
  alone: 77 unit tests, up from the pre-existing baseline).
- `cargo build --release -p freshell-server` and
  `cargo build --release -p freshell-server --target x86_64-pc-windows-gnu`:
  both succeed (`file` confirms the `.exe` is a valid stripped PE32+).
- `cargo test -p freshell-tauri`: 144 + 1 (the `server_spawn_smoke` e2e test,
  which needed a `target/debug/freshell-server` rebuild to pick up the axum
  route fix) — all green; `main.rs` changes did not regress it.
- Oracle T1 (`t1-equivalence-rust.test.ts`): **10/10 PASS**.
- Oracle T0 (`t0-equivalence-rust.test.ts`): **4/5** — see residue above.
- Sweep (`results-2026-07-11-postfix.json`): **110/111 PASS** (1 pre-existing,
  already-adjudicated `.put`-row handshake-snapshot caveat).
- Purity: `git diff --name-only server/ shared/ src/` → empty.
- Orphan sweep (post-sweep, printed by the harness itself): `rust='' nodeDist=''
  ss=''`.

## Postfix2 — RULING 1–4 implementation (2026-07-11, same day)

Continuation implementing the antagonist's four rulings (adjudication session
`0000000000000000-dc849de1bd584a39_self-driving-reviewer`, 2026-07-11) on top
of the postfix pass above.

### RULING 1 — T0 harness cwd parity

`port/oracle/harness/external-server.ts`'s `startRustServer` now spawns from
an isolated scratch root (`createIsolatedRustRuntimeRoot`: mkdtemp under
`<PROJECT_ROOT>/.worktrees/`, copying only `package.json` + `dist/`, no
`extensions/`), matching `startNodeServer`'s isolated `TestServer` runtime
root exactly. Cleaned up in `stop()` alongside `homeDir`/`probeHome`. Result:

```
$ npx vitest run --config config/vitest/vitest.oracle.config.ts \
    test/unit/port/oracle/t0-equivalence-rust.test.ts
[T0-eqv] original≡rust deep-equal: true
 Test Files  1 passed (1)
      Tests  5 passed (5)
```

T0 is now **5/5** (was 4/5). See the "T0 residue — RESOLVED" section above for
full detail.

### RULING 2 — comparator bite-proof

New `test/unit/port/oracle/t0-known-providers-discovery-rust.test.ts` +
a new `cwdMode: 'project'` option on `startExternalServer` (both targets boot
from the real checkout instead, so a genuinely non-empty `extensions/` dir is
discovered by both). Mutation-demonstration transcript (revert → fail →
restore → pass) is recorded in the "T0 residue — RESOLVED" section above; the
mutation was not left in the tree.

### RULING 3 — `.put` broadcast row hardening + report correction

`sweep.mjs`'s `.put`-row now waits for and consumes each side's post-hello
handshake `settings.updated` frame before clearing observers (mirroring the
`.patch` row's protocol), eliminating the handshake-vs-broadcast capture race.
The report's prior "unchanged behavior" claim about the `.put` row is
corrected above (§"`.put`-broadcast row — CORRECTED").

Full re-sweep:

```
$ node port/oracle/rest-parity/sweep.mjs --out port/oracle/rest-parity/results-2026-07-11-postfix2.json
REST parity sweep — node :17871 vs rust :17872
...
PASS        settings.put.happy
PASS        settings.updated-broadcast.put
PASS        settings.patch.happy
PASS        settings.updated-broadcast.patch
...
summary: 111 cases — 111 pass, 0 diverge, 0 deferred
orphan check: rust='' nodeDist='' ss=''
results written to port/oracle/rest-parity/results-2026-07-11-postfix2.json
```

**111/111 PASS** — no residue; `.put` row is deterministic. Machine results:
`port/oracle/rest-parity/results-2026-07-11-postfix2.json`.

### RULING 4 — updater timeout deviation ledger entry

`crates/freshell-server/src/updater.rs`'s 5-second request timeout on the
live GitHub update-check (absent from the original's un-timed fetch) is
recorded as **DEV-0004** in `port/oracle/DEVIATIONS.md`.

### Final gate evidence (this pass)

- `cargo build --release -p freshell-server`: succeeds.
- `cargo build --release -p freshell-server --target x86_64-pc-windows-gnu`: succeeds.
- Oracle (all 6 files incl. the two new T0 files): all green — see command
  transcript in the delivery report.
- `cargo test --workspace --exclude freshell-tauri`: green.
- `cargo test -p freshell-tauri`: green.
- Sweep: **111/111 PASS** (`results-2026-07-11-postfix2.json`).
- Purity: `git diff --name-only server/ shared/ src/` → empty.
- Orphan sweep: `pgrep -x freshell-server` / `pgrep -f 'dist/server/index[.]js'` /
  `ss -ltnp | grep 1787` all empty; no leftover `.worktrees/` mkdtemp dirs or
  scratch homes.

---

## task-005e addendum (2026-07-12) — owed T3 spot-check + REST spot-sweep vs the NATIVE-WINDOWS server (17873)

### Owed T3 spot-check (post-44a4dc4d /api/terminals change): NO REGRESSION — EQUIVALENT

`terminal-lifecycle.spec.ts` + `terminal-background-freeze-catchup.spec.ts` +
`sidebar.spec.ts` (22 tests) run standalone against FRESH servers via the T3
target config: rust-WSL 17872 → **2 pass / 20 fail**; ORIGINAL 17871 (same
seeding, same subset) → **2 pass / 20 fail** with **test-by-test IDENTICAL
verdicts** (diff of per-test verdict lists empty). The 20 standalone failures
are a SUITE-ORDER artifact, not a server defect: on a fresh server with default
settings (`panes.defaultNewPane: "ask"`) the first-load tab shows the pane-type
picker (no auto `.xterm`), and these specs implicitly depend on state left by
earlier specs in the full alphabetical single-worker run (the 118/8 full-suite
baseline of task-003/004b, where they pass on both sides, still stands).
Fail-identically-on-both ⇒ equivalence holds; the 44a4dc4d terminals change
introduced no regression. Logs: `~/freshell-scratch-005d/t3-spot-{rust,orig}.log`.

### Spot-sweep vs 17873 (§5.3 boot, cmd set-wrapper, reached at $WINIP) — findings

Two REAL parity gaps found (unreachable by the WSL sweep until now — neither
`/api/bootstrap` nor `POST /api/logs/client` had sweep cases) and **fixed in
this commit**, ground-truthed against the LIVE original:

1. **`GET /api/bootstrap`** — rust omitted `shell.ready`, `shell.tasks`
   (`{sessionRepairService, codingCliIndexer}` — `startupState.snapshot().tasks`,
   `server/index.ts:191,886,901`) and the top-level `perf: {logging}` key
   (`getPerfLogging` → `parseBoolean(PERF_LOGGING)||parseBoolean(PERF_DEBUG)`,
   `perf-logger.ts:20-35`). Fixed in `boot.rs` (steady-state task snapshot; the
   port completes equivalent init before binding). Key order + shapes verified
   equal vs live original on BOTH rust builds.
2. **`POST /api/logs/client`** — rust accepted ANY body with `200 {ok:true}`;
   the original strictly validates `ClientLogsPayloadSchema`
   (`server/client-logs.ts:6-29`) → `400 {error:"Invalid request", details:
   <zod-v4 issues>}` and **204 No Content** on success. Fixed in `boot.rs` with
   exact zod-v4 issue objects (invalid_type/invalid_value/too_small/too_big/
   unrecognized_keys incl. issue ORDER: client → per-entry fields in schema
   order → strict-keys last; express strict-JSON 400-HTML for non-object/array
   top-level, empty body = `{}`). Ground truth: 21-case live battery — 21/21
   byte-identical after fix (WSL build), 4-case re-probe vs 17873 (win build)
   identical.

Sweep extended: `bootstrap` group (2 cases) + `logsclient` group (12 cases) →
**166/166 PASS** (`results-2026-07-12.json`). Oracle T0/T1/batch/mutation
98/98; cargo workspace green; BOTH release binaries rebuilt.

Host-legit / launch-condition diffs vs 17873 (verified, not defects):
- `platform`/`bootstrap.platform`: `platform: "win32"`, `availableClis.opencode:
  false` (opencode absent on Windows) — host-legit.
- `extensions: []` + `settings.codingCli` extension-derived content: the
  original scans `$CWD/extensions` (`index.ts:227`); the win server runs with
  cwd=`%TEMP%` (no manifests). PROVEN launch-condition-legit by booting the
  ORIGINAL with a fresh home + cwd WITHOUT an `extensions/` dir → `extensions:
  []` identically.
- `network/status`: port/accessUrl/`remoteAccessRequested:true` (bind
  0.0.0.0)/`platform:"windows"`/netsh suggestion — config/host-legit.

### OPEN items (carried to the 005e close-out)

- **`codingCli.knownProviders` under cwd-neutral boot — RESOLVED 2026-07-12
  (005e part 2)**. The prior "cwd-neutral original reports 5 providers while
  `/api/extensions` is `[]`" observation was an ARTIFACT: a re-run of the exact
  probe (fresh `$HOME` scratch, cwd WITHOUT `extensions/`,
  `NODE_ENV=production node <repo>/dist/server/index.js`, port 17875) shows the
  original serves `extensions: []`, `knownProviders: []`, `availableClis: {}` —
  internally consistent. The earlier probe's launch call almost certainly ran
  with cwd=repo (each shell call resets cwd to the repo checkout; the leftover
  `.freshell-qa-orig005e*` homes all contain the 5-name seed, which a cwd=repo
  boot reproduces exactly: `extensions` AND `knownProviders` both = 5). The
  REAL mechanism, pinned by reading `server/index.ts:224-299` and a two-server
  live differential:
  * `builtinExtDir = path.join(process.cwd(), 'extensions')` — the "builtin"
    extension dir is CWD-RELATIVE; there is NO compiled-in path;
  * `knownProviders` is PERSISTED, PATCHABLE settings state: seeded at boot
    with the discovered CLI names when missing from config.json (even `[]`),
    grown by append when new names appear (which also auto-enables them in
    `enabledProviders`), otherwise served as persisted;
  * the settings PATCH allowlist is the boot-discovered name set
    (`validCliProviders: allCliNames`, unknown names → 400 custom zod issue).
  THREE real rust divergences found by the differential and FIXED:
  (1) `resolve_builtin_extensions_dir` had a compiled-in `CARGO_MANIFEST_DIR`
  fallback → cwd-neutral `/api/extensions` served the 5 bundled manifests where
  the original serves `[]`; (2) `cli_detection_specs()` fell back to the
  built-in CLI set → cwd-neutral `availableClis` was a 5-key map where the
  original serves `{}` (the original's `DEFAULT_CLI_DETECTION_SPECS` is a JS
  default *parameter* that never applies on the boot path); (3) `knownProviders`
  was a boot-frozen discovery snapshot, immutable via PATCH and never persisted
  → diverged on persisted-list growth/auto-enable (S3), on cwd-neutral reboot
  of a seeded home (S4), and on PATCH (patch-wins in the original). The full
  startup migration (`index.ts:271-299` + `settings-migrate.ts` legacy
  enabled-providers path) is now ported in `settings_store.rs`, provider-name
  PATCH validation added with zod-v4 issue shapes/aggregation byte-matched live
  (M1–M8/E1–E5 battery, 17/17 byte-identical rust-vs-original), scenario
  differentials green (fresh cwd-neutral, fresh cwd=repo, kp=[]→repo growth,
  legacy [claude,codex] migration, kp=["claude"] growth, seeded→cwd-neutral
  no-shrink), sweep extended +9 provider-validation/patch-wins cases →
  175/175 PASS.
- **`network/status` on native Windows**: `machineHostname: "localhost"`
  (rust reads `/proc/sys/kernel/hostname` → `HOSTNAME` env → fallback; neither
  exists on win32 — the original's `os.hostname()` works there) and
  `lanIps: []` (pre-documented unwired live edge off-WSL, `network.rs:233-240`).
  Fix hostname faithfully (win reference value obtainable via the Windows-side
  node: `os.hostname()`) or ledger as PORT-GAP.
