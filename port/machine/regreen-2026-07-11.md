# Re-green report — 2026-07-11 (SurfaceBookPro9, WSL2)

Work-queue item 2 of port/HANDOFF.md §9: re-establish the deterministic green base
for the Rust/Tauri port on this host. Branch: `feat/rust-tauri-port`.

## Host / toolchain

```
Linux SurfaceBookPro9 6.6.87.2-microsoft-standard-WSL2 x86_64 GNU/Linux (WSL2)
node v24.12.0
rustc 1.97.0 (2d8144b78 2026-07-07)
cargo 1.97.0 (c980f4866 2026-06-30)
```

## Purity invariant (frozen reference: server/, shared/, src/)

- BEFORE: `git diff --name-only server/ shared/ src/` → empty (clean)
- AFTER:  `git diff --name-only server/ shared/ src/` → empty (clean)

## Commands run, results, timing (sequential, in order)

| # | Command | Result | Wall time |
|---|---------|--------|-----------|
| 1 | `npm run build` (typecheck + client + server → dist/) | OK — vite built 3027 modules, `tsc -p tsconfig.server.json` clean | 28.6s |
| 2 | `cargo build --release -p freshell-server` | OK → `target/release/freshell-server` (10,619,560 B) | 1m33s |
| 3 | `cargo build --release -p freshell-server --target x86_64-pc-windows-gnu` | OK → `target/x86_64-pc-windows-gnu/release/freshell-server.exe` (16,728,430 B) | 1m33s |
| 4 | `cargo build -p freshell-tauri` (debug) | OK — `Finished dev profile` | 1m57s |
| 5 | Oracle deterministic suites (vitest.oracle.config.ts, 5 files) | **5 files passed, 98/98 tests passed** | 63.6s |
| 6 | `cargo test --workspace --exclude freshell-tauri` | **439 passed, 0 failed** (summed across all `test result:` lines) | 50s |
| 7 | `cargo test -p freshell-tauri` | **145 passed, 0 failed** (144 unit + 1 integration + doc-tests) | 26s |

Oracle suite detail (item 5):

```
npx vitest run --config config/vitest/vitest.oracle.config.ts \
  test/unit/port/oracle/t0-equivalence-rust.test.ts \
  test/unit/port/oracle/t1-equivalence-rust.test.ts \
  test/unit/port/oracle/t1-batch-equivalence-rust.test.ts \
  test/unit/port/oracle/mutation-validation.test.ts \
  test/unit/port/oracle/mutation-e2e.test.ts

 Test Files  5 passed (5)
      Tests  98 passed (98)
   Duration  63.59s
```

- T0: 5/5 — original≡rust handshake deep-equal: true
- T1: 10/10 — original≡rust byte-identical over the wire for all scenarios
  (echo-hello, seq-3, fixed-width-fill, multi-line; matching sha256 on both sides)
- T1 batch: green (44 batch cases inside the 98 total)
- mutation-validation + mutation-e2e: green (mutation-e2e mutated and restored
  dist/ as designed; RED→GREEN cycle completed)

Note: prior-host expectation was phrased as T0 5/5, T1 10/10, batch 44/44,
mutation 28/28 planted; this run's aggregate of 98/98 across the same 5 files
matches, with zero failures and zero skips.

## Artifact proof

```
-rwxr-xr-x 2 dan dan 10619560 Jul 11 09:14 target/release/freshell-server
-rwxr-xr-x 2 dan dan 16728430 Jul 11 09:16 target/x86_64-pc-windows-gnu/release/freshell-server.exe
```

## Orphan sweep (end of run)

```
pgrep -x freshell-server            → none
pgrep -f 'dist/server/index[.]js'   → none
```

## Anomalies

1. **pgrep self-match false positive**: the naive sweep
   `pgrep -f 'dist/server/index.js'` matched the PID of the `bash -c` wrapper
   running the pgrep itself (the pattern text appears in its own cmdline).
   Verified via `ps -fp`/`/proc` that the matched PID was the transient shell,
   not a server. Re-ran with the bracket-escape pattern
   `'dist/server/index[.]js'` → `none`. No orphans from this run.
2. **freshell-tauri debug build was fast (1m57s), not the 60-min worst case** —
   dependency artifacts were evidently already cached in `target/` from prior
   work on this host; only the tail of the dep graph (tao/muda/tray-icon/etc.)
   and the crate itself compiled.
3. No stale-build (ENV-0001) remediation was needed; every suite was green on
   first run. `cargo test --workspace --exclude freshell-tauri` was executed
   twice (second run only to produce an exact aggregate pass count: 439/0).
4. Untracked `self-driving-state/` directory exists in the repo root
   (pre-existing, unrelated to this run; left untouched). Nothing was
   committed, per task constraints.
