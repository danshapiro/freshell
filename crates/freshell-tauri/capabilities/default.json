{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the freshell main window. Grants core IPC (invoke) so the injected window.freshellDesktop shim can call the app-local `open_external_url` command. App-defined commands are not permission-gated in Tauri v2; only core/plugin commands are, hence just core:default here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
