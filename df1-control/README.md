# DF1 — massively-parallel parity-checklist campaign control plane

Created 2026-08-08. Branch `df1/control-plane` (infra only). Integration branch:
`df1/integration` (starts at `origin/main` = `4c2297667`). State lives outside git at
`~/.freshell/df1/` (survives worktree teardown).

**Status: STAGED, NOT KICKED OFF.** The user gives the explicit go. Until then: no
worker/gatekeeper agents, no item worktrees, no PRs.

## Goal

Drive every outstanding item of
`docs/plans/2026-07-14-rust-tauri-parity-completion-checklist.md` to closure using up to
32 concurrent fire-and-forget subagents (one per item), each running the full pipeline:
superpowers plan → load-bearing audit → TDD execution → verify → code-review loop (≤5
rounds or no serious findings). The orchestrator (opencode session) NEVER reads agent
work — only the small status files in `~/.freshell/df1/status/` and ≤3-line hails.

## Queue snapshot (generated by scripts/build-queue.py)

- **235 items parsed** (the docs say "233"; the file actually contains 235 unique
  checkbox IDs — documented, not silently rounded; no duplicate IDs).
- 7 already checked: HARNESS-01, HARNESS-02, SESSION-10, TERM-02, TERM-18, TERM-28, AGENT-08.
- 66 host-limited (native Windows / packaged installer / Electron profile lanes) —
  **excluded from the swarm; pending user decision on a plans-only pass.**
- **162 queued** = the swarm scope.

## Architecture

- `~/.freshell/df1/items.json` — work queue; states:
  `queued → claimed → in-progress → review → (merged-unverified-e2e | done)`,
  plus `blocked(dep)`, `needs-human`, `failed`, `host-limited`, `done`.
- `~/.freshell/df1/events/launches.jsonl` — append-only launch/kill/lease/merge ledger
  (also GATE-08 parity-receipt raw material).
- `~/.freshell/df1/status/<ID>.json` — per-item agent snapshot (phase, sha, heartbeat,
  note, tests, decisions). `updated` epoch doubles as the reaper heartbeat.
- Leases (`df1-control/scripts/acquire.sh`): atomic mkdir leases w/ TTL heartbeats,
  stale-reaping, and live system guards (MemAvailable ≥ 4G, load1 ≤ 40, disk ≥ 100G).
  | lane | cap | guards |
  |---|---:|---|
  | agent | 32 | total swarm size (ramp 8→16→24→32) |
  | provision | 2 | npm ci + first cargo build |
  | cargo | 3 | heavy cargo builds/tests |
  | pw | 4 | Playwright (RAM-bound; also system-guarded) |
  | sandbox | 2 | destructive docker suites |
  | gate | 1 | coordinated repo suite (gatekeeper only) |
- `df1ctl.py` — claim/update/requeue/set-state/report/events/item (see `--help`-style
  docstring at top of script).

## Agent roles (prompt templates in prompts/)

- **worker** (one per item, fire-and-forget): sees only its prompt + repo files.
  Commits at phase boundaries so a killed agent's worktree IS the checkpoint; a
  replacement resumes in place. Never pushes, never PRs, never edits the checklist.
- **verifier** (one per completed item): re-runs the claimed green commands at the
  claimed SHA; PASS/FAIL one-liner. The anti-hallucination layer.
- **gatekeeper** (one per merge batch of ~10): merges verifier-PASSed branches into
  `df1/integration`; per-branch spec ordering runs (pre-merge fail → post-merge green);
  batch gates (workspace cargo tests, typecheck, lint, canary set, coordinated suite).
- **reaper** (periodic): mechanical kill criteria (stale heartbeat > 60m, phase
  timeouts, review over-budget) → KILLED event + requeue. Never deletes worktrees.

## Deferred-Playwright policy (agreed 2026-08-08)

Main-phase workers mostly skip Playwright; one mass close-out campaign at the end.
Three NON-NEGOTIABLE carve-outs:
1. **Spec-deliverable items** (`pwMode: self-verify`, assigned per item at launch —
   e.g. the 29 rust-blind specs and other "Missing: re-target/PW-RUST spec only"
   PARTIALs) must run their own spec RED→green before completion.
2. **Canary set per merge batch** (gatekeeper): `harness-01-rust-server`,
   `terminal-lifecycle`, `settings-persistence-split`, `restore-matrix`,
   `server-restart-recovery`, `session-directory-matrix`, `multi-client`,
   `browser-pane` — both server kinds. Attribution window stays ~10 merges, not 160.
3. **Ordering proof at merge time**: a branch's new/changed specs run against the
   pre-merge integration tip (expect failure-per-annotation), then post-merge (green) —
   preserves the checklist's fail-before/pass-after semantics at batch granularity.

Items merged in the main phase are `merged-unverified-e2e`, NEVER `done`. Close-out
converts them one by one. Close-out also runs GATE-01's full-suite-both-kinds bar and
doubles as the mass-debug phase: failures auto-attribute to items via the ledger and
requeue as df1 follow-up tasks.

## Anti-conflict conventions

- **Checklist annotations via evidence files**: workers write
  `docs/plans/df1-evidence/<ID>.md` in their own branch (same annotation style as the
  checklist). A final consolidation pass merges them into the checklist. Direct
  checklist edits by workers are forbidden (160-way merge conflict otherwise).
- **Matrix spec registration**: until an auto-registration enabler lands, adding a
  `rust-chromium` leg means appending one regex line to `MATRIX_SPECS` in
  `test/e2e-browser/playwright.config.ts` — gatekeepers resolve the additive conflicts
  by unioning. (Proposed wave-0 enabler: per-spec registration so the shared edit
  disappears entirely — see open decisions.)

## Hard safety rules (baked into every prompt)

Work only in your own `.worktrees/df1-*`; ephemeral ports/homes for every server you
boot; kill by recorded PID only; destructive suites only via `scripts/sandbox-test.sh`
(sandbox lease); never touch ports 3001/3002/17871/17872/17874 or the live server;
nice/ionice all builds and tests; no broad suite without the gate lease; no git pushes
or PRs; no git config changes.

## PR policy

No PR is created without the user's explicit approval (repo rule). Intended shape
(subject to that approval): themed PRs cut from `df1/integration` → `main` at wave
boundaries, then self-merge on green checks per repo norm.

## Pre-flight (base greenness)

- 2026-08-08 before worktree creation: coordinator `test:status` showed
  `latest-suite: default:test/unit failure exit=1`, `test=failure`, `test:server=failure`
  (but `check=success`, `verify=success`, `test:integration=success`). Provenance of
  those runs is ambiguous (other agents/worktrees share the box).
- Definitive check: a coordinated `npm test` against this control worktree
  (`origin/main` content) is launched in the background on 2026-08-08. Result:
  **PENDING — see `~/.freshell/df1/events/preflight-npm-test.log` and the orchestrator's
  session notes. KICKOFF REQUIRES THIS GREEN (or explicit user override).**
- `cargo test --workspace` baseline: deferred to kickoff pre-flight unless time allows
  overnight; same green requirement before spawning item worktrees.

## Open decisions awaiting the user (from the 2026-08-08 proposal)

1. Existing `parity-campaign` (20260805) + `qa-campaign` (20260806) worktrees/branches:
   superseded by df1, complementary, or ignore? (Collision risk on merge.)
2. Plans-only pass for the 66 host-limited items: yes/no.
3. Themed PRs (~15–25) vs other granularity at the end.
4. Wave-0 enabler items: matrix auto-registration + evidence-file convention (this
   README assumes evidence files already; auto-registration still unbuilt).
5. Final `agent` lane cap (24 → 32 ramp) subject to observed machine health.

## Kickoff checklist (orchestrator, on user "go")

1. Confirm pre-flight `npm test` green (this section); if red, report the failing
   suites to the user and STOP.
2. `df1ctl.py init df1-control/queue/items.json` (already done at staging time —
   verify) and `df1ctl.py report --states queued` to size the first wave.
3. Wave 0: harness-enabler items first (HARNESS-03, 04, 05, 06-Linux-parts, 11, 12, 14
   + stress-project enabler + the matrix auto-registration enabler, if approved).
4. Ramp waves 8→16→24→32; keep the reaper sweeping; gatekeeper batch per ~10 verified
   items; user approves PR creation per wave.
5. After the last item wave: close-out campaign (full suite both kinds + the deferred
   Playwright inventory), then consolidation (evidence files → checklist; fresh
   reconciliation doc; GATE-08 receipt from the ledger).
