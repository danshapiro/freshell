{
  "//": "TERM-04 item-scoped typecheck gate. The repo-owned typecheck configs (tsconfig.json) deliberately exclude test/, so this config gives the dedupe spec an EXECUTABLE, file-attributed gate: `npx tsc -p test/e2e-browser/tsconfig.term04-check.json`, then require zero error lines attributed to the TERM-04 spec file. Errors in pre-existing dependency files (e.g. helpers/fixtures.ts's worker-scope tuple typing) reproduce identically on the base commit and are out of scope. Mirrors the HARNESS-05 convention (tsconfig.raw-clients-check.json). See docs/plans/df1-evidence/TERM-04.md.",
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "incremental": false,
    "types": ["node"]
  },
  "include": [
    "specs/terminal-create-dedupe.spec.ts"
  ]
}
